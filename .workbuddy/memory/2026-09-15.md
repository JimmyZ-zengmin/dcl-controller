# 2026-09-15 工作日志

---

## 用户要求"恢复到稳定版本固件，开始闭环伺服实验"

### 已做
- `git checkout -- src/ CMakeLists.txt build.sh` 恢复到 HEAD(`089fd97`)，重新构建烧录（48128 字节，
  与实验版 51200/52224 大小不同 ⇒ 确已换回）。
- **改动已备份成补丁**：`D:/STM/8.29 AIAutoFactior/9.10 H723_verified-fixes_2026-09-14.patch`（895 行）。
- 清掉 `build/CMakeCache.txt` 里残留的陈旧 `DCL_DO_LATCH`（源码回退后它已与编译器无关，留着会误导）。

### ★★ 但查证发现两个**硬阻塞**（恢复稳定版后这个实验做不了）
1. **PE8~PE15 是死的**：恢复后的 `do.c` 是 `CTCR=0x00020000`（byte 尺寸+地址固定）⇒ MDMA 只写 ODR 低字节；
   而 `do.h` 的 `DCL_DO_LATCH` 兜底 = 1（编译器没收到 `-D`，已用 buildlog 核实）
   ⇒ 稳定版**只有 PE0~PE7 能输出**。而 U9 排针（文档整理过的那一个）引出的 DO 是 **PE7~PE15**
   ⇒ **U9 上只有 PE7 一路可用**。PE0~PE6 在**另一个连接器**（原理图里 x≈630/702 那一簇，
   文档没整理；U10 在原理图 (659.5,256.9)，其网络名是 PD15/PG3/PC7/PD0/PA15… 里没看到 PE0~PE6，
   ⇒ 需读板上丝印才能定位）。
2. **`OFF_CTRL_GPIO_MASK` 从来没有代码写它（恒 0）** —— `engine.c:1140` 自己就写着"**当前不可达**"。
   ⇒ `do_poll` 每拍 `if (mask == 0u) return;` **早退，DO 面从来没被引擎驱动过**。
   ⇒ 稳定版里**没有任何命令能让 DO 动**；唯一能动 PE0~PE7 的是 `0x39 op=1` 引脚码型诊断，
   但它是 **5 kHz 方波**（`pat = tick & 0x7F`）—— 接继电器会把它打成蜂鸣器，还会烧触点。
   ★ 这是一个真正的"未收口"：P3-A/P3-B 的 DO 面建好了，但**没人把掩码接上**。

### 另需向用户讲清的工程判断
- **继电器驱动不了步进电机**：步进要换相序（A+/A−/B+/B−），继电器只能通断电源；
  机械继电器吸合 5~10ms ⇒ ≤ ~100 Hz，远低于可用步频，触点还拉弧。
  正确链路 = `MCU → 步进驱动器(STP/DIR/ENA) → 电机`；继电器只当**电源闸门**，
  且**切 ENA 优于切电机电源**（带载断电丢保持转矩 + 反电动势打驱动器）。
- **"闭环"的反馈从哪来**：本板可用输入只有 AI(PA0/PA1/PA4/PA5) 与 DI(PC0~PC3)，**没有编码器接口**
  ⇒ 步进本身开环，没编码器闭不了位置环，只能做开关量闭环（走到限位/接近开关就停）。

### 下一步（待用户决定）
打回补丁（16 路全活 + A 档 + op=14/op=16 仪器）⇒ 再用 **PE8~PE11 = U9 pin 35/36/37/38**
接 4 路继电器；并补上"让 `GPIO_MASK` 能被设置"这一小步（deploy 路径或一条命令），
否则 DO 面依然通不到引脚。

---

## 闭环伺服接线设计（用户给了完整硬件清单）

**硬件**：24V/3.2A 开关电源 · 24V 4路光耦继电器模块 · **TB6600** · 42步进 · **AS5600 磁编码器（I2C）**
⇒ 链条是**对的**（有 TB6600 就真换相；AS5600 给位置反馈），继电器只当电源闸门。

**接线表已落文档**：`docs/WIRING-servo-TB6600-AS5600.md`（三个域 / 分段接线 / 先量的量 / 引脚占用表）

### 关键设计决定
- **步进脉冲 = PA6 (TIM3_CH1)**：本来跑 HIL PWM 1kHz/10bit，本实验改作步进脉冲源
  （TIM3 计数时钟 1MHz ⇒ 频率 = 1MHz/(2·(ARR+1))，可达几十 kHz）。
  ★ 与 HIL 输出面互斥，本轮 HIL 让出。
  ★ PA6 网络名亦为 `DCMI_PIXCLK`，板上有摄像头座 ⇒ 接之前确认座上没插东西。
- **DIR = PE8 / ENA = PE9**（U9 pin35/36）⇒ **必须先接通 DO 面（`GPIO_MASK` 恒 0 那个阻塞）**
- **AS5600 = I2C2 on PB10(SCL)/PB11(SDA)**（U9 pin44/42），3.3V 供电、4.7k 上拉到 3.3V、
  地址 0x36、原始角度寄存器 0x0C/0x0D。**工程内没有任何 I2C 代码 ⇒ 驱动要从零写。**
- TB6600 用**共阳接法**（三个正端并到板 +5V，MCU 拉低灌电流）；**先量 +5V→PUL− 的电流**再决定要不要串 220~330Ω。
  ENA 有效极性必须实测（TB6600 有正/反两种标注）。
- **继电器建议第二阶段再接**：24V 模块输入侧以 24V 负极为参考 ⇒ 接上就把逻辑地与 24V 负极短接，
  且 24V 通断噪声会串进 I2C（正好打在最脆的 AS5600 上）。要接时触点只串 24V 主回路，绝不串电机相线。

### 仪器状态（查证）
CH340 与 DAPLink **当前都没枚举**（USB 在位只剩键鼠/蓝牙/根集线器；串口列表空；
14:07 有 3 条 PnP 移除事件）。已让用户重插（直插主机口，别过 `214B:7260` 集线器）。

### 下一步
1. 用户接线 + 量那几个电流
2. 固件：接通 DO 面（GPIO_MASK）+ TIM3 改步进脉冲 + 新增 I2C2/AS5600 驱动 + DCL 位置环

---

## AS5600 连通性排查（4 组引脚对扫描）—— 已定位到"供电电压给错"

### 做过的
工程里**没有任何 I2C 代码** ⇒ 新写位操作 I2C 探针（`0x39 op=17`），并在主循环每 200ms 跑一次，
结果全落全局量 ⇒ **串口不在时用 SWD 直接读内存验收**（`tools/h723_swd_read.py`，halt 模式、不复位）。

### 实测（v2 = 一次扫 4 组候选引脚对）
| 组 | SCL,SDA | 内部下拉时 IDR | 写 ACK |
|---|---|---|---|
| 0 | PB10,PB11 | **0x0C00(两线高)** | 0 |
| 1 | PB11,PB10(反接) | 0x0C00 | 0 |
| 2 | PB6,PB7 | 0x0000 | 0 |
| 3 | PB8,PB9 | 0x0000 | 0 |
⇒ **外部上拉确实在 PB10/PB11 上**（有线、那一侧有供电），**但 0x36 不应答**；**接反已排除**。

### 根因判断：**模块要 5V，我表里写了 3.3V**
用户给的模块端子：`GND / 5V / PGO / SDA / SCL / DIR` —— **只有一根电源脚且标 `5V`** ⇒ 5V 模式
（板载 LDO 降 3.3V）。喂 3.3V ⇒ LDO 输出可能低于 AS5600 的 **3.0V 下限** ⇒ 芯片不工作，
**但它的上拉仍把线拉高** ⇒ 正好解释"有上拉、无 ACK"。

### 两处接线错误（按 ams 官方 datasheet 更正）
- **`PGO` 必须悬空**：官方说 `PGO` **内部上拉，悬空/VDD = 正常工作，接 GND = 进编程模式**。
  ★ 我第一版凭印象写了"PGO → GND"，**正好会把它推进编程模式** —— 又一次"照印象推"的教训。
- **`DIR` 必须接 GND**（或 VDD）：`DIR=GND` ⇒ 顺时针角增。**它没有内部上拉，悬空不确定**。
  ★ 我第一版写"DIR 悬空"，错。
- 另修正：磁铁检测位在 **STATUS=0x0B**（0x1A 是 AGC，不是磁状态）。

### 下一步（待用户量测）
1. 模块 5V ↔ 板 +5V、GND 通断确认
2. **量 SDA/SCL 空闲电压**：≈3.3V 可直接接；**≈5V 必须先别接**（H7 IO 不 5V 容忍）
3. 改完重跑扫描看 0x36 是否 ACK

### 工具
`tools/h723_swd_read.py`（SWD 读内存，绕开串口）、`0x39 op=17`（探针读回 64 字节）

---

## ★★★★ AS5600 打通 (2026-09-15) —— 卡点是**我自己的位掩码 bug**

### 结果
```
组0 (PB10=SCL, PB11=SDA): ACK=1 ✓  RAW_ANGLE=2818 (247.7°)
组1 (接反): ACK=0     组2/3 (PB6,7 / PB8,9): 无上拉无应答
输出通路自检: 拉低读回 0x0000 ✓  释放读回 0x0C00 ✓
STATUS(0x0B)=0x0B → MH=1 (磁场太强); 之前读 AGC(0x1A)=0x8007 (AGC≈7, 接近最小)
```

### 根因（与 `CTCR=0x00020000` 同族：**位偏移错一位**）
`GPIO_MODER / PUPDR / OSPEEDR` 是**每引脚 2 位**；`OTYPER / ODR / IDR / BSRR` 是**每引脚 1 位**。
我图省事只写了一个 `as_pair_mask() = (1<<10)|(1<<11)`，然后
`GPIO_MODER |= as_pair_mask()` → **实际改的是引脚 5 的两个位**，PB10/PB11 **从未切成输出**
⇒ 线一直是输入、被模块上拉拉高 ⇒ I2C 的 **START 条件根本不成立** ⇒ 永远收不到 ACK。
★ 现象与"器件不应答"**一模一样**，害我往硬件上猜了三轮（供电电压/PGO/DIR）。
修法：拆成 `as_bit_mask()`(1位/引脚) 与 `as_2bit_mask()` / `as_moder_out()`(2位/引脚)。

### ★★ 抓住它的那个判据（值得复用）
**"输出通路自检"：把引脚驱动到低，再读 IDR，期望读到 0。**
前几轮只看了"有没有上拉、有没有 ACK"，**从没验证过自己能不能拉低** ——
这是本项目"凡写进去就不再读一眼的配置都违规"的又一实例。**加上它，一轮就定位了。**

### 接线结论（已实测）
`5V`→板 +5V、`GND`→板 GND、`SCL`→PB10(U9 pin44)、`SDA`→PB11(U9 pin42)、
`PGO` **悬空**（接 GND 会进编程模式）、`DIR`→GND。
★ 磁铁太近（MH=1, AGC≈7）⇒ 间隙调到 1~2mm。

### 工具
`tools/h723_as5600_check.py`（SWD 读回 + 解码 + `--watch 秒数` 连续看角度）、`0x39 op=17`（72 字节）

### 下一步
① 正式 I2C 驱动（把位操作换成真驱动，放主循环，角度进 SENSOR[]）
② TIM3_CH1 改步进脉冲  ③ 接通 DO 面（GPIO_MASK）  ④ DCL 位置环

---

## 正式 I2C 驱动落地 (2026-09-15) —— 新增 4 个源文件 + 两个新教训

### 交付
- `src/i2c_bb.{h,c}` — 位操作 I2C 主机 (引脚可配置、有界等待防挂死、NAK/卡死计数、
  **`i2c_bb_selftest()` 输出通路自检**)。
- `src/as5600.{h,c}` — AS5600 驱动：读 RAW ANGLE(0x0C/0x0D) + STATUS(0x0B)，
  写 `SENSOR_MAP[0]`(原始值) 与 `[1]`(度数)；**读失败时保留上次值，不写 0**
  （把"没读到"伪装成"角度=0"会让闭环跑飞）。
- `main.c`: 主循环每 10ms 调 `as5600_poll()`；`op=17` 上电诊断(4 组引脚对扫描)、
  `op=18` 运行态读回。CMakeLists 加了两个源文件。
- **架构决定**：不上硬件 I2C。实测需求只有 10ms 读 2 字节(≈250µs，占主循环 2.5%)，
  不值得引入 `TIMINGR/ANFOFF/H7 errata` 一堆新失败面。★ 将来预算吃紧再换，那时有判据可对照。

### 验收 (走 COM21 串口，CH340 已恢复)
`ok_n` 3 秒 +279 ⇒ **93.0 次/秒**(期望 100)；`err_n=0`；I2C 事务 186/s = 93×2 ✓ 自洽；
`raw=2944 (258.75°)`、`STATUS=0x27 MD=1`。读数 6 秒内**不抖**(只有 1 个不同值)。

### ★★ 新教训 1：**"模运算当调度器"会让周期任务跑多次**
`if ((g_tick_count % 100u) == 0u) as5600_poll();` —— 主循环**一拍内转很多圈**，
命中那一拍里会**反复调用** ⇒ 实测 190/s(期望 100)。改成**边沿触发**:
`if ((int32_t)(g_tick_count - s_next) >= 0) { s_next = g_tick_count + 100u; ... }`
(无符号减法天然处理回绕)。★ 同族: 凡"用取模做节拍"的地方都要查一遍。

### ★★ 新教训 2：**8 位寄存器不能用 16 位读法解码**（我又踩了"宽度"这一族）
我用 `as_read16(0x0B)` 读 STATUS，返回值是 `[0x0B, 0x0C]` 两个字节拼成的 16 位数
⇒ 解码时把 **0x0C(角度高位)** 当成了状态的高字节，于是把 `0x270B` 读成"MH=1 磁场太强"，
还让用户去调磁铁间隙。**正确单字节读**下 `STATUS=0x27` ⇒ **MD=1 磁场正常**。
★ 与 `CTCR=0x00020000`(宽度/偏移错)、`MODER` 掩码错 是**同一族**，这已经是第三次。
  ⇒ 规矩：**读寄存器前先确认它的宽度**；多字节读出来的量, 解码必须逐字节对齐地址。

### 下一步
① 转轴验证跟随性(待用户)  ② TIM3_CH1 改步进脉冲  ③ 接通 DO 面(GPIO_MASK)  ④ DCL 位置环

---

## ★★★★ AS5600 跟随性验收通过 (2026-09-15) —— 编码器链路彻底打通

### 实测（用户转轴，25 秒记录，`tools/h723_as5600_watch.py 25`）
| 指标 | 值 | 判读 |
|---|---|---|
| 采样变化次数 | 1278 | 明显在跟 |
| **raw 范围** | **2 .. 4094 ⇒ 跨度 4092 = 359.6°** | **转满一整圈** |
| 增量方向 | 正 1237 / 负 41 ⇒ 单向 **96.8%** | 方向一致 |
| 增量幅度 | 中位 +11 LSB(1.0°) | 手转的典型步长 |
| **err_n / nak** | **0 → 0 / 0 → 0** | **全程无误码、无 NAK** |
| STATUS | 0x27 | MD=1 磁场正常 |

### 两个有用的副产物
- **静止噪声底 = ±1 LSB = ±0.09°**（15 秒静止只看到 ±1 LSB 抖动）——
  这比 LA 好得多，闭环保守可用 ±1 LSB 做死区。
- **采样周期 10ms ⇒ 测速上限**：一圈 360° 至少要 36 个样本，
  即**可测最高约 1000°/s**（≈2.8 rev/s）。再快就靠不上单圈绝对值了，
  要么降周期、要么改用多圈/增量法。

### 当前完整链路（已逐段验证）
```
AS5600 --I2C(PB10/PB11)--> H723 (93Hz 轮询, SENSOR_MAP[0]/[1])
TB6600  <--PA6(PUL)/PE8(DIR)/PE9(ENA)--  (下一步: TIM3 改脉冲 + 接通 DO 面)
24V 电源 --> TB6600 --> 42 步进 ; AS5600 同轴
```

### 下一步
① TIM3_CH1 从 HIL PWM 改**步进脉冲源**（频率由拍写 ARR；脉冲数用"硬件定时"保证精度）
② 接通 **DO 面**（`GPIO_MASK` 恒 0 那个阻塞）→ DIR/ENA 才能动
③ DCL 位置环：`SENSOR_MAP[1]`(度) → P 控制器 → 步进频率/方向

---

## 步进脉冲源 + DO 面接通 (2026-09-15) —— 软件就绪, 安全态已验证

### 交付
- `src/step.{h,c}` — 步进脉冲源 (TB6600)：
  · PUL = **PA6/TIM3_CH1**（PWM 50% 占空比，频率 = 1MHz/(ARR+1)，ARR+1 限 [2,65536] ⇒ 15Hz~500kHz）
  · DIR = PE8 / ENA = PE9（经 **DO 面** ACTUATOR[8]/[9]）
  · **上电安全态：CC1E=0（无脉冲）+ PE8~PE11 全高（光耦不导通）**
  · `step_set_deadline_ms()` **限时截止**：到点自动 `step_stop_safe()`（按 g_tick_count 拍差算，
    不受主循环快慢影响）
  · **ENA 极性做成运行期可切**（`op=19 sub=5`）—— TB6600 有"拉低使能/拉低失能"两种标注，
    省掉一整轮"编译→烧录→复位"去试极性
- **接通 DO 面**：`step_init()` 写 `OFF_CTRL_GPIO_MASK = 0x0F00`（PE8~PE11）。
  ★ 这是 `engine.c:1140` 自己标着"**当前不可达**"的那个阻塞 —— 现在通了。
- `do.h`: `DCL_DO_LATCH` **1 → 0**（CPU 一次 32 位 BSRR 直写）。
  理由：① 影子+MDMA 锁存只覆盖 **ODR 低字节** ⇒ PE8~PE15 死，而 DIR/ENA 在 PE8/PE9；
  ② 实测直写抖动 3.6ns vs 锁存 54~60ns。
- `hil.c`: 输出臂开头 `if (g_step_owns_tim3) return;` —— **同一引脚的排他使用必须显式串行化**，
  否则 HIL 每拍把 `TIM_CCR1` 改回占空比，步进脉冲被**静默毁掉**。
- `op=19` 步进控制/状态读回（52 字节，含引脚**实际**电平）。

### 验收（寄存器级干跑，未接 24V）
```
① 安全态      脉冲=0  CC1E=0  GPIO_MASK=0x0F00  PE8~PE11=[1,1,1,1]  ✅
② pulse 1000Hz ⇒ CC1E=1  ARR=999  CCR1=500  (1MHz/1000=1000 计数 ✓)
③ 限时 1500ms 到点 ⇒ 自动停: CC1E=0 脉冲=0                        ✅
```

### ★★★ 又一次"静默无效"—— 这次差点让电机上使能
第一次把 `step_init(g_shm);` 插在 `hil_init` 之后，**那次字符串替换没匹配上、静默没生效**
（我没加断言）。后果：`GPIO_MASK` 一直是 0 ⇒ `do_poll` 早退 ⇒
**PE8~PE11 停在 0 = 光耦导通 = ENA 可能使能**。若那时合 24V，电机就会上使能锁死。
**是"上电安全态验收"（读 GPIOE_ODR 的实际电平）把它抓出来的。**
★ 两条规矩焊死：
  ① **做替换/编辑必须加断言**（assert 锚点存在），否则"我以为改了"和"真的改了"无法区分；
  ② **凡是"上电安全"这类宣称，判据必须落在引脚的实际电平上**，不能只看意图变量。

### 工具
`tools/h723_stepper_test.py`（`safe`/`ena`/`dir`/`pulse Hz ms`/`stop`/`state`/`enapol`）

### 下一步（等用户接 24V）
按脚本分步：safe → ena 判极性 → pulse 500Hz/2s 试转 → 看 AS5600 是否跟着变。

---

## TB6600 拨码设置（用户问 SW1~SW6 怎么拨）

★ **查到 4 份来源的拨码表互不相同**（Sun Electronics / Longruner 手册 / DIY Electronics /
一篇调试实录）：1/8 细分分别被写成 `OFF ON OFF` / `ON OFF OFF` / `ON ON OFF`；
1.5A 分别被写成 `ON ON OFF` / `ON OFF ON` / `OFF ON OFF`。
⇒ **不能给通用表，必须以驱动器丝印上的表为唯一权威。** 已写进接线表 §1.1。

给出的判据（与具体表无关，所以可复用）：
- **SW4~SW6 电流 = 电机铭牌额定电流，且不超过**。★ 更正我之前说的"1.5~2.0A"——
  那是按"驱动器能力"想的，**对 42 电机偏高**（42 通常 0.9~1.7A）。
- **SW1~SW3 细分 = 8（1600 脉冲/圈）**：TB6600 的 PUL 光耦带宽有限，细分高 ⇒ 脉冲频率翻倍；
  且细分不提高精度（由机械决定），只让运动平滑。
- **丝印看不清就从最小电流档往高试**，绝不一次给最大档（过流烧电机不可逆）。
- ★ 这两个数**都不影响闭环**：位置由 AS5600 给，脉冲只是推力。

---

## ★ 拿到本机 TB6600 的实际拨码表（用户拍照）

之前查网上的表 4 份互不相同 ⇒ 已按**用户照片上的丝印**抄录进 `docs/WIRING-servo-TB6600-AS5600.md` §1.1：

- **细分（S1 S2 S3）**：8 = `OFF ON OFF` (1600 脉冲/圈) ← 选用
- **电流（S4 S5 S6）**：1.5A 档 = `ON ON OFF`，**该档峰值标注正好 1.7A** ← 选用
  （17HS4401 额定 1.7A，严丝合缝；仍以电机铭牌为准，绝不超过）
- ★ 壳子右侧标注顺序是 `SW6 SW5 SW4 SW3 SW2 SW1`（SW6 最上）⇒ **按丝印编号对齐，别按物理位置**
- 端子确认：高压侧 `VCC GND A+ A- B+ B-`；信号侧 `PUL+ PUL- DIR+ DIR- ENA+ ENA-`
  ⇒ 与"共阳接法（三个正端并到板 +5V）"一致 ✓

---

## ★★ 电机不转的定案 —— "A 0.116V / B 4.5V / C 5V" 判读

### 三个读数
| 点 | 位置 | 读数 | 期望 |
|---|---|---|---|
| A | PA6（U9 pin19）对 GND | **0.116 V** | 500Hz/50% ⇒ DC 档读**平均值** ≈1.65V |
| B | TB6600 `PUL−` 对 GND | **4.5 V** | 接通时应被光耦 LED 钳到 ~1.0~1.5V |
| C | TB6600 `PUL+` 对 GND | **5 V** ✓ | 稳定 +5V |

### 判读（两条独立结论）
1. **测量时脉冲已经停了**（A=0.116V ≈ 静态低）。限时 60s 到点自动 `step_stop_safe()`
   ⇒ CC1E=0 ⇒ AF 引脚回到非激活态(低)。**所以这次测量没在"脉冲在跑"的条件下做**，
   不能用它判断"PA6 有没有输出"。⇒ 这是**限时机制**（正确行为）造成的假象，不是故障。
2. ★★★ **A 与 B 本来是同一根线的两端，测出来差 4.4V ⇒ 这条通路没连通。**
   更细一层: PA6=0V 时若线是通的，光耦 LED 会被 +5V 经内部限流电阻正向点亮，
   `PUL−` 必然被钳在 LED 正向压降附近(~1.0~1.5V)。实测 4.5V ⇒ **几乎没有电流流过光耦**
   ⇒ 断线 / 插错端子。（4.5V 而非 5V，符合"`PUL−` 悬空、仅经内部电阻+LED 挂到 +5V，
   10MΩ 表头形成微弱分压"。）

⇒ **结论: PA6 → TB6600 `PUL−` 这根线没接通（或插进了旁边的端子）。与软件无关** ——
   软件侧三件套已自证: `PA6_MODER=2(AF)` + `AFRL[PA6]=2(TIM3)` + `TIM3_CNT` 在跑 + `CCER.CC1E=1`。

### ★★★ 为此新增的判据工具: **PA6 静态电平探针**（`0x39 op=19 sub=7/8/9/10`）

**为什么必须新增**: 万用表 DC 档量 500Hz 方波读的是**平均值(≈1.65V)** ⇒
"线通(被 LED 钳到 ~1.2V)"与"线断(浮空 ~4.5V)"在表上只差一点点，判不了；
而且"脉冲还在不在跑"本身还是个变量（带限时）。
⇒ 把 PA6 **从 TIM3 上摘下来**、输出**确定的静态电平**，读数就没有任何歧义:

```
python tools/h723_stepper_test.py probe high   # PA6 → GPIO 输出 3.3V
python tools/h723_stepper_test.py probe low    # PA6 → GPIO 输出 0V
python tools/h723_stepper_test.py probe af     # 还原 TIM3_CH1 (CC1E=0, 仍无脉冲)
python tools/h723_stepper_test.py probe od 1   # PA6 输出类型改为**开漏** (候选修复)
```

判定表（在 **U9 pin19** 与 **TB6600 `PUL−`** 两处对 GND 各量一次）:

| U9 pin19 (high/low) | `PUL−` (high/low) | 结论 |
|---|---|---|
| 3.3 / 0 | ~2~4 / ~1.0~1.5 | ✅ 线通、端子对 |
| 3.3 / 0 | **4.5 / 4.5 不动** | ✗ **线断 / 插错端子** ← 当前最可能 |
| 3.3 / 0 | 0 / 0 | ✗ `PUL−` 被短到 GND |
| 本身不是 3.3/0 | — | ✗ U9 pin19 认错了 / **板载摄像头座器件拉住 PA6**（PA6 网络名亦为 `DCMI_PIXCLK`，见 `HARDWARE-PINOUT.md` §4.1） |

★ 探针**零风险**: 进入前先 `step_set_rate(0)` 关 CC1E（保证无脉冲），只切
`MODER/AFRL/OTYPER/PUPDR`，不碰别的资源；`probe af` 完整还原。已实测三档切换正确。

### ★★★ 顺带修掉的真 bug: `0x39 op=19` 应答**栈越界 28 字节**

`h_pin_pattern()` 里声明 `uint8_t r[40]`，而该分支写到 `r+64..r+67`、`ack(r, 68u)`
⇒ **栈上越界写 28 字节**。它不报错、不崩，只会把调用者的局部量/保存寄存器写坏 ——
正是本项目最恨的"运行期行为不对、却查不出谁写坏的"形态。
★ 最阴的一点: 读回来的 17 个字段**全是合理值** ⇒ 越界在观测面上**完全看不见**。
判据只能来自"数一数最多写到哪个偏移"这种**静态**检查。
⇒ 已改 `uint8_t r[80]`，顺带把应答扩到 76B（尾部加 `GPIOA_ODR` / `GPIOA_OTYPER`，供探针回读）。

### 工具
`tools/h723_stepper_test.py` 新增 `probe low|high|af|od <0|1>`；`cmd()` 改为
"有多少字节解多少字"（兼容 68B/76B 两种应答，避免"同一个量两处定义"）。

---

## ★★★★ 更正上一条判读 —— 是**软件侧**，不是接线（2026-09-15 晚）

### 用户给的完整现象（推翻了前一条结论）
`DIR− = 3.4V`、`ENA− = 3.4V`、所有 `+` 端 = 5V、`PUL− = 4.5V`；
**换了一根线，结果完全一样**。

### 先立一个物理硬约束
**推挽输出不可能把自己脚上的电平抬到 VDD 以上。**
`PUL− = 4.5V > 3.3V` ⇒ 那一刻 **MCU 根本没在驱动这个脚**（高阻），
4.5V 是外面经"驱动器内部电阻 + 光耦 LED"把脚拉上去的。
⇒ 那 4.5V **不是"断线"的证据**，而是"**脚是高阻**"的证据。前一条结论作废。

### 新增的硬判据: `GPIOx_IDR`（引脚**真实**电平，与谁在驱动无关）
`h_pin_pattern` 的 `op=19` 应答尾部加 `GPIOA_IDR`(bit6) / `GPIOE_IDR`(bit8..11)。
比对 **ODR 与 IDR**：

| ODR | IDR | 含义 |
|---|---|---|
| 0 | 0 | 我们在驱动低 ✓ |
| 1 | 1 | 我们在驱动高 ✓ |
| **0** | **1** | ★★ **不是我们在驱动** —— 高阻 + 外部上拉 |

### 实测（芯片自读，无万用表参与）
| 状态 | MODER | CC1E | **IDR(PA6)** |
|---|---|---|---|
| `probe high`（GPIO 输出 3.3V） | 1 | 0 | **1** ✓ |
| `probe low`（GPIO 输出 0V） | 1 | 0 | **0** ✓ |
| **`probe af`（TIM3 AF，无脉冲）** | 2 | 0 | **1** ⇒ **高阻** |
| AF + 500Hz 脉冲（`idrwatch`） | 2 | 1 | **0 出现 38 次 / 1 出现 49 次** ⇒ **真的在翻转** |

### ⇒ 两条结论
1. **☆☆☆ PA6 在"AF 模式 + CC1E=0（无脉冲）"时是高阻，不是低电平。**
   于是"没脉冲"时 `PUL−` 会浮到 ~4.5V —— 极易被误判成"断线 / 插错端子 / 软件没输出"。
   **本次就是这么被带偏的。**
2. **脉冲其实一直都在出**：CC1E=1 时 IDR 同时出现 0 和 1 ⇒ AF2/TIM3 通路接到了引脚上。
   ⇒ **"MODER=AF + AFRL=2 + CNT 在跑 + CC1E=1" 这四件套读回全绿，之前被当成"引脚必然在翻转"，
     而真正能证明的只有 IDR。**（这次四件套恰好是对的，但当时它并没有证明力。）

### ★★★ 新的判据纪律
- **凡"引脚在无脉冲时读不到电平"，不能推出"引脚不工作"** —— 必须**在有脉冲时**判，
  且判据用 **IDR**（引脚真实电平），不用"寄存器配置"。
- ★ 万用表量"某个信号端"时，**必须同时知道那一刻引脚在被驱动还是高阻**。
  否则 4.5V 这种数既像"断线"又像"高阻上拉"，**根本不可区分**。
- ★ 这正是本项目"配置全对 ≠ 功能可用"的又一次：这次连"功能可用"的**观测手段**本身也是错的。

### 下一步（待办）
- 用 `probe high`（PA6 推挽 3.3V，形态与 `DIR−/ENA−` 完全相同）让用户量 `PUL−`：
  **3.4V = 通路好 / 4.5V = 通路断**。这是唯一可比的口径。
- 若通路好 ⇒ 电机不转的原因转向 **§2.2"3.3V 高电平关不断 5V 光耦"**（LED 在 3.3V 仍有电流
  ⇒ 驱动器看到"一直导通"⇒ 不计数）⇒ 修法 = **PA6/PE8/PE9 改开漏**。

---

## ★★★★ 审计报告（用户要求：写报告，他去审计）

交付物：**`docs/AUDIT-stepper-no-rotation-2026-09-15.md`**（新建）。

### 报告里列出的**代码级可疑点**（全部"只报不修"，先审计）

| # | 位置 | 问题 |
|---|---|---|
| **C1** ★★★ | `hil.c:167` `hil_outputs_safe()` | **没有 `g_step_owns_tim3` 门**，无条件 `TIM_CCR1(TIM3)=0`。而每拍输出臂 `hil_out_apply`(`hil.c:106`) **有**门。⇒ 任何 STOP/`eng_outputs_safe` 都会把脉冲变成**恒定低**（CC1E 仍=1）⇒ 光耦**持续导通** ⇒ 不计数、轴不动。★ 同族老毛病"一个语义两处存放只守一处"。★ 观测盲区：原来只报**缓存** `g_step_ccr1`，查不出来 |
| **C2** ★★ | `main.c:3458` + `do.c:98` | `DCL_DO_LATCH=0`（CPU 直写档），但 **`do_latch_init()` 没有 `#if`、也无早退**，MDMA 链仍全开 ⇒ 每拍把 `OFF_DO_SHADOW`(A 档下**从没被写过**，恒 0) 锁存进 `GPIOE_ODR` 第 0 字节 ⇒ **PE0..PE7 恒低**。本轮不影响步进（用 PE8/PE9），但 DO 0..7 不可用；且 CPU 写 BSRR 与 MDMA 写 ODR **同拍竞争** = 随机故障 |
| **C3** ★★ | `step.c:53-60` | `step_init` **从不写 `TIM_CCMR1`**，通道模式全靠 `hil_init`(`hil.c:67`)。⇒ 若 hil_init 被跳过 ⇒ `OC1M=000` ⇒ **永不产生脉冲**，而 `CCER/ARR/CCR1/CNT` 读回全"正常"。"该写而没写"型 |
| **C4** ★★ | `step.c:103/138` | 限时里 **`0` 同时表示"不限时"与"已到期"** ⇒ 到期后再 `step_set_rate()` = **无限跑**，保护静默消失。应拆 `deadline_armed` + `deadline_tick` |
| C5 | `step.c:14` | `g_step_owns_tim3` 硬编码 1，**无回收路径** |
| C6 | `step.c:77-82` | 改频率时先清 CC1E ⇒ 开出**高阻窗口**（E2 已证 AF+CC1E=0 是高阻） |
| C7 | `hil.c:57` | PA6 被设成**最高速档**，500Hz 不需要，EMI 更差 |

### 报告里列出的**未定案疑点**
U1(C1 现场抓 `CC1E=1` 而实时 `CCR1=0`) / U2(mA 档量 PUL 回路电流判"3.3V 能否关断光耦") /
U3(`probe high/low` 两档量 `PUL−`) / U4(手转轴判 ENA 极性) /
U5(数据手册三处未核：PA6 是否 FT、PA6=TIM3_CH1 是否 AF2、DCMI_PIXCLK 座子器件) /
U6(拨码/相线/24V)。

### 本轮新增的仪器（报告 §5）
应答 **68B → 96B**：
- `r+76 = GPIOA_IDR`（PA6 真实电平）、`r+80 = GPIOE_IDR`
- **`r+84/88/92 = 实时 TIM3_CCMR1 / CCR1 / ARR`** ← ★ 这是查 C1 的唯一手段
（原 `r+20/r+24` 是 `step.c` 的**缓存** `g_step_arr/g_step_ccr1`，别处改寄存器它不动。）

★ 地址速查：`TIM3_CCER=0x40000420` / `TIM3_CCR1=0x40000434` / `TIM3_CCMR1=0x40000418` /
`TIM3_ARR=0x4000042C` / `GPIOA_IDR=0x58020010` / `GPIOA_ODR=0x58020014` / `GPIOE_ODR=0x58021014`。

### ★ 纪律沉淀
**"缓存量"与"实测量"必须分开报。** 只要观测面报的是软件缓存，
"别处改了硬件"就**永远查不出来** —— 这与"配置全对 ≠ 功能可用"是同一族：
**观测手段本身会骗人。**

---

## ★★★★ 复核 H723PLC 上的 6 个提交（另一会话做的审计）+ 我的回应

`origin/main = edfa860`（本地 main 同步）。6 个提交：`9876d13 → edfa860`。
★ 工作区里的未提交内容（i2c_bb/as5600/step、多份 docs 与 tools）**仍未提交**。

### 他们解决了什么（我复核后认可）
1. **C1 被实测排除**（逻辑矛盾：E4 证明引脚在翻 ⇒ CCR1 不可能为 0），但 C1 仍是**真缺陷**（条件性）。
2. **步进不转真因定案**：这块 TB6600 的 **ENA = 「光耦导通 = 失能」**（与多数相反）
   ＋ **3.3 V 推挽高电平关不断 5 V 光耦**（mA 档实测两档电流差 ≈ 0）
   ⇒ 无论 PE9 高低都是失能 ⇒ 电机永远不转。
3. ★★ **开漏方案被数据手册否决**：`DS13313 Rev 5` Table 7 ——
   **PE8/PE9 = `TT_ha`（3.3 V 容忍，不是 FT）**，PA6 同为 TT 类
   ⇒ 开漏高阻会被外部拉到 ~4.5 V，超绝对最大额定值。**这正是我上一轮列为"未查证"的 U5-1。**
4. ★★★ **修法：光耦正端从 5 V 改接 3.3 V**（`19ea345`）。
   根因重新定位为"**供电域与逻辑高电平不一致**"（5 − 3.3 = 1.7 V 正好高于 LED 的 Vf）。
   改 3.3 V ⇒ 高电平时压差 **0** ⇒ 彻底关断；且**保住 MCU 只做 sink**（不倒灌进 TT 脚）。
   ★ **"共阳 + 3.3 V"才是同时满足"不倒灌"与"关得断"的组合** —— 我原接线文档只考虑了前者。
5. 运动品质实测（`H723-MOTION-QUALITY-AUDIT.md`）：速率偏差 ≤0.4%、2 min 长跑 −0.0%、
   10 轮往返累积 **0.00 步**、位置锁定 **+0.176°（0.78 步）/ 25 s 零修正零漂移**（峰峰 = 1 LSB）；
   挖出 D1–D5 五条固件缺陷；最高转速 1170 rpm 稳定（带斜坡）/ 627 rpm（突加，因无加减速）。

### ★★★ 我的回应（`docs/audit/H723-MOTION-QUALITY-RESPONSE.md`，新建）

**① D2 根因更正 —— 不是"时钟/分频按错"，是 `step_tick()` 每次调用取整**

    uint32_t dm = dt / 10u;    /* dt ≈ **主循环一圈**, 不是固定的 1ms */

- **判据**：固定倍率的时钟错**只能给恒定倍率**；但实测倍率**随主机负载变 21%** ⇒ 直接排除。
- 取整 ⇒ 累计**永远 ≤ 真实** ⇒ **只会慢、永不快**；丢多少取决于"一圈多长" ⇒ 随轮询变。
- ★★ **零运动实测**（`g_step_deadline_tick` 的递减与 `CC1E` 无关 ⇒ 不开脉冲也能量）：

  | 档 | 实测/秒 | 反推一圈 |
  |---|---|---|
  | 静默 | 648.9（−35.1%） | 15.4 拍 = **1.54 ms** |
  | 密轮询 | 789.1（−21.1%） | 12.8 拍 = **1.28 ms** |

  两档都落在 `k = floor(dt/10) = 1` ⇒ 指纹就是"**每次记 1 ms，真实过了 1.3~1.5 ms**"。
  `dt_max = 684 < 1000` ⇒ **未撞钳位**，钳位不是成因。
- **修法（两行）**：限时改成 **目标拍号**，用一次减法判到期，**不做除法**：
  `s_dl_tick = tick_now + ms*10; if (armed && (int32_t)(tick_now - s_dl_tick) >= 0) 到期;`

**② D3 机制 —— 顺序 × 一个值两个语义**（原文只有现象，没机制）

  `cmd_units` 是 `d.limit(units)` → `d.rate(500)`，**两条独立帧，中间隔着主循环**。
  小限时在 `rate()` 到达**之前**就被吃光；吃光走 `step_stop_safe()`，它把 `tleft` 置 **0 = 不限时**（C4）
  ⇒ 紧接着的 `rate(hz)` **无限跑**，直到下一条命令。
  ★ 零运动复现（全程 `ENA=0` ⇒ 驱动器失能 ⇒ 轴保证不动）：`CC1E=1` 而 `tleft=0`、`stop_n` +1 ✓

**③ D2 / D3 是同一个函数的两面** ⇒ 一次改动（目标拍号 + 独立 `armed` 位）同时收掉 **D2 + C4 + D3**，
   并为 D4（固件内计数/定位）打下正确的时间基。

**④ 修了他们点的两个工具判据**（都是我写的 `tools/h723_stepper_test.py`）：
- `probe af` 假报警（AF + `CC1E=0` 时引脚**本来就是高阻**，`IDR=1` 正常）⇒ 已改
- `safe` 判据写死 `PE8~PE11 全 1` ⇒ 在 `enapol=1` 时**永远失败** ⇒ 已改成跟着 `enapol` 走，并改用 `IDR`

### 新增文件
- `docs/audit/h723_limit_tick_probe.py` —— 零运动时基探针（静默 vs 密轮询 + D3 机制）
- `docs/audit/H723-MOTION-QUALITY-RESPONSE.md` —— 本回应

---

## ★★★★ 新提交（§9–§15）与**项目整体评估**

### 新提交（`0ff985a` → `979ebb2`，7 个）
§9 算法复杂度上限（命令通路 **26.3 命令/s** ⇒ 环频 **13.5 Hz** / 滞后 **80 ms**）
§10 ★ 重大更正：**编码器读路径已挂**，"轴不动"是假象（SWD 实证）
§11 固件内引脚自检 ⇒ 定案在 AS5600 器件侧
§12 ★ 定案：**器件是好的，SCL/SDA 两根线接反了**（连测 3 次可复现）
§13 编码器修好后**连时基一起恢复** ⇒ **D2/D9 的"ms 时基慢"原是 I2C 全失败的下游症状**
§14 速度变化曲线（低速→极速→中速→低速）全线跟得上
§15 驱动方式评估 + 新缺陷 **D10**（频率静默下界 **15.26 Hz**：TIM3 是 16 位，ARR ≤ 65535）

### ★★ 重要 git 事实（与本仓库 .git 事故史有关）
- **`origin`(H723PLC) 实际已是最新**（`git ls-remote origin main` = `979ebb2`）。
- **本地 `refs/remotes/origin/main` 与 `dcl/main` 都停在 `089fd97`，fetch 报了更新却没落盘** ⇒
  `git branch -vv` 会显示 "ahead 13"，**这是假象**。
- ★ **纪律：判断"推没推上去"必须用 `git ls-remote <remote> <branch>`（直连，不看本地 ref）。**
- `dcl`(dcl-controller) **实际落后 7 个提交**（停在 `070d9f3`）。

### ★★★ 交付物：`docs/ASSESS-exp-standardization-2026-09-15.md`

用户问："之前的实验怎么做的？标准化怎么样？有没有遵循系统架构？"

**结论**：
- **方法学（怎么测）= A 级**。`exp-<日期>-<主题>/`（README + 固件 patch + raw + 脚本 + analysis）
  是最规范的一次；审计 doc 有 **AUDIT/RESPONSE 配对**；每个数字带复跑命令；
  多次**自我撤回**（D5 / 累积丢步 / §10 编码器假象）；LA 测不动就换固件内累積；
  §7.4 主动声明"可测速度上限，≥40000Hz 不上报"。
- **架构一致性（测的东西放在哪）= C 级**。对照 `CORE-ENGINE.md` 的 4 公理 + 8 不变量：
  - **公理③（计算即查表）被完全绕开**：位置环在 PC 上，引擎 128 路由 / 19 原语一个没用
    ⇒ 实测环频 13.5 Hz、滞后 80 ms，**架构最值钱的"100 µs 确定性"丢了 ~700 倍**。
  - **公理②（单写者）破两处**：`step_init()` 又写了一次 `GPIO_MASK`（权威写者是协议）；
    `OFF_DO_SHADOW` 在 A 档下**没有写者**但 MDMA 还在读（= C2）。
  - **公理④（时间预算是公民）**：`step_tick` 挂主循环，**无标价、无静态门、无动态门**。
  - **不变量⑤（观测变量要 `obs_anchor` 登记）**：`0x39 op=19` 的 96B 应答**手工拼、不进 SHM、无静态断言**。
  - **输出链契约"一个输出面一个驱动者"**：脉冲是从 **HIL 的 TIM3 输出面抢来的**，
    被迫加 `g_step_owns_tim3` 让路门 ⇒ **C1 的根因就是"绕开架构之后的双写者问题"，
    不是"忘了加一行"**。

### ★★★ 因果链（本文最有价值的一节）
**六个已编号缺陷全部落在"架构边界被绕开/被动过"的地方**：

| 绕开处 | 导致 |
|---|---|
| 脉冲从 HIL 抢 TIM3 | **C1** |
| 频率/限时不在 SHM，靠自定义协议字段 | **D10**（静默下界 15.26 Hz）、**D6**（复位后 `enapol=0` 与接线语义相反） |
| 限时递减挂主循环（不在拍上） | **D2**（取整致时基失真）、**D3**（小限时变无限跑）、**D4**（精度钉死 ±0.57°） |
| 位置环在 PC 上 | §9.1/§9.2/§15.5 的复杂度上限 |
| 应答手工拼、不进 SHM | 栈越界、"缓存 vs 实时"盲区、两处假报警 |
| `GPIO_MASK` 两写者 / 影子无写者 | **C2** |

**而"遵守了架构的那部分"（方向/使能走 `ACTUATOR` → `do_poll` → BSRR）一直没出过问题。**
⇒ **架构不是纸上规矩，绕开它的代价是具体的 bug。**

### 建议优先级（详见文档 §5）
P0: 步进改成真·输出面（注册 + 删让路门，一次修 C1）；闭环下沉固件（用现成 PID/RATE 原语）
P1: 限时改成"目标拍号 + armed 位"（一次收 D2+C4+D3）；加减速进固件（+47% 转速）
P1: 诊断面收敛回 SHM + `obs_anchor` + 布局静态断言
P2: `RUNBOOK-experiment.md` 固化方法学；8 条不变量做成评审/CI 清单

---

## ★★★★ DCL 程序分层与标准化（讨论稿，用户要为 dcl-controller v2 定口径）

交付物：`docs/ASSESS-dcl-program-standard-2026-09-15.md`（新建，**未改任何代码**）。

### 用户给的口径
"用我们的系统要用 **dcl 程序**；程序要**持久化**，但**不能走传统 flash 烧录**
（烧录的是**引擎核心**）；dcl 程序要能**走通信上传**；程序要用到固件的一些功能
（如 I2C 我们系统没有，就自己又搭了一套）。"

### ★ 读架构文档后查到的五条硬事实（决定标准能不能立）
1. **★★★ "程序持久化"现在是不成立的**：交付档 `DCL_PERSIST_SAVE=0`，
   `0x43` 保存**明确 NAK**（`main.c:2686`）。原因：擦 128KB 内 flash 时 **ISR 卡死 210.6 ms**
   ⇒ 喂狗停 ⇒ 看门狗复位 ⇒ 旧行为是"点保存 = 重启机器且没落盘"。降级处置正确，但**功能确实没有**。
2. **★★★ 满配程序刚好等于最大帧载荷**：`6 + 128×16×3 = 6150`，而 `FRAME_PAYLOAD_MAX = 6150`
   ⇒ **一个字节余量都没有** ⇒ 加不进版本/CRC/清单/program_id。
3. **★★ 三个程序模型并存且规则不同**：路由表(`0x10`, ISR, 可热更, 不要求 STOP) /
   顺序域(`0x44`, ISR, **不热重载**, 必须 STOP) / macro(`0x41`, **主循环**驱动, 落盘口径未决)
   ⇒ macro 的循环挂主循环 = **与 D2/D4 同一条病**。
4. **★★ 能力位里没有"外设"这一类**（只有 MULTICYCLE/HOTRELOAD/PERSISTENT/…/AI/MACRO）
   ⇒ 程序**无法声明"我要 I2C"**，上位机**无法在上传期拒绝** ⇒ 违反本项目自己的判据纪律
   （"未实现的能力必须在下载期失败" —— `SRC_HMI` 已是好范式）。
5. **★ 架构里已埋了正解但没做**：macro VM 有 `0x20-0x23 SPI **未迁移 → -4**`
   ⇒ **"通用外设事务 op"的思想早就有**。I2C 本可做成 macro 的通用事务 op，
   那样"支持新传感器"=写一段程序，而不是改固件。实际却是写了专用驱动 `as5600.c` +
   专用诊断命令 `0x39 op=17/18` —— **恰好绕过了这条分界**。

### 我提的四层模型与最小改动集（§3 / §5）
① 引擎核心（烧录）/ ② 外设能力（随核心发布、**版本化 + 能力位**、做成**通用事务 op**）/
③ DCL 程序（**通信上传 + 持久化**）/ ④ 程序工程（`.dclp` 包 + 清单）

最小改动：①写 `REF-program-contract.md` 契约 ②能力位扩 u32 并补 `I2C/SPI/SD/RTC`
③程序包 + `BEGIN/DATA/COMMIT` 事务上传（顺带解零余量）④持久化走 **SD 裸块**（推荐，
绕开 flash erase、且程序可搬运）⑤文档标注 macro 定位。

### 待用户确认的三个取舍
① 持久介质：**SD 裸块（我推荐）** / 修内 flash / 两者都做
② macro 定位：辅助（不保证实时、不持久化）/ 升格进拍内
③ 外设能力边界：**通用事务 op**（程序可自便）/ 只保留核心内置的固定几个外设

---

## ★★★★ 用户给了「大刀阔斧标准化」的 mandate（2026-09-15 夜）

### 用户定下的
- 持久介质 = **SD 卡**（同意）。
- **两条硬要求**：① 上传到 SD 的程序**必须正确、不能漏错**；② **SD 卡必须分区**，
  否则日志写满会覆盖程序。
- macro → 要我**按我们的架构与设计目的给出分析**（他在迟疑）。
- 外设能力边界 → 要我**去搜权威资料**看业界怎么做。
- remote → 看 H723PLC（**已核：`origin` = `979ebb2` = 本地 HEAD，无新东西**；
  `dcl` 仍停在 `070d9f3`）。
- ★ **"你必须对项目的资料清晰了然"**：分清老文档 / 现行理念 / 实验下来最符合理念的架构。
- **授权改革，但必须负责任改**。

交付物：`docs/PLAN-dcl-standardization.md`（**执行稿**）。

### ★★★ 我查到的关键事实（全部有取证）

**1. SD 卡上现在没有任何空地方 —— 用户担心的覆盖是必然的**
```
src/sd.c:  LBA0 = 日志头;  LBA1..capacity-1 = 数据区, **环形回卷**
           h[5] = 1u (数据区起始 LBA, **硬编码不可配**)
           h[6] = g_sd_capacity_blocks - 1u  (数据区块数 = **整张卡**)
           if (s_log_blk >= cap_data) { s_log_blk = 0; wrapped = 1; }   /* 回卷 */
```
⇒ 黑匣子日志**占满 LBA1 到卡尾并回卷**。⇒ 程序区必须先从日志区里划出来。

**2. ★★ 两处"陈旧注释会害人"的实例（正是用户要我找的）**
- `src/sd.c:6-7` 声称日志"写在卡的**末尾区** LBA 3000000 起，不碰盘头"，
  **与实现相反**（实现从 LBA1 线性回卷）。★ 照它安排布局的人会把程序区放到**最先被覆盖**的位置。
- `src/sd.c:949` 一次性吞吐自检**真的写 LBA 3000000** —— 落在**日志区内**（卡 ≥1.46GB 时）。
  ⇒ 同卡双写者，破坏日志；是"单写者原则"被破坏的又一例。

**3. ★★ 架构文档停在 09-12，系统已经往前走了 3 天**
`CORE-ENGINE.md`/`ARCH-H723.md`/`ARCH-engine-datalink.md`/`FRAMEWORK-MAP.md`/
`MEMORY-LAYOUT.md` 最后修改都是 **2026-09-12**；而 09-13/14/15 的
VTOR→ITCM 修复 + ISR 闸门、IWDG、**持久化降级为 NAK**、RTC、步进/AS5600/运动品质
**全部没有回填**。⇒ 权威源不是没有，是**过期**。
另：`STATUS-*` 只到 09-12（09-14/15 缺）；`README.md` 停在 09-13。

### 权威资料调研结论（外设能力的边界）
- **IEC 61131-3**：控制动作分**逻辑运算**与**硬件动作**两半；逻辑统一标准化，
  **硬件动作针对各硬件设计专属固件函式库**。
- **CODESYS**：两层架构 —— 应用层只用**内部符号**，**唯一平台相关文件是 I/O Mapping POU**；
  "application POUs **never** reference physical I/O addresses"。适配靠**换设备描述文件**。
- **Zephyr**：**"设备树是硬件信息的唯一真相源"**；驱动不含引脚号/地址；
  **bindings(YAML) 在编译期校验** ⇒ "错误在下到硬件之前就被拦住"。
- **现场总线强制设备描述档**：EtherCAT=**ESI**、PROFINET=**GSDML**、CANopen=**EDS**、IO-Link=**IODD**；
  内容 = **Vendor ID / Product Code / Revision / PDO 映射 / OD 条目**。
- **CANopen CiA 301 对象字典分区**：`0x1000-1FFF` 通信 / **`0x2000-5FFF` 厂商专属** /
  **`0x6000-9FFF` 标准器件 profile**；必备 `0x1000 Device Type` / `0x1001 Error Reg` /
  **`0x1018 Identity(Vendor/Product/Revision/Serial)`**；`0x1010 Store` 分
  **_Save all / _Save Communication / _Save Application**。
- **存储**：ESP-IDF 官方对比 —— **FatFS 电源故障保护=无；LittleFS=有（COW + 元数据对 +
  原子提交 + CRC + 磨损均衡 + RAM 有界）**；NVS「**原子更新**」且官方建议
  **每类数据独立分区**。★ 结论：分区是权威做法；但我们用的是"裸块 + 双副本 + seq + CRC"，
  **本项目已完成验证过**，暂不引入文件系统（`[待拍 W2]`）。

### 我的定案（写入 PLAN）
- 四层模型（核心/外设能力/程序/工程）+ 层间判据。
- **SD 分区**：LOGHDR(LBA0) / LOG(1..LOG_END) / PROG_A / PROG_B / 尾部余量；
  **日志头加 `LOG_END` 扩展字段**（旧头无该字段 ⇒ 退化为现状 ⇒ **向后兼容不毁卡**）。
- **"不能漏错"五道闸**：① 帧 CRC16 ② 清单 CRC32+total_len ③ **写完回读 memcmp**
  ④ A/B + 单调 seq ⑤ ★★★ **装载前重跑与上传时同一套静态校验**（不过就不装载）。
- **macro 判为 B 档（调试工具，非实时、不持久化）**：它**突破单写者**（可写任意 SHM 地址）、
  **无预算门**、有**忙等**、**挂在主循环**（主循环一圈 1.28~1.54 ms = D2 的病根）。
  未来若要承载 I2C/SPI 事务（方向 C），**前提是先把它从主循环搬走 + 标价 + 预算门**。
- 执行顺序 S0..S7，**S3（SD 分区）是第一个动代码的**（可逆、向后兼容）。

### 待用户拍：W1 程序区放卡尾还是卡头（建议卡尾）；W2 是否引入 LittleFS（建议暂不）

### ★ 资料普查（子代理全量扫描）—— 最关键的三条
1. **★★★ SHM 布局有【三套互相打架的表】**：`MEMORY-LAYOUT.md` / `ARCH-engine-datalink.md`
   （PARAM_STAGING 0x2040、STATE_STAGING 0x3040）/ `CORE-ENGINE.md`（PARAM 0x1840、STATE 0x2840）
   ⇒ **必须以 `src/engine.h` 为唯一权威**，另两份改为"引用"。
2. **★★★ 架构四件套停在 09-12**，而 09-13/14/15 的 VTOR→ITCM 修复 + ISR 闸门、IWDG、
   **持久化降级 NAK**、RTC、步进/AS5600/运动品质**全部未回填**。`STATUS-*` 只到 09-12。
   `README.md` 停在 `7911dd3`/09-13，**落后约 30 个提交**，且**三处作废宣称**
   （195 PASS 已无处复跑；persist"已打通"与"NAK"**同文档自相矛盾**；"拍周期抖动 0"易被读成"输出零抖动"）。
3. **★★ 8 份文档需要立刻加"过时/已修正"横幅**。优先级前 4：
   `MEMORY-LAYOUT.md`（整段建立在已推翻的七级链 + "硬件锚定"上，且"建议新增 `OFF_DO_SHADOW`"**与交付方向相反**）、
   `FRAMEWORK-MAP.md`（§3.1 P3-C 仍称"输出沿=拍头"）、
   ★ `docs/AUDIT-stepper-no-rotation-2026-09-15.md`（**我写的送审稿**，把 C1 排第 1、断言 AS5600 已排除，
   **已被全面推翻** —— 最危险的一份）、
   `docs/ASSESS-architecture-as-controller-2026-09-14.md`（P0-1 标"已修复验收"但已被换档架空）。

### ★★ 十条已作废结论的共同根因族（改革要立的第一条规）
**「同一个语义两处存放 / 一个常量两个语义 ⇒ 静默失效」**
成员：`MB_MAX_FRAME`、`USART1 BRR 16×`、`ADC PCSEL`、ISR 调用树漏 `DCL_ITCM`、
定时器 DMA（路由+门控分开）、`step_tick` 的 `dt/10`、限时里 `0` 双语义、
DWT 被 pyocd 停（空判据）、C1（两个驱动者+门只守一处）、**SD 卡两个写者**。

### ✅ 已执行（S1 起手，两项，零行为变化 + 构建已验证零警告）
1. `docs/AUDIT-stepper-no-rotation-2026-09-15.md` 顶部加**作废横幅**（列明哪条被谁推翻、哪些仍成立）。
2. `src/sd.c` 头部注释更正：原写"写在末尾区 LBA3000000"，**实为 LBA1..capacity-1 环形回卷**；
   并标注 `sd_dump_write()` 写 LBA3000000 **落在日志区内**（同卡双写者，破坏性）。

---

## ★★★ 用户要求把「架构使用规约」写进 memory（2026-09-15 23:07）

### 已写入 `.workbuddy/memory/MEMORY.md`（**该文件在仓库内、会被提交，属共享契约**）新增 §九
1. **9.0 一句话：用这个系统 = 写 DCL 程序，不是改固件。** 固件是平台（烧录=换机器），程序是资产（通信上传+持久化）。
2. **9.1 四层 + 层间判据**（怎么算→③ / 和外设怎么说话→② / 要新每拍行为→① / 只给上位机看→SHM+`obs_anchor`）。
3. **9.2 做实验必须按架构做（七条）**：观测走协议内通道 → **pyocd 是最后手段**；
   控制走程序面（`0x10`+`0x11`）不是怼寄存器；要新能力先判层、**不要默认改固件**（I2C 的弯路）；
   **每拍动作进 ISR/引擎，服务性动作进主循环 —— 别再往主循环加实时量**（主循环一圈 1.28~1.54 ms vs 拍 100 µs）；
   实验代码独立成 patch；运行期改动复位即恢复；每条判据要能失败 + 配对照。
4. **9.3 pyocd 纪律**：会静默杀 DWT（用前 `0x39 op=7` 重新校时，Δ=0 判无效）；
   读全局量前**绝不能 reset**；只读用 `connect_mode=halt`；收尾必须 `-c go`；能用协议表达就不用它。
5. **9.4 ★ 标准操作替代表（8 行）**：pyocd 摇引脚→`op=19 sub=7/8`；pyocd 改 TIM3→`sub=1`（报实际频率）；
   pyocd 读全局量→`0x38`+`0x22`；加协议 op→SHM+`obs_anchor`；反复 start/stop→固件定长原语；
   改固件支持新传感器→②层外设能力；直改持久化扇区→`0x43`（现 NAK）→改走 SD；
   **用"无脉冲时引脚电平"判脉冲→必须在 `CC1E=1` 时判且看 `IDR`**。
6. **9.5 三条红线**：程序里不得出现物理引脚号/总线地址；不得破坏单写者（TIM3 两个驱动者=C1、**SD 卡两个写者**）；
   不得把实时量挂在主循环上。

### 已写入 `~/.workbuddy/MEMORY.md`（用户级、跨项目）新增一节
**「观测手段纪律：先找不影响被测对象的入口」** ——
入口优先级（协议/无副作用探针 → 外部仪器 → 调试器最后）；
调试器的三个隐性代价（静默停 DWT、复位清证据、会话边界有语义）；
通用原则 **"先做一个可复用的观测入口，再用它"**；用完必须**核对**已恢复；
两条"看起来在测其实没测"的形态（**空判据** / **量纲·口径错**）。

### 下一步自然动作
把 §九 的第 9.1/9.2/9.4 三节**落进仓库契据** `REF-program-contract.md`（S0）——
memory 是给"我和后续会话"看的，仓库文档才是给"第三方审计"看的。

---

## ★★★ 标准化改革正式开工（用户："继续吧"）2026-09-15 23:35+

### ✅ S0 —— 契据 `docs/REF-program-contract.md`（新建，v0 待批准）
内容：三条不可协商要求（R1 程序可通信上传且必须正确 / R2 核心与程序物理隔离 /
R3 双方能互认版本与能力）→ 四层 + **层间判据表** → 每层的允许/禁止 →
①核心对外只承诺三样（`fw_ver` / 能力位 / 命令表+SHM，**SHM 以 `src/engine.h` 为唯一权威**）→
②外设能力（**边界 = 程序里不得出现任何引脚号/总线地址**，三边权威确认；三种形态 A/B/C；
**身份四元组**照抄 CiA 301 `0x1018`；新增能力 5 步流程；设备描述=我们的 ESI）→
③程序（**事务式上传 BEGIN/DATA/COMMIT**；`.dclp` 清单含 `min_fw`/`required_caps`/`crc32`；
**装载时重跑与上传时同一套静态校验**）→ ④工程 →
**SD 分区契约**（LOGHDR/LOG(受 `LOG_END` 限)/PROG_A/PROG_B/保留；向后兼容规则）→
**五道闸**（含每道的对照做法）→ 判据纪律 →
★ **§9 GAP 表 = S3..S7 的待办清单**（10 条，其中 GAP-1/2/4 是 🔴 P0）。

### ✅ S2 —— `docs/STATUS-2026-09-15.md`（新建）
补上 09-12 之后**断档的三天**。含：交付档关键开关表（`DCL_DO_LATCH=0` /
**`DCL_PERSIST_SAVE=0`** / `DCL_VTOR_ITCM=1` / `DCL_WDT=1`）、**现行权威入口表**
（"想知道什么看哪一份"）、三天发生的事（09-13 可靠性一档 / 09-14 推翻硬件锚定 /
09-15 伺服 + 标准化）、**缺陷总账**（D1–D10 / C1–C7 / GAP-1,2,4）、
**三处"文档会骗人"**、未决项 7 条、下一步。
★ 口径声明：**不引用"12 套 195 PASS"这类已无处复跑的旧总数**；
**不把"已修复"当"可用"**（凡修复先问"在当前交付档下成立吗"）。

### ✅ S1 —— 15 份文档横幅**已完成**（worker `general-purpose-1`，我已核）
**只改文档**（`git status` 核实：仅 14 份 docs + `README.md`，`src/` 无 worker 改动），
共 **122 insertions / 2 deletions**，横幅全部插在首个 `#` 标题之后、长度 ≤18 行（README 13 行）。
已抽查两份（`MEMORY-LAYOUT.md` 与 `README.md`）质量合格：**逐条给"本文的说法 / 现状 / 依据"，
并明确列出"仍成立的部分"**。
- README 除横幅外有 **5 处就地标注**（195 PASS 那处在 bash 代码块内用 shell 注释；
  persist 矛盾处在正文后续行；拍周期口径在指标表与阶段 1 两处）。
  ★ worker 多标的两处（第 642 行 persist"已修好并实测可用"、第 169 行"拍周期抖动 = 0"）
  **属同款问题，我认可保留**。
- worker 报的 4 处偏差（T26 无 §5 横幅 ⇒ 合并成顶部一个；FRAMEWORK-MAP §3.2 现值列本就已是 0；
  ASSESS P0-2 摘要表与 §标题措辞不一致；README 同类表述另有两处）**全部处理得当，无需返工**。

### ⏭️ 下一步 S3（SD 分区，第一刀动代码）
设计已定：日志头加 `LOG_END` 扩展字段（放在 `h[0..15]` 之外的扩展段，**不升 `LOG_VERSION`**——
升版本会触发 `sd_log_open` 当新卡重建 ⇒ 从 LBA1 覆写毁掉卡上已有记录）；
**旧头无该字段 ⇒ 读回 0 ⇒ 退化为 `capacity-1`（行为不变、不毁卡）**；有它 ⇒ 日志在 `LOG_END` 折返。
判据（能失败）：① 旧卡行为不变 ② 把 `LOG_END` 故意设小 ⇒ 写入必须在折返而不越界
③ 对照：故意不设 `LOG_END` ⇒ 仍然绕整卡（证明字段真的在起作用）。

### ⏸ 未做（等用户发话）
**提交**：本地 HEAD 仍是 `979ebb2`（= `origin/main`），今天的产物**全部未提交**。
建议分两笔（都用 `git add <具体路径>`，绝不用 `git add -A`）：
① `docs(standard): 程序契约 + 标准化纲领 + STATUS-09-15 + 15 份过时横幅`；
② `feat(step/as5600): 闭环伺服组件 + op=19 诊断 + sd.c 注释更正`。

---

## ★★★ S0–S7 实施记录（2026-09-15 23:46 起）

### ✅ 已提交（两笔，工作区当时干净）
- `686fb88 docs(standard)!` —— S0 契约 + S1 横幅 + S2 状态文档（27 文件，未碰 src/，已核）
- `f732ede feat(step/as5600)!` —— 闭环伺服线（23 文件）
提交前已按纪律 `cp -r .git ../.git_bak_2026-09-15`（11MB）。

### ✅ S3 SD 分区 —— 已实现
`src/sd.c` + `sd.h`：日志头扩展段 `h[112]` = `LOG_END`、`h[113]` = 魔数 `"LEND"`；
`sd_part_layout(cap)` 由**容量**算出卡尾两区（`SD_PROG_BLOCKS 16` 块/副本 = 8KB）；
**旧头无该字段不影响任何既有校验**（`h[0]/h[1]/h[2]/h[6]` 四条判据照旧）⇒ 向后兼容。
★ 关键防护：**`s_log_blk` 必须钳位**（旧卡游标可能超出新上限 ⇒ `cap_data - s_log_blk` 下溢 ⇒ 写飞出卡外）。

### ✅ S5 的"单源"重构 —— 已实现
`main.c`：把 `h_deploy` 的全部静态校验**抽成 `prog_validate()`**，
上传路径与装载路径都调它（契约 §7 闸5 的全部意义就是"同一套"，不能是两份实现）。
`persist.c`：`crc32_ieee` → 导出 `dcl_crc32`（prog_store 复用，不写第二份）。

### ✅ S4 `prog_store` —— 已实现（未验证）
`src/prog_store.{h,c}`：A/B 双副本 + 单调 seq + CRC32 + **写完回读 memcmp**（闸3）+ **头最后写**；
`boot_load` 调上层传入的 `prog_validate`（闸5）。载荷缓冲 7680B，**.bss 落 0x20003dc4，DTCM 余量充足**。

### ✅ S7 macro 守卫收紧 —— 已实现（worker 做，未验证）
读宽写窄：写只允许 SENSOR/ACTUATOR/WIRE 三区（用 engine.h 宏算区间），其余 -2；
新增 `volatile const uint32_t g_macro_realtime = 0u`（worker 自己发现 `--gc-sections` 会回收 `const`，加了 pin）。

### ❌❌❌ S5 的协议层与开机装载 —— **失败：交付固件进复位循环**

**现象**：烧入我的构建后，串口上**每条命令都只返回开机 banner**（`c1 00 04 00 0002f70d f4c2`）。

**SWD 取证（connect_mode=halt，不复位）**：
| 量 | 值 | 判读 |
|---|---|---|
| `g_sd_capacity_blocks` | `0x03A3D000` = 61067264 | **32GB 卡在位** ✓ |
| `g_sd_part_ok` | 1 | 分区算出来了 ✓ |
| `g_prog_boot_rc` | 5 = `PROG_RC_NONE` | "卡上没有已存程序" —— **符合预期** ✓ |
| `g_stage` | 7 | "ISR 在跑"的正常值 ✓ |
| `g_loop_entered` | 1 | 到过主循环 ✓ |
| `g_banner_count` | **1** | 读到的永远是 1 ⇒ **每次读都是刚复位** |
| `g_cmd_count` | **0**（发 3 条命令后仍 0） | ★ **命令从未被分发** |
| `g_tick_count` | **4839 → 1993（下降）** | ★★★ **板子确实在复位** |
| `g_reinit` | 0 | 主循环计数不动 |

**A/B 对照（已做，成立）**：`git archive f732ede` 导出上一提交 → 构建 → 烧录
⇒ `0x38` 连读 3 次都返回 **39 字节** ⇒ **基线健康；是我的改动导致复位循环。**
⇒ **板子已烧回基线**（当前可正常工作）。

**最强嫌疑（尚未隔离）**：
1. 第一次 `0x48` 之前，`0x01/0x38/0x4A` **都是正常的**（探针 1：4B/39B/44B）
   ⇒ 是 **`0x48 → h_prog_status → prog_store_probe → sd_read_block`** 把系统打坏的；
   之后**连老命令 0x38 也不再应答** ⇒ 伤害是**持久**的。
2. 因此重点怀疑：**从协议上下文读 SD**（`copy_hdr` / `copy_payload` 连续读最多 30 块）
   —— 可能与 `sd_log_poll` 的写路径或 SDMMC 状态机冲突，或耗时超看门狗窗口。
   ★ 而看门狗由 **ISR** 喂（`DCL_WDT_FEED_GATE=1`）⇒ **主循环卡死也不会复位**这一点需要重新核实。
3. 次要嫌疑：开机 `prog_store_boot_load` 也在 SD 上下文中跑（但它返回 5 就退出了，没读载荷）。

**下一步（受控二分，每一步都可回退）**：
- 先**注释掉开机 `prog_store_boot_load` 那一小段** → 烧 → 看链路是否恢复；
- 再**只保留 0x4A、去掉 0x45–0x49 的分发** → 烧 → 定位是"装载路径"还是"协议 handler"；
- 若定位到 `prog_store_probe` 的 SD 读：**先不读 SD**（只用 `g_sd_part_ok` + 头块的 1 次读），
  把"体检"降级为"只读 2 个头块（2 次 `sd_read_block`）"，避免连续 30 块读。

★ **纪律提醒**：本次交付**不能宣称完成**。S3/S4/S5/S7 的代码都在工作区、构建零警告，
但**"构建通过"≠"功能可用"**——这正是本项目记了一整本的那句话。
工作区当前**未提交**，基线固件在板上。

---

## ★★★★ 根因定案（2026-09-16 00:40）—— **SD 驱动的一个潜伏 bug：`sd_read_block` 的数据通路没关**

### 事故链（全部有取证）
1. 我的程序存储在**开机**要读 SD（`info_scan` 2 块 + `prog_store_load` 2 块），
   于是 **"第一次多块写之前的最后一次 SD 操作是【读】"** —— 基线从未出现过这个转移
   （基线的 `sd_log_open` 是"读 LBA0 → 写 LBA0"，紧接着就是写）。
2. `SD_DIAG[38] STA@CMD25 = 0x00081000` ⇒ **bit12 `DPSMACT` + bit15 `RXFIFOHF` 仍置位**
   ⇒ 上一次读的 DPSM 还在跑，本次写的 DPSM 启动不了。
3. `SD_DIAG[39] STA@DATAEND = 0` ⇒ 既非 DATAERR 也非 DATAEND ⇒ **等 DATAEND 超时**。
4. `SD_DIAG[58] blk_this_boot = 0` ⇒ 本次上电一个块都没写成功。
5. `sd_log_poll` 每轮卡在那里 > **1.2 s**（`OFF_WDT_STAT+24 = 12000 拍`）
   ⇒ 撞上 `DCL_LOOP_RESET`（停滞 ⇒ `eng_outputs_safe()` + `SYSRESETREQ`）。
6. **复位循环**：`g_loop_reset_cnt = 20`、`g_loop_hb = 402`（基线 106584）、
   被动监听 3 s 收到 **20 字节 = 开机 banner ×2**（周期 ≈1.5 s = 1.2 s 停滞 + 0.3 s 启动）。

### 已做的修复与结果
- 新增 `sd_data_path_idle()`：**DPSM disable → 等 `DPSMACT` 落下 → 清静态标志**
  （照抄 ST HAL 的顺序），在 4 处数据传输前调用。
- 同时把 `prog_store_probe` 改成**只回缓存、零 SD 访问**（体检只在开机扫一次）。
- **效果**：**复位循环消除**（被动监听 3 s = **0 字节** ✓），
  但**变成永久卡死**（`find_board()` 失败、3 s 窗内 0 回应）
  ⇒ **`sd_read_block` 自身还缺 HAL 的读收尾序列**（排空 RX FIFO、确认 DATAEND、
  关 CMDTRANS/DPSM、清标志）——本次只补了"进入前停干净"，没补"读完之后收干净"。

### 下一步（唯一正确的方向）
**把 `sd_read_block` 逐句对齐 ST `HAL_SD_ReadBlocks` 的 polling 路径** ——
这正是本项目自带 skill **`baremetal-periph-align-to-vendor-ref`** 的方法论
（"厂商已验证例程当权威实现，逐句比对找偏离"）。
★ 判据：`SD_DIAG[38]` 的 `DPSMACT/RXFIFOHF` 在读完之后必须为 0，且紧接着的多块写能拿到 DATAEND。

### ★★ 我自己犯的两个方法学错误（都影响了结论）
1. **`build.sh | grep ... && flash`** —— `grep` 的退出码掩盖了构建失败
   ⇒ 把**旧镜像**又烧了一遍，并据此误判了一轮。
   ⇒ **纪律：绝不用管道给构建"把关"；要看 `$?`，或把构建输出落盘后再判。**
2. **用错版本的 `nm` 读 SWD 地址** —— 板上是基线，却用我那次构建的符号表
   ⇒ 读到的值**看着合理但属于别的变量**。
   ⇒ **纪律：读运行期内存前，先确认"符号表版本 == 板上固件版本"。**

### 当前状态
- **板子 = 基线固件**（`f732ede`），已验证 `0x38` 连读 4 次都是 39 字节 ✓
- 我的 S3/S4/S5/S7 改动**在工作区、未提交**；构建零警告但**功能未验证**

---

## ★★★★★ 2026-09-16 01:00 —— **修复成功一半，并挖出一个潜伏的 SD 驱动 bug**

### ✅ 已修好并**验证通过**的
1. **`sd_read_block` 按 ST `HAL_SD_ReadBlocks` 的 polling 路径逐句重写**（见 skill
   `baremetal-periph-align-to-vendor-ref`）。旧实现三处偏离厂商，**三处都指向"读完之后数据通路没收干净"**：
   - 轮询条件用 `STA_DATAOK = DATAEND|DBCKEND`，而 **DBCKEND 是"每块结束"就置位**；
   - **开了 IDMA 却从不排空 RX FIFO**，而厂商 polling 路径**根本不用 IDMA**（它是"见 `RXFIFOHF` 就手工读 8 个字"）；
   - 收尾只关 `CMDTRANS`，**不清残留 FIFO、不等 DPSM 落下**。
   ⇒ 读完之后 `SD_DIAG[38]` 里 `DPSMACT(bit12)+RXFIFOHF(bit15)` 仍置位 ⇒ 紧接的**多块写**等不到 DATAEND。
   **新实现**：进入前 `sd_data_path_idle()` + 按 HAL 的 while 手工排 FIFO + **收尾再排一次 + 清标志 + 等 DPSM 落下**。
2. **`prog_store_probe` 改成零 SD 访问**（缓存式，只在开机扫一次）。

### ✅ 用**可靠客户端**验证通过的判据
★ 客户端用的是 `docs/audit/h723_motion_probe.py` 的 **`Dut`**（已加 CRC16 校验 + 错位重找）。

| 判据 | 结果 |
|---|---|
| 被动监听 3 s | **0 字节** ⇒ **复位循环消除** ✓ |
| `0x38` × 3 | 39/39/39 字节 ⇒ 老命令无回归 ✓ |
| `0x4A DEVICE_DESC` | ACK 44B，`fw=0x0200 caps=0x0DF7` + 6 条设备表**逐条正确** ✓ |
| `0x48 PROG_STATUS` | ACK 64B：`part_ok=1, ab_valid=0, active=无, last_rc=5` ✓ |
| **闸2**（篡改 1 字节） | `COMMIT → NAK "crc mismatch"` ✓ |
| **闸5**（结构非法 `nr=100` 而载荷 6B） | `COMMIT → NAK "payload short"` ⇒ **上传期就拦** ✓ |
| 独立判据：`0x10 DEPLOY` 受理同一份载荷 | `ACK, deploy_seq=1` ⇒ 我的测试载荷**确实合法** ✓ |

### ❌ 仍未通过：`COMMIT` 的**落盘**（SD 写 + 回读）
`BEGIN → ACK`、`DATA → ACK`、**`COMMIT` 返回 4 字节假应答，之后板子完全不应答**。
SWD 侧 `txn_commit` 保持 0 ⇒ **COMMIT 的 handler 没走完**。

**最强嫌疑（有据）**：SD 的等待是**按循环计数**的 —— `#define SD_TMOUT_DATA 40000000u`
⇒ 一次失败等待就约 **500 ms**；而 `prog_store_save` 要"写 1 块 + 读回 1 块"，
两次超时叠加 > **12000 拍 = 1.2 s** 的**主循环停滞阈值** ⇒ 又触发 `DCL_LOOP_RESET`。

**★ 项目自己已经有正解**：`block_window_begin(PERSIST_BLOCK_TICKS)` / `block_window_end()`
—— `persist_save` 用它声明"我要阻塞 1.55 s，别判我停滞"。
⇒ **程序存储的 SD 操作必须用同一个机制包起来**（这是"已知阻塞窗口内不判停滞"那条既有纪律的应用）。

### ★★ 两个上位机工具**都不可信**（这是本次最大的方法学发现）
1. `tools/h723_client.py` 的 `Dcl.send()` **会返回假 ACK**（帧错位）——
   实测：它报 `BEGIN/DATA/COMMIT 全部 ACK`，而固件侧 `txn_begin/commit/abort` **全是 0**
   ⇒ **它报的 ACK 根本不是我的 handler 发的**。
2. 我自己临时写的裸读器在一次运行里对**所有**命令都收不到东西。
⇒ **纪律：验收必须用 `docs/audit/h723_motion_probe.py` 的 `Dut`（带 CRC 校验）；
   并且"ACK"必须与固件侧的计数器对账，不能只信客户端说 ACK。**

### ★ 下一步（按优先级）
1. **把 `prog_store_save` / `prog_store_load` / `info_scan` 包进 `block_window_begin/end`**。
2. **把 SD 的等待从"循环计数"改成"拍计数"**（`SD_TMOUT_DATA` 4000 万次循环 ≈ 500 ms，不可控）。
3. **修 `h723_client.py` 的 `send()`（加 CRC 校验 + 错位重找）** —— 否则后面每一次测试都在骗人。
4. 然后再跑五道闸的 3/4 与"上传→复位→开机自动装载"端到端。
5. 最后才谈提交。

---

## ★★★★ 2026-09-16 01:20 —— 已做 1/2，卡点转移到"计数对不上"

### ✅ 已做
1. **阻塞窗口**（S5 落盘的 SD 操作全部包进来，与 `persist_save` 同一机制）：
   `PROG_BLOCK_TICKS = 20000u`（2 s）；包了 `h_prog_commit`(save) / `h_prog_erase` / 开机 `boot_load`。
2. **`SD_TMOUT_DATA` 40000000 → 2000000**。
   ★ 算清了机制：每次循环读一次外设寄存器 ≈10~20 周期 @400MHz ⇒ **原值单次超时 ≈1~2 秒**，
   而停滞阈值只有 **1.2 s** ⇒ SD 一超时就必然触发"主循环死了"⇒复位。**这才是精确机制。**
3. **`prog_store_save` 的载荷写入从 `sd_write_multi` 改成逐块 `sd_write_block`(CMD24)** ——
   多块写在本项目里**只被日志以 128 块大批量用过**，"1 块的多块写"是未验证路径。

### 效果
- **复位循环仍然没有**（被动监听 0 字节 ✓）、`0x38` 正常 ✓
- **`COMMIT` 不再卡住**：现在会返回（NAK）⇒ handler 至少走完了 ✓

### ❌❌ 新的、也是最关键的卡点：**客户端报的 ACK 与固件侧计数对不上**
| 量（SWD 直读） | 值 |
|---|---|
| `g_prog_txn_begin / commit / abort` | **0 / 0 / 0** |
| `g_prog_ok_n / fail_n` | **0 / 0** |
| `g_prog_last_rc` | 5 |
| `SD_DIAG[13]`（写失败 STA） | **0x00001004 = CTIMEOUT(bit2) + DPSMACT(bit12)** ⇒ 卡**没响应命令** |
| `g_sd_prog_a` | 0x03A3CFE0 = 61067232 ✓（分区值正确）|

**而可靠客户端（`Dut`，带 CRC）报 `BEGIN → ACK`。** ⇒ **矛盾未解。**

★ 这正是我上一轮刚写进 memory 的那条纪律：**"ACK"必须与设备侧计数对账**。
现在两边的账对不上 ⇒ **在解开之前，任何"协议测出来的结论"都不能采信**
（包括上一轮用 `Dcl` 客户端得到的"闸2/闸5 通过"—— **那两个结论现在要重新判定为待验证**）。

### 下一刀（唯一动作，很便宜）
**单帧验证**：SWD 读 `g_prog_txn_begin` → 用 `Dut` 发**一帧** `0x45`（带合法清单）→ 再读。
- 若计数 +1 ⇒ handler 真的跑了，之前是**测试脚本/时序**的问题；
- 若不变 ⇒ **`0x45` 没被分发到我的 handler** ⇒ 回去查 dispatch（而 `0x48`/`0x4A` 是通的，说明 switch 本身没问题）。
★ 注意：上一次尝试这个测试时**壳里 `python` 没找到、帧根本没发出去**，所以那一轮是无效实验
（又一次"看起来测了、其实没测"）。

### 板子状态
当前板上是**我这版固件**（未被砖：`0x38`/`0x48`/`0x4A` 都正常应答）。基线随时可用 `../dcl_base` 烧回。

---

## ★★★★ 2026-09-16 10:40 —— 又一次被自己的仪器骗了；真问题在 **SD 子系统整体**

### ★★ 血的教训 #1: **pyocd halt 会把核留在停住状态**
前面那一长串"`g_tick_count` 冻结 / ISR 死 / `g_cmd_count=0` / 板子不复位"的结论
**全部是假的** —— 是我连日几十次 `pyocd halt` 之后**核没有真正 resume**。
**干净 `pyocd reset` 后不发 halt，串口连发 6 次 `0x38` 全部 39 字节** ✓。
⇒ ★★★ **这正好违反我自己刚写进 memory 的第 1 条纪律**："观测先走协议内通道, pyocd 是最后手段"。
⇒ 而且 **`0x48` 的应答本身就带 `txn_begin/commit/abort`**，本来完全不需要 SWD。

### ✅ 全程串口拿到的**真**结论（无 pyocd 参与）
```
0x38 -> 39B ✓     0x48 -> 64B ✓（part_ok=1）
BEGIN -> ACK（应答 36000000 = total_len 54）  ← handler 真的跑了
DATA  -> ACK
COMMIT-> NAK "store failed"
txn = BEGIN 1 / COMMIT 0 / ABORT 1   ← 与协议层**完全对账**
last_rc = 3 = PROG_RC_WRITE
```
⇒ **事务、handler、计数、拒绝语义、闸2(CRC) 全部正确。** 只剩"写不进卡"。

### ✅ 这轮真正修掉的三个 bug
1. **`sd_data_path_idle()` 被我 `replace_all` 误插进 3 处"写"路径** ⇒ 日志写挂。已回退（只在读路径保留）。
   ★ 纪律：**修一个转移，不要顺手改另一个已经好的转移。**
2. **缓冲必须在 AXI 且在对的那块 AXI**：
   - 先发现 `s_pay/s_blk` 在 **DTCM(0x20003xxx)** ⇒ SDMMC 的 **IDMA 读不到 TCM 端口** ⇒ 写失败。
   - 改用链接器 `.axi_buf` 后**又撞上**：`.axi_buf` 落在 `0x24000000`，而**低 16KB 是固定地址诊断结构区**
     （`SD_DIAG=0x24000200` `BB_DIAG=0x24000300` `SD_CFG=0x24000400` `BOOT_AXI=0x24000500`
     `LATCH_SNAP=0x24003000` `LNODE=0x24003100`）⇒ **`s_pay` 正好是 `SD_DIAG` 本人**
     ⇒ 每次 SD 操作都在写我的载荷 ⇒ NAK "crc mismatch"。
     ★ `blackbox.h:8` 早写了"避开低 16KB 预留"，**但链接器不知道**。
   - ⇒ 改**显式地址** `0x24024000`（黑匣子环之后、`SD_STAGE` 之前，112KB 空闲）
     + **构建期 `#if/#error` 硬判据**（地址落进保留区就编译不过）。★ 之后 CRC 关**过了** ✓
3. `SD_TMOUT_DATA` 两次调值记录：4000万(≈1~2s, 太大→复位) / 200万(≈50~100ms, 太小→正常写被判超时)
   ⇒ 取 2000万 + **由调用方阻塞窗口兜底**。★ 教训：这类值**两边都有坑**，
   正解是"窗口开够 + 超时给足"，不是把超时往小里挤。

### ❌❌ 真问题（比程序存储大得多）：**SD 子系统整体不工作**
经协议 `0x20 READ` 读 `SD_DIAG`（★ **无需 pyocd**，`0x24000000..0x24000600` 是放行窗口）：
| 项 | 值 | 判读 |
|---|---|---|
| `[45]` 1续写/2新建 | **2** | 日志头对不上 ⇒ **当新卡重建** |
| `[63]` batch_err | **3099** | **连续 3099 次批写失败** |
| `[58]` blk_this_boot | **0** | 本次上电**一个块都没写成功** |
| `[55]` polls | 3089 | 每轮都在失败 |
| `[47]` 1+blk | 1 | 日志游标从没前进 |
| `[53]` last_rc | 0xFFFFFFF8 | 负错误码 |
| `[13]` 写失败 STA | 0x1004 | CTIMEOUT + DPSMACT |
| `[37]` CMD25 rc | 0xFFFFFFFE | `sd_cmd` 失败 |
| `[28]` 读 STA | 0x29020 | **RXOVERR + DPSMACT + RXFIFOHF + RXFIFOF** ⇒ 读侧 RX 溢出、FIFO 满 |

⇒ **日志（基线里一直在正常工作的那条路）现在也一个块都写不进去。**
⇒ **这不是程序存储的问题，是 SD 驱动/卡的整体问题**；程序存储只是第一个把它暴露出来的调用者。
⇒ `[45]=2`（当新卡重建）也说明**卡上的日志头已经对不上了** —— 可能被我前面几轮坏固件
  （`.axi_buf` 撞 `SD_DIAG`、复位循环）搞坏了。

### 下一步（**必须换方法**）
**把 SD 当成"完全没通"重新 bring-up**，而不是"基本能用的东西加个功能"：
1. **只读工具**：全程用 `0x20 READ` 读 `SD_DIAG`（协议内通道，零扰动）——
   不再用 pyocd（它会停核，已骗过我一次）。
2. **把 `sd_init` / `sd_read_block` / `sd_write_block` 逐句对齐商家例程**
   （`Hardware Official Documentation/板子资料/【1】参考例程/9.SDMMC-SD卡基本数据读写/`，
   skill `baremetal-periph-align-to-vendor-ref` 的方法论），**先让"读一块 → 回读比对"这条最小闭环成立**。
3. 判据顺序：`sd_init` 成功 → 读 LBA0 得到**已知**内容 → 写 LBA1 再读回**逐字节相同**
   → 才谈日志/程序存储。**不许跳步。**
4. 卡本身要复核（`RXOVERR` 也可能是卡/接线退化）。

### 板子现状
板上是我的固件，**协议层健康**（`0x38`/`0x48`/`0x4A` 正常应答，`0x20 READ` 正常）。
基线在 `../dcl_base`，随时可烧回。

---

## ★★★★★ 2026-09-16 11:00 —— **S5 程序持久化打通！根因是我自己那版"读实现"**

### ① 决定性对照（**方法：基线 vs 我的改动，只看 SD_DIAG，全程协议**）
| 项 | 基线 | 我的改动 |
|---|---|---|
| `[58] blk_this_boot` | **5632** ✓ | **0** ✗ |
| `[63] batch_err` | **0** ✓ | **3099** ✗ |
| `[45]` 续写/新建 | **1**（续写）✓ | **2**（当新卡重建）✗ |

⇒ **SD 路本身是好的，是我的改动把它搞挂了。**

### ② 真凶：**我按 HAL 骨架重写的 `sd_read_block` 返回的数据是错的**
`[45]=2` 是决定性指纹：`sd_log_open` 读 LBA0 没读到有效头 ⇒ 判"卡不对"⇒ 重建日志
⇒ 再写又失败。**⇒ 是读出来的数据不对。**

**「对齐厂商例程」要整条路径一起对齐**（含 IDMA/缓存/时钟前提）；
**只照抄 while 循环的骨架、换掉其下的数据搬运机制（IDMA → 手工排 FIFO），是半个对齐 = 更坏。**

### ③ 修法：**回退成原实现（保留 IDMA）+ 两处最小外科修复**
1. **只认 `DATAEND`，不再认 `DBCKEND`** —— `DBCKEND` 是"每块结束"就置位，单块读会提前收工；
2. **读完之后显式把数据通路关干净**（`SD_DCTRL &= ~DTEN` → 等 `DPSMACT` 落 → `SD_ICR = STA_STATIC`）
   —— 这半正是原来真正缺的（"读完 → 紧接多块写"会卡死的原因）。
★ `SD_TMOUT_DATA` 回 40000000（**不许再挤小**：200 万/2000 万都让正常批写被判超时；正解是"超时给够 + 调用方声明阻塞窗口"）。
★ `sd_data_path_idle()` 已 `unused`（保留注释与函数体作档案）。

### ④ 修好后的验收（**全程串口，零 pyocd**）
```
SD_DIAG: [13]写失败=0  [28]读STA=0x100(DATAEND)  [45]=1续写  [47]1+blk=8147201
         [58]blk_this_boot=6144 ✓  [63]batch_err=0 ✓      ⇒ 日志**恢复**
程序持久化:
  第1次 BEGIN=ACK COMMIT=ACK(rc=0)   第2次 BEGIN=ACK COMMIT=ACK(rc=0)
  后: seq A/B=1/2   CRC A=0x22479A09 B=0x39659650   len A/B=54/86
      生效=B   txn=2/2/0   last_rc=0        ⇒ **闸2/3/4/5 全部通过** ✓
  0x38 复检 39B ✓ 无回归
```
★ **A/B 轮换正确**（第1次写 A、第2次写 B，生效取 seq 大的 B）；**回读比对(闸3)、单调 seq(闸4) 都验证到位**。

### ⑤ 最后一处待收口：**开机自动装载**
复位后 `0x38`: `n_routes=0`、`run=1`、`deploy_seq=1`；`0x48`: `seq A/B=1/2 生效=B`（**开机扫描读到了两份 ✓**）。
- `deploy_seq=1` ⇒ **我的开机装载那段确实跑了**（`g_deploy_seq++` + 写 `OFF_CTRL_DEPLOY_SEQ`）；
- 但 `n_routes=0` ⇒ **staging 的结果没反映到 ACTIVE 路由数**（可能 RELOAD 没生效/没换表）；
- **`run=1` 也不该是 1**（复位后应是 0）⇒ 要么 ENGIN_RUN 被别处置了，要么我读的偏移含义与 S3 语义不同。
⇒ 下一步：查 `engine_stage_program` 之后 RELOAD 有没有被置、以及 `OFF_CTRL_N_ROUTES` 是谁写的。

### 板子现状
板上是**修好的我这版固件**（日志在写、程序持久化可用、无回归）。基线仍在 `../dcl_base`。

---

## ★★★★★ 2026-09-16 11:30 —— **S3–S7 完整交付，开机自动装载成立**

### 最终验收（全程串口，零 pyocd）
```
上传: BEGIN=ACK COMMIT=ACK (rc=0, budget=224)
上传后: seq A/B = 3/4
复位后: boot_rc=0  boot_loaded=1  **deploy_routes=4**
        载荷头 040001000000... nr=4 flags=0x01
        0x38: **n_routes=4**  deploy_seq=1  applied=1
SD 日志: blk_this_boot=6144  batch_err=0
```
⇒ **通信上传 → SD A/B 双副本 → 上电自动装载进引擎，全链通。不过 flash 烧录。**
⇒ 闸2(CRC 篡改拒)/闸3(回读比对)/闸4(A/B+单调 seq)/闸5(上传期与装载期**同一套**校验) 全部通过。

### 促成它的两个"同一语义两处存放"修复（都是我自己犯的）
1. **开机装载漏了 `SHM_U8(OFF_CTRL_RELOAD) = 1`** ⇒ staging 写好了但永不换到 ACTIVE
   （指纹：`deploy_seq=1 applied=1` 而 `n_routes=0`）。
   ⇒ 抽成 `eng_apply_program()`，与 `h_deploy` **共用一份**。
2. 缓冲区地址撞进黑匣子环（详见 §十·10.2 的 AXI 地图）。

### 提交（工作区干净）
```
686fb88 docs(standard)!  S0–S2          f732ede feat(step/as5600)!
30e0ad2 feat(prog)!      S3–S7         22dbbfb docs(standard)! 收口
70ddbc2 docs(arch)       §5.2 命令表补 0x45–0x4A
```
★ 本地 HEAD = `70ddbc2`；`origin`(H723PLC) = `979ebb2`（**落后 5 个**）；`dcl` = `070d9f3`（落后 7 个）。
★ **未推送** —— 推 GitHub 属对外动作，等用户明确说了再推。

### 未闭合（已写进 `docs/STATUS-2026-09-16.md` §6）
`0x49 PROG_ERASE` 曾报 erase failed 未复测 ·
能力位仍 u16 · 程序面"不得出现引脚号"的静态检查 · 通用外设事务 op · **S3–S7 未跑 12 套回归**。

---

## ★★★★★ 2026-09-16 11:40 —— **回归跑完（零回归）+ 已推送两个远程**

### 1. 12 套回归（**关键：必须烧回基线跑同一批，否则"多少 PASS"没有意义**）
| 套件 | 基线 | 本版 | 判读 |
|---|---|---|---|
| `proto` / `seq` / `macro` / `jitter` | — | **全 0 FAIL** | ✓ |
| `w2_probe` | 7P/**7F** | **12P/2F** | ★ 本版更好 5 项 |
| `persist` | 8P/18F | **9P/17F** | ★ 好 1 项（失败面是 `DCL_PERSIST_SAVE=0`，按设计 NAK） |
| `audit_m234` | 7P/5F | 7P/5F | 相同 ⇒ 预先存在 |
| `w1` | 27P/1F | 27P/1F | 相同 ⇒ 预先存在（`R11 0x23 含 NaN` 非原子） |
| `modbus` | 5P/10F | 5P/10F | 相同 ⇒ 接线/环境（需 RS-485 回路） |
| `w5` | 10P/3F/4SKIP | 10P/3F/4SKIP | 相同 ⇒ **设计内**（3 项是 HIL/TIM3，而 TIM3 已被步进接管） |
**⇒ 零回归。**（`w5` 曾一次 9/4，连跑两次回到 10/3 ⇒ 抖动项。）
★ 跑法：`python tools/h723_<名>.py`（**自动找板子**，别传 `--port`；`seq`/`persist` 不收 `--port`）。
★ `audit_m234`/`t26` 用 `subprocess` 调**裸 `python`** ⇒ PATH 里必须有 Python，否则 `WinError 2`。

### 2. ★★ 更正一条我先前的错误结论
我写过"`h723_client.Dcl.send()` 会返回假 ACK（帧错位）" —— **错了**。
`h723_modbus.Link.xact()` 里有 `crc16` 校验 + 错位重找，**它没有撒谎**。
真因：当时固件在复位循环，串口上不停出现**开机 banner**，而 banner 是**CRC 完全合法**的帧；
**协议帧既无命令码回显、也无序号** ⇒ 客户端无法判断"这条应答属不属于我这条请求"。
⇒ **这是协议层的真缺陷**（建议 v1：应答回显命令码或带序号）。已同步更正 `STATUS-2026-09-16 §7.1` + 用户级 memory。

### 3. 推送（`ls-remote` 直连核对，**不信本地 ref**）
```
本地 HEAD               = 9f5c3fa
origin(H723PLC) main    = 9f5c3fa   ✓
dcl(dcl-controller) main= 9f5c3fa   ✓
```
本次会话 6 个提交：`686fb88`(S0–S2) → `f732ede`(闭环伺服) → `30e0ad2`(**S3–S7 程序持久化**)
→ `22dbbfb`(契约收口) → `70ddbc2`(ARCH 命令表) → `9f5c3fa`(回归+更正)。**工作区干净。**

### 4. 新暴露（**不属本轮**，已留档）
- ★ `0x23 WRITE_BURST` 的 NaN 判据**非原子**：NAK 了但**前面的字已写入**（`word0 0x40A00000 → 0x45513000`）。
  ⇒ 契约 §4.5 应补一句：**失败前不得产生副作用**。
- `0x49 PROG_ERASE` 未复测。

---

## ★★★★ 2026-09-16 11:40 —— "查新问题"逐条归因（**2 项是我判错了 / 1 项待查**）

### ① ✅ `0x49 PROG_ERASE` **本来就是好的** —— 关闭
实测 `ab_valid 3→0`、`seq A/B 3/4→0/0`、`生效=无`。先前那次 `erase failed` 是**坏固件期**的事。
（★ 副作用：把卡上程序擦掉了 ⇒ 已重新上传一份 `nr=2` 恢复：`ab_valid=1 seq A/B=1/0 生效=A`）

### ② ✅ `w1 R11` = **测试缺陷，固件正确** —— 我先前判它"非原子"是**错的**，撤回
- 测试把 marker 写在 `SHM + OFF_SENSOR_MAP` = **`SENSOR[0]`**，
  而 **`as5600_poll` 每 10 ms 就往 `SENSOR[0]` 写角度原始值** ⇒ marker 被固件自己覆盖 ⇒ 判据必误报。
- 读代码确认 `h_write_burst_w1` 是**两相**的：**先全查 NaN、再全写** ⇒ **拒绝时不产生副作用** ✓
- ⇒ 正确姿势：测试要用**固件不拥有的槽**（如 `WIRE[100]`），或者先把该槽的写者停掉。

### ③ ✅ `audit_m234` M3-b/M3-d/M3-e = **设计内**（`DCL_PERSIST_SAVE=0` ⇒ `0x43` 明确 NAK）

### ④ ⬜ `audit_m234` M2-1/M2-2 = **未归因**（大 qty 的 Modbus `0x10` 写）
`MB_MAX_FRAME` 的修复**在位**（常量已 **255**，`modbus.c` 顶部写"≤253 字节"），成因不在那条；
输出 `resp=None` ⇒ 根本没拿到 MB 应答。**预先存在，待查。**

### ★★ ⑤ 顺带挖出一个**设计耦合点**（比上面几条更值钱）
```
src/uart.c:35       RX_RING_SZ        = 512       ← 接收环
src/transport.h:148 FRAME_PAYLOAD_MAX = 6150      ← 协议宣称的最大帧
```
**协议宣称能收 6156 字节的帧，而接收环只有 512 字节。** 它能工作（主循环**边收边排空**，环只是延迟缓冲），
**但代价是：主循环一旦阻塞 > 约 44 ms（= 512 B ÷ 11.5 KB/s），环就溢出、大帧必然损坏。**

⇒ 两个推论：
1. ★ **事务式小片上传（140 B/片）在这个平台上不只是"为了省帧余量"，它是必要的。**
2. ★ 凡"声明会阻塞主循环"的区段（`PROG_BLOCK_TICKS=2 s` / `PERSIST_BLOCK_TICKS=8 s`），
   **期间收到的大帧都可能被截断**。⇒ 建议 v1：**加大环到 ≥ 一个最大帧，或把协议上限与环容量写成 `_Static_assert`。**

### ★ 另一处过期注释（第 N 次同族）
`modbus.c` 里写 "`MB_MAX_FRAME(128, 请求上限)`" —— **128 早已升到 255**。已更正。
⇒ **纪律：注释里不要写常量字面值，写常量名**（或紧跟 `_Static_assert`）。
★ 本项目已因过期注释白查过一整轮（`blackbox.h` 的 "AXI 完全空闲 / 128KB / 512 槽"）。

### 提交与推送
`8e5e257` 已提交并推送；`ls-remote` 直连核对：本地 = origin = dcl = **`8e5e257a`**。

---

## ★★★★ 2026-09-16 11:55 —— 剩 2 项 + 设计耦合点，全部查完（**结论被实测推翻两次**）

### ① ★★ 设计耦合点：**帧大小不受环限制**（实测，推翻我先前的判断）
`0x23` 往固件不拥有的 `WIRE[100]` 写，扫 qty：
| qty | 50 | 100 | 124 | **125** | **126** | 128 | 160 | 200 | **256** |
|---|---|---|---|---|---|---|---|---|---|
| 帧总长 | 212 | 412 | 508 | **512** | **516** | 524 | 652 | 812 | **1036** |
| 结果 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | **✓ 整帧收到并写对** |
**⇒ `RX_RING_SZ=512` 不限制帧大小** —— 主循环**边收边排空**，环只是延迟缓冲。
★ **顺带实证了 `w1 R11`**：它那 1036 字节帧是**完整到达**的 ⇒ 它的 NAK 确实是为 NaN ⇒ **测试缺陷**（marker 写在 `SENSOR[0]`，被 `as5600_poll` 每 10 ms 覆盖）。

### ② ★★ "阻塞 > 44.4 ms 必丢字节" —— 实测到 **108% 预算仍 0 丢字节**
| 载荷 | 块数 | COMMIT 往返 | 占预算(44.4ms) | ore/drop/frame_bad |
|---|---|---|---|---|
| 54 B | 1 | 31.8 ms | 72% | **0/0/0** |
| 2054 B | 5 | **48.2 ms** | **108%** | **0/0/0** |
| 4102 B | 9 | **48.4 ms** | **109%** | **0/0/0** |
⇒ `512 B ÷ 11.52 B/ms = 44.4 ms` **偏保守**（环实际可吸收量更大，或有额外硬件/驱动缓冲）。
★ **诚实结论：已量化、已加观测闸门，但当前实测未触发** —— 既不能说"出问题"，也不能说"余量充足"。

### ③ ★ 为此补的观测闸门（**本节的真正交付**）：`0x38` 39 → **51 字节**
```
+39 uart_ore    +43 uart_drop    +47 frame_bad
```
判据：**正常收发下三者恒为 0**；非 0 ⇒ 有帧被截断 ⇒ 该次"NAK/无应答"**不是固件的判断，而是数据没到全**。
★ 这三个量原来**只刷进全局变量、不进任何应答面** ⇒ "帧被截断"在协议面上**完全不可见**（只能 SWD）。
  违反"新观测变量必须能被外部读走"，与 `r[38] out_surfaces` 同一个理由：**把"读数一样、事实不同"的两件事分开。**

### ④ ✅ M2-1/M2-2 定案（**与 qty 大小无关**）
```
0x61 实测: state=0  **tx_len=0** | rx=6  tx=6  crcerr=0  **exc=1**
```
`exc=1` ⇒ **确实产生了异常响应**；`tx=6` ⇒ **确实发过字节**；但**回读 `tx_len=0`** ⇒ 测试拿到 `resp=b""`（期望 8/5 字节）⇒ BAD。
**qty=10 也一样失败** ⇒ 不是"大 qty"的问题。
⇒ **待定案**：`0x61` 的 `tx_len` 语义（"已发送帧"还是"待发送帧"？发完是否清空？）—— **观测面与事实不符**，同族。

### 提交与推送
`002f8db` 已提交并推送；`ls-remote` 直连核对：本地 = origin = dcl = **`002f8dbb`**。
★ 本次会话累计 10 个提交，`979ebb2 → 002f8db`。

---

## ★★★★ 2026-09-16 12:00 —— 发布就绪评估（用户问"能发新版本了吗"）

### ✅ 全链终于实测通过（**发布前最该做的一件事**）
```
① dclc 编译 examples/hiloop.dcl → 7 路由 / 4 参数, 载荷 182 字节
② 0x49 ERASE 清掉测试残留 → ACK
③ 0x45/46/47 通信上传 → BEGIN=ACK COMMIT=ACK
   卡上: ab_valid=1 seq 1/0 len=182 生效=A
④ 复位后: **n_routes=7** run=1 deploy_seq=1 applied=1 out_surfaces=2
   uart_ore=0 uart_drop=0 frame_bad=0
```
⇒ **`.dcl 源 → 编译 → 通信上传 → 存 SD → 上电自动装载` 全链通**，且**卡上已从"测试残留(nr=128)"换成真实示例**。

### ★ 收口时发现的状态不一致（已修）
**引擎在跑 `nr=2`，卡上却存着 `nr=128`（4102 B 的测试载荷）** ⇒ 下次上电会装载测试程序。
⇒ 已 ERASE + 上传 `hiloop.dcl`（nr=7）收口 ✓ **发布前必须做这类"运行态 vs 持久态"对账。**

### 结论：**可以发布**（详见 `docs/STATUS-2026-09-16.md` §9）
- ✅ 工作区干净、两个远程已同步、零警告构建、**12 套回归零回归**、**全链实测通过**
- ★ **release notes 必须写**：`0x38` **39 → 51 字节**（尾部追加，前 31 字节与 S3 同布局未动 ⇒ `len>=39` 读法兼容）
- ⬜ **建议发布前修**：`0x61 MB_RESP` 的 `tx_len` 口径（`exc=1 tx=6` 却 `tx_len=0`）—— 观测面与事实不符
- ⬜ `dclc.py` 成功提示不严谨（`(None)` 也打 `[OK]`）
- ★ **未打 tag**（现有 v0.1.0/v0.1.1/v1.0.0 ⇒ 下一个自然 **v2.0.0**，打 `71f3adf`）—— 对外动作，等用户确认

### 提交
`71f3adf` 已提交并推送；本地 = origin = dcl = **`71f3adfc`**。本次会话累计 11 个提交。
