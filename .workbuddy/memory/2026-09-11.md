# 2026-09-11 · ★★★ 串口卡点结案: USART1 BRR 公式错 16 倍

## 一句话
困扰三轮的"串口完全不通"**不是接线问题** —— 是 `BRR` 公式写错 16 倍,
实际波特率 7200 而非 115200。修完后**双向全通 + 协议栈 12/12 PASS**。

---

## ★★ 根因 (P1, 两处同源)

```c
/* 错 (原代码) */
s_brr = (pclk2_hz * 16u + baud / 2u) / baud;
/* 对 */
s_brr = (pclk2_hz + baud / 2u) / baud;
```

USART 默认 **OVER8=0 (16 倍过采样)**, 此时 `BRR = fCK / baud` **就是分频值本身**。
乘 16 那套写法属于 OVER8=1 场景, 套错了地方。

| | BRR | 实际波特率 |
|---|---|---|
| 修复前 | `0x22B9` = 8889 | 64e6/8889 = **7199** |
| 修复后 | `0x022C` = 556 | 64e6/556 = **115108** |

8889/555 = **16.02**。

### 为什么极隐蔽 (三重伪装)
1. **寄存器全"正确"**: CR1=0x0D (UE|RE|TE) / ISR=0x006000D0 (**TXE=1, TC=1**) /
   GPIO AF 全对 / DWT 递增 (核在跑) / RCC 位全对 ⇒ 所有"配置类"检查全绿
2. **TC=1 是假的安心**: 它说明移位器空闲 —— 因为字节**确实推出去了**,
   只是节拍是 7200
3. ★★ **`uart.c` 注释里有一条自洽的假验证**:
   > `100MHz × 16 / 115200 = 13889 = 0x3641`, 误差 0.0004%

   数字**算得没错**, 但它是"16 倍分频"的值。这条注释用它"证明"公式正确,
   **反而把错误锁死了三轮** (第一轮怪接线 / 第二轮怪线材 / 第三轮怪驱动)
4. **现象与"线没接上"完全一样**: PC 侧 0 字节 ⇒ 常规排障路线永远查不到固件

---

## ★★ 定位手法 (可复用, 本次真正的突破)

### 第一步: 四组对照 — 把"接线"与"固件"彻底解耦
用 **SWD 直接写 GPIOA_MODER/BSRR 手摇 PA9** (核心 halt 也能翻), 同时听 PC 串口:

| 组 | PA9 状态 | COM14 收到 |
|---|---|---|
| A | 恒低不动 | **0 字节** |
| B | 手摇方波 | **13 字节** |
| C | 恒高不动 | **0 字节** |
| D | 手摇(重复) | **13 字节** |

- 若 A/C 也有字节 ⇒ 是悬空噪声, 之前所有"收到"都是假的
- 若 B/D 收不到 ⇒ 线断/接错脚, 与固件无关
- 实测 **B/D 有、A/C 无** ⇒ **线是通的**, 因果在线本身 ⇒ 剩下的只能是固件

**这一步的价值**: 它把"双向都不通"这个笼统现象, 收敛成"线没问题"这个确定结论,
从而把搜索空间从"接线+线材+驱动+固件"缩到只有固件。

### 第二步: 读 BRR, 一眼结案
`USART1_BRR = 0x22B9` vs 期望 `0x22B`, 当场定性。

### ★ 教训: "配置全对" ≠ "功能可用" 的第三次复现, 且最隐蔽
- A1 (NVIC_ISER 位移溢出): GCC **会**报警告, 只是被噪声淹没
- N1 (CRC 覆盖少一字节): 至少能在固件内部查到
- **本次**: 编译器**完全无话可说** —— 语法/类型/逻辑都合法, 只是数学错 16 倍

---

## 修复

| 文件 | 改动 |
|---|---|
| `src/uart.c` | 公式 + 顶部注释**勘误段**(写明症状与错误值) |
| `src/uart.h` | `uart1_brr()` 注释 `100MHz/115200 → 0x364` (原误写 0x3641) |
| `src/min_uart.c` | 公式 + 详细踩坑注释 |

★ 三处都加了**显式勘误注释**, 防止未来被"修正"回去。

---

## ★ 验证 (实机, 双向 —— 本项目第一次真实串口全通)

### 最小固件 (COM14 = CH340)
```
==== MIN UART (no engine, no IRQ, HSI 64MHz) ====
PA9=TX PA10=RX  115200 8N1  BRR=0x022C
[PASS] 命中: ['MIN UART', 'PA9=TX']
```
横幅自报 `BRR=0x022C` —— 与理论值、SWD 读回值**三者一致**。

### 全双工回声
```
发 'H723ECHO' × 5 → 5/5 逐字节原样返回
H1-6(PA9) → CH340-RXD   ✅
H1-5(PA10)← CH340-TXD   ✅
```

### 主固件 (DCL 引擎 + 协议层) — 12/12 PASS
```
[PASS] T0 链路活性 (GET_VERSION→ACK)   payload=00 02 33 00
[PASS] T1.1~T1.4  fw=0x0200  cap=0x0033 (宣称=实现)
[PASS] T2 未知命令→NAK 'bad cmd'   [PASS] T3 CRC错→无响应
[PASS] T3b/T4b 阳性对照   [PASS] T4 长度超限   [PASS] T5 分片   [PASS] T6 连发两帧
结果: 12 PASS / 0 FAIL / 0 SKIP
```
**此前全部协议验证都只能经 SWD 间接进行 —— 这是第一次跑在真实串口上。**

---

## ★ 附带确认 (修正旧文档错误)

### H1 脚位实测确认 (旧 `docs/HARDWARE-PINOUT.md` 把 5/6 写反了)
| 脚 | 信号 | 实测 |
|---|---|---|
| 5 | **USART1_RX_PA10** | ✅ CH340-TXD 接此 (回声成功) |
| 6 | **USART1_TX_PA9** | ✅ CH340-RXD 接此 (收到横幅) |
| 7 | **RST** (串 1K) | 原理图确认 (旧文档误标为 PA9!) |

★ 已修正文档。**7 脚是 RST** —— 接错会把板子按住复位, 现象像"板子死了且 SWD 连不上"。

### 原理图权威信息 (板商 `LXB723ZG-P1原理图.pdf`)
- **H1 = "SWD和USART1接口"** 2×4P, 原理图注: **"核心板不具备硬件 USB 转串口电路!!"**
  ⇒ 板子**没有**板载 USB-TTL, CH340 是可选配件, 不是开发前提
- 排列(奇数/偶数): 1=SWCLK PA14 | 5=RX PA10 / 2=SWDIO PA13 | 6=TX PA9 /
  3=GND | 7=RST / 4=+5V | 8=3.3V

### 资料包 `【5】下载器驱动` 三个 zip 均无本问题所需信息
- `DAP串口驱动.zip` = **CMSIS DAP USART** (VID_C251&PID_F001) —— 第三方 DAPLink 的虚拟串口,
  与手上 Luxiaoban 的 `VID_0D28&PID_0204` **对不上, 装了没用**
- `USB转串口CH340驱动.zip` = CH340 通用驱动
- `STLINK Utility.zip` = 文档明说 **H7 系列不能用** ST-Link Utility

### 时钟树假设经 SWD 实测确认
HSI 64MHz / SWS=HSI / HPRE=1 / D2PPRE2=1 → PCLK2=64MHz (`min_uart.c` 的"复位默认"假设成立)

---

## 本轮新增的判据设计经验

1. **"能一次决定"优于"能扫描"** —— 上一轮做逐脚碰线扫描是**盲猜**
   (前提是脚位未知); 脚位已知后盲扫毫无意义。真正缺的是**一次决定**的判据
2. **判据用"内容匹配"而非"字节数/有无电平"** —— 噪声和波特率不匹配都会产生字节,
   但产生不出 `"MIN UART"` 这个串。`tools/h723_wire_probe.py` 按此设计
3. **四组对照(恒低/摇动/恒高/再摇)** 是判定"信号归属"的通用武器 ——
   只有"动时有、静时无"才能排除悬空噪声
4. ★ **一次性的打开瞬态不可采信**: 第一轮 COM14 收到 443 B 全 `0x00`, 之后完全消失
   (再测 0 B)。若不识别这个特征, 会据此编出"线接上了但数据不对"的错误结论
5. **别急着烧固件测** —— 先读寄存器看板子现状 (本轮先读 IDCODE/RCC 才动手)

---

## 新增工具
- `tools/h723_wire_probe.py`: 双口同时监听 + 内容匹配判定 + 结论直给 (含"下一步接哪根线")
- 复用现有 `tools/h723_loopback.py` (哨兵回环 + T1 噪声基线 + T4 break 探针, 判据比新写的更严)
- 报告: `docs/FIX-REPORT-usart1-brr.md`

---

## 未修 / 遗留
- A6: `0x38` 字段语义 (需与 S3 逐字段核对)
- A9: tick u32 4.97 天回绕
- ITCM 自身落位敏感性
- DAPLink CDC (COM11) 始终只有打开瞬态 1 字节 —— RXD/TXD 是否引到板上**未确认, 但已不需要**
- **迁移进度因此解锁**: W1 (运行控制 0x11/12/13 + SHM 读写 0x20/21/22/23) 现在**可验收了**
  (此前所有协议命令都无法真实验收, 因为串口根本不通)

---

## ★ 全量源码通读 + Xmind 导图 (本轮第二件事)

把 `9.10 H723newest` 全量读了一遍并生成 Xmind 思维导图。

### 阅读覆盖面 (实测行数)
- `src/*.c + *.h` = 含 engine.c(667) / engine.h(475) / main.c(1060) / regs.h(272) /
  clock.c+h(344) / transport.h+c(205) / uart.h+c(234) / primitives.h(228) /
  lsym.h(49) / min_uart.c(206) / syscalls+sysmem(128)
- `ld/*.ld` 206 / `CMakeLists.txt` 135 / `build.sh` 90 / `cmake/*.cmake` 23 / `startup/*.s` 696
- **总计 5018 行** (不含 tools) + `tools/*.py` **2292 行** (10 个脚本)
- 14 份 docs 文档

### 导图
- Xmind: `fileId = jEDBCMzo`，团队 "My Works"
- 结构 LogicChartRight (左→右大纲，适合文档结构) + 主题 `Constancy` (克制商务蓝)
- 15 个一级分支：项目定位 / 硬件平台 / 时钟与拍 / 内存落位 / 引擎与 SHM / 19 原语 /
  deploy 热重载 / 协议层 / USART1 驱动 / 工具链与构建 / 验证工具 / 方法论纪律 /
  已知缺陷与遗留 / 下一步 W 波次

### 通读中新确认的细节 (之前没记牢的)
- `OP_MAX_EXCL` 双名字设计：OP_MAX 恰好**等于** OP_SR ⇒ 极易被写成 `< OP_MAX` 静默漏原语
- `engine_bucket_dead_slots()` 是 H9 修正的**可失败判据**本体（槽 64..99 非零数恒 0）
- `k_op_cost_itcm` 顺序 = op 0..18，最大 PID 140 cyc
- `EXEC_DEPLOY_BUDGET = 26000` = 65% 拍长
- main.c 的 `h_deploy()` 有**六道门**（比我之前记的更细）
- `DCL_CAP_H723_IMPL = 0x0033`（阶段 3.1 时只报 0x0021，A4 修正后升到 0x0033）

---

## W2.1-W2.3 完成 (Force 域) · 提交 376d2ed

### 交付
- SHM: `OFF_FORCE_MASK=0x47F0`(u32[4]) / `OFF_FORCE_VAL=0x4800`(f32[128]) /
  `OFF_RSVD_EXEC_TAIL=0x4A00`(0xA0)。三段**精确相接**，断言用 `==` 不用 `<=`
- ISR: `engine_force_apply()` 拍首覆写 (ITCM) + 扫描宏内写端屏蔽
- `eng_force_clear()` → deploy / RESET 联动
- 命令 `0x24 FORCE`；能力位 `DCL_CAP_FORCE=0x0080`，`IMPL=0x00B3`
- 工具 `tools/h723_w2_probe.py` (**免串口**)

### ★★ 抓出三个真 bug（都在"配置全对、效果为零"族）
1. `engine_fill_tables()` **从不写 N_ROUTES** → 该路径下恒 0 →
   `h_start_w1` 的 F11 预算兜底**被静默跳过**（W1 那条判据在 profile 路径下
   从未真正走到过）。修：装载路径也要维护这个量。
2. force 覆写第一版写在 `engine_tick()` 内 → ISR 有**两条扫描路径**，
   `BOOT_SCAN_MODE=0` 走全表扫 ⇒ force 完全不生效，而 SHM 里 fmask/fval 全对。
   修：提成独立 `engine_force_apply()`，在两条路径**之外**统一调用。
   ⇒ 纪律：**凡"每拍都必须发生"的动作不能挂在某条分支里**。
3. `engine_fill_tables()` 经 `cold_start_reset()` 清掉 `ENGINE_RUN` →
   运行期 reinit 后**永久停摆**（双门 `gate && RUN` 恒不成立）。
   实测：阶段 2 回归 15 组从第 3 组起全 0。修：填表只动配置域，保存/恢复运行态。

### 验证
- `h723_w2_probe.py` **13 PASS / 0 FAIL**
- 关键手法：用**非零强制值**（7.5 → 3.25）。OA9 事故的判据盲区正是"测试全用 0.0"
- wire[3] 轨迹：1.5(路由) → 7.5(强制) → 3.25(换值) → 1.5(释放**精确**恢复)
- 强制期间路由持续写它但值纹丝不动 ⇒ 写端屏蔽生效
- 回归 `h723_stage2_read.py`：15/15 守卫 ✓、5 种校验和互异、拍周期恒 40000、
  分档收益 2.52~2.58×、ITCM 全表 7161 cyc（基线一致）

### ★ 新踩坑（跨项目通用）
- **pyocd 默认 connect_mode 在芯片进 WFI 后会 SWD 失步**
  （"Device entered sleep" / memory transfer failed / AP#0 IDR 读失败）
  ⇒ **必须 `-O connect_mode=under-reset`**；代价是每次连接都 reset，
    所以全部用例必须塞进**一条命令链**
- **pyocd 3.x 的 `read32` 输出没有 `0x` 前缀**
  （格式 `20003acc:  3fc00000`）—— 解析器按 `startswith("0x")` 匹配会恒 0 个字，
  工具会自己报"读回不足"。判据要能失败，但不能因**解析器**而失败
- **`g_reinit` 路径的副作用面积**：任何"整段清零"的单一入口被运行期调用时，
  都要问一句"它清掉的量里有没有运行态"

### 待办
- W2.4 persist（裸 Flash 双副本 A/B，扇区 14/15）—— PERSISTENT 位仍未声明
- W1/W2 的 **PC 侧串口验证**仍被物理接线阻塞（需短接 CH340 TX/RX 或接 H1-6/PA9）

---

## ★★ 外部审计 W4 批次（HEAD = 3075135）— 1×P1 + 3×P2, **全部未修**

报告 `docs/audit/H723-W4-AUDIT.md`（目标 `b690b15`；报告本身即 HEAD 提交 3075135，已推送）。
上一批 W1W2W3 的 8 项（A~H）已在 `c5bfbf0` 全部处置并复验通过。

**W4 新发现**（共同点：官方套件全绿仍被挖出 ⇒ 边界值取了不触发的那个）：
- **M1（P1，实锤）** `0x03` 读 **qty≥62** → BUILD 里 `c->b_pos < MB_MAX_FRAME(128)` 卡死
  （`total=2qty+5>128`，`b_pos` 永远到不了 `total`）⇒ 通信域**永久 busy**，只能 `0x13 RESET`/断电。
  边界精确 `total>128 ⇔ qty≥62`；**一条完全合法的请求即可触发**。
  根因：`MB_MAX_FRAME` 被同时当"RX 帧上限"与"响应组装上限"，但响应长度与请求长度无关
  （RTU ADU 上限 256，`OFF_MB_TX` 缓冲本就 256B）。`modbus.c:288`。
  修：`b_pos < MB_MAX_FRAME` → `< MB_TX_SIZE(256)` + 加 BUILD 未完成保护。
  ★ M1/M2 在 **S3 是同一份代码**（`core0/modbus.c:267`）⇒ 移植继承；上轮审 S3 也漏了。
- **M2（P2）** `0x10` 写 **qty≥60** → 请求帧 >128 被截断（隧道显式 NAK；物理口静默丢弃→CRC 失败）。
  修：`MB_MAX_FRAME` 提到 256（与 RTU ADU 一致）。
- **M3（P2）** `0x43` 报的"持久化条数"取 **A 副本（旧）** —— `persist.c:148/151` 固定
  `out->n_routes = ha.n_routes`，两副本条数不同时报错（连续两次 deploy 后落盘即可复现）。
  修：按 `seq` 大的一份取 `n_routes`。
- **M4（P2）** pyocd 工具链尾不 `go`/resume ⇒ 留 halt ⇒ 后续串口测试**假性失败**；
  ★ 观察者效应：用 pyocd 查"为何串口无响应"本身让串口无响应。修：收尾加 go + 套件间串口哨兵。
- P3：`regs.h` 的 USART2_BRR 推导表述与 ST BRR 布局不符（实测 115200 正确，待核推导）；
  `0x38 r[22]` 语义与 S3 不同；`h723_la_modbus.py` timeout 偏紧、应带 T0c 自检。

**验证通过（审计方重跑）**：proto 12/12 · w1 28/28 · w2 13/13 · seq 27/27 · persist 26/26 ·
modbus 13/13 · 构建零警告。LA 独立裁决 USART2 位宽 8.625μs ≈ 115200 ✓。

⇒ 建议顺序（用户尚未指派）：**M1（P1）→ M3 → M2 → M4**。
⇒ 通用教训：**"合法但极端"的输入（大 qty / 两份不同 / 未 resume）应成为每个模块的必测项**。

---

## ★ M1 (P1) 已修 + W5.1 macro VM 落地（2026-09-11 下午）

### M1 修复（提交 `265b9b3`）— 三处必须同改
- `engine.h` 新增 `MB_TX_SIZE=256`（**响应**缓冲）；`MB_MAX_FRAME=128` 是**请求**上限，M1 根因就是把这俩当一个用。
- `modbus.c` BUILD 界限 `MB_MAX_FRAME`→`MB_TX_SIZE` + 加"未完成保护"（撞上限丢帧回 IDLE）。
- ★★ `main.c h_mb_resp` 栈缓冲 `2+MB_MAX_FRAME+16`(146B)→`2+MB_TX_SIZE+16`(274B)：**审计没点的连带项** —— 只改 modbus.c 会让 `tx_len` 达 255，拷贝循环写穿 146B 栈（拿栈溢出换卡死）。
- 验证：`h723_modbus.py` **15/15**（新增 ⑨b 合法大 qty 62/63/100/125→len 129/131/205/255；⑨c 大 qty 后域仍活）；proto 12/12。
- 遗留：M2（0x10 写 qty≥60 请求帧>128 截断）需提 MB_MAX_FRAME 到 256，但 `rx_len` 是 uint8_t(255→0 回绕)，不能顺手改；M3/M4 未动。

### W5.1 macro VM（提交 `57ada8f`）— 18/18 全绿, 零外部硬件
- 来源 S3 `components/macro/*`；字节码集合/栈语义/NaN 守卫/索引掩码/SHM float 判定逐条保留。
- 移植三类改动：① 64KB flash 分区 + FreeRTOS 任务 → **4KB 驻 SHM(RAM) + 主循环按 loop_ms 自节流**（macro 是 ms 级慢动作, 进 100μs 硬拍会吃爆预算）② `gpio_config`/`rsr.ccount`/`vTaskDelay` → GPIOx MODER/BSRR/IDR + DWT（★ DWT 计 **CPU 400MHz** 非 HCLK 200MHz）③ SPI op 0x20-0x23 **不迁移**（S3 里只服务 ST7735; display 不迁）→ 遇即返 -4, 不静默。
- H723 加固：裸地址 load/store 限 SHM 窗口（无 MMU, 越界=HardFault）。
- ★ 边界：macro 驻 RAM ⇒ 上电/0x13 RESET/deploy 都清掉（S3 是 flash 持久）；flash 持久化留后续。
- 新命令 0x40 一次性执行(回栈)/0x41 上传/0x42 控制；能力位 `DCL_CAP_MACRO=0x0800`（H723 **扩展位**, S3 无）→ IMPL=**0x02F7**，`tools/h723_proto.py EXPECT_CAP` 已同步。
- 判据手法（可复用）：效果一律用 **SHM 类字节码 0x30-0x33 制造 → 0x22 burst 独立读回核对**，做到零外设验证；否定性判据必须"NAK **且** 目标未变"成对。
- 剩余 W5: DI(需跳线回环) / AI-ADC16bit(可用内部 VREFINT+温度通道零外设) / HIL(需 PWM→ADC 外部回路)。
- 落点: `OFF_MACRO_CTRL=0x5DE0`/`OFF_MACRO_CODE=0x5DF0`(4KB), 在 `OFF_MB_TAIL` 备用区内, 已补 3 条 `_Static_assert`。

---

## ★★ W5.2/5.3/5.4 外设域 (提交 `a41cf1a`) — DI 已证 · AI/HIL 待接线

新增: `src/adc.{h,c}`(ADC1 16bit + AI 3 通道 PA0/PA1/PA4→SENSOR[8..10]) ·
`src/di.{h,c}`(4 路数字输入 PC0..PC3, 30ms 去抖→SENSOR[3..6]) ·
`src/hil.{h,c}`(PWM PA6=TIM3_CH1 ← WIRE[20]; 反馈 PA5=ADC1_INP19 → SENSOR[2]) ·
命令 `0x36 PIN_SELFTEST`(内部上/下拉自检) · 能力位 `DCL_CAP_AI` 移入 IMPL → **0x0DF7**。

### ★★ 本轮最大的坑: ADC12_COMMON 地址 (4 轮 pyocd 才定位)
- 板商 `stm32h723xx.h` 把 `ADC12_COMMON_BASE` 定义成 `D2+0x2300`(0x40023000), 与它**自己的**
  `ADC_Common_TypeDef` 注释 "ADC1/3 base address + 0x300" **自相矛盾**。
- 实测裁决: **0x40022300 可读; 0x40023000 → "memory transfer failed"(未实现)**。用 0x40022300。
- ★★ 写空地址 → **非精确总线错 IMPRECISERR** → HardFault, 但**报到下一条**寄存器访问
  (g_stage 指向 ADC_CR, 真因是 ADC_CCR 那次写) ⇒ **非精确错会把现场挪到邻居**, 别顺着 g_stage 咬。
- 教训: **两个"权威"冲突时用实测裁决**(pyocd 直接读候选地址), 并把这个矛盾记在原地。

### ★ 实测得到的硬件事实 (写进代码注释)
- **H723 analog 模式下 GPIO 上下拉对 ADC 无效**; input 模式下 ADC 又接不到引脚
  ⇒ **ADC 无法零接线自证**(必须外部 3.3V/GND); 而 **DI 可以**(内部上下拉直接改 IDR, 已证)。
- **内部 VREFINT/温度通道挂在 ADC3(12bit)**, ADC1/2 才是 16bit ⇒ 16bit 只能外脚。
- **ADC_CR.BOOST 是 2bit(bits 9:8)**, 值 3 = fADC≤25MHz; 本例 adc_ker_ck=**HSE 25MHz**
  (per_ck: RCC_D1CCIPR.CKPERSEL=HSE; ADCSEL: RCC_D3CCIPR=CLKP)。CKMODE=00 才是异步。
- `ADVREGEN/DEEPPWD` 在 **ADC_CR**(bits 28/29), 不在 CCR (H723 与 H743 的差异)。

### 修复
- `di.c`: 改"仅变化时写"→**每轮无条件回填 SENSOR** —— cold_start_reset 整段 memset 把槽清 0,
  而去抖状态在 C 静态里 ⇒ 旧写法下一次 0x13 RESET 后 SENSOR[3..6] 永远停在 0 (实测到)。

### 验证状态 (诚实区分)
- `tools/h723_w5.py` **7 PASS / 0 FAIL / 4 SKIP**: S1 DI 自检(内部上下拉 1/0)✅ ·
  A1 AI 电压在范围(ADC 确在转换) · D1 DI SENSOR=[1,1,1,1]。
- **SKIP(未宣称已验证)**: S2/ADC 零接线自检 · E1/E2 外部 AI(需 `--ext`: PA0→3.3V, PA1→GND) ·
  H1 HIL(需 `--hil`: PA6→PA5 跳线)。
- 回归: proto 12/12(cap 0x0DF7) · macro 18/18 · modbus 15/15 · 零警告。

### 接线 (下轮逐个人工测)
- AI: PA0→3.3V, PA1→GND, 共地 → `python tools/h723_w5.py --ext`
- HIL: PA6→PA5 直连(有 1kΩ+1µF 串 RC 更干净) → `python tools/h723_w5.py --hil`
- DI: PC0..PC3 各接 GND(或用一根线依次碰) → 看 SENSOR[3..6] 翻转

### 人工测试 1: DI 实验已通过 (提交 `6dc3ce6`)
- 用户接法: **PC0→GND**(1 根线)。实测 `SENSOR[3..6]=[0,1,1,1]`, 0x36 自检 PC0 的 (pu,pd)=(0,0)。
- ★ 结论: **DI 输入通路双证完毕** —— D-1 4 路均可解释; D-2 PC0 被**外部 GND 压过 40kΩ
  内部上拉**读成 0.0(外部信号真被采到); D-3 PC1..PC3 悬空(内上拉)=1.0。
- ★★ **判据设计缺陷(本轮撞出)**: 旧 S1 要求"4 路全部 (1,0)", 外部接线后被判 FAIL ——
  **把"按设计接线"报成"通路故障"**。修: 每路 (1,0)=跟随内部上下拉 或 (0,0)=被外部钉低
  **都合法**, (1,1)=下拉无效才是故障; AI/HIL 改为**先探测后判定**, 未接线记 SKIP(附原因)。
  ⇒ 教训: 判据要区分"外部驱动"与"通路故障"; **未接线 ≠ 失败**。
- 当前 `tools/h723_w5.py`: **8 PASS / 0 FAIL / 3 SKIP** (A-1/A-2 需 AI 接线, H-1 需 HIL 跳线)。

### 人工测试 2: 用户把线从 PC0 移到 PC1 + HIL 输出臂零接线证明 (提交 `f41b799`)
- 用户"C0→C1": 实测 `SENSOR[3..6]=[1,0,1,1]`, 0x36 自检 PC0=(1,0)、PC1=(0,0)
  ⇒ **外部信号"移开→1 / 接上→0"两个方向都被抓到**, 比单点接地更强的证据。DI 实验收口。
- ★ 新增 `OFF_HIL_DUTY=0x6E00` (u32 占空比镜像) + `hil.c` 回写 + 判据 **H-0**:
  动机 = 本项目铁律"凡'写过了就算'的状态必须补一个能被外部读走的量" —— HIL 输出臂写的是
  **TIM3_CCR1 硬件寄存器**, 协议读不到 = 不可验证的宣称。实测 **u=512→500 / 1024→1000 / 0→0**
  (逐值等于 `round(u*1000/1024)`) ⇒ **输出臂零接线验证完毕**, 只剩 PA6→PA5 物理回环待接。
- 现状: `h723_w5.py` **9 PASS / 0 FAIL / 3 SKIP** (仅剩 A-1/A-2 需 PA0→3.3V+PA1→GND; H-1 需 PA6→PA5)。
- 剩余 W5 待人工验证: **② AI 外部** (PA0→3.3V, PA1→GND) · **③ HIL 物理回环** (PA6→PA5)。
- ⚠️ 环境: 本轮 bash 的 PATH 被污染 (`grep/sed/mkdir/tail/head/cmake/ninja` 全缺, `tail` 被解析到
  `D:/BtSoft/panel/script/tail`)。绕法: 构建用绝对路径 `"/c/Espressif/tools/cmake/4.0.3/bin/cmake.exe" --build build`；
  跑脚本**不要接管道** (管道会把输出吃掉)。

---

## ★★★ W5 外设域**全部收口** (提交 `bce8a06`) — 14 PASS / 0 FAIL / 0 SKIP

用户接好 AI(PA0→3.3V, PA1→GND) + HIL(PA6→PA5) 后**仍读不到电压**。本轮把"接线 vs 我的固件"
彻底分开 —— **三个都是我的 bug, 接线一直是对的**。

### ① ★★ 缺 **PCSEL** (ADC 通道预选) —— 真正根因
- `stm32h7xx_ll_adc.h::LL_ADC_SetChannelPreselection()`: **每个要用的通道都要置 PCSEL 位**,
  否则该通道输入**根本不接到 ADC 内部**。H72x 上 ADC1/2 在 offset **0x1C**。
- 后果: `CR/CFGR/CCR/MODER` **全部正确**, 但读到的是漂浮值 ⇒ 三条外部线 + 内部上下拉
  **全都不跟随**。**"配置全对≠功能可用"第 4 次复现**, 且**落在我核对过的那几个寄存器之外**
  ⇒ "读回核对"没抓到。**只有去读 HAL/LL 源码才发现**。
- 教训: **"我核对过的寄存器" ≠ "全部必需的寄存器"**。功能不通时, 除了逐位核对已写的,
  还要问一句"**有没有该写而没写的**"——去翻官方驱动的初始化序列。
- 修: `regs.h` 加 `ADC_PCSEL(n)=REG32(n+0x1C)`; `adc.c` 全开 20 通道 (0xFFFFF)。

### ② 连读时残留 EOC → 读到**上一次的 DR**
- 旁证: `ADC_ISR` 实测出现过 `0x100B` (bit3 = OVR 置位)。修: `adc_read` 启动前写 1 清 EOC/OVR。

### ③ ★★ **错误哨兵落在合法值域内**
- v0 用 `0xFFFF` 当"超时哨兵", 而 **0xFFFF 正是 16bit 的合法满幅读数**! `hil.c` 的
  `if (r==0xFFFF) r=0;` 把**真实满幅**清成 0 ⇒ 满幅时 `SENSOR[2]` 读 0, 与 `0x37` 扫描
  读到的 `65535` **直接矛盾**。靠新加的 `OFF_HIL_FB_RAW` 镜像才钉死。
- 教训: **错误码/哨兵绝不能落在合法值域内**。修: `adc_read` 改"状态码 + 出参"。
- ★ 顺带证明的通用手法: **两者读数矛盾时, 加一个"原始量镜像"直接把矛盾摆平**
  (别在两个二手结论之间猜)。

### 纠正一条**基于 bug 的错误结论**
- 此前记的"**H723 analog 模式下 GPIO 上下拉对 ADC 无效**"是 **PCSEL 缺失造成的假象**。
  修后实测 PA4 跟随内部上下拉 (pu=6354 / pd=1) ⇒ **ADC 零接线自检其实是可行的**。

### 排障手法沉淀
- **pyocd 读运行期外设寄存器必须 `go` 再 `halt`**: 只 `reset` 读到的 `CR=0/CALFACT=0` 是
  **没跑起来的核**(M4 同族); 且会话以 `halt` 结束会**把板子留在暂停**⇒ 下一次串口测试全失败,
  需重新烧录解锁。
- 新增 `0x37 ADC_SCAN` (全通道扫描): 一次把"通道映射错"与"线没落到脚"分开。
- 新增 `OFF_HIL_FB_RAW` (反馈原始码镜像)。

### 最终验收 (实机, 用户真实接线) `tools/h723_w5.py` 14/14/0
- ① DI: PC1 接地=0.0 · PC0/2/3 悬空=1.0 (通路+外部输入双证)
- ② AI: **PA0=3.300V · PA1=0.000V · 跨度 3.300V** (16bit + 外部 3.3V/GND 实证)
- ③ HIL: H-0 输出臂镜像逐值吻合 · **H-1a 满幅=3.300V / 零幅=0.001V** ·
  H-1b 50%占空=1.856V (期望1.65, 无 RC 的相位误差, 在容差内)
- 回归: proto 12/12 · macro 18/18 · modbus 15/15 · 零警告

### W5 收口后的剩余
- HIL 50% 精度可再提: 需要 RC 滤波 (1kΩ+1µF) 或把反馈改成"跨 PWM 周期同步平均"。
- 仍在外部的审计项: M2 (0x10 写 qty≥60 请求帧截断) / M3 (0x43 取旧副本) / M4 (pyocd 收尾 go)。

---

## ★★ 外部审计 M2/M3/M4 全部收口 (提交 `43ad7a0`) — 9 套件 164 PASS / 0 FAIL

### M2 `MB_MAX_FRAME` 128 → **255**
- ★ 为什么是 255 而不是 RTU 的 256: `MbCtrl_t.rx_len` 是 **uint8_t**(最多 255), 设 256 会让
  `rx_len < MB_MAX_FRAME` **永真** ⇒ rx_len 到 255 再 ++ 就**回绕成 0** (静默丢帧)。
  而 qty≤123 的最长**合法** 0x10 请求 = 9 + 2×123 = **255** ⇒ 255 已全覆盖 (qty=124 超 ADU, 本就非法)。
- 代价: 请求 CRC 单拍 WCET 上界 126B → 253B (仍随常量收敛)。
- 实测: qty 59/60/64 → 回显+CRC ✓; qty=65 → **异常 02**(地址越界) —— 证明帧被正常**解析**了,
  而不是像旧代码那样按**长度**拒收。

### M3 `0x43` 报"最新副本"的条数
- `persist_probe` 两份都有效时改取 **seq 较大**那份的 n_routes/n_params/n_states;
  旧的"待覆盖副本"条数另开 `n_routes_old`(仅供排障, 不进协议载荷)。
- ★ 同时把 `active` 的语义写进头文件: 它是"**接下来该写**哪份", 与"最新"**不是一回事**
  (旧注释含糊, 正是这个含糊让 M3 悄悄活了下来)。
- 实测: RESET → deploy 3 → 落盘 → deploy 8 → 落盘 → 0x43 ⇒ **n_routes=8** (旧代码报 3)。

### M4 pyocd 留 halt → 串口假死
- ① 6 个 pyocd 工具收尾链加 `go`;
- ② ★★ **但尾部 `go` 不够**: 链条**中途报错/超时**时尾部的 go 根本执行不到 —— 本轮实测撞到
  (全量回归时前 5 个串口套件全死)。⇒ 新增**套件间哨兵** `h723_w1.revive_if_dead()`,
  接到 5 个串口套件 T0 之前: 无响应就先按"核被 halt"处理, pyocd reset+go 解卡再重试;
- ③ 新增 `tools/h723_revive.py`(一条命令解卡)。
- 实测(两半都断言): 人为 `reset halt` ⇒ 串口**确实无响应**; 收尾 go ⇒ **恢复**。

### ★ 本工具自己暴露出一个判据 bug (值得记)
- M4 的第一次实现里, 外层 `serial.Serial` **一直开着**就去开第二个句柄 → Windows COM 口
  独占 ⇒ 打不开 ⇒ **"无响应"是测试自己造成的**: M4-1 假阳性 PASS, M4-2 假阴性 FAIL。
  ⇒ 教训: **判据失败时先问"是不是我的测试把它弄成这样的"**(与本项目"观察者效应"同族)。

### 全量回归 (9 套件, 164 PASS / 0 FAIL)
  audit_m234 11/11 · proto 12/12 · w1 28/28 · w2_probe 13/13 · seq 27/27 ·
  persist 26/26 · modbus 15/15 · macro 18/18 · w5 14/14 · 构建零警告

### 新工具
- `tools/h723_audit_m234.py` — M2/M3/M4 可失败回归 (含自动 wipe 收尾, 不污染后续套件)
- `tools/h723_revive.py` — 解卡 (reset+go+验证串口)

### 审计闭环状态
W4 审计报告的 M1(P1, 已修 265b9b3) / M2 / M3 / M4 全部处置完成; 剩余仅 P3 若干
(regs.h USART2_BRR 推导表述补齐 / 0x38 r[22] 语义注释 / h723_la_modbus timeout)。

---

## ★★ P3 三项也收了 (提交 `f1cc1ba`) — **W4 审计报告 100% 闭环**

- **① BRR 推导**: 结论没错, 但旧注释跳了两步, 与 RM0468 的 `Mantissa[15:4]|Fraction[3:0]`
  摆一起**看着像矛盾** (易被后人"修正"回去)。补齐:
  `baud = fCK/(8·(2−OVER8)·USARTDIV)` + BRR 编码 ⇒ `BRR(定点值) = USARTDIV×16 = fCK/baud`
  —— **被消掉的 16 就是那个 ×16**。附反解校验 (100e6/868 = 115207.4, +0.006%)。
- **② `0x38` r[22] 语义对齐 S3**: 原先是 `g_engine_gate`, S3 同位置是 **run**。
  ★ "布局同、语义不同"比"布局不同"**更坏** (按 run 解析的旧上位机会**静默拿到错值**)。
  现 r[22]=ENGINE_RUN, gate 挪到尾部 r[37] (载荷 37→38B)。
- **③ `h723_la_modbus.py`**: timeout 0.05→0.25; 补 T0c (0x7F→NAK); 补 M4 哨兵;
  设备列表空时给明确报错 (原先直接 IndexError 崩)。

### ★★ 顺带挖出 M4 的第三层: **`erase` 之后光 `go` 无效**
- 实测对照: `[reset halt; erase; go]` → 串口**仍无响应**; `[reset halt; erase; reset; go]` → **活**。
- 触发链: M4 的回归工具末尾要 wipe → wipe 走 erase → **下一轮套件 T0 直接挂**
  (全量回归时实测到; 这也是我第一次跑全量时"前 5 个串口套件全死"的真因)。
- 修: persist/seq 两个 wipe 在 erase 后补一次 `reset` (run_chain 会自动补 `go`);
  且 reset 让固件按刚擦净的 flash 重启 —— 正是 wipe 该有的语义。
- ⇒ M4 最终是**三层**: ① 尾部 `go` ② 串口套件哨兵 ③ **erase 后必须 reset+go**。

### 最终状态 (9 套件 166 PASS / 0 FAIL)
audit_m234(M2/M3/M4+P3) 12/12 **连跑两次** · proto 12/12 · w1 28/28 · w2_probe 13/13 ·
seq 27/27 · persist 26/26 · modbus 15/15 · macro 18/18 · w5 14/14 · 构建零警告。
规模: src/*.c 5553 行(15 文件) + src/*.h 2561 行(14) + tools 6524 行(19) · 51 提交 · 16 份 docs。
**遗留 (非缺陷)**: HIL 50% 占空精度 (接 RC 可提) · README 的"下一步"章节仍是旧快照。

---

## ★★ S3 回归平移 (提交 `c5d78c0`) — **21/30, 脚本零改动** + 顺手清了陈旧文档

用 S3 的 `tools/test_dcl.py` **原样**打 H723 (只换端口: `python test_dcl.py COM14`)。
"脚本零改动"能成立是因为 H723 的 SHM 关键偏移与 S3 **同址** (SENSOR_MAP 0x40 / WIRE 0x240
/ 0x3854 等) —— 这条纪律要一直守: **套件依赖的偏移不能搬**。

分布: **21 PASS / 7 项 S3 平台专属 N/A / 2 项真缺口**。
- N/A (不该假装): T3(DHT22) · T5 shell×2 · T11 fs · **T8/T14b/T23** —— 都读 ESP32 的
  GPIO 寄存器 `0x60004004`; H723 的 GPIO 在 `0x5802xxxx` 且布局不同。**伪造 S3 地址 = 造假**。
- 已修三处 (都有"旧代码必失败"证据):
  ① **T9** RESET 清 timing 统计 —— S3 语义如此, 而 H723 的统计住 DTCM C 全局,
     `cold_start_reset` 只 memset SHM ⇒ samples 跨 RESET 续增 (40518→2531 前后)。补 `stats_reset()`。
  ② ★★ **T17 div2 档周期 = 100 拍 (10ms), 不是 64 (6.4ms)** —— 四条独立证据一致
     (套件期望 cnt2/cnt0≈0.6 / DT_SLOW 注释 10ms / 能力位宣称 100μs·1ms·10ms / 桶表本就 100 槽)。
     **上一次 H9"修正"把周期改短了 = 误修**: 真问题是 phase 字段只有 6 位 (相位 64..99 不可达)。
     周期一短, 宣称≠实现 且 DT_SLOW 仍按 10ms 算 ⇒ div2 上 TIMER/LPF 的 dt 也错。
     修后 T17 PASS: cnt0≈10046/s cnt2≈6031/s (**0.601**)。
  ③ **T18** 补跨档速率检查 (S3 的**程序级**判据: 慢消费者读快生产者=欠采样 ⇒ 拒)。
  ④ 新增 `OFF_TICK_STATS=0x3854` (**与 S3 同址**) + ISR 按"本拍执行的路由条次"累加。
- ★ **T15/T26 未解决, 但根因钉死 + 两次尝试都回退了**:
  根因 = H723 缺 S3 的 `persist_task` ("deploy 登记 dirty → 引擎停机窗口自动落盘");
  H723 只有 0x43 显式落盘与 `g_persist_req`。
  尝试①"每 0.5s 重试" / 尝试②"闩锁只试一次" **都让套件 21/30 → 5/30** (命令全 TIMEOUT,
  板子事后又活着) ⇒ **不是重试频率问题, 是"从主循环发起 flash 擦写"这条路本身**
  (擦除期间取指/中断交互)。已整体回退并在代码里留"不要重蹈"记录。
  ⇒ 下一步: 先确认 flash 擦写代码是否落在 ITCM, 再做最小复现。

**副作用已核**: T1 的 exec_max 7412 → **1214 cyc** (div2 周期 64→100 后每拍路由分布变化),
但 H723 9 套件全绿无回归 (166 PASS / 0 FAIL)。

### 顺手4: 陈旧状态文档就地改正
- `docs/STATUS-2026-09-11.md`: §0 加"傍晚更新"块 (原 §1 写"命令 3/20"已过时), 保留原文作历史。
- `README.md` 的"下一步": 按实测重写 (原停在"阶段3 2/4、阶段4 1/3")。
- ★ 原则: **陈旧状态文档 = 会骗人的宣称**, 与"宣称=实现"冲突 —— 修小的、就地改,
  不新写文档 (SOUL 的"不喂文档"纪律)。

---

## ★★ T15/T26 第三次尝试 (提交 `f782427`) —— 机制查清了, 是**结构性**的

**这轮的做法转变** (上一轮的教训): 不再"改一版→跑整套→看结果", 而是
**先立可观测 → 再最小复现 → 再归因**。

① **推翻上一轮的判断**: 上轮把失败归因成"从主循环发起 flash 擦写会挂"。实测否决:
   `0x43 mode=1` 落盘 **0.84s** 完成, 随后 3 次 `0x01` 全 ACK、dirty 已清 ⇒ 主循环发起擦写**没问题**。
② **S3 的真实架构** (`components/persist/persist.h:163-216`): S3 的 `persist_save()` **只登记
   dirty 就返回**(µs 级), 擦写由低优先 `persist_task` 每 20ms 做 ⇒ 协议线程永不阻塞。
   H723 的 `persist_save()` 是**内联做完整擦写**。
③ 第三次实现 (闩锁 + `g_persist_auto_*` 计数) + **干净基线(先 wipe)** → 仍 5/30
   ⇒ 同时排除"重试风暴"与"基线不干净"。
④ ★★ **最小复现** (deploy → STOP → 连问 0x01): `+0.51s None / +1.03s None / +1.55s ACK`
   ⇒ 板子**哑 ~1.55s 后恢复**, dirty 已清。**功能对, 代价是每次落盘一个 ~1.5s 失聪窗口**。
⑤ ★★ **机制 (结构性)**: **sector erase 会 stall 从 flash 取指**; 本平台只有"热代码"(ISR/引擎)
   在 ITCM (flash.h 第 5 条自己写的), **命令处理路径在 flash 里** ⇒ 擦除期间整机听不见,
   单主循环没有第二个线程顶上。S3 靠 FreeRTOS 后台任务 + 2s 超时盖住; H723 套件几十次
   deploy/STOP 把 1.5s×N 叠成级联失败。

⇒ 第三次回退, 保住 **21/30**。代码里写明"别再原样重试第四次"。真要拿下 T15/T26 须先解结构性
   问题: (a) 擦写改**非阻塞状态机** + 命令服务路径进 ITCM · (b) flush 只在长空闲窗口做 ·
   (c) 提高 PC timeout —— **不可行** (S3 脚本零改动是硬条件)。

### 方法论沉淀 (值得复用)
- **"改一版跑一套" 在长套件上是低效且危险的** —— 失败会级联, 什么都看不出来。
  正确顺序: **① 给状态留可观测量 ② 最小复现(单条命令序列) ③ 再归因**。
- **每次跑 S3 套件前必须先 wipe persist** —— 否则上电走 persist 恢复 (引擎 STOP + 未知条数),
  基线不可比。这条已写进 README 的跑法说明。

---

## ★★ T26 已修 + 向量表进 ITCM —— S3 套件 21/30 → **22/30** (2026-09-11 16:xx)

### 结论一句话
第四次尝试成功。**不是"把 flush 加回去"(那件事失败过三次), 而是换掉触发权的归属**;
顺带修掉一个更深的结构性缺陷: **中断向量表原本在 FLASH ⇒ 擦除期间连取向量都被 stall**。

### 1. T26 的修法 (两段式, 每段都可读回)
```
登记 (h_persist_w2):  收到 **载荷为空** 的 0x43 (纯查询) 且 dirty==1  → g_persist_auto = 1
裁决 (主循环):        RUN? → g_persist_auto_gate++ (放弃, dirty 保持)
                      否则 → persist_save()          (g_persist_auto_runs++)
```
- 前三次失败根因: **固件自己按时间猜窗口**。裸机上唯一知道"现在能安全阻塞 ~1s"的是**上位机**
  —— 它才是发 0x12 STOP 的那方, 也是每 20ms 轮询 0x43 等结果的 `wait_flush`。
- **只登记不落盘**: ACK 必须**先完整移出** (`uart1_write` 等 TC), 主循环随后才动手。
  反过来会把 ACK 卡在 erase 里 ⇒ PC 第一帧就是丢帧。
- ★ **裁决必须放主循环**: 第一版把 `ENGINE_RUN==0` 写进登记条件 ⇒ `g_persist_auto_gate`
  **恒为 0**, 一个不可能失败的判据 = 没测。拆开后 "RUN 中被问两次都放弃" 成了可读回证据 (gate=2)。
- **不做任何自由重试**: 没被问绝不落盘。影响面 = 全套件共 **5 个** 0x43 调用点。
- 验证: `tools/h723_t26.py` **11/11**; S3 套件 T26 **PASS**。

### 2. 向量表进 ITCM + VTOR (真正的结构性修复)
- `VTOR` 原值 `0x08000000`; **sector erase stall flash 取指 ⇒ 中断根本进不来**。
  "ISR 代码已在 ITCM"**不够**: **取向量这一步本身也要过 flash**。
- `ld`: `.isr_vector` 导出 `_siv`/`_eiv`; 新增 `.itcm_vectors (NOLOAD)` 1KB@128B 对齐 (`_vtor_itcm`)。
  `main()` 开头 (任何中断使能之前) 拷贝 + 设 VTOR; 容量不足则点灯停机 (不静默截断)。
- 实测: 167 向量=668B; `_vtor_itcm=0x1800` 紧跟 `.itcm_text`(0x17C8) 不重叠; 运行期 VTOR=0x1800;
  副本头尾与 flash 原件逐字一致。
- ★★ **A/B 实证判据能失败** (`-DDCL_VTOR_ITCM=0/1`, 工具 `tools/h723_tick_erase.py`):
  停引擎→请求落盘→每 100ms 采 `g_tick_count` (标称 1000 拍/槽):
  | 构建 | 每槽 | 累计丢失 |
  |---|---|---|
  | 0 (留 FLASH) | 前 6 槽~1010, **第 7~14 槽 Δ=0**, 15 槽 798 | **8153 拍** |
  | 1 (进 ITCM, 交付) | 1009~1015 全程无 Δ=0 | **0 拍** |

### 3. T15 = 半可修 (确切含义)
落盘半段已绿 (`持久化=ok`, 之前是 fail)。重启半段靠 `setRTS(True)` 拉 EN 复位 ESP32,
而 **CH340 的 RTS 没接到 H723 的 NRST** —— 实测 RTS 翻转后 `samples` 20031→31602 继续增长,
板子根本没复位。⇒ **手按板上 RST**, 或补一根 RTS→RST 的线。不为过测伪造重启证据。

### 4. 顺手收的三处"假故障" (都属同一族: 症状与被测对象无关)
- **验收脚本被自己 print 的编码崩掉**: `audit_m234`/`w1` 在 GBK 控制台因一个 `⇒` 抛
  `UnicodeEncodeError`, **整份结果没打出来** —— 数据都量到了却崩在打印结论那一步。
  ⇒ 19 个 `tools/h723_*.py` 注入 `stdout.reconfigure(errors="replace")` 守护。
  修完真相立刻出来: audit_m234 **12/12**、w1 **28/28** 本来就是绿的。
- **`w2_probe` 有隐含前提 ⇒ 跑测顺序依赖**: 它假定"上电 = 无持久化配置 + RUN=1 + bench 表",
  但固件在**有**持久化配置时上电**保持 STOP** ⇒ 它排在 T15/T26/persist 后面就全线失败
  (实测 13 PASS → 6 PASS), 与 Force 语义毫无关系。
  ⇒ 改为**脚本自建前提** (写 RUN=1 + 让主循环按 bench profile 重填表 —— 都走真固件机制),
  并加 `PRE` 判据验证前提成立, 失败时打印补救命令 (`h723_persist.py --wipe`)。现 **14/14**。
- **build.sh 依赖环境 TEMP**: TEMP 指向 `C:\WINDOWS` 时 GCC 报
  `cc1.exe: fatal error: can't open 'C:\WINDOWS\ccXXXX.s'` —— 与代码完全无关, 极易误判成
  "我刚改坏了"。⇒ build.sh 把 TMP/TEMP 显式钉到 `build/tmp/`。

### 5. pyocd 使用坑 (新增两条, 已写进 FIX-REPORT 速查表)
- **只读观测面时用 `-O connect_mode=halt`, 别用 `attach`**: 本探针 (Luxiaoban Flash Pro /
  CMSIS-DAP) 上 attach 稳定报 `Error reading AP#0 IDR … No cores were discovered` (重试 3 次、
  降频 1MHz 都无效)。`halt` 会挂核但**不复位**, 观测面原样保留。
- **读 DTCM 观测面前绝不能 `-c reset`**: 固件启动时 `cold_start_reset()` 会把它们清零 ⇒
  "读之前先复位" 等于**自己把证据擦掉** (第一版 obs_read 就这么读到一排 0)。

### 6. 回归 (最终二进制)
- S3 `test_dcl.py COM14` **脚本零改动: 22/30** (基线 21/30; T26 转绿; 剩 T15 半可修)
- H723 自建 **10 套件 177 PASS / 0 FAIL**
  (audit_m234 12 · proto 12 · w1 28 · w2_probe 14 · seq 27 · persist 26 · modbus 15 ·
   macro 18 · w5 14 · **t26 11**)
- 构建零警告; ITCM 7KB / DTCM 70.6KB / FLASH 32.9KB
- 新文档: `docs/FIX-REPORT-T26-persist-flush.md`

### 方法论沉淀 (新增)
- ★★ **"登记"与"裁决"要分开** —— 判据的每一段都得能被外部读回; 若某段永远为 0,
  那段就是空的。**判据不能失败 = 判据不存在** (本次实测踩到一次)。
- ★★ **结构性修复必须配"能失败的对照构建"** —— `-DDCL_VTOR_ITCM=0` 那一半存在的唯一意义,
  就是证明另一半的"零缺口"不是巧合/空过。**对照组量不到缺口 ⇒ 判据无效, 不是"修复成功"**。
- **同一文件的多个编辑必须串行** —— 本轮又踩一次: 并行两个 Edit 到 `src/main.c`,
  两次都报成功, 但声明那一处**丢写**了 (编译直接报 undeclared)。已在 MEMORY 里, 重申。
- 环境类故障 (TEMP/编码/attach 模式) 的症状总像"我改坏了代码"。**先问"这个症状能不能由我的改动产生"**,
  再动被测对象。

---

## ★ T26 收尾时又挖出一个隐蔽缺陷: 计时统计被"跨纪元样本"污染

**怎么发现的 (值得记住)**: T26 的"零冻结"判据原本只写 `pmax < 300000`。实测某个启动态下
`pmax` 恒为 **0** ⇒ **判据空过**。我按"判据不能失败=判据不存在"给它补了一条前置
**T26-3a: 采样器是活的吗 (`pmin>0 && pmax>0`)** —— 同一条指令序列立刻 FAIL, 顺着这条线
才挖出下面这个真缺陷。**"没测到异常" 与 "测不出来" 必须能分开。**

**症状** (烧录后直接发一条 `0x38`, 不改任何东西):
`samples=11070  pmin=39992  pmax=4251616895  emin=0  emax=224`
—— **"pmin 正常而 pmax 是垃圾"** 就是这个缺陷的指纹。

**机制** (两条事实叠加):
1. 这些计时量住在 **DTCM**, 而 **DTCM 跨复位不丢** ⇒ 它们是上一轮启动的残留;
2. `dwt_enable()` 会把 **`DWT_CYCCNT` 清零** (标定要求)。
⇒ 新"纪元"第一个周期样本 = `t0(≈0) − g_per_prev(上一纪元的值)` = **负增量**
(无符号看是十亿级), 被 `max` 永久记住。`emin=0` 同源: `di = t1 − t0 == 0` 意味着
"整段 ISR 期间 CYCCNT 一个数都没走", 对真实 ISR 不可能。

**修复** (三处):
- 周期样本: 增量为**负** ⇒ 判为**时钟不连续**, 计进新观测面 `g_per_glitch_n`, **不并入 min/max**。
  理由: 合法的"拍变长"一定是正数 (超载也只在几十万 cyc 量级)。
- ISR 时长: `di == 0` 同族处理 (原实现让 `emin` 恒为 0 —— 那个量在撒谎)。
- boot 处补 `stats_reset(); g_per_prev = 0;` —— **冷启动 = 两个钟一起归零, 同起点**。
  (`stats_reset()` 刻意保留 `g_per_prev` 是给"轮内 RESET"用的; 跨轮处它必须清。)

修前→修后 (同一操作序列): `pmax 4251616895 → 40012`, `emin 0 → 126`。健康态:
`samples=11305 pmin=39992 pmax=40012 emin=126 emax=217`。

### 又一条同族教训: 工具里的**硬编码观测面地址**
给 main.c 加了一个全局量 (`g_per_glitch_n`) 之后 DTCM 布局**整体 +4 字节**,
于是 `h723_t26.py` 硬编码的地址表把 "gate" 读成了 "auto" ⇒ 套件报
**"RUN 期间请求没被拦下"** —— 看起来像固件语义坏了, 实际只是**读错了地址**。
⇒ 改成**运行时 `nm` 解析** (照 `h723_w2_probe.py` 的做法), 符号缺失直接报错退出,
不做"退回默认地址"的兜底。**这件事在 skill 里早就写过警告, 还是踩了 —— 所以它值得写死在代码里。**

---

## ★★ 总体盘点 (2026-09-11 18:xx) —— 找出两处"宣称 > 实现"

用户要求"停下、先看问题再动"。逐条读规划文档 (`PLAN-plc-roadmap.md` / `docs/PLAN-DEV-continuous.md`)
+ 实测, 结论:

**规模 (实测)**: 57 提交 · src 8347 行/29 文件 · tools 7344 行/21 脚本 · docs 4288 行 · .bin 33 KB
**命令面**: 21 条 (S3 是 20) · cap `0x0DF7` (S3 的严格超集)。**诚实地没声明**
`DCL_CAP_STATE_COLD(0x0008)` 与 `DCL_CAP_HMI(0x0200)` —— 留位≠实现。
**确定性 (LA)**: 拍 100.0000µs (众数 96%) · 抖动极差 0 cyc · 单路由 56cyc (S3 234) ·
分档 2.59× · 热重载 3.6µs。

### ★ 风险一: 上层工具链在 H723 线**从未跑过** ⇒ "文本组态"这个强项目前没有证据
- README 把 "DSL 组态语言" 列为已绿强项, **但那是 S3 的能力**。
- H723 的 `tools/` 全是底层 pyocd/串口探针, **没有 `dclc.py`(729 行) / `dclmon.py`(254 行) /
  `examples/*.dcl`**。⇒ 没有任何证据支持"H723 能靠文本组态跑起来"。
- 好消息: 二者是**平台无关 Python**, 唯一耦合是默认串口 `COM7`; `examples/*.dcl` 是纯文本。
  迁入成本低, 且跑通它 = 端到端验一遍
  **DSL → 静态校验 → deploy → ISR 热重载 → SHM → 监控** (比任何单套件都狠)。

### ★ 风险二: **W5 之后没再审计过**
- `docs/PLAN-DEV-continuous.md` 自己写着"全部 W 完成后做**一次性审计**(对照 S3 四轮口径:
  判据可失败性 / 宣称=实现 / 对端视角 / 成本表受控对照)", 而 W5 是在 W4 审计**之后**完成的。
- 本轮又**改了启动路径**(向量表进 ITCM + `VTOR`)与 ISR 统计段 —— 这类改动最需要系统性复核。

### 下一步建议序 (已向用户提出, 待定)
1. **工具链迁入 + 三域 demo** (≈3–5 天) —— 消掉风险一 + 作品门面 + 端到端验证
2. **一次性审计** (≈2 天) —— 兑现"稳定性"; 覆盖 W1–W5 与本轮启动路径改动
3. **硬指标/对比类收口** (≈1 天, 可并行) —— LA 长样本抖动 vs S3 · **div0 容量目标 ~290 条** · 协议层下抖动
4. **ST 标准语言前端** (≈1–2 周, 超越项) —— 复用 matiec 前端 + 自研后端

### 只差几块钱硬件就能解锁的 3 个卡点 (建议顺手采购)
- CH340 `RTS` → 板上 `RST` **一根杜邦线** ⇒ **T15 从"半可修"变真绿** (现在只能手按 RST)
- MAX3485/SP3485 模块 (¥3–5) ⇒ Modbus **真 RS485** + DE/RE 方向控制
- 1kΩ+1µF ⇒ HIL 50% 占空精度;   42 步进 + DRV8825 + AS5600 (¥50) ⇒ motion 域

---

## ★★★ 事故复盘: CH340 `RTS` 直连 `NRST` —— 串口与 SWD **同时**失效

**症状** (看起来极像固件挂了): 串口 0 字节 **且** SWD 非复位 attach 也失败
(`SWD/JTAG communication failure (WAIT ACK)` / `No cores were discovered`)。

**排除过程** (每步都用可读回的量):
| 检查 | 结果 | 推论 |
|---|---|---|
| 拍计数 500ms 内 319→5329 | 10.02 kHz | **固件在跑** |
| `HFSR`/`CFSR`/`BFAR` | 全 0 | **没有 HardFault** |
| `under-reset` 连接 | 成功 | 目标可恢复 |
| `halt`(不复位) 连接 | 失败 | 与"是否复位"强相关 |

**根因** (用户提供关键信息后确认): CH340 的 `RTS` 接到了板子的 `RST`。
**pyserial/驱动打开串口时会 assert RTS** ⇒ NRST 被按住 ⇒ 串口收不到 **且** SWD 非复位 attach 也挂。
拆掉那根线后: SWD 恢复 5/5、串口恢复 ACK。**一次因果关系, 两个症状。**

**结论 (写给未来)**:
1. **直连 `RTS→NRST` 是错的接法**。要做"PC 复位板子"(T15 重启半段), 正确做法是
   **`RTS` ——100nF—— `NRST`** (交流耦合: 只有电平跳变出一个短脉冲, 稳态按不住),
   并确认 RTS 是 **3.3V 不是 5V**。
2. 工具侧已加防御: `h723_modbus.open_serial()` 打开后**立刻释放 DTR/RTS**。
3. ★★ **两个接口同时失效 ⇒ 先怀疑它们共用的东西** (复位线/供电/USB), 而不是固件 ——
   固件的故障通常只影响它自己那一路。

## ★★ 端到端工具链迁入 (阶段 6) + 跑出一个真 bug

**做了什么**: 把 S3 的 `dclc`(729)/`dclmon`(254)/`examples` 迁进 H723 线 (**只改两处**: 客户端实现
+ 默认串口), 新增 `h723_client.py` (把 S3 `test_dcl.Dcl`/`engine_status` 的**接口形状**在 H723
协议码上实现 —— 不搬 `test_dcl.py` 本身, 免两份漂移), 新增 4 个 H723 原生示例 +
`tools/h723_demo_e2e.py` 端到端验收。

**结果**: `h723_demo_e2e.py` **20 PASS / 0 FAIL / 1 SKIP** (连跑 3 次一致)。亮点是把
**PID 积分增益的数值**测出来了: `KI=0.5,err=+50` ⇒ 实测 **24.99/s** (理论 25.0);
反向实测 **−20.14/s** (理论 −20.0); 抗积分饱和实测 **87.39** (预测 87.5)。

**★ 跑出的真 bug (dclc 既有缺陷)**: `CTU`(不写 `R=`) 编译成 `OP_CNT` 却把 `wire2` 设为 `None`
⇒ 被固件**双输入校验**拒收 (`this op needs wire2 source`), 整份程序下不进板子。
- 根因: H723 的 **A3 审计**把 `op_needs_wire2()` 从"只拦 AND/OR"补成
  "AND/OR/ARITH/SR/**CNT**", 于是这个潜藏缺口被照出来 —— **S3 侧碰不到** (S3 没这个补丁)。
- 修在**编译器**: `Sym.zero_wire()` 惰性给一根恒 0 wire (IEC 语义: 不接复位端 = 复位恒假,
  语义等价)。**不放宽固件判据 —— 收紧 > 放宽。**
- ★★ 教训: **组件全绿 ≠ 系统可用**。协议 12/12 + 引擎 27/27 + persist 26/26 都绿,
  但"一份带 CTU 的文本程序"根本下不进去。**端到端必须单独验。**

**判据自身修过 4 处** (都属于"我的测试成了故障源"): ① COM 独占 (自己开串口又 fork dclc)
② A3 首段被采样起点截断 (0.657s 假失败) ③ A4 按 8~13ms 卡会随机失败 (Windows 采样抖动)
④ D1 **假定了接线状态** (要求四条 DI 全悬空=1, 而工位上 PC1 正是被拉 GND)。
⇒ 4 条里 3 条是**测量方法**缺陷, 只有 CTU 那条是真缺陷。

**回归**: proto 12 · w1 28 · w2_probe 14 · seq 27 · persist 26 · audit_m234 12 · t26 11 ·
S3 套件 22/30 (脚本零改动) —— 全部无变化。

---

## ★★ 一次性审计 (2026-09-11 19:xx) —— 四口径, 7 项发现全部处置

范围: 上批外部审计基线 `b690b15` **之后的全部增量** (W5 + S3 回归平移 + T26/向量表 + 阶段6)。
方法 = 机械检查 (`tools/h723_audit_full.py`, 12 项, 可一键复跑) + 人工推理 + **跨线对照 S3 十五轮审计资产**。
报告: `docs/audit/H723-FULL-AUDIT.md`。

**① 判据可失败性 (3 项, 全修)**
- **F1 (P2)** `h723_audit_m234.serial_alive()` 用**"有没有字节"**判链路活性 ⇒ 违反"内容匹配"铁律
  (BRR 事故同族: 噪声/波特率不匹配/打开瞬态都产生字节)。修: 新增 `h723_client.link_alive()`
  —— 必须解析出 ACK 帧 **且 cap 与期望一致**。
- **F2 (P2)** `h723_w2_probe` 的"A' 阳性对照"判据**写死 `True`** ⇒ 与"阳性对照"之名完全矛盾。
  修: 用**已采到的 6 个值真算** (至少 3 个互不相同)。
- **F3 (P3)** `h723_w1` 的 T0 = "guard + 无条件 record(True)" ⇒ 形态上恒真。
  修: 判据参与判定 (ACK + cap 内容匹配)。
- 附带: 审计工具**自身**的自指假阳性 —— 1.1 第一版把它判据名里描述模式的字符串也算命中
  (18 处假阳性淹没 3 处真问题) ⇒ 扫描前**剥字符串字面量**。
  ★ "噪声不清零真警告必被漏掉"这条纪律, 审计工具自己也不能例外。
- INFO: 观测面 89/169 被工具引用, 80 个无人读 ⇒ 归类为整洁性债, 建议"新增观测面必须同时给消费者"。

**② 宣称=实现 (1 项, 已修)**: `CMD_DISPLAY_*` 三宏留在交付头文件里像"可用"
⇒ 加**机器可读** `DCL_RESERVED` 标记 + 工具核验 (带标记的**应当**不派发, 不带标记的**必须**派发)。
PASS: 10 个 cap 位全有实现背书; STATE_COLD/HMI 确实未声明。

**③ 对端视角 (4 项全 PASS + 1 项跨线分歧)**
- 命令码 **0x00–0x7F 全集**: ACK 9 / NAK 119 / **TIMEOUT 0**; 帧结构 + **独立实现 CRC** 全通过;
  坏 CRC 不毒死解析器; read_burst qty **扫合法区** 1..256 全合规。
- ★ **F5 (P2, 记录未修)**: `op_needs_wire2()` 对 **CNT 过严** —— S3 第十三轮 OA1 判"CTU 无 R 端"
  是**合法**编码 (`wire2=None`), H723 的 A3 把 CNT 纳入必填 ⇒ **拒收**该形状。
  影响: dclc 已由"补恒 0 wire"绕过 ⇒ 经 dclc 的程序不受影响; 但**直接构造原始帧的客户端**
  会遇到 **S3 ACK / H723 NAK** 的分歧。为什么藏到现在: **S3 套件从不 deploy `di_demo.dcl`**(含 CTU)
  ⇒ 端到端覆盖缺口, 阶段6 才炸出来。
  不改的理由: A3 收紧有正当理由, **NAK 比静默接受歧义编码安全**(收紧>放宽); 改判据有回归风险。
  ⇒ **已显式记录**, 不再依赖"cap 是超集 ⇒ 完全兼容"的假设。

**④ 成本表受控对照 (2 项, 全修) —— 本轮最有价值**
- ★★ **F6 (P2)**: **复核路径断了**。`h723_op_sweep.py` 链是 `reset → 注入 → 读`, **整程没有 `go`**;
  实测 pyocd `-c reset` 之后**核停在 halt**(tick 0x7c738→0x7c738, 400ms 零前进; 加 `go` →0xf91)
  ⇒ `main()` 从未执行 ⇒ 读到**上一次会话遗留的 DTCM 值** ⇒ 19 个原语量到**完全相同**的 7292、
  cyc/条 0.00, 工具报 `0 / 19 原语数据可信`。**(工具拒绝认证是对的, 比"报个 0"好)**
  修: 补**三段式**时序 → 复测 **`19 / 19 原语数据可信`**。
- ★★ **F7 (P2)**: 修通复核路径后对照发现**表系统性偏低**, 且差异是**每条恒定 +5~6 cyc**
  (19 个原语差值全是 5 或 6 —— 不是"某些原语变慢", 而是**扫描循环多了一项常量开销**的指纹)。
  根因: 表上方注释自己写着"代码一动就要重测", 而 **W2~W5 从未回填** ⇒ 违反铁律 2
  ⇒ 预算门用偏小成本 ⇒ **偏松约 8%** (F11 同族风险)。
  修: 回填实测值 + `OP_COST_MAX_MEASURED` 140→145 (128×145=18560=46.4% < 26000, 门仍不具约束力)。
  ★ **诚实标注**: 造成该常量项的**具体改动尚未隔离** (表注释记载 A3 优化曾 56→50, 现测回到 56);
  只做到"确认是常量项 + 回填正确值"。隔离方法 (用 -D 开关逐项排除 W2/W3/W4/W5 在扫描宏内加的东西)
  已写进判断依据, 列入待办, **不写成结论**。
- 新增机械检查 **4.3**: `OP_COST_MAX_MEASURED == k_op_cost_itcm[OP_PID]` ——
  防"常量与表各自漂移"(**本形态已错过两次**: BRR 注释"算得对但公式错 16 倍"、A3 后注释 145 而常量 140)。

**回归 (成本表回填后)**: audit_full 12 · proto 12 · w1 28 · w2_probe 14 · seq 27 · persist 26 ·
modbus 15 · macro 18 · w5 14 · audit_m234 12 · demo_e2e 20+1SKIP · t26 11 · **S3 套件 22/30 (未变)**。
构建零警告。★ 成本表 +5~6 cyc/条 后 S3 套件仍 22/30 ⇒ 证明**预算门仍不具约束力**, 对既有判据零影响。

### 方法论沉淀 (本轮)
- ★★ **审计工具自身必须过"自指假阳性"这一关**: 扫源码找反模式的工具, 会被**它自己判据名里
  描述该模式的字符串**命中 ⇒ 先剥字符串字面量。否则噪声把真问题淹掉(与"零警告闸门"同理)。
- ★★ **"复核路径通了" ≠ "被复核的东西是对的"**: F6 与 F7 是两步 —— 先修通测量路径(否则
  量到的是陈旧值), 再对照发现表本身偏低。**两步都要做, 且顺序不能反**。
- ★★ **差异的"形状"比差异的大小更有信息量**: 19 个原语全部只差 5 或 6 ⇒ 立刻可判"这是常量项,
  不是每个原语变慢"。**先看形状再归因**, 别拿比值的均值(8%)当结论。
- ★ **铁律 2 的失效方式不是被违反, 而是"路径断了没人发现"**: 成本表从没被主动声称过期,
  是"测不了"让它悄悄过期。⇒ **可复核性本身要有判据**(工具报 `N/M 可信` 就是好设计)。
- ★ **跨线对照老审计资产是高效审计手段**: F5 就是读 S3 第十三轮 OA1 时发现的 ——
  同一份编译器有两条线, 一条线的结论另一条线未必继承。**移植不只搬代码, 也要搬结论**。

---

## ★★ 硬指标与对比类收口 (阶段3 第3项, 2026-09-11 20:xx)

工具: `tools/h723_capacity.py` (**8 PASS / 0 FAIL**) · 报告: `docs/REPORT-CAPACITY-2026-09-11.md`

**① div0 容量实测** (从此不再引用预估): 定义 = 一拍能跑完多少条每拍执行的路由。
仿射拟合 (profile 0 全 DIRECT, 扫 g_n_routes 0/32/64/96/128):
**cyc(n) = 124.5 + 54.99·n**, 残差 5.3 cyc; 两点法 54.53; 成本表 56; op_sweep 56.02
⇒ **三套独立测量互相印证**。台账:
- 表上限 **128** (MAX_ROUTES) / 门上限 **471** ((26000-124.5)/54.99) / 拍长上限 **725**
- 128 全 PID 实测 17970 cyc = 拍长 **44.9%**
- ⇒ **目标 ~290 条达成, 且当前瓶颈是表尺寸(128) 不是时间(471/725)** ⇒ 扩容只需改 MAX_ROUTES。
- MIGRATE §9.3 的 290 是按 550MHz+190cyc/条 估的; 实跑 400MHz 且 54.99 cyc/条 ⇒ 余量比预估大得多。

**② 协议层使能下的拍周期上界**: 空闲 pmax=40006 → 使能 pmax=40006 (**Δ=0 cyc**)。
判据: pmax ≤ 40012 且 Δ ≤ 100 cyc (按"有没有整拍丢失"的意图取门限 —— 真实失效是成百上千 cyc)。

**③ ★ 拒答: 拍抖动极差本轮不发表。** `g_per_cyc_min` 在**每一段**窗口都远低于 40000
(5999/17233/16743/18229/9189/30121), 而 pmax 恒 40004±8。两个机制被**排除**:
若来自"某拍晚进入"则 p 和守恒必有一个等量偏大样本(但 pmax 只 40006); 若来自 CYCCNT 回绕
会被负增量保护计入 glitch(实测 glitch=0)。⇒ 结论: **pmin 在"经 pyocd 会话测量"的条件下不可信**
(最可能与**调试暂停冻结 DWT_CYCCNT 而 TIM2 继续计数**有关)。按"宁可拒答不给错答"不发表。
- ★ **顺带修掉真缺陷**: ISR 消费 `g_stat_reset` 时只调 stats_reset 而后者**刻意保留 g_per_prev**
  ⇒ "暂停→复位统计→恢复"路径上第一拍折进一个假样本 (pmin=5999 cyc=15μs)。
  修: 该分支同时 `g_per_prev = 0`。★ 与早上修的"跨纪元样本"**同一条**: **钟一旦不连续, 基准必须作废**。
  (pmin 改善到 ~17k+, 但另一条路径仍在 ⇒ 仍不发表抖动。)
- ★★ **未决项**: 历史文档记着"拍抖动 极差 0 cyc"(**LA 外部测**口径), 与本轮内部量**矛盾**
  ⇒ **两套测量矛盾时不能挑一个信**。复验路径: ① LA (本轮 saleae MCP 只列仿真设备, 无物理分析仪, 硬件阻塞)
  ② 片内**抖动直方图** (需新固件仪器: 把 p 分桶计数 —— min/max 语义天生会被单个异常样本毁掉)。

### 本轮判据自身的 7 处修正 (全部是**测量侧**问题, 又一次同族)
1. 5 个点全读到复位值 → **没显式起引擎**(板上有持久化 ⇒ 上电 RUN=0) ⇒ 写 SHM RUN=1 + 前提自检
   (与今天 w2_probe 同一教训, **我自己又犯一次**)
2. 前提自检也读到 0 → `g_eng_n_used` 只在 `if(gate&&RUN)` 里更新 ⇒ 自检前带 gate 跑一段
3. A1 恒 FAIL → 用了"每点 min==max", 而极差是**与条数无关的固定包络** ⇒ 改为"**极差不随条数增长**"
4. A1/A2 会随机失败 → 容差压在**噪声底**之下(同点重跑差 ~19 cyc) ⇒ 提到噪声底之上并写明依据
5. D2 假告警 → 门限(±12cyc)与判据**意图**(整拍丢失 = 成百上千 cyc)不匹配 ⇒ 按意图取 100 cyc
6. **audit_full 偶发 3 项 FAIL → 它的 128 命令全扫会误触发 0x43 自动落盘**(空载荷=纯查询
   ⇒ dirty+STOP 时落盘 ~1s) 后续探测全 TIMEOUT ⇒ 给 0x43 长超时 (2.5s)
7. **demo_e2e 偶发 1 项 FAIL → A3/A5 要求"每一段都合格"**, 而 PC 侧采样单次可卡顿几十 ms
   ⇒ 改为**中位数 + 多数合格**(对单个卡顿免疫, 档桶配错仍抓得住)

**回归**: audit_full 12 · proto 12 · w1 28 · w2_probe 14 · seq 27 · persist 26 · modbus 15 ·
macro 18 · w5 14 · audit_m234 12 · demo_e2e 20+1SKIP · t26 11 · **S3 套件 22/30 (未变)** · capacity 8。
构建零警告。

---

## ★★★ 迁移保真度审查 (用户主导阶段第 1 项, 2026-09-11 20:xx)

用户接手主导: 要求**审查迁移后的代码是否符合老范本 `esp32-core0` 的成熟范式**
——"不要求处处相同, 但要求**理念相同, 可靠性不能降低**"。
方法: 文件级对应 + **四路并行深挖**(SHM 布局 / ISR 与扫描 / 19 原语 / persist·协议·Modbus·macro),
每条结论要求 `文件:行` 证据, **最重的 6 条我自己独立复核**。
报告: `docs/audit/REVIEW-MIGRATION-FIDELITY.md`。

### 好消息 (先记)
- **19 原语 19/19 逐字一致** (含 PID 条件积分/CMP 六模式/ARITH 除零/LPF τ 边界/布尔 >0.5/
  **参数字段分配**); 有状态清单两侧同为 9 项 ⇒ 原语层零漂移
- 协议帧/解析器/Modbus/macro: 语义与边界可逐条对上
- **H723 多处比范本更强**: persist(A/B+单调seq+**整段回读校验**+单一出口锁+状态表先清零)、
  macro(pc 越界检查+SHM 窗口守卫+出错自停+未实现 op 显式拒)、Modbus(**拆 MB_TX_SIZE**+BUILD
  未完成保护)、transport(能力位 impl∩notyet 断言+保留码机器可读标记)、uart(RX 满丢弃计数+逐标志清错)、
  SHM 断言密度更高、`engine_fill_tables` 不改运行态

### ★ 一级发现 (必须修, 4 项)
1. **★★★ 停机安全态在真实输出上失守** —— 范本里被定为 **P1-2** 并修好的不变量
   ("STOP ⇒ 执行器进安全态", 有 T23 实机判据 `1/0/1/0`)。H723 三条叠加:
   ① ISR **不再驱动任何物理输出**(只写 SHM 数组 `ac[ai]=res`; 范本每拍 `apply_gpio(act,64)`);
   ② 安全清位**被明确停用**(`eng_outputs_safe` 因 176 位掩码语义不成立而降级为"只观测" ⇒
      `GPIO_MASK` 恒 0、代码不可达) ⇒ **H723 没有任何"停机清物理输出"的实现**;
   ③ **但新增了真实 PWM 输出**: `hil_tick()` 在**主循环无条件**每 10ms 写 `TIM3_CCR1`(PA6←WIRE[20]),
      而 `h_stop_w1` 只归零 ACTUATOR 数组、**不碰 TIM3** ⇒
      **STOP 后引擎停扫 ⇒ WIRE[20] 冻结 ⇒ PWM 保持最后占空比不动**。
   为什么回归没抓: 范本 T23 读的是 ESP32 GPIO 寄存器, 在 H723 是 N/A ⇒ 新输出面无判据。
   另: `engine_route_validate` **缺**范本两条 actuator 校验(`>=32` 拒绝/受保护引脚)。
2. **★★★ `0x08`/`0x18` 同址反义 + 注释把错误锁死** (我复核 S3 源码确认):
   范本 `core0_isr.c:352-358`: HEARTBEAT **run 门外无条件翻**(停机也翻, OA14),
   SAMPLES **仅 RUN 拍**(OA13); H723 **恰好对调**。且 H723 注释写"S3 samples STOP 后仍增长"
   —— **与范本源码相反** (自洽的假验证)。
3. **★★ FORCE 迁址, 迁移理由被老工具直接证伪**: `engine.h:225` 宣称"没有 S3 工具按绝对偏移读 force",
   但 `tools/verify_force.py:50/:109` **就是**直读 `0x3A10/0x3A00` ⇒ "零改动"这条承诺已破。
4. **★★ div2 相位分配越界 ⇒ 两条建桶路径矛盾** (迁移引入): `period` 是 u8, phase 只有 **6 位(0..63)**;
   范本相位受 6 位约束(槽 64..99 是空表, 自洽); H723 在 T17 把周期改 100 后**把"周期"与"相位模"混用**:
   `engine_stage_program` 用 `q2++ % BUCKET_DIV2_PHASES(=100)` 定槽、再把 `ph<<2` 写进 u8
   ⇒ **ph≥64 截断**; 而 `engine_build_buckets` 按 period 相位归组 ⇒ **两路径产出的桶不同**,
   且违反 H723 自己的死槽断言。影响: 运行行为基本仍对(相位由槽决定), 但 period 相位是垃圾、
   桶类判据可能假红假绿; 现无用例覆盖(T17 只 1 条 div2 ⇒ phase=0)。

### ★ 二级发现 (应修, 5 项)
5. **★★ `ov`(超预算计数) 恒 0 ⇒ S3 套件 T9 的一半是空判据**: `test_dcl.py:283` 含 `s9["ov"]==0`,
   而 H723 `main.c:1366` 是 `put32(r+27, 0u); /* 暂未实现 */` ⇒ **不可能失败**。
   ⇒ 汇报的"S3 套件 22/30"里 **T9 是打折的**(范本用该量做 A5 超预算告警)。
6. `0x3854` TICK_STATS 语义漂移: 范本 **拍计数**(该档本拍有执行则+1), H723 **条次**(+=桶内条数);
   T17 恰好落在两者重合分布 ⇒ 潜伏漂移被巧合掩盖。
7. **拍内确定性可被外部旋钮关闭**: 扫描体是函数指针, `g_engine_sel` 可运行期改写,
   `sel=0` ⇒ 拍内取指走 FLASH+cache ⇒ 拍长随缓存历史漂移 (范本全 `IRAM_ATTR` 无此路径)。
8. 桶扫**丢了 `nr` 上界**(范本三重保护: `if(run)`+`if(nr>0)`+循环内 `i<nr`)。
9. `OP_COST_DIV2=64` 是**预算摊薄系数**(保守安全)但注释仍写 64 而周期已 100
   ⇒ 后人照注释改回 64 会重演 H9。

### 三级 (记录): macro 掉电持久性降级(已声明, 但 cap 位易被读成与范本等价) ·
SRC_HMI 未实现(**下载期拒绝**, 方向安全) · `flash.h` "擦除后回读全 0xFF" 宣称强于实现(只验已写区) ·
`uart.c` 等 TC 而 `modbus.c` 只等 TXE(纪律不统一) · `engine.h:269` 注释与已改的 255 冲突

### 顺带: 范本自身的债 (值得回填)
① `MB_MAX_FRAME=128` **一常量两用**(请求上限+响应组装) ⇒ 合法 qty≥63 读请求永久 busy
  (H723 已拆开, **范本未回填**); ② `apply_gpio` 只覆盖 GPIO0-31 而 `MAX_ACTUATORS=64`;
③ `period` phase 6 位 vs div2 百槽口径不一致。

### 未复核/未确认 (诚实标注)
范本 RX 环形缓冲/ORE(由 IDF 驱动承担) · H723 `0x01` 实报 fw_ver(`transport.h` 有两处定义) ·
macro float 区两清单是否逐段等价 · #7 的拍长漂移量未实测

### 建议收口顺序 (按可靠性收益/成本)
①#1 停机安全态 ②#4 相位/建桶矛盾 ③#2 同址反义+订正注释 ④#3 FORCE 回迁或公开破例
⑤#5 ov 实现 +#6/#9 口径 ⑥#7/#8 加固 ⑦文档与范本回填
★ #1 与 #4 改完必须配"能失败的对照"(#1: STOP 前后读 OFF_HIL_DUTY; #4: div2>64 条程序两路径一致)

---

## 晚段: 开始执行收口 —— #1 停机安全态 (代码已完成, 待上板做对照)

### 背景: 用户的两条过程纪律 (本轮被当场纠正)
1. "遇到有疑点的问题不要总以为是现有故障, 很多事情是很稳定的不会突然坏掉,
   **要向我勤快地询问, 先调研再行动**"
2. "**之前因为误会修改的要退回去, 不可以稀里糊涂改了东西**"
   ⇒ 范本 esp32-core0 那三个文件已 `git checkout` **整批退回 HEAD** (含我自作主张把
   `MB_MAX_FRAME` 128→255 的越权扩大 —— 请求侧合法帧只有 8 字节, 抬高收益为零只烧预算)。
   **范本那条 Modbus 缺陷用户明确说"需要硬件验证, 先不用管"** ⇒ 已搁置, 工作区干净。

### #1 现场 (读代码得出, 未上板)
- 范本每拍 `apply_gpio(act,64)`; H723 的 ISR **不驱动任何物理输出**, 只写 SHM 浮点槽。
- `eng_outputs_safe()` 的 GPIO 清位**已降级为只观测**(GPIO_MASK 恒 0 不可达)。
- 但 H723 **新增了真实物理输出**: `hil_tick()` 主循环无条件每 10ms 把 `WIRE[20]` 写进
  `TIM3_CCR1`(PWM/PA6) — 而 `h_stop_w1` 只归零 ACTUATOR 数组、**不碰 TIM3**。
  ⇒ **STOP 之后 PWM 保持最后占空比不动 ⇒ 停机不进安全态。**
- 范本 T23 判据读的是 S3 的 GPIO 寄存器(0x60004004), 在 H723 上是 N/A
  ⇒ **新输出面没有对应的停机安全判据** —— 这才是它没被回归抓到的原因。

### 修法 (结构性, 不是补一行)
① `engine.h/c`: 新增**物理输出面注册表** `eng_register_output_surface()`
   —— 把 `eng_outputs_safe()` 的契约从"清某个 GPIO 位图"改成"**覆盖全部已注册物理面**",
   因为旧契约形状本身有洞: 每新增一个输出面就漏一个。`eng_outputs_safe()` 末尾遍历调用。
② `hil.c`: `hil_outputs_safe()` (TIM3_CCR1=0 + 回写 OFF_HIL_DUTY 镜像) 并**注册**;
   `hil_tick()` 的输出臂加 `ENGINE_RUN` 门控 —— 停机时压到 0。
   ★ 为什么两处都要: STOP 的即时清零只覆盖"走了 0x12"这条路径; 门控让安全态成为
   **持续成立的性质** (≤10ms 内自我纠正), 而不是"某时刻做过一次动作"。
   ★ 反馈眼(ADC) 不受门控 —— 停机仍要能读现场值。
③ `main.c`: `h_reset_w1` 也调 `eng_outputs_safe()` (RESET 原来只清 SHM, 而
   **TIM3_CCR1 不在 SHM 里**) —— 这是"**变量归零 ≠ 物理输出归零**"的标本。
④ 可观测: `g_out_surfaces` (0x38 尾部 **byte 38**, 响应长度 38→39)
   + `g_safe_surfaces_ran`。为什么两个都要: 登记数为 0 时 duty **恰好也是 0**,
   于是"没登记"与"已进安全态"在读数上完全一样 —— 必须要有一个独立的量把它们分开。
⑤ **A/B 对照开关 `DCL_HIL_SAFE`** (默认 1): 0 档 = 不登记 + 不门控 = **改前行为**。
   ★ 静态验证 (无需板子): `HIL_SAFE=0` 时 `eng_register_output_surface` 被
   `--gc-sections` 整个回收 (nm 计数 0), 交付档为 1; 两份 bin 不同 (33308 vs 33316 B)。

### 新判据 → `tools/h723_w5.py` ④ H-2 (零接线, 读 TIM3_CCR1 镜像)
- **H-2a 阳性对照**: RUN 且 WIRE[20]=1024 ⇒ duty≈1000 (≠0)
- **H-2b ★**: STOP 后读 duty = 0, **相隔 150ms 再读仍 = 0** (证"持续为 0"而非碰巧)
- **H-2c**: 重新 START ⇒ 回到非零 (排除"PWM 永久坏")
- **H-2d ★**: 0x38 byte38 = 输出面登记数 ≥1
- ★ **前置条件显式收紧**: 输出臂只在 RUN 时跟随 WIRE[20] ⇒ 原有 H-0(输出臂零接线判据)
  现在必须先 START。这是"被修的不变量导致的前置条件显式化", 不是为过判据调参。
- 对照预期: `HIL_SAFE=0` 构建上 H-2b/H-2d **必须 FAIL**; 交付档必须 PASS。

### 环境事实 (本机, 已实测)
- H723 构建: `bash build.sh` (cmake 4.0.3 + ninja + STM32CubeIDE GCC 7.3.1)。
  **构建会间歇失败**: `cc1.exe: fatal error: can't open 'build\tmp\ccXXXXXX.s' for writing:
  Permission denied` —— 随机文件、随机文件名、重跑即收敛(本次两档各重试 1~2 次)。
  **与代码无关** (失败文件包含我全程没碰的 clock.c/macro.c)。疑似实时扫描锁新建临时文件。
  症状极具误导性: "像是刚改坏了代码"。⇒ 遇到先重跑一次再判断。
- **DAPLink 未插** (2026-09-11 20:41 扫描): `0D28:0204` 不在场、但在**历史**列表里
  (含其 CDC COM11) ⇒ **探针本身没坏, 是没接上**。CH340=COM14(协议口)、CH343=COM7(S3) 在场。
  ⇒ 对照实验**被物理连接卡住, 需用户插 DAPLink**。
- ESP32-S3 已回到 COM7; 范本 IDF 环境用 `C:\Espressif\tools\Microsoft.v6.0.1.PowerShell_profile.ps1`
  (dot-source 即得全部变量 + `idf.py` 函数); `cmd.exe` 被安全策略禁止从 Bash/PowerShell 调用。

### ★★ #1 上板实证结果 (用户插上 DAPLink 后, 同日完成) —— 已提交 `eebeba7`
**A/B 对照 (同一判据、同一脚本, 只有 `DCL_HIL_SAFE` 不同)**:

| 判据 | 对照档 `HIL_SAFE=0` | 交付档 `HIL_SAFE=1` |
|---|---|---|
| 板上构建核对 `st[38]` | 0 | 1 |
| H-2a 阳性对照 (RUN→duty) | PASS 1000 | PASS 1000 |
| **H-2b STOP 后归零 (150ms 复查)** | **FAIL** 1000/1000 | **PASS** 0/0 |
| H-2c 重新 START 恢复 | PASS 1000 | PASS 1000 |
| **H-2d 输出面登记数** | **FAIL** 0 | **PASS** 1 |
| 合计 | 16 PASS / **2 FAIL** | **18 PASS / 0 FAIL** |

**回归**: 10 套 **181 PASS / 0 FAIL** (基线 177 + H-2 四条; w5 14→18)。
**HIL 跳线已接**, 物理回环一并过: H-1a 满幅 3.300V / 零幅 0.001V;
H-1b u=512→1.650V(对照档)/1.857V(交付档) —— 两次不同因无 RC 时走跨周期平均, 受采样相位影响。

### ★ 两条必须记住的实测教训
1. **MSYS 会把 `/c/Users/...` 传给 Windows 程序时变成 `D:\c\Users\...`** ⇒ `pyocd flash`
   报 `No such file`。**第一次"对照档 FAIL"其实是旧固件跑出来的** —— 差点把"没刷进去"
   当成"对照档复现了缺陷"。⇒ **刷写后必须先核对板上构建身份** (本次用 `0x38` 的 `st[38]`:
   交付档=1 / 对照档=0; 长度两档都是 39, 不能靠长度区分)。传路径用 `C:/...` 正斜杠。
2. **RESET 路径的判据没有判别力** (两档实测同为 0): `h_reset_w1` 走 `cold_start_reset()`
   整段 memset, 会连 `WIRE[20]` 一起清零 ⇒ 未加门控的输出臂也算出 0 ⇒ **侥幸安全**。
   真正可被利用的洞只在 **STOP** 这条 (h_stop_w1 刻意不清 WIRE)。⇒ 该路径的补丁属
   纵深防御, **无法被黑盒判据证实**, 已在报告与代码注释里标注"不记分, 不假装有用"。

### 顺带发现 (工具/环境, 与本修复无关)
- **`find_port()` 会认错口**: 只按 `"CH340" in d or "1A86" in d` 匹配, 而 **CH343(COM7) 也是
  1A86** ⇒ 同时插着 ESP32 时可能选中 COM7。**跑套件一律显式 `--port COM14`**(已写进 README)。
- **`build/tmp` 间歇 Permission denied**: 随机文件/随机名/重跑即收敛, 含从未改动的文件
  ⇒ 与代码无关, 症状像"刚改坏了代码"。见 MEMORY 同级记录。

### ★ R1 子项 ④ actuator_idx 下载期校验 (用户定调"补 H723 等价校验")
- 现场: `engine_route_validate()` **完全没有** actuator_idx 校验, 而 ISR 是
  `if (ai && ai < MAX_ACTUATORS) ac[ai] = res;` ⇒ **>=64 静默丢弃** = "配置接受、物理无输出"。
- ★★ **不能照搬范本的 `>=32`**: 范本是单端口 u32 位图(位=引脚)所以上界 32;
  H723 没有 GPIO 执行器面, `actuator_idx` = **SHM 浮点槽索引** `ACTUATOR_STATUS[0..63]`,
  上界 `MAX_ACTUATORS=64`。**照搬 32 会误杀合法的 32..63 槽 —— 那不是更严格, 是换个错法。**
- 改法: `engine.c` 加 `if (r->actuator_idx >= MAX_ACTUATORS) return "actuator_idx out of range";`
  (0 = 不驱动执行器, 不拦); `engine.h` 的 `RouteEntry_t::actuator_idx` 写明新语义。
- 判据 `tools/h723_r1_actuator.py` (5 条, **两侧都有**): T1 阳性对照 idx=63→ACK /
  **T2 ★idx=64→NAK "actuator_idx out of range"** / T3 阳性对照 idx=0→ACK /
  T4 工具自检 dst_channel=200→NAK。
  **对照证据**: 旧固件(= §3 那份 `HIL_SAFE=0` 产物, 构建于本子项之前)上 **T2 FAIL** (被 ACK);
  交付档 5/5 PASS。**不需要另做对照构建** —— 现成的旧固件天然不带这条校验。
- 回归(交付档在板上): **11 套 186 PASS / 0 FAIL** (177 基线 + H-2 四条 + actuator 五条)。

### ★ 一条工具教训 (与"工具坏了会伪装成被测对象故障"同族)
用 `grep -c "\[FAIL\]"` 数日志里的失败条数, 得到 **26 的假阳性** (实际 0)——
反斜杠转义在当前 shim 里被吃掉。**数固定串一律用 `grep -F`**。
另注意: 套件日志里 `[PASS]` 行数 ≠ 官方项数 (有的套件内联+汇总各打一次),
**项数一律以各套件自己打印的汇总行为准** (如 `11/11 通过`、`28 PASS / 0 FAIL`)。

---

## 晚段二: #2 HEARTBEAT/SAMPLES 同址反义 (已修 + 已实证 + 已回归)

### 事实 (两侧源码逐行核对, 不是转述)
- 范本 `core0_isr.c:355` HEARTBEAT `+=1` 在 `if(!run) return` **之前** ⇒ **每拍无条件**
  (OA14 原文: "心跳必须在 run 门外无条件翻 — 停机也翻 (外部可观测 CPU+定时器存活)");
  `:358` SAMPLES 在**之后** ⇒ **仅 RUN 拍** (OA13)。范本 0x38 的 samples 取自 SAMPLES
  (`main.c:242`) ⇒ 同口径。
- H723 修复前**正好对调**: 0x08 写在 `gate && RUN` 门内, 0x18 = `g_isr_n` 每拍加。
- ★★ **为什么回归没抓到**: w1 的 S2/S2b 判据当初**就是照着对调后的语义写的**
  ("STOP 后 HEARTBEAT 停、samples 仍涨") ⇒ **判据跟实现一起错, 两侧自洽全绿**。
  又一例"自洽的假验证"。

### 修法 (判据跟着契约走, 不是反过来)
① `main.c` ISR: `HEARTBEAT++` 移到**门之外** (放 `g_tick_count++` 之后、任何门之前);
② `main.c` ISR: `g_isr_n++` 加**同一个门** (gate && RUN) ⇒ 仅 RUN 拍;
③ `engine.h`: 0x08/0x18 注释改成 S3 契约 + 写出三态表 (标注与修复前是对调的);
  顺带订正那句"0x38 samples STOP 后仍增长"(与范本源码相反的声称);
④ `tools/h723_w1.py`: S2/S2b/S3/S4 四条判据 + 两个 helper docstring 同步。
★ 信息量无损: 三态仍可辨, 只是标签对调 (0x08↑0x18↑ 在跑 / 0x08↑0x18=停 停机态 / 0x08=停 固件死)。
★ 正面副作用: STOP 后 samples 冻结 ⇒ "0x12 STOP 是可失败判据"更硬。

### 能失败的对照 (又是拿旧固件当对照, 不需要另做构建)
| 判据 | 旧固件 | 修复档 |
|---|---|---|
| S2 STOP 后 samples 冻结 | **FAIL** samples 5131→9235 | PASS samples 0→0 |
| S2b 同时刻 HEARTBEAT 仍增长 | **FAIL** HEARTBEAT 0→0 | PASS HEARTBEAT 5102→9207 |
| w1 合计 | 26 PASS / 2 FAIL | **28 PASS / 0 FAIL** |
★ S2 与 S2b **必须在同一时刻给出相反结论** —— 只测一个的话, 即便对调了也看不出来。

### 回归
- 11 套 H723: **186 PASS / 0 FAIL** (与 R1 后一致)
- **S3 平移套件 22/30, 未退化**, 且**失败项集合与基线逐条一致**
  (T3 DHT22 · T5 shell×2 · T8 GPIO · T11 FS · T14 MACRO GPIO · T15 半可修 · T23 GPIO = 8 项)。
  ★ 交叉印证: **T23 正是 R1 在 H723 侧用 H-2 替代的那条** (读 ESP32 GPIO 寄存器 = N/A)。
  ⇒ R1 给实现 + H-2 给判据, 两条合起来才让"停机=进安全态"在 H723 上成立。

### 未做 (诚实标注)
时段统计**口径**差异: H723 的 period/exec min/max 每拍更新, 范本只在 RUN 拍更新。
与 #2 相邻但**是另一个语义差异**, 未擅自改; 属审查 #5/#6 一族, 按收口顺序处理。

### 用户决定: #4 (div2 相位/两条建桶路径) **搁置**
用户原话: "可以先不管, 后续我会对时间槽进行深度的优化" ⇒ 不动代码, 保留本次分析。
★ 但本次**实测**到一个新事实, 必须留给那次优化: `engine_bucket_dead_slots()` 把
`off2[p]`(前缀和/"桶起始偏移", 只要有路由就非零)也当死槽数 ⇒ **任何非空程序恒报 36**
(实测板上 `g_bucket_zero_slots = 0x24`) ⇒ 这条"死槽不变量"判据**恒红、零判别力**。
而"槽 64..99 恒 0 是被断言的"正建立在它上面。修这条**不依赖 A/B 选择**(只数 cnt2 即可),
但边界(哪些槽算死)取决于 A/B, 所以留给时间槽优化一起收。

---

## 晚段三: 项目整体完成度盘点 (用户要求"看完成度, 之后再精调")

已写入 `docs/STATUS-2026-09-11.md` 新增的 **§7 收口阶段完成度盘点** (基线 `a2140cd`)。
口径: **完成度 = 有实现 + 有判据 + 判据能失败**; 不接受"宣称完成"。

### 四维结论 (口径与数字都已实测)
| 维度 | 完成度 | 依据 |
|---|---|---|
| 功能面 (相对 S3) | **≈100%** | 命令 **21** 条全实现; S3 的 19 条里唯一差集 = display 3 条(不迁) |
| 验证面 | 自建 **11 套 186 PASS / 0 FAIL** | S3 平移 **22/30**, 失败 8 项全是平台专属/半可修 ⇒ 无功能缺口 |
| 确定性指标 | **4/6 达标** | 单路由 56cyc ✓ / 分档 2.59× ✓ / 热重载 3.6µs ✓ / 向量表不进 flash ✓; **拍周期 + 抖动未复测** |
| 可靠性收口 | **2/13 ≈15%** | 审查一级 #1(含④)+#2 闭环; #4 搁置; #3 + 二级5 + 三级5 未决 |

- **规模**: src 8571 行/29 文件 · tools 9912 行/28 脚本 · docs 4543 行 · **64 提交**
  (范本自研 4340 行 / 91 提交 —— 口径不同, 仅作量级参考)
- **两项待复测同源**: 阶段2 的"拍周期极差 0"是**引擎独占拍**时的结果; 之后拍内加了
  协议收发 / Modbus 状态机 / macro 节流 / HIL 输出 ⇒ **必须重新量过才能宣称**。
- **T15 半可修**: 需 `RTS —100nF— NRST` 一根线, 或手按板上 RST。

### ★ 本次对账新发现 (又一处"注释声称 ≠ 事实")
`0x41/0x42`(macro 循环 upload/ctrl) 的注释写着 **"同 S3"**, 但**范本没有这两个命令号**
(范本只有 `0x40`; grep 全仓 `CMD_* 0x41/0x42` 无命中, 命中的都是字库/字节码操作码)。
⇒ H723 实际有 **5 条范本没有的命令**(0x36/0x37/0x41/0x42/0x43), 其中两条注释把自己的
"扩展"说成了"对齐"。**已记进 §7.2 的"口径待核"**, 收口时应改成"H723 扩展"或核清范本真实触发方式。

### ★ 方法论确认 (本次对账时发现并已订正)
`docs/STATUS-2026-09-11.md` 是**分次更新的基线文档**, 它的傍晚/深夜两节数字
(10 套 177 PASS 等)在 R1/R2 之后已过时 ⇒ 我在文末新增 §7 并**显式声明"本节取代上面
所有更新行的数字"**。这正是本项目"陈旧状态文档 = 会骗人的宣称"的具体处置:
**不删旧值(历史价值), 但必须显式作废**。

### 待用户定调的一点 (§7.6 第 5 条)
S3 线路线图里还有 4 项未完成: **顺序域示范 demo / README / 录屏 / ST 标准语言前端**。
H723 线的既定目标是"达到 S3 完成度" ⇒ **这四项要不要在本线一并承接, 需要定调**
(它决定"完成度"的终线画在哪)。






