# 2026-09-10 · 新项目 9.10 H723newest 立项

## 项目定位
- 从 esp32-core0 (ESP32-S3) 迁移到 STM32H723ZGT6 的**干净重建线**
- 与同区旧 `../h723-core0`（历史探索项目）**无代码继承**; 只借鉴硬件事实级发现
- esp32-core0 保持 S3 冻结 (只修不增), `esp32-core0/h723/` 是迁移期孵化产物

## 当前状态 (阶段 0 完成, git 首提交 164df7e)
- 工具链: GCC 7.3.1 (STM32CubeIDE 自带) + cmake 4.0.3 + ninja 1.12.1 + pyocd 0.44.1
- 时钟: HSE 25MHz → VOS0 → PLL1 (M=5/N=80/P=1, 整数模式) → **CPU 400MHz**
        HCLK 200 / APB 100 / TIMxCLK 200 (全链路整数); FLASH_ACR WS=3
- 100μs 拍: TIM2 ARR=20000-1
- ★LA 外部实测: 边沿间隔众数 100.0000us (96% 命中) → 反推 CPU 400.00MHz
- ★抖动: 去毛刺后极差 = 1 个采样点 (16MHz→62.5ns) → 低于 LA 分辨率

## ★ 本板实测频率天花板 ≈465MHz (docs/REF-frequency-ceiling.md)
- 450/460 可跑; 470 起不运行; 480/500/520/550 全不运行
- 已排除: 晶振频率 / PLL 分频 / FLASH 等待态 / 总线带宽 / VOS 写入 /
  PLL 输入频率 / CPUFREQ_BOOST
- 剩余最可疑: VCORE 未达 VOS0 水平 或 板级供电/VCAP (需万用表量;
  本机 LA 是 Logic 8, 无模拟输入)
- 用户决策: 测试期固定 **400MHz**; 芯片电路设计留到 PCB 阶段

## ★ 方法论 (写进 README 与 skill)
- **频率/时序类结论必须用 LA 外部证据** —— SWD 在 CPU 跑不住时会失步,
  会给出完全错误的结论 (曾据此误判"300~340MHz 断崖", LA 实测 450MHz 正常)
- 用 SWD 读"运行态"寄存器前**不要先 reset** —— `pyocd reset halt` 会把 RCC 打回复位默认值
- 测频工具判据: 跳变数 < 200 一律判"没在跑"(否则少量跳变也会算出"正确频率")

## 硬件接线 (docs/HARDWARE-PINOUT.md)
- LA: CH4 ← PA8 (拍输出) / GND ← GND (必接) / CH1 ← PA9 (阶段1 协议, 待接)
- 本机克隆 Logic 8 **双通道不可靠** → 只用单通道
- 调试口 H1 (2×4P): 1=SWCLK_PA14 2=SWDIO_PA13 3=RST 4=+5V 5=USART1_RX_PA10 6=USART1_TX_PA9 8=3.3V
- 板载 LED 在 PG7; PA8 引在 2×30 扩展排针

## 下一步 (阶段 1)
1. 拍 ISR 搬 ITCM + 引擎表搬 DTCM
2. 移植 port_* 平台层, 逐个跑通 S3 的 20 套回归 (目标: 脚本零改动)
3. 满载电流下复测 400MHz 稳定性 (本次是 wfi 低负载实测)

## 阶段 1 完成 (提交 08dca52) — 拍定时器 + 空拍骨架

### 验收对照 (方案 §8 阶段 1) 全部达成
- TIM2 100μs 拍 (ARR=20000-1 @ TIMxCLK 200MHz) ✓
- DWT 测空拍 ISR: **min/max/last = 79/95/95 cyc**, 均值 94.99 cyc
  对照 S3: 49 cyc @240MHz = 204ns;  本平台 95 cyc @400MHz = 237ns
- ★**拍周期抖动 = 0 个 CPU 周期** (min=max=last=40000 cyc → <2.5ns)
  比 LA 的 62.5ns 分辨率更细 → 两条独立路径一致, NVIC 入口延迟恒定
- LA 外部复核: 100.0000us, 众数 96.1%, 偏差 0.000%

### ★★ 关键发现: flash 常驻 ISR 成本被"代码布局/取指"主导
- 同一份 ISR 的不同构建: 86 cyc (首版) / 79~95 (改 calibrate 后) / **98 (模式2, 删了10条统计指令)**
- **代码更少反而更慢** → 加 10 条指令的成本 < 对齐/取指波动
- ⇒ flash 常驻代码 WCET 在 ±10% 量级不可预测
- ⇒ **阶段 2 搬 ITCM 是必要性, 不是优化** (这是阶段 2 的核心假设, 待验证)

### DWT 标定 (证明计数可信)
- nop×1000 (noinline + volatile 计数) → 9050 cyc ≈ 9.05 cyc/迭代 (合理小整数)
- 读对开销 1 cyc
- ★**权威标定**: 拍周期 40000 cyc ÷ LA 实测 100.0000us = **400.00 MHz**
  → 内部计数频率被外部仪器独立确认 (比 nop 循环硬得多)

### 阶段 1-C: PA9 线路验证 (新增 LA CH1)
- 新增 ISR_MODE=3: 固件在 PA9 每 32 拍翻转 → LA 单通道抓 CH1
- 实测边沿间隔 **3200.0000us** (众数 92.1%) = 预期 32×100us ✓
- → **CH1←PA9 线路可用**; 排掉"以后 UART 不通是线还是代码"的不确定性

### 新增/修改
- `src/main.c`: ISR_MODE 编译期四形态 (0纯骨架/1全量/2仅读对/3+PA9输出)
- `tools/h723_stage1_read.py`: 单会话读回 DWT 测量 + 派生量 + 源码形态解析
- `docs/STAGE1-REPORT.md`: 阶段 1 完整报告
- `README.md`: 状态/目录/命令/纪律/下一步 全部同步

### ★ 踩坑 (已写进 README 纪律 6/7)
1. **默认 halt 连接在目标调试态异常时报 `No cores were discovered`**;
   `connect_mode=under-reset` 能恢复。读**运行态**统计必须**单会话**完成
   (连接 → reset → sleep → 读), 否则重连语义上等于从头开始
2. **产出 ELF 无扩展名**: CMake 目标是 `build/dcl_h723` (不是 .elf)
3. **`--gc-sections` 会回收无人读的全局** (`volatile` 也保不住):
   `g_isr_mode` 就这么从符号表消失 → 报告值改从源码解析
4. **nop 标定必须 noinline + volatile 计数**: 否则编译器把不同迭代数的循环
   优化成不同形状 → 首版 n=1000 得 1602cyc / n=2000 得 60007cyc, 差分算出
   58 cyc/迭代这种荒谬值

### 下一步 (阶段 2)
1. 链接脚本分 ITCM/DTCM, ISR 搬 ITCM → 复测空拍成本 (预期抖动收窄到 0~2cyc,
   成本降到接近真实指令数) ← 检验本阶段的假设
2. 引擎表搬 DTCM, 移植路由扫描 (先全表扫, 不分档)
3. DWT 测"单条路由"周期数 (对照 S3 的 234 cyc)

---

## 阶段 2 完成 (提交 5ef63c3) — ITCM/DTCM 落位 + 路由扫描 + 同镜像 A/B

### ★★ 核心结论 (阶段 1 的假设被证实)
同一份 **2136 B 逐字节相同**的扫描机器码 (一个宏实例化两次, `-fno-ipa-icf` 保住),
**同一次上电 / 同一个调用点 / 运行时选择器切换**:

| | FLASH | ITCM |
|---|---|---|
| 单条路由 (全DIRECT) | 203.6 cyc | **56.0 cyc** |
| 128 条全表扫 | 26229 cyc (占拍 **65.8%**) | **7225 cyc (18.3%)** |
| 128 条 19原语混合 | 38923 cyc (占拍 **98.9%** 近锁死) | 11419 cyc (29.0%) |
| 128 条 全PID | — | 15275 cyc (44.8%) |

- 空拍外壳: ITCM **44 cyc** vs FLASH **160 cyc** (3.6×)
- 单条路由 56.0 cyc = 140ns (对照 S3 234 cyc = 975ns → 快 7 倍)

### ★ 关键实验一: I-cache 对照 (决定性, H7 复位默认 IC 关闭)
- 开 IC 后 FLASH 26229 → **8164** (3.2×);  ITCM 7225 → **7225 (逐位相同)**
- ⇒ FLASH 成本取决于**引擎不控制的全局状态变量** (cache 内容);
  ITCM 与该状态无关。即使开着 cache, FLASH 仍 62.69 cyc/条 > ITCM 56.02
- ★诚实边界: 本微基准工作集 2136B 完整装进 16KB L1I, 所以**这次没测到 cache 抖动**
  (所有组 min==max)。ITCM 的价值是"把依赖整个移除", 不是"实测到抖动"

### ★ 关键实验二: 同一 flash 函数换个地址成本变 5%
- `ISR_ITCM` 编译期开关做外壳 A/B; 顺带发现 `engine_scan_flash` 地址从
  0x08000660 → 0x080008B0 (挪 0x250B), 同一份机器码 26229 ↔ 24986 (±5%)
- ⇒ 正面证实阶段 1: **flash 常驻热代码的 WCET 无法预算**

### ★ 拍周期: 11 组全部极差 0 (含占拍 98.9% 那组)
- 引擎占满 98.86% 拍, 下一拍入口时刻一位不差 ⇒ 硬拍与负载完全解耦
- LA 外部复核 (满载 98.9%): 100.0000us, 众数 96.1%, 极差 63ns(=LA 量化极限)
- ★之前看到的 11~23cyc 极差来自 ICIALLU/DSB 那一拍的一次性扰动;
  `g_per_cyc_*` 改为**按组复位**后消失

### ★ 可复现性: 两次独立上电跑完整矩阵, 所有 eng_min 逐位相同

### 新增文件
- `src/engine.h` 表结构/SHM 偏移/OP·SRC 码 (与 S3 逐字节同构) + 11 条布局断言
- `src/primitives.h` 19 原语移植 (算法逐字保留: M3/M8/P3/v0.2 dt 体系)
- `src/engine.c` SHM(DTCM) + 冷启动清零 + 落位自检 + 读源 + 分发 + 双实例宏
- `tools/h723_stage2_read.py` 单会话 11 组 A/B + 落位/前提/守卫三重验证
- `docs/STAGE2-REPORT.md` 完整报告

### ★ 踩坑 (5 个, 已写进 README 纪律 7~10 + 代码注释)
1. ★★**指针相等的自检会被 GCC 折叠**: `(uintptr_t)g_shm != (uintptr_t)_shm_start`
   被折成恒返 0 (minimal repro in `.tmp/t.c` 确认, GCC 7.3.1 -O2)。
   原因: C 标准保证不同对象地址不同 → GCC 无需知道数值即可判定不等。
   修法: 取值路径过 `volatile`; 权威比对交给外部工具 (本项目两者都做)
2. **`--gc-sections` 吃"只被静态初始化"的全局**: `g_isr_itcm` 只在声明处赋值 →
   整段被回收, 符号消失。`__attribute__((used))` **只挡 GCC 层, 挡不住链接器** →
   必须在代码里真读/真写一次
3. **`pyocd read32 ADDR LEN` 的 LEN 是字节不是字** (`read32 0x0 256` → 64 字);
   且输出带 ASCII 转储列 → 正则必须先按 `|` 切掉 (加 `$` 锚点会一行都匹配不到)
4. **清统计与写配置的时序**: "先清统计再写 gate" → 中间夹进的拍把 `isr_min`
   污染成另一个配置的值 (实测差 ~26000 cyc)。正解: 先定 gate → sleep 30 → 再清
5. **多字变量 (u64 sum) 必须逐字读** —— 少读一个字让整条解析链错位

### 下一步 (阶段 3)
1. 桶化分档 (div/phase 三档 + 桶表) —— 现在还是全表扫不分档
2. deploy 路径 (0x10 staging → 热重载 → 生效确认; 顺带还 S3 的 "ACK≠已生效" 语义债)
3. USART1 通信域 (Modbus RTU 从站) —— `SRC_HMI` 现在是留位返回 0
4. 顺序域 Sequencer v0;  把 S3 的 Python 回归脚本平移过来 (协议不变, 脚本零改动)

---

## 18:05-19:0x 阶段 2 审计（对照 esp32-core0 最后两轮审计口径）提交 62aa58a

### 审计依据 = 老文档最后两轮
- 第二十五/二十六轮 (6c39f81): OA16-19 / cold_start_reset 单一入口 /
  CONST 源用 src_index 索引 param 表 / 脚本非幂等两形态
- 第二十七轮 (a09bb88): OA20(新内存区零断言) / OA21(保留语义) / OA22(成本模型漏维度)
  / OA23(**判据恒真**) / 归因纪律 / 三组同族缺陷族谱
- 记录位置: `esp32-core0/.workbuddy/memory/2026-09-10.md` + `REPORT-S3-closure.md` §5-6

### 自审抓出 8 项 (全部逐条核实后才写)
- ★H1 (P2) `g_table_ck` 用 route[0].op 当哨兵, 而 MIXED[0] 恰好也是 DIRECT
  → profile 0/1 同读数 → **判据恒真** (OA23 同族)。
  修: 整表 FNV-1a 校验和 + 工具用 Python **独立重算期望值**比对。
  复验: 11 组逐位吻合, 三 profile 校验和互不相同 (0x329A37C5/0xE6021511/0x806C1845)
- H2 (P2) 无"引擎真算过"的正向证据 (g_eng_ck 读了从不校验)
- H3 (P3) SHM 0x00-0x3F 无名无断言的 64B 空洞 (OA20 同族) → 按 S3 原偏移命名+断言
- H4 (P3) SHM 与栈之间毫无保护 → _shm_end 之上 128B 哨兵 + 主循环巡检
- H5 (P3) SRC_HMI 落 default 返回 0 → 显式 case (deploy 侧拦截留阶段 3/4)
- H6 (P2) cold_start_reset 与 engine_fill_tables 各自 memset → 统一到单一入口
- ★H7 (口径) **反转"硬拍与负载完全解耦"这一过度宣称**: 本轮改版真的踩到超载
  (FLASH·混合 41166cyc = 102.9% 拍 → 周期 41334~41910, 极差 576cyc)
  正确口径: 解耦的前提是 ISR 在拍内跑完; 一旦超载拍周期由 ISR 时长决定且不再确定
- H8 (归因) "flash 落位敏感"机制仍未直接测量 → 诚实标注 + 给出 padding 实验方案

### ★★ 审计期派生的最关键事实: 拍长落在构建摆幅中间
| 构建 | B1 FLASH·DIRECT | C1 FLASH·混合 | ITCM·DIRECT |
| A 初次 | 26229 (65.8%) | 38923 (97.3%) | 7225 |
| C 加 checksum/哨兵 | 27818 (69.8%) | **41166 (102.9%) 超载** | 7225 |
| D 加 BOOT_* (交付) | 26229 (65.8%) | 38813 (97.0%) | 7225 |
→ 同一引擎同一机器码, FLASH 版 ±3% 摆动, 拍长 40000 恰好落在中间
→ "程序跑不跑得下"由构建布局决定; ITCM 三次一字不差
外部复核 (构建D, C1 97.0%): LA 100.0000us + 墙钟 99.9567us/拍, 两法一致

### ★★ 工位坑 (3 条, 都白费过时间, 已进 README 纪律 11-13)
1. `connect_mode=under-reset` **不带 `-c reset`** ⇒ 核心被按在复位态,
   读回**陈旧 SRAM**: tick 冻结、per_cyc_max 出现 0xFD7A1A92 垃圾值
   → 每次会话必须 `-c reset -c "sleep 300"`; 判据先读 DHCSR(0xE000EDF0)
2. `la_tick_freq.py` **默认会复位板子** ⇒ 测运行期写入的配置必须 `--no-reset`
   (为此白跑三次抓取, 每三次都返回 100.0000us 骨架态, 一度被误读成"LA 与 DWT 矛盾")
3. pyocd 会话结束核心被 HALT, 运行期配置不跨会话
   → 要用外部仪器测非默认配置就**编进固件**: 新增
   `-DBOOT_PROFILE/-DBOOT_GATE/-DBOOT_SEL` (CMake + build.sh 透传 -D)

### 未决
- H8 的 padding 扫描 (-DSCAN_FLASH_PAD=N 得到"落位 vs 成本"曲线) ← 唯一能把相关性变机制的实验
- SRC_HMI 的 deploy 侧拦截 (阶段 3/4)
- deploy 预算模型第一版就要带 src_cost[] (S3 用两轮审计才发现)

---

## 19:0x-19:4x H8 闭环: flash 落位机制实测坐实 (提交 09c05ef)

### 手段
- `-DSCAN_FLASH_PAD=N`: 在 engine_scan_flash 之前插 N 字节填充
  (链接脚本新增 `.scan_pad` / `.scan_flash` 两专区; .scan_flash 独占一段)
  → **只挪地址, 不改一条指令** (段大小 16 次恒 2136 B)
  → ITCM 版 VMA 固定 0x0 作控制组
- 工具加 `--sweep` (4 组快跑) / `--json` (供批量汇总)
- `tools/h723_pad_sweep.py`: 一键 构建+烧录+测量+汇总 (默认 10 点 ≈3 分钟)

### 结果 (16 次独立构建)
| | min | max | 极差 |
|FLASH·全DIRECT | 24957 | 28827 | 3870 = **15.5%** |
|FLASH·混合 | 36967 | 41500 | 4533 = 12.3% (拍长 40000 → 一个落点 **103.8% 超载**) |
|ITCM 控制组 | 7225 | 7225 | **0** (16/16 逐位相同) |

### ★★ 成本 = f(addr mod 32), 周期 32 字节
8 个同余类各取两个独立样本 (地址相差恰好 32B), DIRECT 与混合读数**都逐位相同**:
%32=16 → 24957/24957 · 12 → 26056/26056 · 0 → 26229/26229 · 8 → 26385/26385
%32= 4 → 27818/27818 · 28 → 28043/28043 · 24 → 28553/28553 · 20 → 28827/28827
解释: H7 flash 读通路 **256 位 = 32 字节一条**, 跨界循环体每轮多付一次取指请求
(本页只声称"是 addr mod 32 的确定函数"; 256 位解释与手册一致但需厂商级取指计数才算坐实)

### 工程推论 (比"ITCM 更快"重要) + 交付现状
① "用 flash 地址算 WCET 预算"不可能 —— 随下次无关改动落位变化 15%
② "编译过 + 实测跑得动"不是证据 —— 16 落点里 1 个超载, 换构建可能翻面
③ 热代码进 ITCM 从"优化"升级为**设计前提** (ITCM 侧误差项为零)
★ 交付固件现状: FLASH·混合 isr_max=39891, **距拍长仅 109 cyc (0.27%)**;
  ITCM 版余量 28476 cyc (71%) —— "恰好没事"不等于是对的

### 顺带修 + 新增
- 工具超载判据: 原用 周期max>40001 → 把 12cyc 零散抖动误报超载;
  改与②表统一为 **isr_max > 拍长** (真超载会让周期涨到 ISR 时长量级)
- 新增 `docs/REF-flash-placement.md` (机制+数据+工程用法+交付现状)
- 交付固件最终 11 组验证: 18 个 ✓ / 0 个 ✗

### 下一步 (阶段 3)
桶化分档 (div/phase 三档+桶表) → deploy 路径 (第一版预算模型就带 src_cost[]) →
USART1 通信域 (SRC_HMI 落地 + deploy 侧拦截) → Sequencer v0 → 平移 S3 的 Python 回归

---

## 19:5x-20:2x 阶段 3 (一): 档桶分档调度 (提交 4d82413)

### 做了什么 (S3 OA15 治本语义)
- 路由按 (div, phase) 归组重排: [div0 全部] → [div1: phase0..9] → [div2: phase0..63]
- 桶表 [off1[10]][cnt1[10]][off2[100]][cnt2[100]] = 220×u16 @ 0x4480 (沿用 S3 偏移)
- ISR 每拍只跑三段: div0 段 [0,off1[0]) + div1 桶[off1[tick%10]] + div2 桶[off2[tick%64]]
- `g_scan_mode` 运行期可切 0=全表 / 1=分档 → 同一会话内直接对照
- engine_build_buckets 幂等 (桶表先清零 + 稳定排序; 用 STAGING 2KB 当暂存)
- engine_tick 放 ITCM; profile 3/4 = 三档混合

### 结果 (同一份 128 条三档程序, 只切扫描方式)
| | 全表 | 分档 | 效果 |
|三档·全DIRECT | 6762cyc(17.2%) | **2606cyc(7.2%)** | 2.59× |
|三档·19原语混合 | 10878cyc(27.9%) | **4208cyc(11.9%)** | 2.59× |
- 每拍 47~49 条 (不是 128); 条数比 2.67× ⇄ 成本比 2.59× 一致
- 128 条满表 ITCM 混合: 28.89% → 分档三档程序 11.86% (余量 71%→88%)

### ★ 判据的可失败性 (22 ✓ / 0 ✗)
- 桶表校验和 vs Python 独立复刻 → 逐位吻合
- ★本拍条数必须 ∈ 桶表允许的**可行值集合** {47,48,49}: 退回全表扫会变 128 → 报 ✗
- H9 断言: g_bucket_zero_slots (槽64-99 非零个数) 必须 0
- 5 种配置表校验和互不相同: 0x90DA0A50/0x89C79BD2/0xFF09DAF8/0xDF45BF11/0xBD829087

### ★★ 移植期发现 S3 真缺陷 H9 (宣称≠实现, 已记档)
- S3 声明 div2 相位 **100 个** (桶表 100 槽 / 注释 "phase 0-99"),
  但 `period` 的 phase 字段只有 **6 位** (PERIOD_PHASE_MASK=0x3F)
- deploy: `ph = q2 % 100` → `(uint8_t)(dv | (ph<<2))` 截断 → 实存 `ph & 0x3F`
- 后果: 槽 64..99 **恒空**; q2=0 与 q2=64 **相位重叠** → div2>64 条时每拍最坏
  ≈ n/64, 而预算模型按 n/100 摊薄 → **静默吃掉部署门余量** (成本模型族 OA12/OA22)
- 为什么 27 轮审计没抓到: verify_capacity.py 的"慢档 PID×128"正好 127 条 div2,
  **会触发**; 但该用例只看 emax 是否超预算 (2 条 PID ≈1200cyc vs 门 16000, 差一个数量级)
- H723 修法: BUCKET_DIV2_PHASES = 64 (与字段严格一致) + 不可达槽断言
- ★ 配套必做: deploy 预算对 div2 的摊薄系数改成 **除以 64 而不是 100** (+56%)

### 途中修的两个"判据太松"坑 (被更严的判据自己抓出来)
1. 表校验和原为**字段位拼装**: `state_offset<<20` 与 `src_type<<16` 在 bit20-23 **重叠**,
   且**完全没覆盖 period** → 改**逐字节 FNV-1a** (无重叠 + 覆盖全部字段 + 顺带覆盖归组重排)
2. `period` 在 **offset 14**, offset 15 是编译器**尾部填充字节** → Python 复刻时写成 b[15],
   症状极隐蔽: "profile 3/4 对不上而 0/1/2 对得上"(因为只有 3/4 的 period≠0)
   → 固件侧把填充字节**显式命名** `RouteEntry_t.reserved`, 使逐字节校验和不依赖填充内容

### 下一步
deploy 路径 (0x10 staging → 热重载 → 生效确认; 桶表 ST 已预留) → 通信域 → Sequencer

---

## 20:3x 总体盘点 (用户: "看看总体, 我们到了哪里, 下一步做什么")

### 位置
- H723: 13 提交 / **2,246 行自研** / 阶段 0·1·2 完成 + 阶段 4 第 1 项(分档)完成
- 对照 S3: 15,860 行自研 / 12 组件 → **只搬了约 14%**
- MIGRATE 六阶段位置: 0✓ 1✓ 2✓ | **3 未开始(最大空缺)** | 4 做到 1/3 | 5 未开始

### ★ 盘点发现的编号错位 (已就地在文档订正)
- 桶化分档按 `MIGRATE-H723.md` 属 **阶段 4 第 1 项**, 我此前写成"阶段 3(一)"
- 已在 STAGE3-REPORT.md 加"编号自纠"框 + README 两处改写为 MIGRATE 口径
- 提前做的理由 (写进文档): 分档改变每拍执行条数 128→47~49, **直接决定 deploy
  预算模型的摊薄系数**; 先做协议栈再回头改预算 = 预算模型写两遍 (S3 OA12→OA22 教训)
- 文件名保留 STAGE3-REPORT.md 避免破坏引用; **编号以 MIGRATE 为准**

### ★ 真正的缺口不是引擎, 是"能被外部使用"那一层
- H723 现在**没有任何协议层**: 所有观测靠 SWD + 编译期 BOOT_* 旋钮
- 12 组件里只有 core0(部分)搬了; 最高优先 = **transport(协议)** + **persist(掉电)**
- display/fs 判定"不迁" (H723 无屏, 无文件系统需求)

### 下一步顺序 (已写进 README)
阶段 3 (0/4): UART+协议帧 → deploy(含 H9 /64 + H5 拦截) → persist → 20 套回归零改动
阶段 4 剩余: Modbus 通信域(SRC_HMI 落地 + LA 验证) → Sequencer v0
阶段 5: ADC16+DMA → LA 长样本抖动对比 → div0 容量实测
★ 终态: deploy 落地后 `g_scan_mode` 应从"全局开关"变成"表自带属性"

### ⚠️ 盘点期操作事故 (第 2 次同类, 必须立纪律)
- **经过**: 写记忆时用了 `cd A && cat >> A的记忆` 然后 `cd B && cat >> B的记忆` 最后
  `git add -A && git commit` —— 那个 `git add -A` 落在 **esp32-core0 (S3 仓库)** 上,
  把 S3 工作区里**残留的 55 个文件**(`h723/` 720K + `.h723/` 43K + `tools/h723_ports.py`)
  一次性提交进了 S3 历史 (5234101, 28 files / +3009 行)
- **发现方式**: commit 输出的 CRLF warning 里全是 `h723/...` 路径 —— 与"只改了两个 md"不符
- **修复**: `git reset --soft HEAD~1 && git reset` → 只撤提交与暂存, **不删文件**;
  HEAD 回到 724c8b8, 已跟踪文件零改动 (porcelain 过滤 `??` 后为空), 且**未 push**
- ★★ **新纪律 (跨项目操作专用)**:
  ① 每次 `git add` 之前必须显式 `cd` 到**目标仓库**并 `git status --short` **先看清单**;
     提交信息里声称改了 2 个文件、`--stat` 却有 28 个 → 立即停手
  ② 禁止跨仓库连写命令链 (`cd A ... ; cd B ... ; git add -A`) —— `git add -A` 极易
     落到最后一次 cd 的仓库; **要 add 就写相对本仓库的显式路径**
  ③ 回滚误提交用 `git reset --soft` + `git reset`, **不要用 `--hard`**(会连文件一起删)
  ④ 提交后第一件事看 `--stat`, 与预期不符就当事故处理

---

## 21:0x-22:0x 阶段 3.1 协议层: transport 逐字沿用 + USART1 驱动 (提交见下)

### 做了什么
- `src/transport.h/.c` **逐字沿用** S3 uart_protocol(52 行状态机 + CRC16-CCITT);
  唯一新增 = H723 平台段(版本 0x0200 + **能力位图只报 2 位 0x0021**)
- `src/uart.h/.c` USART1: PA9=TX/PA10=RX(AF7, 就在板上调试口 H1)、115200 8N1、
  RXNE 中断+环形缓冲、发送轮询等 TC、ISR 放 ITCM、**NVIC 优先级 0x80 < 拍 0**
- `src/regs.h` +USART1 寄存器(逐条标权威来源行号)/RCC_APB2ENR/NVIC_IPB(字节访问)
- `src/main.c` +协议分发(GET_VERSION/未知命令 NAK "bad cmd") +上电横幅
  +回环自检(仅计数不分发, 防回声风暴) +**obs_anchor()** +8 个编译期开关
- `tools/h723_proto.py` PC 侧 **6 个可失败用例**(T1 协商/T2 未知命令 NAK/T3 CRC错无响应
  /T4 长度超限无响应/T5 分片慢发/T6 连发两帧)

### ★★ 超分辨率证据 (字节级)
- BRR **0x3641 = 13889** (推算 100e6*16/115200=13888.889), 实际 115199.1 Bd, 误差 0.0008%
- CR1=0x2D(UE|TE|RE|RXNEIE), CR2=CR3=0; MODER PA9=10(AF), AFRH AFSEL9=7(**AF7=USART1**),
  PUPDR PA9=01(上拉); USART1_IRQHandler @ **0x00000bec (ITCM)**
- 横幅帧(可手算 CRC): `C1 00 04 00 00 02 21 00 D8 AC` (10B)
  —— CRC16-CCITT 覆盖 [sts][len][payload] **不含 SYNC**
- g_uart_tx_bytes = 10 × g_banner_count (实测 200 = 10×20)

### ★★ 拍抖动: 阶段1/2 的"极差 0"在加协议层后变了 (诚实标注, 机制未测)
| 构建 | 拍周期极差 | 窗口 |
|A 仅 USART 初始化·**零流量** | **0 cyc** | 5122 拍 |
|B 每 20ms 一帧·**稳态(清统计后 3s)** | **4 cyc = 10ns** | 150 帧 |
|B 每 20ms·累计(未清) | 26 cyc = 65ns | 含启动 |
|**交付固件(仅上电一帧)** | 22 cyc = **55ns** | 31131 拍 |
- 可说的: ①UART 外设本身不干扰拍(零流量=0, 与阶段1/2 一致) ②稳态流量仅 10ns
  ③偏差在**上电那次阻塞发送窗口**内产生, 之后不增长 ④55ns/100us=0.055% 仍优于验收线
- **不能说的**: 上述是相关性; "启动窗口为何影响更大"只有合理解释、**无直接证据**
- 变机制的唯一办法 = **分段统计**(ISR 里记录"入口时是否在阻塞发送中"再分类累计),
  但分支本身会扰动被测量 → 未决

### ★★ 第三次撞 `--gc-sections` 吃观测变量 → 结构性修复
①g_isr_itcm(阶段2) ②g_selftest_state ③g_banner_count ⇒ 新增 `obs_anchor()`:
把**全部 64 个**观测变量读一遍(volatile 读→链接器见引用); 纪律: 新增变量必须在此加一行

### ★ 误判与纠正 (方法论, 值得记)
两次读到 `GPIOA_IDR.PA9=0` + `USART1_ISR.TXE=0`, 一度判"PA9 被外部拉低"。
**决定性实验否掉**: 单会话内把 PA9 改 GPIO 推挽输出, 驱动高→低→高 IDR 正确跟随 ⇒ 引脚正常。
真因 = **确定性采样打在确定性发送上**: 两次会话都用 `sleep 400`, 而横幅周期 20ms,
`400ms = 20×20ms` → 采样点每次都在发送窗口起点。**"确定性"反过来咬人**。

### 未完成 (都卡在"要动线")
- **PC 直连**: 需 CH340↔H1 接线(RXD→6/PA9, TXD→5/PA10, GND 共地) → `h723_proto.py` 6/6
- **LA 外部复核**: CH1 抓到的信号被解成连续 0x00+framing error(字长 82.437us=9.5位对,
  但极性/帧结构不对, 事件间隔不规则)。**已排除**: LA 本体正常(CH4 测出 100.0000us 众数95.9%)、
  固件侧寄存器全绿、引脚驱动实验 1→0→1。⇒ 最可能 **CH1 探针没接在 PA9 上**
- **快速自证**: H1 的 6↔5 脚短接 + `-DDCL_UART_SELFTEST=1` → 不需 PC 即可证明 RX+CRC+解析器

### 新增纪律 (README 14-16)
14. 别让采样时刻与周期性事件共振(固定延时 × 固定周期 = 长期同相)
15. **同一文件的多个编辑必须串行** —— 并行会 read-modify-write 竞争, "报成功但没落盘"
    (本轮 main.c 的 include 与 CMakeLists 的 set() 各丢一次, 编译报错才发现;
    判断编辑是否生效要 grep 文件, 别信回执)
16. `under-reset` 不带 `-c reset` 会读回垃圾(不只陈旧 SRAM): 本轮 GPIO 全返回 0xABFFFFFF

### 下一步
PC 直连收口(需接线) → deploy 路径(预算带 src_cost[], div2 用 /64, 拦 div=3/无state槽/SRC_HMI;
★ 6KB 帧必须改 DMA/中断驱动发送) → persist → 20 套回归零改动全绿

---

## 22:0x-23:0x 阶段 3.2 deploy 路径 (逐原语成本表 + 校验 + staging + 热重载 + 生效确认)

### 做了什么
- `tools/h723_op_sweep.py` 新工具: **单一原语模式 (profile=100+op) + 两点法 (n=128/64)**
  → 19/19 原语实测, 每个原语的**表校验和都与 Python 独立预测逐位吻合**
- `engine.c`: k_op_cost_itcm[19] (实测) / engine_op_cost / engine_prog_budget /
  engine_route_validate / engine_stage_program / engine_reload_active
- `main.c`: h_deploy(0x10) + h_engine_status(0x38, 前31B 与 S3 同布局 + 尾6B 扩展) +
  ISR 内热重载 + deploy_selftest(9 例)

### 全表 (cycles/条, ITCM, 全表扫)
DIRECT 56 CMP 75 HYST 79 CLAMP 75 LPF 96 **PID 145** RATE 67 DEADBAND 75
MUX 70 EDGE 88 LUT 89 CNT 77 TIMER 79 ARITH 68 SCALE 67 AND 72 OR 72 NOT 70 SR 78

### ★ 发现1: 成本表不能照抄 —— 加速比不是常数
| 原语 | S3@240MHz | H723 | 加速 |
|DIRECT | 234 | **56** | 4.18× |
|PID | 337 | **145** | **2.32×** |
⇒ 按 DIRECT 的 4.2× 整体缩放会把 **PID 低估 1.8 倍** → 预算门放行超载程序

### ★★ 发现2: 预算门在 H723 上**不可能被触发** (判据不能失败 = OA23 同族)
128 × 145 = 18560 = 拍长 46% < 门 26000 ⇒ 任何合法程序都吃不饱拍。
处理: 不调低数字凑, 而是立**绊线断言** `_Static_assert(MAX_ROUTES*145 <= 26000, ...)`:
通过 = "门还不具约束力"; **失败 = "门现在是真的了, 去实测验证它"**
(触发条件: 扩容路由数 / 加重原语; DTCM 能放 ~2000 条)
★ 附带结论: **H723 当前实际容量约束是 MAX_ROUTES=128, 不是拍长**

### ★★ 发现3 (严重): 热重载用 libc memcpy 会**打崩拍周期**
| | 第一版 | 修后 |
|单次热重载 | **71241 cyc = 178us (比拍还长!)** | **1437 cyc = 3.6us** |
|ISR 峰值 | 78563 cyc | 3332 cyc (8.3us) |
|拍周期 | **1360~78640** (塌了) | 39986~40014 |
|实现 | libc memcpy/memset (flash) | **ITCM 字拷贝循环**(itcm_wcopy/wzero), 落位 0x00000C90 |
- 根因: newlib nano memcpy 按字节拷 + **从 flash 取指(I-cache 默认关)** ≈ **14.8 cyc/字节**
- 机制: ISR 超过拍长 ⇒ 返回后定时器已到期 ⇒ 立刻再进 ⇒ 拍周期塌成 ISR 长度量级
- ★ 教训(写进 engine.c): **热路径上的内存搬运不能交给 libc**, 尤其默认关 cache 的 H7

### deploy 自检 9/9 (判据全部可失败)
0 合法三档→受理 / 1 dst冲突→拒 / 2 SRC_HMI→拒 / 3 PID无state槽→拒 / 4 div=3→拒
5 非法op→拒 / 6 参数非有限→拒 / 7 载荷不足→拒 / 8 全NONACTIVE→受理写0条
- 每例比对调用前后 (ok,nak) 计数器 —— **只数成功/只数失败都会造成假象**
- 实测: 受理2/被拒7, deploy_seq=applied_seq=2, reload_lat=**1 拍**

### ★ 生效确认 (偿还 S3 "ACK=已受理≠已生效" 语义债)
- 0x10 ACK 载荷 = [seq:u16][budget:u32] (向后兼容的尾部追加)
- ISR 切换完 ACTIVE 表后写 APPLIED_SEQ = DEPLOY_SEQ
- 0x38 尾部追加 6B: [deploy_seq][applied_seq][applied_lat]
⇒ "已生效"从**假设**变成**可观测的量**

### ★ 顺手抓到的迁移痕迹 (同一模式: 旧平台的数字/指令, 语法过得去语义是错的)
- `__asm__ volatile("memw")` = **Xtensa(ESP32) 指令**, ARM 上不存在 → 编译报错 → 改 `dsb`
- op_cost[] / OP_COST_DIV2=100(H9) / EXEC_DEPLOY_BUDGET=16000 → 全部按本平台重定

### 未决
协议级 deploy 联调(需 CH340) / 超预算行为(门当前不触发) / 0x38 的 overrun 字段恒 0 /
engine_stage_program 仍用 libc memcpy(主循环里, 178us 一次性可接受) / persist 落盘

---

## 23:0x-24:0x CH340 接线排障 (提交 dcb579c)

### 用户动作
- 接上 CH340: RXD→H1-6(PA9), TXD→H1-5(PA10); **LA 已因接线断开**

### ★★ 第一个真问题是我自己的: CMake `-D` 是缓存
- 上一轮 `-DDCL_DEPLOY_SELFTEST=1` 被 cache 记住 → 后续不带该参数的 `bash build.sh`
  **不会**回到默认 0 ⇒ 我把"自检版"当交付固件烧进板子并读了一轮数据
- 症状线索: 回读到 `nak_count=7 / n_routes=0` (交付固件不该有) + `scan_mode=1`
- 修: `build.sh` **每次显式传全部开关默认值**, 用户参数追加在后 (CMake 以最后一次
  -D 为准) ⇒ 缓存永远被覆盖; 并**打印从 CMakeCache 读回的生效开关** (不是"我以为传了什么")
- 影响面评估: 阶段 3.2 的 9/9 自检与回归 11✓ 仍成立 (自检只在 boot 跑一次,
  不碰引擎); 但"交付固件"这个标签是错的, 且拍抖动统计里含自检那 7 个 NAK 的 boot 窗口

### ★ h723_proto.py 判据缺陷 (上轮发现, 本轮修)
- "无响应"在**链路不通**时也成立 ⇒ 拔掉 USB 线 T3/T4 全绿 (判据恒真, OA23 同族)
- 修①: **T0 链路活性前置** (GET_VERSION 必须回 ACK); 不活则后续全部记 **SKIP**
  (不计 PASS, 退出码 2) —— SKIP 不是 PASS
- 修②: T3/T4 的"无响应"后各加**阳性对照** (再发合法帧必须回 ACK),
  证明沉默是"故意丢弃"而非"链路已死"

### 实测证据 (板子侧全绿, PC 侧无信号)
| 检查 | 结果 |
|RCC_APB2ENR.USART1EN | 1 ✓ |
|GPIOA MODER PA9/PA10 | 10(AF) ✓ / AFRH=7(AF7) ✓ / PUPDR=01(上拉) ✓ |
|USART1 BRR / CR1 | 0x3641 ✓ / UE\|TE\|RE\|RXNEIE ✓ |
|uart1_write | 等 TXE/TC **无超时**; banner_count=3 ⇒ 30B 真出线 ✓ |
|板子健康 | g_stage=7 (ISR 在跑), 信标 300ms/帧 ✓ |
|SWD 可靠性 | **12/12 OK** ⇒ 线**没**接在 SWCLK/SWDIO 上 |
|PC 侧 --listen 4s | 合法帧 **0** 个 / 原始字节 260~280 (**全是悬空噪声**, 解不出 CRC) |
|★决定性对照 | 板子**运行中** 492B 噪声 vs 板子**被按复位(全高阻)** **0B** |

- 结论: **两根信号线都不在 PA9/PA10 上** (RXD 悬空在拾取串扰 / TXD 未到 PA10)
- ★ 判别法本身值得记: **噪声随板子活动出现、随板子高阻消失 = 悬空拾取串扰**,
  而不是"接上了但数据不对"

### ★ 板商原理图给出的权威脚位 (sch_p1.png)
- **H1 = 排针_卧贴_2*4P, 奇数/偶数排列** (行: 1/5, 2/6, 3/7, 4/8):
  1=SWCLK PA14 | **5=USART1_RX PA10** ← CH340 TXD
  2=SWDIO PA13 | **6=USART1_TX PA9**  ← CH340 RXD
  3=GND        | 7=RST(串1K)
  4=+5V        | 8=3.3V
  ★ 交叉验证法: PA9 与 SWDIO 同一行; PA10 与 SWCLK 同一行; 3=GND/8=3.3V 可认整列
- ★★ **U9/U10 = 2.54-2*30 全 IO 引出排针** (第二接点): **U10 pin6=PA9, pin3=PA10**
  (U10 pin2/4=GND); U9 上是 PA0-PA8/PA11/PA12/PC0.. 等

### 交付的工具 (本轮新增/改进)
- `tools/h723_findpin.py` (新): 无需万用表/照片定位 H1 脚位
  · `--listen` 碰 RXD 找 PA9 / `--scan-tx` 碰 TXD 找 PA10 / `--pins` 打印脚位表
  · **判据用 CRC 合法帧**, 不看字节数 ⇒ 悬空噪声不会误判
  · 自检: 合成帧正例通过; CRC 错/全零噪声均正确拒收
- 信标固件: `bash build.sh -DDCL_BANNER_PERIOD=300` → PA9 每 300ms 主动发
  `C1 00 04 00 00 02 21 00 D8 AC` (10B, 带 CRC) —— 碰对了脚立刻能看见

### 诚实标注
- PA9 的**波形**本轮**没有外部仪器直接观测** (LA 已断开)。
  "PA9 在输出"是由 ①寄存器配置 ②TC 完成 ③引脚可驱动(改 GPIO 后 1→0→1 实验)
  三者**推断**, 非直接测量 ⇒ 需要 LA 或通了的 CH340 才能闭环

### 方案 (给用户三选一)
A 拍张照片 (最快, 我直接指出接哪) / B 跑 h723_findpin 逐脚碰 / C H1-5↔6 短接 + 我烧自检版

---

## ★★ 24:0x-25:0x 基础问题修复 (用户: "先停吧, 问题太多...先去解决基础问题")
- 审计位置: `C:/Users/min/WorkBuddy/项目审查官/` — H723-STAGE2-AUDIT.md (17:38) +
  H723-STAGE3-AUDIT.md (20:38) + .workbuddy/memory/2026-09-10.md (审计官日志)
- 报告: docs/FIX-FOUNDATION-REPORT.md

### A1 (P1 实锤, 审计抓到) NVIC_ISER=(1u<<37) 位移溢出 → USART1 中断从未使能
- IRQ_USART1=37 ≥32, 裸移位是 UB; GCC 报了 -Wshift-count-overflow 但被 24 条无害警告淹没
- 后果: 即使接线正确, 上位机每个字节都不触发中断 → 协议层功能上不可用
- 修: regs.h **不再提供 NVIC_ISER 这个名字**, 只给 nvic_enable_irq(irq) 宏/函数
  (按 IRQ 号自动算寄存器索引) + _Static_assert(IRQ<64) 护栏
- ★ 新增 g_uart_irq_en (读 ISER 位本身) — "配置全对≠功能可用", 中断使能必须可被外部读走
- 实机: ISER[1]=0x20 (修前 0x0) / ISER[0]=0x10000000 / g_uart_irq_en=1

### ★★ N1 (P1, **审计未覆盖 — 本轮自查发现**) 组帧 CRC 覆盖少一字节
- `send_response` 写 `crc16_ccitt(s_txbuf+1, 2u+n)` — 应 3u+n
  → 载荷**最后一个字节不在 CRC 保护范围内**
- S3 原文 main.c:62-78 是 `crc16_ccitt(cb, 3+n)` (cb=[sts][len][payload])
- 后果: PC 侧按 7 字节校验 → **每一帧都判 CRCBAD** → 即使 A1+接线都修好也永远收不到帧
  (即叠在 A1 之后的第二道独立的"协议不可用")
- 为什么 3 轮都没发现: ①从来没真收到过帧 ②阶段3.1 报告的"手算横幅帧 …D8 AC"
  是按错误假设手算的, **从未在线上验证** = 宣称>实现
- 发现手法: 读固件静态发送缓冲 s_txbuf, 用**独立实现的 CRC** 校验 → 5 分钟露馅
- 修: 常量 FRAME_CRC_COVER(n)=3u+n + build_frame_into() + frame_build_selftest()
  (期望值 0xC9C9 由 PC 侧独立算出) → g_frame_selftest
- 实机: 帧尾 44 E7 → **C9 C9**; g_frame_selftest=1

### A3 (P2 实锤) wire2 判据缺 flags 分支 → 静默读 wire[0]
- 修: wire2_valid(flags,idx) 单一判据 (ISR 与 deploy 校验共用); op_needs_wire2() 把
  "需要第二输入"从 {AND,OR} 扩到 {AND,OR,ARITH,SR,CNT} (**S3 也漏了这三类, 值得回写**);
  fill_tables 对双输入原语同时置 ROUTE_FLAG_WIRE2
- 实机: wire[5] 17.0 → **10.0**; 阳性对照 (置 WIRE2) 仍 17.0 ⇒ 判据没退化成恒 0

### ★★ A3 引出成本基线位移 10% —— 做对照实验才敢归因
| 构建 | B1 FLASH | B2 ITCM |
|旧判据(其余修复都在) | 29483 | **7270** |
|A3 修复版 | 27727 | **6476** |
- 机制(标为假设, 未做周期级归因): 常见路径得到常量 wb=0.0f → 省掉一次 DTCM 加载 +
  _finite_f(wb) 被折叠
- 据此**重测 19 原语成本表**: DIRECT 56→50 / PID 145→140 (build/op_cost_after_fix.json)
  ⇒ k_op_cost_itcm[] 与 OP_COST_MAX_MEASURED 已同步
- ★ 教训: 成本模型是"代码的函数", 代码一动必须重测

### A4 cap 未声明 HOTRELOAD (实现了却不报) → 0x0021 → **0x0033** (实发帧读出)
### A5 CMake 缓存污染 → build.sh 每次显式传全部默认值 + 打印 CMakeCache 生效开关
### H3/A7 OFF_MB_SET 0x4AC0 → **0x4AA0** (唯一改值的常量); 空洞显式命名 + 断言 <=改==
   (负例: 改坏立刻 rc=1 且消息可读)
### H4 linker 符号折叠 → **src/lsym.h 的 LSYM_ADDR 宏** (空内联汇编强制物化)
   shm_layout_ok 2 条指令 → **34 条**; nm: _shm_start=g_shm=0x200033e0, _shm_end=+0x8000
### H5 统计口径 → 增 g_*_cyc_first 首样本留痕; 工具在 min==首样本 时打 `?`;
   明确"权威口径 = max (WCET)"
### H6 clock.h 五个频率收敛为"CLK_* 宏 = 唯一权威" (550/520/465 标为历史/天花板)
### H7 CLK_TIMXCLK_HZ 改 2×PCLK + 断言 (=HCLK / APB_DIV∈{2,4})
### H8 la_tick_freq --cpu 默认改从 src/clock.h 解析(实测输出 400.0) + 回显来源 +
   与源码不符时拒绝下"时钟树正确"结论; stage1 的"权威标定"改必须传 --la-us (原为循环论证)
### H9 sweep_freq.sh 加 trap 自动还原+重建; H10 AINLINE 加 static; H11 state_offset 避开 0

### ★★ 根因治理 (比单条修复重要): 零警告闸门
- CMakeLists 加 **-Werror**; build.sh 构建后扫日志, warning 数 >0 即失败并全量打印
- 清噪声: syscalls.c 20 条 unused-parameter (只关该文件这一种) / clock.c ppre_div 删 /
  main.c 两个自检函数加 unused
- ★ 断言消息必须 ASCII: GCC 7.3.1 把非 ASCII 打成八进制转义(\37777777745…),
  失败信息不可读 = 断言价值打对折 → 32 条消息全部 ASCII 化
- ★ 我自己也踩了: 用正则批量改断言时吃掉了收尾括号 → 14 处语法错误 (已修)

### 最终: 零警告 (25→0) + 全量回归 22 ✓/0 ✗ + 9 项实机 PASS
### 未修: A6 (0x38 字段语义, 需与 S3 逐字段核对) / A9 (tick u32 4.97 天回绕) /
   PA9 波形外部复核 (LA 已断开) / ITCM 自身落位敏感 (建议照 REF-flash-placement 方法扫)

---

## 25:5x S3 源码走查: 迁移对象精确清点 (用户: "去看 esp32-core0 的源代码, 看看怎么迁移")

### S3 资产规模 (实测, 排除 esp_littlefs 第三方)
自定义代码 **3251 行 / 12 个组件**: core0 1363 / persist 341 / macro 331 / shell 206 /
display 199 / fs 177 / hil 178 / transport 135 / ai 143 / di 92 / sensors 86
PC 侧 tools: test_dcl.py 606 + 19 个 verify_*.py + dclc.py 729 + dclmon.py 254
(= "20 套回归" = test_dcl.py + 19 verify)

### 迁移分档 (逐文件核对)
- 已迁: primitives.h(19原语逐字) / shared_mem.h→engine.h(表结构+SHM偏移) / transport(帧协议逐字)
- 部分: core0_isr.c — 扫描 + 桶化分档已迁; **Force / Sequencer / persist BUSY / mb_tick 未迁**
- 未迁: modbus.c 370 / persist.h 341 / macro 331 / sensors 86 / di 92 / ai 143
- 建议不迁: shell + fs + display (H723 无对应硬件/文件系统)

### ★★ 硬问题1: 协议命令只实现了 3 / 20
H723 现有: 0x01 GET_VERSION / 0x10 DEPLOY / 0x38 ENGINE_STATUS
20 套回归实际用量 (脚本内频次): 0x44 SEQ(45) 0x10(45) 0x22 READ_BURST(37) 0x24 FORCE(35)
  0x20 READ(30) 0x13 RESET(28) 0x11 START(28) 0x60(26) 0x12 STOP(25) 0x40 MACRO(14)
  0x21 WRITE(12) 0x62(9) 0x23 WRITE_BURST(9) 0x43 PERSIST(8) 0x38(7)
★ **dclc.py 下发程序的第二步就是 0x11 START** → 没有它, DSL 编译器连第一个程序都跑不起来

### ★★ 硬问题2: SHM 偏移已破 "不变量"
- OFF_MB_SET: S3 0x4AC0 → H723 **0x4AA0** (H3/A7 修复, "唯一改值的常量")
- H723 把 0x3840-0x47F0 整段标 `OFF_RSVD_DSL_DOMAIN` 注释 "S3 的 macro/force/seq 域,
  H723 未落地" —— 但那段**恰恰装着待迁域**: FORCE_MASK 0x3A00 / FORCE_VAL 0x3A10 /
  SEQ_TABLE 0x4000 / SEQ_CTRL 0x4400 / PERSIST_BUSY 0x3860 / TICK_STATS 0x3854 /
  TIMING_OVERRUN 0x3850。落地时必须重排这段, 0x4AA0 站不站得住要重算
- 缓解: PC 脚本走**协议地址**(40065+)不按 SHM 偏移读 → 目前无脚本被打破

### ★★ 硬问题3: MIGRATE §9.1 "20 套全绿 + 脚本零改动" 做不到
- test_dcl.py T3 依赖 **DHT22**(sensors/), T5 依赖 **Shell 0x50 → 文件系统**(shell/fs)
  —— H723 侧这两个组件不存在
- **verify_force.py:60 硬断言 `fw == 0x0107`**, 而 H723 报 0x0200 (transport.h 明确
  "平台换了, 版本谱系另起") ⇒ 二者不可兼得。仅此 1 个脚本这么写, 其余走 cap 位判断
- 好消息: 串口是**命令行参数**(`sys.argv[1]` default COM7), 非硬编码 → 传 COM14 即可

### ★ 硬问题4: MIGRATE-H723.md 文档与事实脱钩
该文档 (15:10, S3 侧) 仍是 **550MHz 规划态**: §3 全篇 / §5.1 ARR=27500 @275MHz /
§9.3 拍预算 55000; 实际落地 **400MHz VOS0, ARR=20000 @200MHz**
而 H723newest/README 写 "下一步编号以 MIGRATE-H723.md 为准" = 用过时文档当权威

### 迁移顺序判断 (给用户)
1. 最小可驱动集: 0x11/0x12/0x13 + 0x20/0x21/0x22/0x23 → 之后 test_dcl T1/T2/T4/T6 立刻可跑
2. 0x24 FORCE + 0x43 PERSIST_STATUS (**和 persist 本体一起做**, 0x43 是它的观测面)
3. 0x44 SEQ_DEPLOY + Sequencer (回归频次最高 45)
4. 0x60/0x61/0x62 Modbus
5. 0x40/0x41/0x42 macro + sensors/di/ai
★ **不要先做 Modbus/Sequencer**: 频次虽最高, 但依赖 0x20/0x22 才能被验证 —— 没有读命令, 做了也看不见

---

## 25:5x 迁移规划定稿 (用户: "用 daplink 就可以, 开始推进迁移")

### 用户决策
- **不走 CH340 外接线**, 用 DAPLink。探针在位: `Luxiaoban Flash Pro(CMSIS-DAP)`
- 待确认: DAPLink VCP(COM11) 是否物理连到 PA9/PA10 —— 若没连, 协议口仍需 CH340
  (h723_proto.py 默认自动找 CH340, 要显式 `--port COM11`)

### 波次规划 (排序理由 = **依赖**, 不是频次)
| 波 | 优先级 | 内容 | 命令 | 验收 |
|W1|P0|运行控制 + SHM 读写|0x11/12/13 + 0x20/21/22/23|test_dcl T1/T2/T4/T6, SWD 退场|
|W2|P0|Force + persist(本体一起)|0x24 + 0x43|verify_force · verify_seq_persist|
|W3|P1|Sequencer|0x44 + ISR 40 行扫描块|verify_seq · verify_seq8 · verify_seq_demo|
|W4|P1|Modbus 通信域 (先隧道模式)|0x60/61/62|verify_modbus · verify_la_modbus|
|W5|P2|macro 字节码 + 传感器|0x40/41/42 + DHT22/DI/AI|verify_fbd · verify_di · verify_ai|
- 净工作量估计 ≈ 1540 行 (W1 120 / W2 250 / W3 150 / W4 370 / W5 650)

### ★ W1 的本质
不是"加几个 case", 是 **把引擎配置态从编译期搬到运行期** ——
现状 `-DDCL_BOOT_PROFILE/-DDCL_BOOT_GATE/-DDCL_BOOT_SEL` 决定跑哪套表; S3 是协议驱动。
必备: S3 main.c:83-131 的 4 个守卫 (`valid_addr`/`valid_range`/`shm_off_is_float`/
`write_allowed`) 必须**逐字搬** —— 安全边界不能自己发明。
H723 已有可复用件: `cold_start_reset`(接 0x13) / `engine_prog_budget`(接 0x11 的 F11 兜底) /
`outputs_safe` 需补(接 0x12)。

### 三个待拍板决策
1. SHM 的 `OFF_RSVD_DSL_DOMAIN`(0x3840-0x47F0) 必须拆开按 **S3 语义**逐段落地
   (FORCE_MASK 0x3A00 / FORCE_VAL 0x3A10 / SEQ_TABLE 0x4000 / SEQ_CTRL 0x4400 /
   PERSIST_BUSY 0x3860 / TICK_STATS 0x3854), **不要自己重排** —— S3 那段是 27 轮审计定下的
2. 编译期 boot profile: **保留作回归对照, 默认走协议** (同 `g_scan_mode` 的处理)
3. `verify_force.py:60` 的 `fw == 0x0107` → 改 cap 位判断 (20 套仅此 1 个硬编码版本)

### ★ 验收标准必须重定义
`test_dcl.py` 的 T3 依赖 DHT22、T5 依赖 Shell(0x50→文件系统), H723 无 sensors/shell/fs 组件
⇒ 目标是 **"18 套全绿 + 2 套标注不适用"**, 不是 "20 套全绿"。否则在追一个假目标。

### 起点动作
1. 提交那 4 个未提交文件 → 2. 构建烧录当前固件 →
3. 把 S3 的 `tools/test_dcl.py` 拷到 H723/tools/ 跑一次 (`COM11`) →
4. **用失败清单当 W1 的验收清单** (先拿 baseline, 否则改完不知道动了什么)

### ★★ 硬件事实 (用户 21:59 亲口告知)
- **DAPLink 只接了 PA14(SWCLK) + PA13(SWDIO)** = H1 的 1/2 脚
- H1 排针全表: 1=SWCLK PA14 / 2=SWDIO PA13 / 3=RST(串1K) / 4=+5V /
  **5=USART1_RX PA10 / 6=USART1_TX PA9** / 7=待核 / 8=3.3V
- ⇒ **SWD 可用, 协议 UART (PA9/PA10) 悬空** ⇒ 当前**验收路径不通**
- 三条可选路径:
  ① 从 DAPLink 引 TX/RX 两根线 → H1-5/6 (GND 已由 SWD 共地) ← 推荐, 成本 2 根杜邦线
  ② CH340 → H1-5/6 ③ H1 的 5↔6 短接 + `-DDCL_UART_SELFTEST=1`
     (自环只能验 USART 驱动+CRC+解析器, **不能替代 test_dcl.py**)
- 判断: SWD 是"调试口"(烧录 + 读任意内存, 永不退场), 串口是"验收口"(20 套脚本的命脉) ——
  两者不冲突。用 SWD 替代串口验收 = 把"PC 资产零改动复用"这个卖点降级, 不建议

---

## 22:0x 串口验证 (用户接上 DAPLink TX/RX 后, 对调过一次)

### 事实
- **DAPLink VCP = COM11** (VID:PID=0D28:0204, SER 与 pyocd Unique ID 完全一致) —— 可确认
- CH340 = COM14 (未用)
- 对调 TX/RX 前后各测一次, `h723_proto.py --port COM11` 两次都是 T0 FAIL → 全 SKIP

### ★ SWD 取证 (能可靠读 SRAM/PPB, 结论可信)
| 判据 | 值 | 含义 |
|板上固件 = build/dcl_h723.bin | Flash 0x2000 处 4/4 字逐字节一致 | 排除了"板上是旧固件" |
|g_uart_irq_en | **1** | NVIC 使能了 (A1 修复有效) |
|g_uart_brr | 0x3641 | 波特率算对了 |
|g_frame_selftest | 1 | 组帧 CRC 正确 |
|g_uart_tx_bytes | **10** | 横幅确实发出去了 |
|g_tick_count | 在走 | 拍活着 |
|**g_uart_isr_n** | **0** | ★ **PC 发 48 帧, 板子 RX 零中断** |
|PC 侧监听 9 秒 | **0 字节** | ★ 板子的横幅也没到 PC |
⇒ **双向都不通** ⇒ **排除"TX/RX 接反"**(接反只会通一个方向)
⇒ 固件侧自洽, 问题在**物理连接**

### ★★ 工具级发现 (影响后续所有外设验证)
**pyocd 读不了外设寄存器区**: `0x52002000`(FLASH_ACR) / `0x58020000`(GPIO) 均
`memory transfer failed`; `0x40011000`(USART1) 返回 0 也是**读失败**不是真值。
只能可靠读 **PPB** (CPUID=0x411fc272 ✓ Cortex-M7 r1p2) 和 **SRAM**。
⇒ 之前"USART1_CR1=0 / GPIOA=0"的推论**作废**(那是读取失败)。
⇒ 以后外设配置验证**不能靠 pyocd**, 必须 LA 外部证据或间接判据。

### 下一步二分法 (建议用户做)
短接 **H1-5 ↔ H1-6** (PA10 ↔ PA9), PC 发数据看能否自收:
- 收到 → PC + DAPLink + 两根线 全通, 问题在板子侧
- 收不到 → 问题在 PC↔DAPLink 这一段 (引脚认错/线坏)

### 注: SHM 的 OFF_CTRL_MAGIC(+0x00) 恒为 0
而 +0x840 (ROUTE_TABLE) 处非 0 (`00020000 00000100`) ⇒ SHM **被填过**, MAGIC 由别的路径写
(待查, 不影响串口问题)

### 临时脚本 (在 build/, gitignored, 会被 clean 清掉)
`build/diag.py` (符号名→地址→读值) · `build/send_frames.py` (发 GET_VERSION 帧 + 监听)

---

## 22:14 ★ 事故: LA 未接线就采集 (用户叫停)

- 我在 LA 探针**完全没接线**的情况下起了一次 5s 采集, 导出后看到
  CH1 = 3 跳变 (间隔 34.75 / 24.44 µs) / CH4 = 5 跳变 (间隔恒 25.0 µs)
  —— **差点据此编出"这是 UART 字节"的错误结论**
- 真因: 悬空高阻输入拾取环境噪声。跳变数少 + 间隔"规律" 最容易误读
- 教训: **"设备在线"(`get_devices` 有返回) ≠ "探针接着"**。
  采集前必须确认物理接线; 或做"让信号停止再采"的对照
- 已回写 skill `saleae-la-verify` (新增坑 7: 采集前确认接线 + 两条判据)

### ★ 文档冲突 (接线时勿赌)
**H1 第 3 脚**:
- `docs/HARDWARE-PINOUT.md` 写: 3 = RST(串1K) / 7 = 待核
- memory + sch_p1.png 写: 3 = GND / 7 = RST
⇒ **GND 到底接哪个脚无权威结论**。GND 建议接 DAPLink 的 GND 脚 (它已与板共地)
   或大排针 U10 的 pin2/4 (memory: U10 pin2/4=GND)

### LA 接线方案 (待用户执行)
- **LA CH1 → PA9** (板子 TX): 与 **SWDIO(PA13) 同一行的另一端** / 或 U10 pin6
  · 建议**拔掉 DAPLink 的 RX 线**让 LA 独占 PA9, 避免两个负载并联
- **LA GND → GND**: 接 DAPLink 的 GND 脚最省事 (已共地)
- 触发: 现有固件**复位后发一帧横幅**(10B); 若要持续波形需烧 `-DDCL_BANNER_PERIOD=300`

---

## 22:2x-22:4x ★ 补记: 本时段进展只存在于未提交工作区 (22:5x 铆钉盘点发现)

### 未落盘的实质进展 (提交停在 813cfe4 / 21:21)
1. **`src/uart.c` FE/NE 缺陷修复** (真缺陷, 未实测未提交):
   原 ISR 只清 ORE 不管 FE/NE → 实测**每帧 6 字节只收到 1 个**, 且那个字节的 ISR
   原值 `0x006010F4` 里 **NE=1** ⇒ 错误标志不清会**卡住接收通路**, 表现完全不像
   "线断了" (ISR 次数=帧数, ORE=0, drop=0, 一切"看起来正常")。
   修法: 逐个显式清 PE/FE/NE + **ORE 先于 RXNE 处理且清完再读一次 ISR**
   (ORE 置位时 RDR 里是旧数据)。新增 8 个 OBS 诊断量 (isr_n/isr_ore/fe/ne/
   push/last_isr/last_byte) 供 SWD 读走。
2. **`src/min_uart.c` (未跟踪)**: 与 DCL 完全无关的最小固件 —— 不配时钟树 (HSI 64MHz
   默认) / 一个中断不用 (纯轮询 RXNE/TXE) / 不做协议帧 (原样回声 + 每秒心跳)。
   **22:32 已构建出 `build_min/min_uart.bin` (1936B)**, 但**没有烧录实测记录**。
3. 22:41+ 转向 `tmp_dap/DAP串口驱动`(stmcdc.inf + usbser.sys) 与 `tmp_dap/sch/*.png`
   —— 怀疑 DAPLink VCP (COM11, VID_0D28&PID_0204) 本身可用性。

### ★ 与 22:0x 记录的矛盾 (必须解决, 否则证据链断了)
- 22:0x 记: `g_uart_isr_n = 0` (PC 发 48 帧, 板子 RX 零中断) ⇒ 判定物理不通
- 22:2x 证据: **ISR 有进入、收到了字节、NE=1** ⇒ RX 方向**有信号进来**
⇒ 两者不能同时成立。说明这中间**动过接线或重烧过固件**, 但过程没记录。
**下次动手前先写明"此刻的接线与固件版本"**, 否则所有诊断数据不可比。

### ★ 二分法仍未执行 (22:0x 就提出, 到 22:58 未做)
**DAPLink 侧 TX↔RX 短接自收** —— 拔掉板子连接, PC 串口工具发字符能自收 = PC 段全通。
不需要板子、不需要固件、30 秒出结果。**这是当前信息量最大的一步, 别再跳过去查更弱的证据。**

---

## 26:0x 全局盘点 + ★串口卡点定性 (固件工程师接管, 全量走查两平台源码)

### ★★ 决定性结论: 卡点是**两根杜邦线**, 不是代码
证据链 (全部本轮实测, 单会话内可比):
| 判据 | 值 | 含义 |
|pyocd 探针 | Luxiaoban Flash Pro 在线, CPUID=0x411FC272 | 调试口可用 |
|**板上固件身份** | 复位向量 **0x080005FD** / SP 0x20020000 / 0x08002000=0xFFFFFFFF | **是 min_uart.bin(1936B), 不是 dcl_h723.bin**(其复位向量 0x08002295, 15568B) |
|板子存活 | min_uart `g_heartbeat@0x20000020`: 0x51→0x5A (4s) | 板子活着, 在跑 |
|**RX 方向** | PC 发 32 字节→COM11, `g_rx_bytes@0x20000024` = **0** | 一个字节没进 USART1 |
|**TX 方向** | PC 监听 + 回声测试: **0 字节** | 心跳也没到 PC |
⇒ **双向都不通** ⇒ 排除"TX/RX 接反"(接反只通一个方向) ⇒ **物理连接不存在**
⇒ DAPLink VCP(COM11, VID:PID 0D28:0204) 的 TX/RX **没接到 PA9/PA10**

- ★ 同时解决 22:0x vs 22:2x 的矛盾: 22:2x 那轮**板上跑的是 min_uart**(那时它还没被记录烧录),
  所以"ISR 有进入/NE=1"与 22:0x 的"isr_n=0"分属**两份不同固件** —— 不是同一状态的自相矛盾
- ★ 新纪律: **诊断前先核对"板上固件是谁"** —— 读 0x08000004 复位向量与候选 bin 比对, 10 秒。
  否则所有计数都不可比 (本轮若不做这步, 会把 min_uart 的 0 当成引擎的 0)
- ★ 第二卡点: 板上**不是交付固件**, 修好线后必须先烧回 dcl_h723.bin

### 下一步 = 一个 30 秒对照实验 (用户侧物理动作)
**拔掉 DAPLink 接板子的 TX/RX, 把这两根线互相短接, PC 自发自收**:
- 收到 → DAPLink VCP 是好的, 问题在板侧接线 (接 H1-5/6 或 U10 pin3/6)
- 收不到 → DAPLink VCP 不能当串口用, 换 CH340(COM14)
此后: 烧回 dcl_h723 → `python tools/h723_proto.py --port COM11` 要 6/6

### 两平台规模实测 (wc -l)
- esp32-core0: **4340 行** / 12 组件 / 20 命令 / 20 套回归 (已冻结只修不增)
  大件: main.c 1089 · core0_isr.c 474 · shared_mem.h 378 · modbus.c 370 · persist.h 341
- 9.10 H723newest: **3854 行** (src/*.c+*.h) / 14 提交; 大件 main.c 1060 · engine.c 667 · engine.h 475
- ★ 行数接近但**语义覆盖率差很远**: 命令只实现 **3/20** (0x01/0x10/0x38);
  Force / Sequencer / Modbus / persist / ADC **全部为 0 引用**

### 迁移进度 (MIGRATE 六阶段)
0✓ 1✓ 2✓ | 3: 2/4 (协议帧✓ deploy✓ · persist✗ 20套回归✗) | 4: 1/3 (分档✓) | 5: 0
- 引擎内核已迁且**优于 S3**: 分档 2.59× · 单路由 56cyc vs S3 234cyc · 拍抖动 <10ns
- 缺的全是"能被外部使用"那一层: 运行控制(0x11/12/13) + SHM读写(0x20/21/22/23) 是**最小可驱动集**,
  没有 0x11 START 连 dclc.py 编译出的第一个程序都跑不起来

---

## 23:0x-23:2x ★★ 二分法定案: PC 段不通, 板子侧洗清嫌疑

### 实验 1: DAPLink VCP (COM11) TX<->RX 短接自收 —— 全 FAIL
新工具 `tools/h723_loopback.py` (T0~T4 五项判据, 每条都能失败):
| 判据 | 结果 |
|T0 端口可打开| PASS (COM11 @115200) |
|T1 静默 2s 噪声基线 = 0 字节| PASS (0 字节 ⇒ **不是悬空拾噪**, RX 稳定在高电平) |
|T2 哨兵回环 8/32/128 字节| **FAIL 0/8, 0/32, 0/128** |
|T4 break 探针 (TX 强制拉低 0.5s)| **FAIL 0 字节** |
- ★ T4 是决定性的: TX 与 RX 若真短接, TX 被拉低 0.5s 时 RX **不可能毫无反应**
- ★ 哨兵序列用 `(i*37+11)^0x5A` 伪随机, 与 0x00/0xFF/单字节噪声可区分,
  避免"只通第一个字节"被误判成全通
- ⇒ 结论: **DAPLink 的 VCP 与物理引脚之间没有电气连接**
  (CDC 接口只在 USB 层存在, 引脚多半没引出 / 或短接的针认错了)

### 实验 2: 换 CH340 (COM14) 接板子 TX/RX —— T0 仍 FAIL
`python tools/h723_proto.py --port COM14` → T0 链路不活, 其余 12 项全 SKIP (不是 PASS)
- CH340 VID_1A86 PID_7523, 驱动正常, 端口能开能写
- ⇒ 两段都指向"板子侧", 但**不能直接这么归因** —— 见下

### ★ 归因纪律: 还差一个阳性对照
CH340 这条线**没被独立证明是好的** (它也可能坏/线断)。下一步用 **S3 做阳性对照**:
S3 是已知 20 套全绿的设备。
- **方案 A (零接线, 先做)**: S3 插 USB → COM7 → `python tools/test_dcl.py COM7`
  · S3 协议口 = **UART0, GPIO43=TX / GPIO44=RX, 115200** (`main/main.c:41-44`)
  · `_flash.bat` 的 COM7 就是板载 USB 桥接在这一对脚上 ⇒ 插上就能跑, 不用 CH340
  · 全绿 ⇒ PC 侧协议工具 + 固件实现 + 板载链路 全部无罪 ⇒ 嫌疑精确落在 H723 板子侧
- **方案 B (要验 CH340 本身才接)**: CH340 **RXD→GPIO43, TXD→GPIO44** (交叉!), GND→GND
  · ★★ **CH340 的 VCC 跳线必须拨 3.3V**, 5V 的 TX 会灌穿 S3 的 IO
  · 从 COM14 发 GET_VERSION, 能收到响应 ⇒ CH340 双向通 ⇒ H723 板子侧才是唯一嫌疑

### ★ 方法论沉淀 (跨项目)
- **链路二分: 先测"PC+转换器+线", 再碰板子。** 短接自收是零硬件成本的第一步
- **break 探针是廉价阳性对照**: `ser.send_break(0.5)` 强制拉低 TX, 短接有效则 RX 必有反应
  (0 硬件投入, 判据能失败, 专治"数据发出去了但不知引脚动没动")
- **噪声基线必须前置**: 静默期 0 字节 ≠ 通信正常, 但能排除"悬空拾噪"这类误判源
- **哨兵序列 > 0x00/0xFF**: 短路/噪声最常表现为全 0 或全 FF, 用伪随机序列才不会假绿

---

## 23:2x ★★ CH340 短接自收也全 FAIL —— 判据自己造了一次假 (已修)

### 事实
`tools/h723_loopback.py --port COM14` (CH340 短接 TXD/RXD):
| 判据 | 结果 |
|T0 打开| PASS |
|T1 静默 2s 噪声基线| PASS (0 字节) |
|T2 哨兵 8/32/128 @115200| **FAIL 全 0 字节** |
|T2b @9600 / @57600| **FAIL 全 0 字节** |
|T4 break 探针| FAIL 0 字节 |
- 新增 `write` 返回值校验: **8/8、32/32、128/128 全被驱动接受** ⇒ 不是本地吞数据
- ⇒ **DAPLink VCP 与 CH340 表现完全一致**: 两个不同芯片/不同驱动同时坏的概率极低
  ⇒ 高度怀疑**短接根本没形成电气连接**, 而不是设备坏

### ★★ 我自己的判据造假事故 (必须记住)
第一版 T3 用"往返延迟斜率"区分硬件回环 vs 驱动回显。当三轮**全部超时**时,
延迟都 ≈ 1002 ms, 斜率是**纯随机噪声** —— 输出过一次
`斜率 52.2 us/字节 => 数据真的从引脚绕回来了`。**这是假的**, 差点据此写进结论。
- 修法: T3 只在 `T2 回环真的成功` 时才输出判读, 否则打印
  `[不判读] 回环未成功, 三轮延迟均为超时值, 斜率无物理意义`
- ★ 通用铁律: **任何"从超时值算出来的量"都无物理意义。判据宁可不出, 也不能出会骗人的数。**
  同理可推: 从失败的测量里算均值/方差/斜率, 全是噪声
- ★ Windows `usbser.sys` 会**静默丢弃**带 framing error 的字节 ⇒ T4 break 探针
  **只能证真、不能证伪** (收不到 ≠ 没短接)。硬证据只有 T2。

### 下一步怀疑: 短接做在了板子侧
若把 H723 的 PA9/PA10 短接(而不是 CH340 的 TXD/RXD), 则:
固件把 PA9 配成 USART **推挽输出 → 强驱高电平 → 压死短接信号**
⇒ RX 恒高 ⇒ 收不到任何字节 + 静默期 0 字节 —— **与全部观测完全吻合**
正确做法: **拔掉所有接板子的线, 在 CH340 模块上用一根杜邦线直接连 TXD 与 RXD**
(不经板子、不经长线)。这样还 FAIL ⇒ CH340 模块本身坏。
