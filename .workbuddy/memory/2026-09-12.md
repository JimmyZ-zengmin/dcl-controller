
---

# ★★★★ P3-B 破案 (2026-09-12 下午第二轮): SBUS/DBUS 语义 + 软触发验证 + 影子模式开启

## ★★ 真因: CTBR 的 SBUS/DBUS 写反了
**MDMA CTBR 的两个总线标志** (旧项目注释 + 软触发实验双重确认):
- **SBUS(bit16) = 源在 DTCM/TCM 路径** (源地址经 TCM 端口读取)
- **DBUS(bit17) = 目的在 DTCM/TCM 路径** (目的地址经 TCM 端口写入)
首版误写 DBUS(1<<17) ⇒ MDMA 从 TCM 端口去写 GPIOE(AHB4) ⇒ **CESR=0x28**
(TEA=40=SAR 低 7 位, TED=0=读错误) ⇒ ODR 恒 0。**修为 SBUS(1<<16)** 后一切正常。

## ★ 软触发验证 (pyocd 直控 MDMA 寄存器)
| 步骤 | 结果 |
|---|---|
| 写 shadow=0xFFF, 配 CTCR=0x40000000(SWRM)|TLEN3|DINC/SINC=2, CTBR=DBUS | CESR=0x28 读错误 |
| 改 CTBR=SBUS(1<<16), 触发 | **CISR=0x1001E (完成+buffer完成), CESR=0, ODR=0xFF** ✅ |
| 源/目的地址 | CSAR=0x2000F02C CDAR=0x58021018 (+4 漂移是 SINC/DINC=2 递增) |
**⇒ 软触发验证通 = MDMA 基础通路确认; 错误原因 = CTBR 总线标志, 不是节点格式/CTCR/链表。**

## ★ 影子模式开启 (DCL_DO_LATCH=1) + 硬件链验证
STOP 注入 ACTUATOR=[1.0,0,1.0,1.0] + GPIO_MASK=0xFFFF:
- shadow (0x2000F028) = 0x0D ✅ (do_poll 打包)
- **GPIOE_ODR = 0x0D ✅ (经 MDMA 硬件锁存链写入, 逐位精确)**
- CESR = 0 ✅ 无错误
⇒ **完整数据链: do_poll 打包 → 写 shadow → TIM2 上溢 → DMA2 哑桥 → MDMA → GPIOE**
⇒ **输出沿与计算时长解耦** (MDMA 锁存时刻=拍边界, 由 TIM2 硬件钉死)。

## ★ 编码注意 (byte 尺寸的锁存宽度)
影子模式首测 ODR=0xFF (只有低 8 位) — 因为 byte 尺寸 + BNDT=4 的传输粒度是
byte, 但旧项目验证的 CTCR_4B_SW 就是 byte 尺寸 ⇒ ODR=0xFF 是**MDMA byte 传输
写 ODR 低字节的正确行为**; 要写完整 32 位 ODR, 需要 **PSIZE/MSIZE=word + BNDT=4**
(但旧项目警告"word 尺寸会 BSE 勿用") ⇒ 需实测 word 尺寸 + SBUS 是否也报 BSE。
**暂以 byte 模式运行** (低 8 位 DO 有效, 高 8 位待调)。

## ★ 全天遗留项处理进度
| 项 | 状态 |
|---|---|
| git push (8 提交) | ✅ 推送到 JimmyZ-zengmin/H723PLC |
| P3-B TEIF 精查 | ✅ **破案** (SBUS/DBUS 写反) + 影子模式开启 |
| P3-C adc_poll 拆两半 | ✅ 已提交 (5ece232) |
| ADC 提速 (fADC 12.5MHz + SMP 32.5) | ✅ 已提交 (d149adc) |
| PCSEL 写位置修复 | ✅ 已提交 (d149adc) |
| 文档 6 份 | ✅ 全部已提交 |
| P1c ST 前端 | 未开始 (1-2 周) |
| RTC + 时间戳 | 未开始 |
| 审计在线项 | 需串口连板 |
| P4 作品化 | README 骨架有, 待更新 |
�子锁存链 | 硬件链路通 (CISR 完成/无错), ODR 细节待调 |
| git 5 个提交 | 7d79fe0 → 447355b → 7dfd20e → 81ebe07 → 本轮 |
| 文档 4 份 | PLAN-io / FRAMEWORK-MAP / MEMORY-LAYOUT / CORE-ENGINE |

  ⇒ **用户答"输入源不清楚"真正卡住的是这个参数**, 不是架构。

### ★★ 输出面定案 (回答用户第 2 问 "你定")
**DO 面 = GPIOE[15:0] (16 路)**, 四条理由:
1. **GPIOE 在本固件中完全未使用** (全工程唯一提及是 `macro.c:37` 一句注释) — grep 确认
2. `h723-core0` 的输出链配方**就在 GPIOE 上验证过** (M0AR→GPIOE_ODR / DMAMUX C8=TIM1_UP / M2P+CIRC+32bit+NDTR=1) ⇒ 原样复用
3. **16 位恰好落进一个 u32** ⇒ 正好给 `engine.h:120-132` 钉住的定案走**选项 ②**
   ("只支持一个端口" + 断言 `mask < (1<<16)`) ⇒ **对 SHM 零偏移代价**
4. 数据源用现成 `OFF_ACTUATOR_STATUS`: `ACTUATOR[0..15] > 0.5f` → 打包 u32, **不需新增 SHM 区**
★ 明确不改: **心跳继续走 PA8** (LA CH4 已接) — 不学 h723-core0 把心跳并进输出字,
  否则把"拍心跳"与"输出面"耦合, 破坏现有确定性判据。
⚠ **唯一前置**: PE0..PE15 是否都引到 2×30 排针 (`HARDWARE-PINOUT.md` 只写"几乎所有 IO", `[原理图]` 未实测)
  ⇒ 开工第一步实测 PE 各脚写读; 若只引出 8 位就退到 8 位 + 断言 `mask < (1<<8)`。

### 预算核算 (结论: 毫无压力)
实测基线 `REF-flash-placement.md:118`: 混合程序 **`isr_max = 11524 cyc` = 占拍 29.03%, 余量 28476 cyc**。
搬入后增量预估: DI 60 + HIL 输出臂 60 + DO 打包 80 + ADC 状态机 25 ≈ **225 cyc = 余量 0.8%**。
⚠ **真正的落地风险不是预算, 是 ITCM 余量只有 0x68 字节** (`ARCH-H723.md:137`) ⇒ 新代码可能要腾 ITCM。

### 分批实施
- **P1** DI + HIL 输出臂进拍 (相位驱动, 去掉 10ms 相对节流) + GPIO_MASK 定案 ② + PE 实测
- **P2** ADC 非阻塞化 (方案 A) + 反馈臂改跨拍累加 ⇒ 主循环 I/O 占比 49% → ~0
- **P3** DO 硬件锁存链 (AXI + TIM1 UDE + DMA2 S0)
- **P4** 可选: ADC 硬件触发 + DMA / MDMA 输入链
建议 **P1+P2 合并** (输入输出对偶, 分开要动两次 ISR 结构), P3 单独 (引入 DMA/DMAMUX 新外设, 变量最多)。

### 对照 (A/B) 设计
| 期 | 开关 | 对照档 | 交付档 |
|---|---|---|---|
| P1 | `-DDCL_IO_IN_ISR=0` | 回主循环 10ms 节流 | 拍内相位驱动 |
| P2 | `-DDCL_ADC_ASYNC=0` | `adc_read` 阻塞自旋 | 拍内状态机 |
| P3 | `-DDCL_DO_DMA=0` | ISR 直接写 GPIO | DMA2 硬件锁存 |
判据必须能在对照档量到 FAIL, 否则判据无效。P3 对照档顺带给出"CPU 直写 GPIO"抖动基线。

### 待用户拍板 (方案 §10)
1. P1+P2 是否合并为一次改动
2. 是否允许先跑 PE 各脚写读实测 (GPIOE 可用位)
3. 采样周期是否改为"**拍相位严格锚定**" (10ms = 严格 100 拍, 去掉 ±1 拍抖动) — 语义变更, 影响现有测试期望值

---

## ★★★ P1+P2 实施 (用户: "按照你的想法开始" ⇒ 三项全批: 合并 / 先 PE 实测 / 拍相位锚定)

### ① PE 可用位实测 —— **16/16 全可用, 0 可疑** ⇒ 定案②成立
新工具 **`tools/h723_pe_probe.py`**: halt 态配 PE0..15 推挽输出 → 4 个 ODR pattern
(0x0000/0xFFFF/0x5555/0xAAAA) → 逐位比对 IDR。**4 个 pattern 全部逐位一致**。
★ **踩坑 (判据写错, 不是硬件错)**: 第一版判"恢复后 MODER 应 == 0" ⇒ 实测 0xFFFFFFFF
被判"未恢复"。**STM32H7 的 GPIO MODER 复位默认就是 0xFFFFFFFF (全 analog)**, 与 F4 的 0
不同。⇒ 改成"对比**配置前的基准值**", 不假设复位值。

### ② `OFF_CTRL_GPIO_MASK` 定案 = **选项②, 端口取 GPIOE[15:0]**
engine.h 注释重写 + **观测面拆成两个**: `g_safe_mask_nonzero`(合法使用) /
`g_safe_mask_oob`(**高16位非0 = 越界写, 独立计数**)。理由: 一个计数只回答一个问题。

### ③ 输入链进拍内 —— **★ 成功, A/B 有证据**
| | DUT (`DCL_IO_IN_ISR=1`) | CTRL (`=0`) |
|---|---|---|
| ① 存活 (tick/heartbeat 增量) | +9016 ✅ | +9014 ✅ |
| ② 拍周期 pmax | 40008 cyc ✅ | 40008 cyc ✅ |
| ④ `isr_max` | **894 cyc** | **218 cyc** (基线) |
| ⑤ **`g_adc_sm_done`** | **2485 → 4739 单调增** ✅ | **0 → 0** ✅ |
| ③ `g_per_glitch_n` / ⑥a 超时 / ⑥b 越界 | 全 0 ✅ | 全 0 ✅ |
**⇒ 核心判据⑤ 是"搬进拍内"的直接证据** (交付必须增 / 对照必须恒 0), 且**两档都全 PASS**。
**ISR 成本增量 = 676 cyc = 拍预算的 1.7%** (DI 采样 + ADC 状态机每拍推进)。
**★ 副产品**: 主循环里 `hil_tick` 那 **16 次阻塞 ADC 采样 (16×259µs ≈ 4.1ms/10ms = 41%)**
在交付档已被 `#if !IO_IN_ISR` 跳过 ⇒ **主循环不再有 4.1ms 阻塞**。

### ④ ⚠️ **未决项: HIL 输出臂进不了 ISR** (如实记录, 未冒充完成) —— **同日已解决, 见本文末尾**
只要在 TIM2 ISR 里调用 `hil_out_poll`, **整机就卡进 Default_Handler**, 而:
| 试过的形态 | 结果 |
|---|---|
| 放 flash (链接器自动 veneer 的 `BL`) | 卡 |
| 放 flash + `long_call` (`BLX`; 常量池地址**带 thumb 位, 已逐一核对**) | 卡 |
| 放 ITCM (与 ISR 同区, 直接 `BL`) | 卡 |
| **函数体改成空实现 (只剩 `bx lr`)** | **仍卡** |
| `di_poll` / `adc_poll` 用**完全相同**的机制 | **一切正常** |

**已排除的假设** (都有实测数据): 跨区 BL 范围 / veneer 缺失 / `long_call` 未生效 /
函数内容 / 缺"硬件就绪门" / ITCM 溢出 / 向量表内容 / 常量池 / 函数位置 / 断点假阴性。
**现场**: `PC` = 某个 `Default_Handler`, `ICSR.VECTACTIVE = 44`, `IABR0 bit28 = 1`,
`CFSR/HFSR = 0`, `PRIMASK/BASEPRI = 0`, **但 `slot[44] = 0x00000001` (TIM2_IRQHandler)
且 `TIM2_SR.UIF` 从未被清、`g_stage` 从未写成 7、`tick_count = 0`** ⇒ 三证表明
`TIM2_IRQHandler` 从未被执行, 而 IRQ 28 却是"活跃"的。**机制未明, 列为待查。**
⇒ **处置**: `hil_out_poll` 留在主循环 (`hil_tick`), 注释里写明"尚未进 ISR"。

### ⑤ ★★ 两条可复用的硬教训 (写进代码注释了)
1. **`static const` 函数指针数组会被 `-O2` 常量传播成直接 `BL`** ——
   源码改了、编译零警告, 但 `pyocd flash` 报 `programmed 0 bytes, identical N bytes`
   (**产物一模一样**)。**⇒ 用声明上的 `__attribute__((long_call))`, 不要用函数指针数组。**
2. **ELF 产物没有扩展名** (`build/dcl_h723`, 不是 `.elf`) —— objdump 报 "No such file"
   时先 `ls` 确认文件名, 别怀疑路径转换。**且 objdump 要 Windows 路径 + 无空格目录**
   (拷到 `%TEMP%\a.elf` 最省事)。

### ⑥ 本轮的验证基础设施
- **`tools/h723_pe_probe.py`** (新): PE 可用位实测 (含"用完复位并核对已恢复", 守铁律 0)
- **`tools/h723_io_isr_check.py`** (新): "搬进拍内"的 A/B 验收读数;
  **判据② 必须用 `pmax` 不能用 `pmin`** —— pyocd 的 halt/go 会让 DWT 冻结后第一个样本变小,
  `pmin` 必然被污染 (与固件无关)。
- **map 解析 + pyocd Python API** (`ConnectHelper` + `read_memory_block32`) 是比
  pyocd 命令行更强的诊断手段: **断点 (`set_breakpoint`) + 逐槽位扫描向量表** 直接定位了
  "哪个 IRQ 活跃 / 哪个 handler 被调用"。

---

## ★★★★ 4 节的"未决项"**已解决** —— 真因是**向量表的落位** (2026-09-12 同日)

用户提示 **"退一步看整体, 视野大了你应该也看得清楚一些"** ⇒ 我停止在 `hil_out_poll`
**内部**找原因 (veneer/常量池/thumb 位/就绪门/ITCM 落位…连续查了十几轮), 改用
**等长替代 + 纯体积对照** 做整体剥离 ⇒ 一轮定论。

### ★★ 真因 (一句话)
> **`_vtor_itcm = 0x1880` 时整机卡死; `0x1800` 和 `0x1900` 都正常。**
> 与"调用谁 / 函数放哪 / 函数体做什么 / 用 BL 还是 BLX"**全都无关**。

### ★★★ 证据链 (10 个构建, 全部真机实测)
| # | 实验 | `_eitcm` | `_vtor_itcm` | 结果 |
|---|---|---|---|---|
| 1 | 基线 (ISR 只加 di_poll+adc_poll) | ≤0x1800 | **0x1800** | **正常** |
| 2 | +hil_out_poll 调用 | 0x1808 | **0x1880** | **卡死** |
| 3 | 同 2 但 long_call (BLX) | 0x1808 | 0x1880 | 卡死 |
| 4 | 同 2 但函数放 ITCM (同区 BL) | 0x1808 | 0x1880 | 卡死 |
| 5 | 同 2 但函数体**空实现** (bx lr) | 0x1808 | 0x1880 | 卡死 |
| 6 | **等长替代**: 换成 `di_poll` 的调用 | 0x1808 | 0x1880 | **卡死** ← 与符号无关 |
| 7 | **纯体积对照**: 换成 4 条 nop (8B, 无调用) | 0x1808 | 0x1880 | **卡死** ← 与调用无关 |
| 8 | **推过阈值**: +200B nop | 0x18C8 | **0x1900** | **正常** |
| 9 | 重现 #7 的 8 字节 + 向量表 **256 对齐** | 0x1808 | **0x1900** | **正常** |
| 10 | **修后交付档** (hil_out_poll 回 ISR) | 0x1808 | 0x1900 | **正常** |

### ★ 修法
`ld/STM32H723ZG_FLASH.ld`: `.itcm_vectors` 对齐 **128 → 256**。
**代价**: ITCM 多留 ≤128 字节对齐填充 (当前 `0x1808 → 0x1900`, 空隙 248B)。
相对"随时可能因 8 字节增删而卡死", 可以忽略。

### ★★★ 必须记住的一条 (比修法更重要)
**ARMv7-M 的 spec 只要求 VTOR 128 字节对齐。**
⇒ **256 是本板实测出来的经验值, 不是规范要求** ⇒ **换板子/换型号/换批次都要重验**。
(这属于项目已知的"平台特性"家族: "本平台对代码体积敏感" ARCH §0.3 + T26 记的
"加 200 字节就改变上电行为" —— **本次把这个家族的一个具体机制钉死了**。)

### ★★ 修复后的交付验收 (A/B 双档全 PASS)
| 判据 | DUT (输入段+输出臂都在 ISR) | CTRL (IO 在主循环) |
|---|---|---|
| 存活 (tick/heartbeat 增量) | +9017 | +9010 |
| 拍周期 pmax | 40008 cyc | 40008 cyc |
| `g_per_glitch_n` (时基) | 0 | 0 |
| **`isr_max`** | **1111 cyc** | **218 cyc** (基线) |
| **`g_adc_sm_done`** | **2493 → 4747 单调增** | **0 → 0** |
| 超时 / 越界 | 0 / 0 | 0 / 0 |
⇒ **输入输出链都已进 100µs 拍内, ISR 成本增量 = 893 cyc = 拍预算的 2.2%。**

### ★★ 两条方法论 (比结论值钱)
1. **"某个东西一加就坏"时, 先证明"换个别的东西加进去会不会一样坏"**
   —— 等长替代 + 纯体积对照, 一轮就把"函数问题"与"平台问题"分开。
   我在 `hil_out_poll` 内部查了十几轮, 却一直没做这件最朴素的事。
2. **单点深挖会让人丧失整体视野** —— 用户一句"退一步看整体"直接点破。
   整体对照表 (三个 I/O 函数的差异) 一行就能看出"位置/体积"是唯一没被动过的变量。

### ★ 顺带记: `build/tmp` Permission denied 又复现
`cc1.exe: fatal error: can't open '...build\tmp\ccXXXXXX.s' for writing: Permission denied`
—— 这次**连跑两次都失败**, 重跑没收敛。**解法: `rm -rf build/tmp` 后重跑即可** (build.sh
自己会重建)。以后遇到这个症状直接清 `build/tmp`, 别怀疑代码。
(项目原记录说"重跑即收敛", 本次证明**不总收敛**, 补上这一步。)

---

# ★ 收工摘要 (2026-09-12 01:20)

## 今天的成果
1. **`h723-core0` 机制精读 + 搬迁评估** ⇒ `docs/ANALYSIS-harvest-h723-core0.md`
2. **PE0..PE15 全可用实测** (4 个 ODR pattern 逐位一致, 16/16) ⇒ `tools/h723_pe_probe.py`
3. **`OFF_CTRL_GPIO_MASK` 定案②** (GPIOE[15:0]; 观测面拆成合法使用 / 越界写两个)
4. **★★ I/O 输入输出链全部搬进 100µs 拍内** ⇒ A/B 双档全 PASS
   - DI 采样 (相位锚定) + ADC 非阻塞状态机 + HIL 输出臂
   - **ISR 成本 218 → 1111 cyc** (拍预算的 2.2%)
   - 主循环摘掉了 **4.1ms / 10ms** 的阻塞 ADC 采样 (原占 41%)
5. **★★ 卡死真因定位**: **向量表落位** (`_vtor_itcm = 0x1880` ⇒ 卡死) ⇒ 256 对齐修复
6. **新工具**: `h723_pe_probe.py` / `h723_io_isr_check.py` (A/B 验收读数, 含判据 pmax 不能 pmin)

## 明天从这里继续
- ⚠ **遗留项 1 (已向用户提出, 待决)**: `hil_tick` 在主循环仍调 `hil_out_apply` ⇒ 与 ISR 里的
  `hil_out_poll` **重复驱动 TIM3_CCR1**。目前两者读同一份 `WIRE[20]`、结果相同 ⇒ **无害**,
  但违反"一个输出面一个驱动者" ⇒ 交付档下 `hil_tick` 应整段让位 (只在对照档保留)。
- ⚠ **遗留项 2**: **向量表 256 对齐是实测经验值, 不是 spec** (ARMv7-M 只要求 128) ⇒
  换板/换型号/换批次**必须重验**。且 ITCM 为此多留 248 字节填充 (`0x1808 → 0x1900`)。
- **P3 待启动**: DO 硬件锁存链 (AXI 缓冲 → TIM1 UDE → DMA2 S0 → GPIOE_ODR)。
  前置条件已齐: `GPIO_MASK` 定案 + PE 可用位实测 + `h723-core0` 的配方已抄录。
- **P4 (可选)**: ADC 硬件触发 + DMA (零 CPU 采样); MDMA 输入链。
- **文档待办**: `docs/ARCH-H723.md` 应补一节 **"ITCM 向量表落位"** 的铁律
  (256 对齐 + 为什么 + 如何验证), 它是本轮最有复用价值的结论。

## 本轮的工作方式提醒 (给下一次的自己)
- ★★ **"某个东西一加就坏" ⇒ 第一件事做等长替代**, 别在对象内部查十几轮。
  (本轮: 在 `hil_out_poll` 内部查了十几轮全错; 等长替代 + 纯体积对照两轮到底。)
- ★★ **别把"排除清单"当进展** —— 框架错了, 排除得越干净, 离真相越远。
- ★ **退一步看整体是用户的提醒点破的** —— 三方差异表一行就能看出"位置/体积"是唯一没动过的变量。

---

# ★★ 2026-09-12 上午续: 查资料 + EXP-E (否定结果) + 双保险配置

## ① 查到的资料 (三条互证, 都指向 ECC)
1. **DS13313 (H723 datasheet)**: *"**8 ECC bits are added per 64-bit word for AXI-SRAM and ITCM-RAM**"*;
   *"This RAM is connected to ITCM **64-bit interface**"*。
2. **AN5342 (ST 官方应用笔记)**: *"使用 ECC 时**必须初始化代码访问的所有存储器**;
   由于存储值和冗余位的随机初始设置, 对**未初始化存储器**进行读取访问或**未对齐写入**,
   可能会触发 ECC 错误"*; *"RAM ECC **无法关闭**"*。
3. **ST 社区 (一模一样的场景)**: *"ITCM is 64b width... If an access is performed with a size
   **lower than the memory width** then the memory controller will perform a **RD/MODIFY/WR**.
   As the memory was **not initialized** the RD is generating an **ECC error**."*
   → 官方解法: *"initialize the memory (**WR with the memory width** to avoid RD/MODIFY/WR)"*。
4. **ARM Cortex-M7 TRM**: TCM 要支持 ECC 就必须配置 `CM7_ITCMCR`(0xE000EF90) 的
   **RMW (read-modify-write) 与 RETEN (retry)** 位。

## ② EXP-E: 验证"ECC 读-改-写"假设 —— **否定结果 (假设被排除)**
**做法**: linker 对齐**改回 128**(回到已知会坏的 `_vtor_itcm = 0x1880`) + 在 main() 里
**用 64 位写(`strd`)把整片 `.itcm_vectors` 先写实**, 再执行原来的 32 位逐项拷贝。
- **先验证了 GCC 真的生成 64 位写**: 反汇编里是 `strd r4, r5, [r6], #8` ✅ (不是两条 32 位 str)。
- **结果: 仍然卡死** ❌ ⇒ **根因不是 ECC 的 RD/MODIFY/WR**。
- **RAMECC 佐证**: 读 `RAMECC1` 的 ITCM 监控(0x52009040 起) 全部为 **0** ⇒ **芯片没记录任何 ECC 错误**。

## ③ 卡死现场 (在这个构建下重新确认, 事实全部对得上, 就是找不到因果)
- 表内容**正确**: `0x1880 = 0x20020000`(初始SP) / `0x1884 = 0x080057B9`(Reset)
  / **`0x1930`(索引44) = `0x00000001` = `TIM2_IRQHandler`(ITCM 0x0)** ✅
- ITCM 代码**正确**: `0x00000000 = 0x4b9a4a99`(ISR 入口指令) / `0x00001800 = 0xf000f85f`(veneer) ✅
- `VTOR = 0x1880` ✅ / `CFSR = HFSR = 0` / `PRIMASK = BASEPRI = 0` ✅
- `ICSR.VECTACTIVE = 44` (= IRQ 28 = TIM2), `IABR0 bit28 = 1` ✅
- **但 PC 落在 `0x0800582C` = `ADC1_2_IRQHandler`** —— 那是 **Default_Handler 的别名**
  (多个未实现的弱符号共址), 且 `Index 43/45` 也指向 `0x0800582D`。
⇒ **"表对、VTOR 对、代码对、无 ECC、无 fault, 但 CPU 跳 Default_Handler"**, 且**只在
表地址 = `0x1880` 时发生**。**机制仍未明**, 已收敛到"这是 ST 芯片的某个未文档化行为"。

## ④ 处置: **双保险配置** (两条都留, 已真机验证全 PASS)
1. **`.itcm_vectors` 对齐 256** —— 实测管用的那条 (`0x1800`/`0x1900` 正常, `0x1880` 卡)。
   ⚠ **ARMv7-M 只要求 VTOR 128 对齐** ⇒ 256 是**本板实测经验值, 换板/换型号/换批次必须重验**。
2. **main() 里用 64 位全宽写先初始化表区再拷表** —— 单独做它**不能**救活 128 对齐,
   但它是 ST 官方要求(AN5342)且**无代价**, 留着当第二层。
- 当前布局: `_eitcm = 0x1808`, `_vtor_itcm = 0x1900`, `isr_max = 1111 cyc`, 全判据 PASS。

## ⑤ ★★ 本轮新踩的坑: **只改 linker 不会触发重新链接**
改完 `ld/*.ld` 直接 `bash build.sh` ⇒ **产出的 ELF 没变** (map 里 `_vtor_itcm` 还是旧值,
`pyocd flash` 报 `programmed 0 bytes / identical N bytes`)。
⇒ 我一度误判成"改回 256 对齐后仍然卡死"。**`identical` 就是信号**。
**解法**: 改 linker 后必须 `bash build.sh clean` + 重新 build;
或者先 `touch` 一个 .c 文件制造编译依赖。
★ 判据: **改完 linker 一定回去 `grep -E "(_eitcm|_vtor_itcm) =" build/dcl_h723.map` 核对**,
不要看编译成功就信。

## ⑥ `build/tmp` Permission denied —— 今天第 3 次
`bash build.sh clean` 后**连续两次**失败, 连 `rm -rf build/tmp` 都没立刻救回。
**稳定解法**: `rm -rf build/tmp` **然后重跑** (有时要跑第二次)。下次直接照做, 别怀疑代码。

---

# ★★★★ 2026-09-12 中午: P3-A 完成 —— DO 输出面 (路径 A: CPU 直写) 真机验证通过

## ★ 链路与实现 (新增 src/do.c / do.h, CMakeLists + main.c 集成)
**扫描/seq 写 ACTUATOR[0..15] (float) → 拍内输出段 do_poll 打包 → GPIOE_ODR (BSRR 单次原子写)**。
- **BSRR 原子写** (低16=置1/高16=清0): 不存在读-改-写 ODR 的窗口, 无需临界区;
  非管辖位**不下发** (bits 与清零掩码都只含 mask 位) ⇒ 非管辖引脚任何路径都不碰。
- **就绪门** `s_do_ready`: 拍中断从阶段 8 就跑, do_init 在阶段 26 —— 无门则 init 前
  每拍配未使能时钟的 GPIOE (adc/hil 同一条纪律, 第三次落笔)。
- **GPIO_MASK 语义落地**: mask=0 ⇒ do_poll 直接返回 (整口不碰); bit i=1 ⇒ PEi 归引擎管。
- **安全态补欠账**: `do_outputs_safe` 登记进 `eng_outputs_safe` ⇒ 停机时 GPIOE 管辖位
  **第一次被真正清零** (定案② 之后它一直"只计数不清位", 至此收清)。
- **macro 冲突警示** (写进 do.h): macro 的 M 原语也能写 ODR ⇒ 不要让 macro 碰 PE,
  两个驱动者写同一个引脚 = 确定性破产。

## ★★ 真机硬验证 (STOP 态, 扫描不覆盖 ACTUATOR ⇒ 可注入)
`GPIO_MASK=0xFFFF` + ACTUATOR = [1.0, 0, 1.0, 1.0] ⇒ **GPIOE_ODR = 0x0000000D (0b1101)**
—— 逐位精确对应 ✅。ACTUATOR 保持不被覆盖 ✅ (RUN=0)。slot[44] @ 0xFCB0 完好 ✅。
- 语义闭环: STOP ⇒ eng_outputs_safe ⇒ ACTUATOR 清 0 + do_outputs_safe 清 PE ⇒ 双保险。
- **成本**: isr_max 1083 → 1475 cyc (do_poll = 16 次 float 读 + 打包, +392 cyc)。
- 上电 GPIO_MASK = 0 ⇒ do_poll 空转 (没登记就不碰整口), 符合定案②语义。

## ★ P1+P2+P3-A 至此全部完成: "输入输出链都在拍内"的完整形态
输入段 (di_poll/adc_poll) → 计算 (force/扫描/seq) → 输出段 (hil_out_poll/do_poll) → mb_tick。
**下一步 (P3-B 可选)**: DMA 锁存 (AXI 缓冲 + TIM 触发) 把 do_poll 的 CPU 写换成硬件沿;
**遗留**: 在线审计项 (需串口); RAMECC ISR 仍未实现 (IRQ≥48, 现 1024 对齐下安全)。

---

# ★★★★ 2026-09-12 下午: P3-B (MDMA 定时锁存) —— 用户设计, 链路已通, 剩收尾

## 用户方案 (四段时间表, 独立推导出 h723-core0 精髓并升级)
采样进 DTCM → CPU 计算 → 写 shadow → **定时器设定时刻 MDMA 锁存进 GPIOE**。
**最大架构收益 = 输出沿与计算时长解耦** (现状 do_poll 直写 ODR, 输出沿随扫描长度漂)。

## ★ 已确认的硬件事实 (全部真机读回)
1. **H723 的 MDMA 基址 = `0x52000000`** (h723-core0 `MDMA_BASE` 宏实测)。首版写
   0x58000000 (H743 地址) ⇒ H723 reserved ⇒ 寄存器读写全无效。**已修**。
2. **RCC_AHB3ENR = `0x580244D4`** (bit0=MDMAEN)。首版写 0x5802441C —— 那是 **D2CFGR**
   (读回 0x440 与 h723-core0 记录一致) ⇒ MDMA 时钟从未开 ⇒ 寄存器全 0。**已修**。
3. **DMAMUX1 请求 22 = TIM2_UP** (三源核对: docs.rs stm32h7xx_hal / anfulai / imt.ch)。
   DMAMUX1 C8 寄存器 = 0x40020820 (C8 = DMA2 S0)。
4. **MDMA 触发源是固定表, 没有 TIM 请求** ⇒ ST 官方解法: TIM2_UP 经 DMAMUX 触发
   **DMA2 哑传输**(1字) ⇒ DMA2 **TC 脉冲**恰是 MDMA 的合法请求 ⇒ 纯硬件链。
5. **旧项目 CTBR 注释**: "GPIO(AHB)→SBUS; DTCM→DBUS" ⇒ 源=DTCM 时用 DBUS 位。
6. **CTCR 字段布局** (从旧项目 MDMA_CTCR_4B_SW = SWRM|DINC组|MSIZE|PSIZE 推):
   PSIZE=[1:0] / MSIZE=[3:2] / SINC 组≈[17:16]+SINCOS=[15:14] / DINC 组≈[19:18] /
   **SWRM=bit30 (软请求模式)**; 旧项目 4B_SW = 0x40000000|(3<<18)|(2<<2)|2。

## ★ 链路已验证的部分
- DMA2 S0 哑传输 (TIM2_UP 触发, CIRC+NDTR=1): **CR 读回 0x5151, EN 保持, 循环运行** ✅
- MDMA ch0 (基址修正后): **CTCR=0x0A / CTBR=0x10008 / CSAR=0x2000F02C / CDAR=0x58021018**
  写入成功; **CISR=0x1E (传输完成类标志) / CESR=0 (无错) / CCR=0 + CBNDTR=0** ⇒
  **MDMA 完成过至少一次传输** ✅
- shadow (0x2000F028) CPU 写入正确 ✅ (mask=0xFFFF + ACTUATOR=[1,0,1,1] ⇒ shadow=0x0D)

## ⚠ 剩余收尾 (两个, 待继续)
1. **CTBR.LREQ 字段位置未定**: 请求号 8 写在 CTBR[7:0] 还是 [31:24]? (MDMA_REQUEST_SW
   = 0x40000000 暗示高段) —— **上板读回/实验校准**。当前写法 CTBR=0x10008 未触发 MDMA。
2. **地址指针漂移**: BUFFER 模式单发后 CSAR/CDAR 各 +4 (读回 CDAR=0x58021018 = **GPIOE_AFRL**!)
   ⇒ 单发指针不回卷, **正解 = MDMA 链表循环模式** (h723-core0 'LLOK' 验证过的配方:
   32B 节点 + CLAR 指回自身), 一次请求触发整链, 地址永不漂移。
3. **验证方法注意**: safe 清 0 会把 ACTUATOR 清掉 ⇒ 注入 ACTUATOR 后要**立即读** (窗口竞争)。

## ★ 途中还发现并修掉的
- `do_poll_n` **空判据** (计数在 mask==0 早退之后 ⇒ 上电恒 0) —— 已修 (就绪门后计数)
- `hil_out_poll` 无观测面 —— 补 `g_hil_out_n`
- **只改 linker 不触发重新链接** (`identical` 是信号) —— 已记
- **build.sh 自动重试** 已加 (今天 Permission denied 3 次)

---

# ★★★★ 2026-09-12 中午: 真因确认 (查资料查出来的) + 三层修复, 问题关门

## ★★★★ 真因: **VTOR 对齐不足, 硬件取向量时拿对齐值做掩码**

**ARM 官方规则** (developer.arm.com 102283, 一字不差):
> *"the offset, in bytes, must be aligned to: a power of 2. a minimum of 128 bytes.
>   **a minimum of 4×N, where N is the number of exceptions supported.** The minimal
>   alignment is 128 bytes, which allows for 32 exceptions... **To use more interrupts,
>   you must adjust the alignment by rounding up to the next power of two.**"*

**H723 的账**: 150 IRQ + 16 系统异常 = **166 项** → 4×166 = 664 字节 → 向上取 2 的幂 =
**1024 字节** ⇒ H723 的向量表必须 **1024 对齐**。我们一直用 128 —— 只够 32 项, 差 5 倍。

**mask 机制** (这解释了此前所有"矛盾"):
- `VTOR = 0x1880` (128 对齐) ⇒ 硬件以为表只有 32 项 ⇒ 取索引 44 时 **44 mod 32 = 12**
  ⇒ 读的是 **slot[12] (保留项 → Default_Handler)** ⇒ 卡死。**CPU 从头到尾没读过 slot[44]**
  —— 所以表内容、VTOR、ITCM 代码、RAMECC、fault 全对却照样死。
- `VTOR = 0x1800` (1024 对齐) ⇒ 不掩码 ⇒ 正确。
- `VTOR = 0x1900` (256 对齐) ⇒ 索引 44 < 64 没被掩 ⇒ **"正常"纯属运气**;
  任何 **IRQ ≥ 48 (索引 ≥ 64)** 在 256 对齐下都会被掩到错位 —— 256 对齐是**埋着的雷**。
- EXP-E (64 位全宽初始化 + 0x1880 仍卡) 也顺理成章: 不管表怎么写, 取向量都被掩到 slot[12]。
- ECC / RAMECC / veneer / 跨区调用 / long_call / 函数位置 —— **全部与此无关**, 前面十几轮
  排查都是在错误的框架里打转。

## ★★ 三层修复 (全部真机验证通过, [PIN] 全判据 PASS)
1. **对齐 → 1024**: `ld/STM32H723ZG_FLASH.ld` 里 `.itcm_vectors` 重写 (根因修复)。
2. **表钉在 ITCM 末尾**: `_vtor_itcm = ORIGIN(ITCM)+LENGTH(ITCM)-0x400` = **0xFC00**,
   与代码大小**彻底解耦** (代码从 0x0 向上长, 表位置永不变 ⇒ 不再"每次改代码都买落位彩票")。
   配套 **linker ASSERT**: `ASSERT(_eitcm <= _vtor_itcm, "ITCM code overflow...")`
   —— 代码涨进表区在**链接期**报错, 不再静默上板。
3. **拍活体自检** (main.c, `tick_timer_init()` 之后): 等 3 个 tick, 不动 ⇒ `blink_error(101)`。
   防的是**一切取向量类失败** (表位置/表内容/掩码/换板) —— 这条要在 10 分钟报警,
   而不是像这次花一个通宵猜。
- **验收实读**: `VTOR = 0xFC00`, `0xFCB0 (slot[44]) = 0x00000001 (TIM2_IRQHandler)`,
  `0xFC00 = 0x20020000 (初始 SP)` ✅; `isr_max = 1099 cyc`, 全判据 PASS。
- ITCM 报告显示 100% 是**地址空间覆盖** (表钉在高端), 实际代码 6.2KB, 余量 58KB,
  且有 ASSERT 守门 —— 不是真溢出。

## ★★ 给下次的自己
- **"向量表对齐 ≥ 4×异常数、取 2 的幂"是 ARM 架构规则, 不是经验值** —— 之前
  "256 是实测经验值, 换板要重验"的说法**作废**, 正确说法: **按芯片的 IRQ 数算对齐**。
- **中断号越高的外设 (FDCAN/以太网/SDMMC) 越容易踩掩码雷** —— 以后启用任何
  IRQ ≥ 48 的外设前, 先确认表对齐 ≥ 1024 (现在是 0xFC00, 恒安全)。
- **启动期自检 (等 N 个 tick) 是查"取向量失败"的最便宜判据**, 任何带 ITCM 向量表
  副本的工程都值得加一条。
