# 2026-09-17 —— 闭环步进 阶段 2：**失速边界与斜坡收益（已证）**

> 接 `2026-09-16.md` 末尾"下一步 = 阶段 2"。那篇记的阶段 0/1 与总根因已完成，本篇只记阶段 2。

## 一、阶段 2 的结论（一句话）

**电机在 1~60 kHz 全频段跟随脉冲的"线性度" ≤0.5%（无随速度增长的丢步）；
但从静止"突加"到 >16500 Hz 必失速** ⇒ **"失速边界"与"斜坡收益"从 ⛔未证 变 ✅已证**（PLAN §4 表已改）。

★ **不许说成"绝对跟随精度 ≤0.5%"** —— 存在一个 **−1.6% 的固定比例因子，归属不可分**：
静态标定 counts/step = 2.6017，而 1/8 微步名义值是 2.56 ⇒ 用 2.6017 算"电机慢 1.6%"、
用 2.56 算"恰好 1.000" ⇒ **分不开"AS5600 角度刻度误差"与"驱动器微步刻度误差"**。

## 二、先纠正度量：两个独立错误叠加，导致第一版结论全错

| # | 错 | 真值（证据） |
|---|---|---|
| 1 | 把 `slot[1]` 当 seq、`slot[2]` 当 tick | `blackbox.h:29-33` = `[0]magic [1]tick [2]seq [3]ctrl` ⇒ **读反了** |
| 2 | `span = 959 × 100 µs` 当分母 | 环是**"变化才记"**（`blackbox.c:345`：内容没变则不占槽）⇒ 跨度**必须由 tick 算** |

后果：比值全档虚高 **3.4~4.0 倍**，报出"31000 Hz ⇒ 4651 rpm"（步进电机不可能）。
★ **荒谬值 = 线索**：这一步应该先怀疑分母，我却先去解释"为什么这么快"。
★ 真跨度实测 **380.6 ms**（Δtick 直方图 2×290 / 4×488 / 6×89 / 8×90 / 10×2，均值 3.969），
  而 `blackbox.h:71` 与 3 个工具仍写 "960 槽 = 96 ms"（那只是**逐拍全量**的算法）⇒ **文档/工具待修**。

## 三、阶段 2 的测量（黑匣子 + 拍内反馈 ~1 kHz + 运动中途 halt）

`build/_stall_v2.py`（主扫 14 档 × 突加/斜坡）、`build/_stall_edge.py`（拐点细扫 + 决定性对照）：

| 组 | 结果 |
|---|---|
| **突加（从静止）** | 15000~16500 Hz ⇒ 比值 1.007~1.012 ✓；**16750 起 = 0.000** ⇒ 拐点 **16500~16750 Hz = 619~628 rpm** |
| **★ 决定性对照** | 先在 8000 转起来再**突跳** 18000/24000/31000 ⇒ **1.010/1.016/1.011** ✓ ⇒ **排除"固件拒发高频"** |
| **失速自愈？** | 等 0.4 s / 2.0 s 都是 0.000 ⇒ **不自愈、持续通电流**（= 客户"响+不转+发热"三现象同一物理态） |
| **斜坡上限（pull-out）** | 31000~50000 ⇒ 0.998~1.007 ✓；**80000 起 = 0.000** |

★ 三条**互不依赖**的自洽（可信度来源）：
① 28 点比值全落 0.995~1.018；② `|deg − raw×360/4096| ≤ 0.001°`；③ **失速行记录率自降至 173~571/s、跨度自我拉长到 452~473 ms**（"变化才记"自己证明轴没动）。

## 四、闭环标定：把结论的两条地基单独量出来（`build/_spr_calib.py`）

全部**零时间假设**（`走 N 步` → `TIM4_CNT` 硬件脉冲 + AS5600 **绝对**角）：

| 判据 | 结果 |
|---|---|
| C2 步/圈 | `counts/step` 中位数 **2.6017 ⇒ 1574.4**（名义 1600，−1.6%），6 行峰峰 **0.91%**，正反对称 |
| C3 脉冲率 | 8000/24000/60000 ⇒ −1.36% / +0.05% / +1.01% |
| **C4 拍长** | **99.95 µs（−0.05%）** ⇒ 100 µs 被**独立反解确认** |
| **C5 频率量化** | `rate_actual = 1e6/floor(1e6/f)`，**7/7 精确命中** |

**★ 新规则（已进 MEMORY §5.16）：脉冲周期被量化到 1 µs。**
反推 ARR = 200000/25000/8200/5000/3200/2400/2000 —— **全是 200（=1 µs@200 MHz）的整数倍**。
⇒ 24000⇒24390(+1.62%)、60000⇒62500(+4.17%)、80000⇒83333(+4.17%)。**周期整除的档精确**。
⇒ **用频率做定量结论必须引 `rate_actual`，不要用请求值** —— 否则会把量化误差当成"电机没跟上"。
★ 这条模型把阶段 2 表里**所有非 1.000 的部分逐行解释掉**（24000⇒+1.6%、28000⇒+2.0%、60000⇒+4.2%
  对实测 1.015/1.018/1.040；40000/50000/20000 周期整除 ⇒ 0.998~1.000）。

## 五、过程中修掉的三个真缺陷（都是我自己的协议/工具）

1. **`走 N 步` 的 N=65535 撞 TIM4 的 ARR(0xFFFF)** ⇒ CNT 回绕、到点判据**周期性漏判** ⇒ 多跑 ~11 圈
   （血证：`65535 步 @60000 Hz` 表观只有 **5193 Hz** = 60000/11.5）⇒ **N 必须 ≤ 60000**。
2. **解卷绕的前提被我自己违反**：脚本写"每程 <180°"却用 N=1400（=315°）⇒ 走错分支，得
   `0.346 counts/step` ⇒ **那是垃圾行不是数据**。⇒ **N < 800 才有效**。
3. **`span` 由槽数算**（见 §二）⇒ 已改成由 tick 算。

## 六、待办

- **文档口径**：`blackbox.h:71`、`_bb_measure*.py`、`_bb_dump.py` 的 "960 槽 = 96 ms" 要改成
  "跨度 = tick 差/10 kHz，实测 ~380 ms（随变化率变）"；`src/main.c:2621` 的"突加 31000 只能用到
  627 rpm"要改成"**突加 >16500 Hz 即失速（0 rpm）；斜坡可达 ≥50000 Hz**"。
- **`走 N 步` 的 ARR 刀锋**：建议在 `step_set_remaining` 里把上限从 0xFFFF 收到 ≤60000，或把到点判据
  改成回绕安全式（`(CNT - goal) & 0xFFFF < 32768`）。
- **工具硬编码端口**：`docs/audit/h723_limit_tick_probe.py`（COM14）等 3 处（昨天已登记，仍未修）。
- **阶段 3（只有 0–2 全过才做）**：多轴实例化、轨迹规划（三角波/正弦）。
- **LA 外部证据**：脉冲频率目前已由 `rate_actual` + `TIM4_CNT` + 编码器三路互证；若要**外部独立**
  证据，可在 PA6 上抓逻辑分析仪（需接线）。

---

# 2026-09-17（下半场）—— ① PE9 失配定案 + ② 端口硬编码 + ③ 失步自动检测（程序面）

## ① `g_step_ena_mismatch_n` 涨到 327616 后冻结 —— **定案：不是野写者**

**真因**：上位机把 `GPIO_MASK` 换掉了。两处 deliberate 注入：
- `tools/h723_do_hold_test.py:97` ⇒ `0x00FF`（只登记 PE0..PE7）
- `tools/h723_w1.py:386` ⇒ `0x7FC00000`（`&0xFFFF = 0`）
⇒ `do_poll` 的管辖范围不含 bit9 ⇒ **PE9 从此没人驱动**，冻结在旧电平 ⇒ 与
`g_step_ena_pin_intent` 永久不一致 ⇒ 主循环每圈 +1。**327616 / 1.2s ≈ 1747 量级一致**（对照档实测 +1747）。

★ 教训：那个数只回答了"不对了多少次"，**没回答"谁把它弄成这样的"** ⇒ 被误读成"有别的代码在驱 PE9"。
⇒ **一个计数只能回答一个问题**（§4.6 的又一次代价）。

### 固件修法（`DCL_DO_MASK_UNION`，默认 1）
- `do.c`：生效掩码 = `host | DO_STEP_RESERVED_MASK(0x0F00)`，**`do_poll` / `do_outputs_safe` / 诊断
  三处共用同一个 `do_mask_effective()`**（一个绑定不能各算一遍）。
- ★ **不偷改上位机字段**：SHM 里的 `GPIO_MASK` 原样保留，只在**使用点**做并集。
- ★ 沿计数 `g_do_mask_step_drop_n`（不是每拍 +1 —— 那会变成不可解释的大数，正是本轮的病因）。
- 新观测面 **`op=19 sub=24`（32 B）**：`mismatch_n ≡ hi_n + lo_n`（**恒等式本身是判据**）+
  `step_drop_n` + `last_mask/idr/tick` + `host_mask`。★ `sub=11` 已满 32 B ⇒ **只能追加**，故开新子命令。
- ITCM 仍 **64/64 KB 100%** ⇒ 记账**内联进 `do_poll`**（不新增 ITCM 函数，否则撑爆）。

### A/B（`tools/h723_do_mask_owner_test.py`，两档都跑）
| 档 | 注入 0x00FF 后**翻意图** | `mismatch_n` | PE9 | 归因 |
|---|---|---|---|---|
| 对照 `UNION=0` | intent=0 | **+1747** | 冻在 1 | `hi_n` |
| 交付 `UNION=1` | intent=0 | **+0** | 跟随 → 0 | `step_drop_n` +1 |

★ **注入后必须"改变意图"**才逼得出来：注入本身不会失配（PE9 本来就是旧意图）⇒ 第一版漏这步**对照档会假绿**。

## ② 三处硬编码串口 ⇒ 一律"按能力字认"
`docs/audit/h723_motion_probe.py` / `docs/audit/h723_stepper_motion.py`（原 `"COM21" if ... else "COM14"`）
· `tools/h723_as5600_bind.py`（原 `port or "COM21"`）⇒ 改成 `显式参数 → DCL_PORT → find_board()`。
★ 解析**放在 `__init__` 里**（不放 import 期）：`limit_tick_probe` 会 import 它，import 期探测会在无板时抛。
★ 我自己在插入注释块时**漏了 `"""` 收尾**（与 `blackbox.h` 漏 `*/` 同一类，一天两次）⇒ 靠 `py_compile` 抓住。

## ③ 失步自动检测 + 恢复正常转动（`examples/h723_step_stall_recover.dcl`，**编译通过**）
判据 = `dem>0` **且** `|每拍角度增量| < 0.25×期望`（期望 = `dem×2.56e-4` counts/拍），`TON` 去抖 200 ms；
恢复 = `TP` 断流 150 ms（让转子落回同步）+ **降额重发**（`CTU` 锁存、主机重下令才复位 —— 不做极限环）。
起转后 900 ms 遮蔽窗（固件斜坡期间不判）。**实测被测对象现成**：阶段 2 的 10 / 1 突加失速拐点。

### 写程序时暴露的三条 DCL 口径 + 一个**架构级静默失效**
- `THR=` 只认**数字字面量**（CONST/信号都不行）⇒ 拿算出来的量当门限要先 `SUB` 再比 0
- `BY=` / `FROM` 只认**已声明的 CONST 名或信号**（内联字面量报"未定义信号"）
- `CONST` 字面量**不认科学计数法**（`1.0e-4` 报语法错误）
- ★★★ **`dclc` 自动分配会撞上固件保留槽**：`SEL der = wire[65]` —— 而 `WIRE[65]` 是**固件每拍写的
  运动镜像** ⇒ 值每拍被覆写（"程序在写槽、硬件一动不动"老族）。
  ⇒ 修：`dclc.py` ① 自动分配**跳过** `FW_RESERVED_WIRES` ② 显式 `OUTPUT TO wire[n]` 落到保留槽**报错**
  ③ **保留集从 `src/step.h` 自动读**（`STEP_MOT_SLOT_*_AP`）⇒ 两边不可能漂移。
  ★ 而"一律禁用 ≥64"是**过度保守**：实测固件只占 64/65，而引擎里 **`SEL` 吃 4 个 wire 槽、`CONST` 占 1 个**
  ⇒ 60 行的程序在 `wire[0..63]` 里就溢出。**约束应是"别碰保留集"，不是"不能用高区"。**
- ★ 我新写的"step.h 与内置表一致"检查**第一版就误报**（比了整个字典，描述串不同也报"不一致"）
  ⇒ 改成只比键集合（误报会被使用者关掉，比没有更坏）。

## 下一步（**未做**，明确挂账）
- ③ 的**实机验证**：部署程序 + `h723_as5600_bind` + `sub=13 arg=1`，注入 18000 Hz 突加失速，
  断言 `stall` 触发、`nfail≥1`、`der` 降额、**编码器恢复转动**。收尾必须 `sub=13 arg=0`。
- **阶段三**：多轴实例化 + 轨迹规划（三角波/正弦）。

---

# 2026-09-17（第三段）—— ③ 失步自动检测 **上板验证通过**

`tools/h723_stall_detect_verify.py` → **全部通过**（板子 COM22，交付档固件）。

| 判据 | 实测 |
|---|---|
| R 反向：4000 Hz 下**不该**报失速 | `stall=0` ✓；确实在转 5236 counts/0.5s（理论 5120，误差 2%）|
| T1 检测 | 突加 18000 Hz（**从静止起转**）⇒ `stall` **1.17 s** 触发（= 遮蔽窗 900 ms + 去抖 200 ms，与设计吻合）|
| T2/T3/T4 | `nfail=1` · `der=0.25` · `hz=4500`（精确 = 0.25×18000）|
| **T5 恢复正常转动** | 末窗 1 s **11432 / 理论 11520 = 99.2%** ⇒ **轴被救回来了** |

## 上板路上踩到的四件（都写进 MEMORY §5.20~5.22 了）

1. **`SRC_HMI` 在 H723 上没实现**（`engine.c` 留位 + deploy 校验拒）⇒ 改裸 `wire[10]/[11]`。
   ★ **`dclc --dump` 只过编译期**；②/①层校验在**上传期** ⇒ "编译通过" ≠ "能部署"。
2. **第一版判据永远不触发**：编码器每 10 拍回填一次，而判据每拍跑 ⇒ `dab` 呈 1:10 占空比
   （实测 `33 / 0 / 48 / 0`）⇒ `TON PT=200ms` 每 10 拍被尖峰复位 ⇒ 凑不满。
3. **改用 `PERIOD=1ms` 降档 —— 被固件正确地拒了两次**：`main.c:1838` 跨档速率检查
   `慢消费者读快生产者 ⇒ NAK "rate mismatch"`，且"下游必须同档或更慢"**一路传染**，
   连 `OUTPUT` 也躲不掉（**`OUTPUT` 不接受 `PERIOD=`**）⇒ 此路不通。
4. **正解 = `LPF` τ=20ms**（div0，不触发跨档检查）⇒ 输出≈平均速率 ⇒ 判据立即可用。
   ★ 一般化：**给"慢采样"的信号做"每拍"判据前，先问它多久更新一次** ——
   否则做出的是**永远不触发**的判据（比误报更坏：看起来在工作）。

## 沿途的工具修复
- `dclc.py`：① 主机输入槽（裸 `wire[n]`）登记进 `in_wires` 并**预扫**（否则被内部信号抢槽 ⇒
  上位机写的值被静默丢掉）；② 保留集从 `step.h` 自动读。
- 新增 `tools/h723_stall_detect_verify.py`（8 条判据 + 反向判据 + 自动收尾）。

## 挂账
- `dab` **绝对刻度偏低 ~2.3 倍**（4000 Hz 实测 4431.9 vs 理论 10240 = 43%）——运动本身是对的
  （编码器同期 10472 counts/s），是 `dab` 口径没对齐。判据仍成立因门限留 4× 余量。
  ⇒ 要收紧就按实测标定 `K_EXP`（本项目纪律：凡刻度一律实测再写死）。
- 板子已收尾（`0x13 RESET` + `0x11 START`，运动源回脚手架、电机失能）。
  `n_routes=0` 属正常：运行时 RESET 后 bench profile 不自己回来，下次回归 step 0 会重建。

---

# 2026-09-17（第四段）—— 阶段三：**部分完成**（如实收口）

## A. 轨迹规划 —— 程序已落地并在实机运行；**形状验收未完成**

交付：`examples/h723_step_traj_tri.dcl`（三角波速度曲线，18 路由）、
      `examples/h723_step_traj_sine.dcl`（阶梯正弦 6 段，40 路由）
      `tools/h723_traj_verify.py`（正对照 + R 反向 + 形状/周期/峰值判据 + 自动收尾）

架构分工（这是本阶段的"亮点"）：**程序只管"何时换目标"（`SEQ` 周期翻 A/0），
固件斜坡本身就是积分器**（`sub=17`），把方波目标积成三角波 ⇒
**程序里不需要累加器**（DCL 也没有通用积分原语），而斜坡走硬件时基 ⇒ 曲线不随主循环抖动。

| 判据 | 结果 |
|---|---|
| 正对照（脚手架 1200 Hz，证明"测量链看得见运动"）| ✅ **1.01 / 1.02** |
| R 反向（A=0 ⇒ 不动）| ✅ 0 / 11 counts/s |
| 无失配（`sub=24`）| ✅ 0 |
| **T2 形状** | ❌ **判无效**（55 Hz 采样 vs 600 ms 段 ≈ 11 点/段，段边界弥散 ±9%）|
| **T1 峰值** | ⚠️ **未解释的 +40%**（1.386 / 1.436，而正对照 1.01 ⇒ 测速器可信）|

### 踩到的两点（都进记忆）
1. **`SEQ` 步号是 1 起**（固件 `engine.c:474`: `out_wire = step_cur + 1`）⇒ 我按 0 起写
   `THR=0.5` ⇒ `GE` 恒真 ⇒ 目标恒 0 ⇒ **轴完全不动**（`wire[30]` 在 1/2 翻，而 `[57]/[58]/[12]` 恒 0）。
2. ★★★ **"期望为 0/小"的判据必然是假绿**：第一版没有正对照，轴不动时 R 判据 `0<阈值` 照样通过。
   加正对照后**当场抓出测速器偏高 3.2 倍**（分子跨 3 采样、分母 1 个 Δt ⇒ 口径不一致）⇒ 修完 1.01。
   ⇒ 处方：**差分量的分子分母必须同口径**；**"期望为 0"的判据必须配"期望为大"的正对照**。

## B. 多轴 —— ❌ 未做（就绪度已评估，**三个真阻塞，当前无法验收**）

固件侧资源**够**（逐项查证）：TIM1/TIM8/TIM12~17 全空闲（只用了 TIM2/3/4/5）；
**PE10/PE11 是 `STEP_DO_MASK` 预留位**；TIM1/TIM8 **有 RCR** ⇒ 走 N 步可硬件精确。

三个阻塞：
1. **第二个编码器** —— AS5600 地址**固定 0x36** ⇒ 同总线必冲突 ⇒ 需 I2C mux(TCA9548A)/第二总线 ⇒ **要硬件**
2. **第二个驱动器/电机** —— **台架没有**
3. **ITCM 64/64 KB 100% 满** —— 新增 ISR 可达代码前必须先腾

⇒ 按纪律**不写半成品、不宣称完成**（"覆盖不到判 SKIP"）。★ **"能做"与"能验收"是两件事。**

## 待补（工具链缺口，登记）
- `LUT` 原语**引擎有、`dclc` 未暴露** ⇒ ③层写不出查表 ⇒ 现在只能阶梯表逼近。
  补它比"在程序里凑"划算（正弦/任意曲线的正解）。
- `OUTPUT` 不接受 `PERIOD=`；`CONST` 不认科学计数法；`THR=` 只认字面量（§5.18/5.21）。

## 板子状态
已收尾（每个验收工具的 `finally` 都做了）：请求清 0 / 关斜坡 / 运动源回脚手架 / 失能。

---

# 2026-09-17（第五段）—— 按"我的 5 项"执行：定案 + 工具收口 + 记忆拆分

## ① +40% 峰值 → **定案：不在程序、不在固件**（新工具 `tools/h723_traj_peak_diag.py`）
一次运行同时录四个量，三分法：

| 量 | 峰值 |
|---|---|
| 程序下发 `wire[58]` | **1200** |
| 固件已应用 `wire[64]` | **1200** |
| `rate_cmd / rate_out / rate_actual` | **1200 / 1200 / 1200** |
| 编码器（PC 侧差分）| 2373 → P95 1690 → **累积量 444.5** |

⇒ **程序对、固件权威对**；**PC 侧差分测速在本装置上失效**。

### 新增规则 §5.27 ★★★
编码器回填率与 PC 轮询率**拍频**：实测 `Δt` 稳定 **37 ms**、而 `Δraw` **中位 2**（1200 Hz 应有 123）、
偶尔 230 ⇒ **极值/分位数全不可信**（第一版用 `max()` ⇒ max/P50 = **119**）；
**只有 `ΣΔraw / 总时长` 免疫**（444.5 Hz vs 三角波理论均值 600 = 74%，这一项待用黑匣子复核）。
⇒ **形状类判据一律走片内黑匣子（1 kHz）** —— 这也是 stage 2 能成的原因。

## ② 跨档速率检查搬到编译期（`tools/dclc.py` + `tools/dclc_rate_selftest.py`）
镜像固件 `main.c:1838` 的规则（**权威仍是固件**），覆盖面对齐（只看主输入）。
自检 **3/3**：违规被拒 ✓ / 合法编过 ✓ / 20 个既有 examples 无误伤 ✓。
⇒ 下次不必再花两次烧写往返去发现它。

## ③ MEMORY 拆分：**46 KB → 22.2 KB（−52%）**
- `RULES-DETAIL.md`（47 KB）= **旧全文逐字归档**（编号不变 ⇒ 别处的 `§5.16` 之类引用仍成立）
- `MEMORY.md` = **§〇 最贵的十八条 + 核心分节 + §5.1–5.27 / §10.x / §11.x 一行索引**
- ★ **校验过位置**：前 **15 KB** 覆盖到 §五全部规则索引 ⇒ 即使再被静默截断，关键规则**都在预算内**

## ④ 多轴启动清单：`docs/PLAN-multi-axis-v1.md`（**不写半成品**）
资源预算已逐项查证（TIM1/8/12~17 空闲、**PE10/PE11 是预留位**、TIM1/TIM8 **有 RCR**）；
**三个阻塞** + 各自解法（B1 建议 TCA9548A；B2 采购；**B3 先做**因为不依赖采购）+ **启动顺序 checklist**
+ 验收需要什么 + 明确不做清单。

## ⑤ 架构判断收口：`docs/PLAN-closedloop-stepper-v2.md` §4/§4.1
- "闭环带宽"行 → **就此收住**（新上限是**编码器回填率**，不是算力；要推得换反馈器件）
- 新增"**失步自动检测 = 最强对外主证据**"行
- 一句话评估改为"**安全 + 可规划**级"；新增 §4.1 四条收口判断

## 本轮的元观察（值得记住）
**7 个错里没有一个是工程判断错，全在"工具与测量层"**（字段映射、Δt 口径、注入条件、
以及 4 次锚点替换误伤注释/标题、1 次引号嵌套）。⇒ 判断：**下一步该投的是"让工具自己会出错"**，
不是加功能。这条已写进 §4.1④。
