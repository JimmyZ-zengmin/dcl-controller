# 2026-09-18 —— 门面收口（承接 09-17 深夜）

> 本篇只记唯一一件：**把两份"对外门面"文档重写并修掉矛盾**。09-17 的技术工作见 `2026-09-17.md`。

## 起因
用户："dcl-controller 的 readme 格式有点乱、外人看不懂、含金量没重点、要展示今天的成果"。

## 诊断（三处格式 + 一处更要命的）
1. 格式：对标表单元格塞整段论述且**同一句重复两遍**；能力边界 A 类行**上千字一格**；
   "抖动三个口径"内容出现两处。
2. 外人看不懂：术语直出（`op=19 sub=22` / `wire[]`），且**没有可运行的最小例子**（原片段语法不是真的）。
3. 含金量没重点：README 最后改于 **09-17 19:24** ⇒ **今晚全部成果一条都没有**。
4. ★★★ **`docs/STEPPER-DEMO.md` 写着"本项目没有闭环步进的 DCL 程序、而且写不出来"**，
   而 README 列着"有界闭环定位**已跑通**" ⇒ **两份门面互相矛盾**，且把外人引向说"没有"的页面。

## 做了什么
- `README.md` 重构：新增 **§一「本版最硬的实证（每条带复跑命令）」**（99.95 µs / σ3.6 ns /
  **失速拐点 16500~16750 Hz** / **失步自检 1.17 s·恢复 99.2%** / **1 µs 量化 7/7** / **线性度 ≤0.5%** /
  轨迹规划 / 走 N 步硬件计数 / fail-closed / 绑定即接器件）；把**已撤回数字单列 ⚠️ 区块**写清来历与"请勿引用"；
  修"轨迹规划 ❌ 没有"与"突加 627 rpm / 被砍 47%"两处过期说法；长单元格拆成表+小节。
- `docs/STEPPER-DEMO.md` 重写为事实描述：§0 改"三条路 + 例程清单"；§4 从"为什么写不出来"改成
  "**现在能写什么、还缺什么**"（真缺口三件：**运动面未占能力位** / **斜坡是全局设置、③层够不到** /
  **`LUT` 未暴露**）；§5 更新 `mismatch_n` **已定案**（掩码剔位，非野写者）+ 新增"PC 侧差分测速不可用"
  与 ③ 层三个口径坑。
- 验证：闸门 RC=0（**F 类 469 条 0 FAIL**、D 类 49 引用 0 消失）；**两文档本地链接逐条核对，坏链 0**。

## ★★ 顺带发现一条**工具盲区**（登记，未实现）
**F 类只查"同一行内"的矛盾，查不到"跨文档"的矛盾。** 本次 README ⇄ STEPPER-DEMO 的矛盾
正是这一类（一份"已跑通"、另一份"写不出来"）——**机器没抓到，是人看出来的**。
⇒ 想加"跨文档否定式断言"检查（"某文档说没有 X，而 examples/ 里存在 X 的例程"），
但**启发式易误报，而误报会被自己人关掉** ⇒ 先登记，等有更硬判据再做。

## ★ 更正留案（不改写历史）
提交 `6f918d5` 的 **commit message 里两处体积数字不准**：
- 写 `README.md` 22.6 → **19.4** KB，实为 **21.6** KB（21550 字节）
- 写 `STEPPER-DEMO.md` 9.0 → **10.5** KB，实为 **14.0** KB（13966 字节）

原因：我写了个小脚本改这两处数字，但**锚点写错**（那两行在消息文件里是 `###` 标题，
我按 `- ` 列表项去匹配）⇒ 脚本的 `assert count==1` **把修补拦下了**（脚本拦得对），
而提交照旧用了未修补的消息。
⇒ **处置：不 amend / 不 force-push（已推送，重写共享历史的风险远大于一个体积数字）**，
按本项目惯例**更正留案**——归档与引用以本行为准。

★ 这条本身也是个教训：**"脚本改文件"必须有断言，而且断言会（也应该）在锚点错时把改动拦下**；
  但**别把"脚本成功"当成"文件已改"** —— 要看脚本的退出/输出，必要时回读一次。
