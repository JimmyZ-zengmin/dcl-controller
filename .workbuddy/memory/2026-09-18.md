# 2026-09-18 —— 门面收口（承接 09-17 深夜）

> 本篇只记唯一一件：**把两份"对外门面"文档重写并修掉矛盾**。09-17 的技术工作见 `2026-09-17.md`。

## 起因
用户："dcl-controller 的 readme 格式有点乱、外人看不懂、含金量没重点、要展示今天的成果"。

## 诊断（三处格式 + 一处更要命的）
1. 格式：对标表单元格塞整段论述且**同一句重复两遍**；能力边界 A 类行**上千字一格**；
   "抖动三个口径"内容出现两处。
2. 外人看不懂：术语直出（`op=19 sub=22` / `wire[]`），且**没有可运行的最小例子**（原片段语法不是真的）。
3. 含金量没重点：README 最后改于 **09-17 19:24** ⇒ **今晚全部成果一条都没有**。
4. ★★★ **`docs/STEPPER-DEMO.md` 写着"本项目没有闭环步进的 DCL 程序、而且写不出来"**，
   而 README 列着"有界闭环定位**已跑通**" ⇒ **两份门面互相矛盾**，且把外人引向说"没有"的页面。

## 做了什么
- `README.md` 重构：新增 **§一「本版最硬的实证（每条带复跑命令）」**（99.95 µs / σ3.6 ns /
  **失速拐点 16500~16750 Hz** / **失步自检 1.17 s·恢复 99.2%** / **1 µs 量化 7/7** / **线性度 ≤0.5%** /
  轨迹规划 / 走 N 步硬件计数 / fail-closed / 绑定即接器件）；把**已撤回数字单列 ⚠️ 区块**写清来历与"请勿引用"；
  修"轨迹规划 ❌ 没有"与"突加 627 rpm / 被砍 47%"两处过期说法；长单元格拆成表+小节。
- `docs/STEPPER-DEMO.md` 重写为事实描述：§0 改"三条路 + 例程清单"；§4 从"为什么写不出来"改成
  "**现在能写什么、还缺什么**"（真缺口三件：**运动面未占能力位** / **斜坡是全局设置、③层够不到** /
  **`LUT` 未暴露**）；§5 更新 `mismatch_n` **已定案**（掩码剔位，非野写者）+ 新增"PC 侧差分测速不可用"
  与 ③ 层三个口径坑。
- 验证：闸门 RC=0（**F 类 469 条 0 FAIL**、D 类 49 引用 0 消失）；**两文档本地链接逐条核对，坏链 0**。

## ★★ 顺带发现一条**工具盲区**（登记，未实现）
**F 类只查"同一行内"的矛盾，查不到"跨文档"的矛盾。** 本次 README ⇄ STEPPER-DEMO 的矛盾
正是这一类（一份"已跑通"、另一份"写不出来"）——**机器没抓到，是人看出来的**。
⇒ 想加"跨文档否定式断言"检查（"某文档说没有 X，而 examples/ 里存在 X 的例程"），
但**启发式易误报，而误报会被自己人关掉** ⇒ 先登记，等有更硬判据再做。

## ★ 更正留案（不改写历史）
提交 `6f918d5` 的 **commit message 里两处体积数字不准**：
- 写 `README.md` 22.6 → **19.4** KB，实为 **21.6** KB（21550 字节）
- 写 `STEPPER-DEMO.md` 9.0 → **10.5** KB，实为 **14.0** KB（13966 字节）

原因：我写了个小脚本改这两处数字，但**锚点写错**（那两行在消息文件里是 `###` 标题，
我按 `- ` 列表项去匹配）⇒ 脚本的 `assert count==1` **把修补拦下了**（脚本拦得对），
而提交照旧用了未修补的消息。
⇒ **处置：不 amend / 不 force-push（已推送，重写共享历史的风险远大于一个体积数字）**，
按本项目惯例**更正留案**——归档与引用以本行为准。

★ 这条本身也是个教训：**"脚本改文件"必须有断言，而且断言会（也应该）在锚点错时把改动拦下**；
  但**别把"脚本成功"当成"文件已改"** —— 要看脚本的退出/输出，必要时回读一次。

---

# 2026-09-18（续）—— 阶段 3A 形状验收：**黑匣子路径打通，但判 SKIP（覆盖不足）**

## 做了什么（代码，不是文档）
1. **纠正上一轮的说法**：我说"补 `LUT` 是纯工具链、代价最小" —— **只对一半**。查证后：
   `OFF_LUT_DATA=0x0440`（256×f32，在 SHM）· 运行时 `lu` 就是它（`engine.c:275/642`）·
   协议**允许**写这段（`engine.c:1093-1094`）⇒ 上位机可写；
   但 **`0x13 RESET` 会 memset 掉它**（与绑定表同族）⇒ 要么每次重写，要么进程序包（**①层**）。
2. **按"收尾优先于开新"改做**：先把阶段 3A 的**形状验收**补上（而不是开 LUT 这个新能力）。
   新增 `tools/h723_traj_bb_verify.py`（采集 + 解码 + 判据 + SKIP 语义）。
   采集顺序踩到并解决：**pyocd 会话开始会复位目标** ⇒ 必须"开会话 → resume → **会话内**重新部署 + 重绑 → 起运动 → halt+dump"；
   首次一次性读 240 KB 报 `TransferFaultError`（这台探针 USB 不稳，已知）⇒ 改**分块 32 KB + 重试 4 次 + 失败零填充并告警**。

## 结论（如实）
- ✅ **已验证**：命令侧 `WIRE[12]` **峰值 = A = 1200**（黑匣子 1 kHz 记录，不是 PC 侧估计）；
  **累积平均 = 1067 Hz**（=0.89A，与"斜坡仍在爬升"一致）⇒ **命令通路是通的、轴在按命令转**。
- ⛔ **形状判据判 SKIP（RC=2，不是 PASS 也不是 FAIL）**：黑匣子**保留跨度 = 960 槽 ÷ 变化率**，
  运动时变化率 ~2.5 kHz ⇒ 只留 ~0.4 s，本次 dump 的最新连续段**仅 51 ms** ⇒
  **覆盖不到 1.2 s 的三角波周期** ⇒ 无法判形状。
  三条出路：① 段长缩到 ≤50 ms ② 扩环（①层）③ 只保留一路映射（也是①层）。

## 本轮又踩到同一族的**第三副面孔**（已升级为通用规则 §5.28）
逐记录 `Δraw/Δtick` 在黑匣子上**照样不可用** —— 因为**环的记录率（2.5 kHz，由任一映射通道变化触发）
≠ 编码器的回填率（~90 Hz）** ⇒ 0.4 ms 尺度上 Δraw 是 0 或 ~34 counts ⇒ 算出 **32812 Hz**。
⇒ 换到"片内黑匣子"**并不自动解决**这个问题；真正免疫的只有 **`ΣΔraw/总时长`**（滑窗 20 ms 仍偏 1.6×）。
★ 顺带查证：环槽位规则 **`slot = seq % 960`**；dump 里**会有多段不连续 seq**（陈旧残余）
⇒ 解码**必须取"最新连续段"**（我第一版全局按 seq 排序 ⇒ 跨代平均 ⇒ "跨度 19.7 s" 假数）。
★ 还有一处**我自己的判据 bug**：L3 的阈值要 `Δt>90 ms` 而窗口只有 51 ms ⇒ **体量恒 0**（假 FAIL）⇒
  已改成"体量 <5 判**无效**"。

## 状态
- 阶段 3A：**仍"部分完成"**，但不再含糊 —— 现在是"**形状验收的路径已打通、且知道为什么做不了**"，
  下一步是**缩短段长到 ≤50 ms** 重跑（不需要采购、不需要改固件）← 这是可直接执行的一步。
- 收尾：板子已复位到收尾态（请求 0 / 关斜坡 / 运动源回脚手架 / 失能）。

---

# 2026-09-18（再续）—— 缩段长重跑：**仍判 SKIP，但阻断点被精确定位**（不是硬件不够，是解码器不够 + 环语义未定案）

## 做法
1. **压缩周期到环保留跨度以内**：三角波段长 600→**100 ms**（周期 0.2 s）、
   正弦段长 250→**35 ms**（周期 0.21 s）；斜率随之 = A/段长 = 12000 Hz/s。
2. **消除第二个真值源**：两支工具（`h723_traj_bb_verify.py` / `h723_traj_verify.py`）
   的段长改为 **`read_dwell()` 从 `.dcl` 里读**（原来各写一份常量 —— 改了 .dcl 就会静默算出错的周期/覆盖度）。
3. 采集时补上 **`sub=22 arg=1` + `sub=20 arg=10`**（把编码器回填提到 ~1 kHz）。

## 一路上抓到的三个错（全部是我这边的）
1. ★★★ **解码器：按 `seq` 排序是错的** —— `seq` **会在环内回绕**（实测 36480…38399 然后 0…1919）
   ⇒ 按 seq 切段会**切出假的不连续段** ⇒ 只取到 128 条（覆盖度 0.33）⇒ **误判 SKIP**。
   看起来像"环太小"，其实是**解码器不够**。
2. ★★ **记录率的归因**：离线统计每个通道的变化次数 ⇒ 是 **4 个通道交错**（SENSOR[0] 编码器 + 
   **SENSOR[8]/[9]/[10] 三个模拟输入**）⇒ 记录率 ~2.5 kHz、**编码器回填其实 ~600 Hz**。
   ⇒ **我上一轮说的"编码器回填只有 90 Hz"是错的** —— 那个数字来自"4 倍交错的 Δraw ÷ 1 个间隔的 Δt"，
     又一次**分子/分母口径不一致**（已由滑窗修正）。
3. ★ **我自己的判据 bug**：L3 阈值要 Δt>90 ms 而窗口只有 51 ms ⇒ 体量恒 0（**假 FAIL**）⇒ 改成"体量<5 判无效"。

## ⛔ 阻断点（精确定位）
同一份 dump **三种排序给出三个互相矛盾的跨度**：
- 按 `seq` 取最新连续段 → **128 条**
- 按 `slot` → **960 条**（slot 不连续处仅 2）
- 按 `tick` → **跨 20.035 s**（而"陈旧残余"计数为 0）

⇒ **环的"写指针/水位"语义未定案**。按本项目纪律（**别把结论建在没解释的机制上**），
`h723_traj_bb_verify.py` 已加**一致性闸门**：检出这种矛盾就**拒绝给结论**（退出码 **2** = 判无效）。
⇒ 下一步应读 **SD 日志头里的环元数据**，或**从固件侧确认写指针语义** —— **不再在 PC 侧猜**。

## 仍然成立（这部分证据可靠）
- 命令侧 `WIRE[12]` **峰值 = A = 1200**（在 51/67 ms 的窗口里）
- 该窗口内**累积速率 ≈1067 Hz = 0.89A**（与"斜坡仍在爬升"一致）⇒ **命令通路通、轴在按命令转**
- 环槽位规则 `slot = seq % 960`（960 槽全满足）
- **变化率的真实成因**：3 个模拟输入通道的噪声在驱动记录率 ⇒
  **若把映射表缩到只留需要的几路（①层），环保留跨度可从 ~0.4 s 升到 ~1.6 s** ⇒ 1.2 s 周期也能验

## 板子状态
已收尾（请求 0 / 关斜坡 / 运动源回脚手架 / 失能）。

---

# 2026-09-18（第五段）—— SD 卡插电脑：日志头读出**写指针语义**（(a) 达成）

## 卡上的真值（`tools/sd_log_read.py`，只读）
```
版本=1  记录=256B  每块2条  块=512B
数据区 LBA 1 .. 61067263 (31.27 GB)   **下一块 = LBA 28597889**   已回卷 = 是
已落盘记录 = 423599290   批次数 = 1689510
丢包(跨上电累计) = 454911 条      ← ★ 跨版本累加值，**不可归因**（技能纪律）
通道映射 60 槽，槽序与固件默认表**逐项一致** ⇒ 我用的下标（WIRE12 = 第 28 槽）是对的
```

## ★★ 两个缓冲被混为一谈过（澄清）
- **在片环**（AXI，`0x24004000`，960 槽 × 256B = 240 KB）：我前几轮用 SWD 读的，
  "变化才记" ⇒ 运动时只覆盖 ~0.4 s ⇒ **本来就不够做 1.2 s 周期的形状验收**
- **卡上日志环**（LBA1..61067263，31 GB，写指针 `h[7]`）：每 512B 两块、持久、跨上电
⇒ 两者是**不同的缓冲**。前者的"陈旧残余"与后者无关。

## ⛔ 但**没在卡上找到我的运动窗口**
`tools/sd_probe_distance.py`（新，只读，从写指针按对数距离取样）跑了 8 个点、
跨 11 M 条记录：**全部 `WIRE12 ≤ 6` + `ACT9 = 0`（驱动器未使能）= 空闲**。
⇒ 两种可能：① 我的 1 分钟运行**落在 8 个采样点之间**（8 点跨 11 M 条，间隔 ~2 h）；② 那几次运行**没进卡日志**。
⇒ 最干净的做法：**把卡插回板子 → 重跑一次运动 → 再把卡拿出来读**（卡的环没有容量限制，
   一旦窗口在卡上，形状验收就能真正做完）。

## ★ 我自己的错（第二次"猜字段"）
`sd_probe_distance.py` 第一版**猜了头部偏移**（把 `w[3]` 当写指针）⇒ 读出"写指针 LBA=2"
⇒ 所有探针落在**数据区开头**、取样全无意义，而**看起来完全正常**。
⇒ 已改为按**已测工具** `tools/sd_log_read.py` 的偏移（`h[7]`），并加**几何自检**
（记录 256B / 每块 2 条 / 块 512B / 起始 LBA1 / 写指针在数据区内）——
**这条自检本该在我第一次跑时就拦住它**。教训：**跨工具的结构偏移不许抄第二遍，要么复用、要么带自检**。

## 板子/卡状态
卡在电脑上（`PhysicalDrive2`）；板子处于收尾态。**卡要插回板子才能继续做形状验收。**

---

# 2026-09-18（第六段）—— **卡上找不到运动的根因定案：日志根本没在记**（已修，并证明这次进了卡）

## 根因（`src/main.c:4734`）
**SD 日志是「开机时」开的**。卡插晚了 / 换卡 ⇒ **必须显式重开**：
调试器写 **`SD_CFG[8]=1` + 魔数 `SD_CFG[15]=0xF00DBEEF`**（`SD_CFG` 在**固定 AXI `0x24000400`**，
不在 SHM ⇒ 协议写不到，用 pyocd）。
⇒ 这解释了前几轮"卡上往回扫 11 M 条全是空闲"——**那几次运行根本没进卡**，不是"窗口在别处"。

## 硬判据（能失败）：`s_log_blk` 的 Δ
读 `s_log_blk`（= 本次上电已写块数）在**运动前后**的差：
- 重开日志后立刻：28648448 → 28651776（**+3328 块**，日志开始写了）
- 运动 6 s：28668672 → 28675840（**Δ=7168 块 = 14336 条记录**）
⇒ ✅ **这段运动确实进了卡**（14336/6 s ≈ **2400 条/s**，与"4 通道交错驱动记录率"一致）

## 我自己踩的三个工具坑（都是"看似正常"的那类）
1. **变量地址不能猜**：我按 map 的换行约定推 ⇒ 差 **4 字节**；又想用"值 == 数据区块数"锚定 ⇒
   那个假设**本身就错**（`s_log_cap_data` ≠ 头部块数）⇒ 锚定失败。
   ⇒ 正解：**用 `nm` 解析符号**（工具链路径从 `cmake/arm-none-eabi.cmake` 的 `TOOLCHAIN_BIN` 读，单一真值源）。
   实测：`s_log_blk @ 0x20003b64`、`s_log_cap_data @ 0x20003b6c`。
2. **pyocd `write_memory` 签名是 `(addr, data, transfer_size)`** —— 我把 `32` 当成 data 传，
   于是 `transfer_size` 变成 `0xF00DBEEF` ⇒ 对齐断言失败（`AssertionError`，不是写失败）。
3. **引号嵌套**：又在自己写的 `print` 里用了中文引号 ⇒ 已改用「」。（累计第 8 次，纪律已进跨项目记忆）

## 环这条路仍然关着
`h723_traj_bb_verify.py` 一致性闸门仍然拦：960 条记录按 tick 跨 **19.933 s**（混年代）⇒ 判 SKIP。
⇒ **卡路径是正解**（31 GB 环、无 960 槽限制），且现在已被证明含运动。

## 下一步
**拔卡 → 插电脑 → 跑 `python tools/h723_traj_card.py analyze`** ⇒ 应当找到窗口并给出形状结论
（T1 方波占空 / T2 逐半周期线性 R² / T2b 斜率反号 / T3 峰值≈A / T5 累积均值≈A/2 / R 反向）。

---

# 2026-09-18（第七段）—— 阶段 3A 形状验收**首次给出结论（FAIL）**，缺陷定位到三条机制

## 卡路径走通了（三个我自己的坑逐个修掉）
1. **扫描窗口要够远**：写指针从 28597889 涨到 28845569（+169,729 块 = 339,458 条）⇒
   我第一版只往回扫 131,072 块 ⇒ **比运动结束时新增的还少** ⇒ 窗口恰在范围外。
   ⇒ 判据：**先看"写指针涨了多少块"，再决定扫多远**（别用固定值）。
2. **必须切上电段**（读卡技能第一条纪律，我漏了）：`tick`/`seq` **每次上电归零**
   ⇒ 跨边界算时间会读出 **429478 s**（5 天）、峰值 23×A、累积 0 这种荒谬值。
3. **不能盲取"最新段"**：运动常在**上一次上电**里，而最新段是运动之后的空闲
   ⇒ 正解：**取"含 A 相最多"的那一段**。

## ★ 结论（窗口 1.471 s / 7.35 个周期；命令侧 51%）
| 判据 | 结果 |
|---|---|
| T1 命令侧 0/A 方波 | ✅ 51% |
| T2b 逐半周期**斜率有正有负** | ✅ 是三角族（k>0:6 k<0:7）|
| T3 峰值 | ✅ **1.09×A** |
| R 0 相末段速度 | ✅ 21 Hz（减速干净收尾）|
| **T2 逐半周期线性度** | ❌ R² 0.40~0.76（要求 ≥0.90）|
| **T5 累积平均** | ❌ **443 Hz vs 理论 600（=74%）** |

## ★★ 三条缺陷（速度轨迹直接看得见）
```
 t     命令Hz   速度Hz
 0.234   1200     781
 0.354      0     488      ← 命令回 0，速度还在降（斜坡正常）
 0.444   1200       0      ← ★ ① 命令=1200 而轴"不动" **40+ ms**
 0.484   1200       0      ← ★
 0.734   1200    3906      ← ★★ ② **3.25×A 的瞬时过冲**
```
- ① **A 相起始有 ~40 ms 的"命令到、轴不动"** ⇒ 直接吃掉平均速率（74% 的主因之一）
- ② **瞬时过冲 3.25×A**（3906 Hz）⇒ 疑与斜坡"轻量路径"写 ARR/CCR1 的瞬态有关
- ③ **降段比升段快得多**（升 ≈ 12–14 kHz/s ✓ 与命令 12 kHz/s 相符；降 0.20 s 内落完）
  ⇒ 停在 0 附近的时间变多 ⇒ 平均变低

## ★ 74% 之谜结案：**不是丢步**
两次**完全独立**的采样路径（PC 侧差分 27 Hz；卡上 1 kHz+ 记录）都得到 **74%**；
且数字**自洽**：752×0.75 + 122×0.72 ≈ `ΣΔraw` 实测的 1668 counts（理想 2259）。
⇒ 是**速度曲线形状**问题（上述三条），不是编码器/丢步问题。

## 下一步（都是①层的具体行为）
① 查斜坡为什么在 A 相起始有 ~40 ms 不动作（拉入延迟？斜率先行？）
② 查斜坡写 ARR/CCR1 的瞬态是否会造成过冲（是否用了预装载）
③ 查斜坡的**下降方向**为何明显快于上升方向（是否只对一侧限幅）

---

# 2026-09-18（第八段）—— **74% 根因定案：`sub=17` 的斜坡只限"上升方向"**（三档斜率判别实验）

## 做法（只改一个参数，不动固件、不重编程序）
固定段长 100 ms（周期 0.2 s），三档斜率各跑一次 6 s：**12000（基准，相位末正好到 A）/ 4000（只到 1/3）/ 30000（40 ms 到 A 后平顶）**。
`run --slope` 覆盖斜率；`analyze-all` 一次把卡上**所有** A 相窗口都判掉（避免为每次运行拔插卡）。

## 结果（窗口按时间序 = 运行序；映射关系由物理自洽确认）
| 窗口 | 斜率 | A相速度均值 | **0相速度均值** | 峰值 | 累积平均 |
|---|---|---|---|---|---|
| #1 | 12000 | 744 Hz | **167** | 1.10×A | 419（70%）|
| #2 | 4000 | 574 Hz | **160** | 1.09×A | **314（52%）**|
| #3 | 30000 | 923 Hz | **161** | 1.09×A | **517（86%）**|

## ★★★ 定案
1. **A 相均值随斜率单调变**（574→744→923）⇒ `sub=17` 确实在起作用 ✓
2. **0 相均值三档恒定 ~160 Hz，与斜率**完全无关** ⇒ **斜坡只对"上升方向"限幅，下降不受它控制** ✓✓
   ⇒ 这就是 74% 的根因：上升越快 ⇒ 在 A 附近待得越久 ⇒ 平均越接近 A/2（52%→70%→86%，**完全自洽**）
3. **上轮那个 3.25×A 过冲没有再复现**（三档峰值都 1.09~1.10×A）
   ⇒ ★ **撤回上轮把它当"缺陷"的说法**，降级为"**偶发测量毛刺**"（诚实留案）
4. **T2 的 R² 低（0.36~0.82）是"梯形形状"的必然结果**（斜坡 + 平顶：斜率 30000 时 40 ms 就到 A，
   余下 60 ms 是平顶）⇒ **不是测量问题，也是我判据设计的问题**：
   对被测曲线本应是"上升斜坡→平顶→下降"三段，用**单直线**去拟合当然差。
   ⇒ 判据应改成**三段模型拟合**（或分别判"斜坡段斜率"与"平顶占比"）—— 登记为下一步工具改进。

## 两种修法（下一次决策）
- **(a) ①层**：让 `step_set_ramp` **双向限幅**（下降侧也按斜率走）⇒ 三角波才真成立。
  改后判据：**0 相速度均值应随斜率变**（而不是恒定 160）。
- **(b) ③层**：程序**自己生成下降斜坡**（`SEQ` 10 ms 档做阶梯下降）⇒ 立刻见效、不改固件。

---

# 2026-09-18（第九段）—— ★★ **更正上一段**：不是"斜坡只限上升方向"，而是 **`hz==0` 走"立即停"**

## 我怎么发现自己错的
上一段只根据**行为**（0 相均值恒定 160 Hz）推断"斜坡只限上升方向"，**没查代码**。
这一段去读 `step.c` 的斜坡推进（`step.c:494-508`）：上下两方向**用同一个 `dv`** ⇒ **代码是对称限幅的** ✗
⇒ 我的结论站不住，继续找真因 ⇒ 在 `step_set_rate`（`step.c:306-318`）找到机制：

```
 * ★★★ 加减速（斜坡限幅）：所有频率写入口都经过这里 ⇒ 两条通路同时获得限幅。
 *   斜坡**关**（默认 0）或**要求停**（hz==0）⇒ 立即生效 —— **停必须是立即的（安全语义）**，
 *   不能"慢慢降到 0"。
```

## 真因（定案）
**`hz == 0` 会绕过斜坡、立即停**（刻意的安全语义）。而我的三角波**用 `wire[12]=0` 表达"下降半周期"**
⇒ 拿到的是**"停"**而不是"斜坡降" ⇒ 轴靠自身惯性快速减速后**停在 0 附近** ⇒
0 相均值只有 ~160 Hz、且**三档斜率下恒定**（那条路根本不看斜率）⇒ 平均 52%/70%/86% ✓✓ 全部解释通。

★ **不是固件缺陷**，是**③ 层程序与引擎安全语义撞车**。⇒ 修法**不改固件**：
**下降段用非 0 的递减序列表达**（`SEQ` 10 ms 档阶梯降到 0.2A 再落 0）⇒ 才走斜坡路径。

## 这条更正本身的价值
- 同一天里我**先写了错的结论并提交**（"斜坡只限上升方向"），**下一个动作就自查推翻**
  ⇒ 靠的是"**别只靠行为推断、去读机制**"这一条（本项目纪律）
- ⇒ 已把它升级为通用规则 **§5.30**（写进 `MEMORY.md`）

---

# 2026-09-18（第十段）—— ③层修法落地：下降段不再写 0，改写**下限 0.15A**（待验证）

## 改动（`examples/h723_step_traj_tri.dcl`，**不改固件**）
```dcl
CONST KLO = 0.15
MUL   lo  FROM wire[11] BY=KLO          # 下限 = 0.15 × A
SEL   tgt G=seg1 IN0=wire[11] IN1=lo    # 升段⇒A；**降段⇒0.15A（不是 0！）**
```
## 为什么（`src/step.c:306-318` 的语义）
`step_set_rate(0)` 是**立即停**并**绕过斜坡**（"停必须立即"，安全语义）⇒ 原程序用 `0` 表示"降"，
拿到的是"停" ⇒ 0 相均值 ~160 Hz 且与斜率无关 ⇒ 平均 52%/70%/86%。
改成 0.15A 后 ⇒ 走斜坡路径 ⇒ 速度**真的斜着下来** ⇒ 0 相均值应 ≈(1+0.15)/2·A = 0.575A，
**并且会随斜率变**（斜率太小就在 100 ms 内降不到下限 ⇒ 形状随之变）—— **这就是本轮要判的判据**。

## 如实声明的代价
速度下限不是 0 而是 0.15A ⇒ **带底的三角波**（理论均值 0.575A 而非 0.5A）；
相位交界处有 0.15A（≈180 Hz）的小台阶。
★ 要让下限真到 0，两条都在案：① ①层让斜坡在 `hz==0` 时也生效/提供"斜坡到 0"接口；
  ② ③层用 `SEQ` 阶梯递减（10 ms 一档降到 0.05A 再落 0）。

## 现状
三档（12000/4000/30000）已跑并**都进卡**（Δ=8192/8192/7552 块）⇒ **待拔卡后在 PC 侧 `analyze-all` 验证**。

---

# 2026-09-18（第十一段）—— ★★ 修法**验证通过**：`hz==0` 机制判断被证实，T5 由"乱跳"变"稳定一致"

## 结果（三档斜率，修法：下降段写 0.15A 而不是 0）
| 窗口 | A相均值 | **0相均值** | 峰值 | **T5 累积平均** |
|---|---|---|---|---|
| #1 | 996 | **365** | 1.10×A | **676** |
| #2 | 918 | **452** | 1.12×A | **677** |
| #3 | 1053 | **327** | **3.26×A** ❌ | **683** |

## ★★ 两条判据（开跑前写好预测 ⇒ 可证伪）
1. **`hz==0` 的指纹消失**：0 相均值从"三档恒定 **160/167/161**"变成"**365/452/327 随斜率变**"
   ⇒ **下降段确实走斜坡路径** ⇒ **机制判断证实** ✓✓
2. **T5 三窗口全 PASS**（676/677/683 vs 预测 **0.575A = 690**）✓✓
   —— 修法前是 **52%/70%/86%**（乱跳），现在**稳定一致** ⇒ **"平均速率不足"这条修好了**
   （带底三角波的均值 0.575A 而非 0.5A，与预测吻合）

## 仍然不通过的两项（如实）
- ❌ **3.26×A 尖峰在第 3 窗口复现** ⇒ **上轮"撤回它"说得太早**（1/3 出现率，不是孤例）。
  ★ 新线索：**3906 Hz ≈ 1e6/256** ⇒ 恰好是 `ARR=256` 的周期 ⇒ 像"某一拍 ARR 被写成 256"的瞬态。
- ❌ **T2 线性度低**（R² 0.30~0.76）：部分是**真实现象**（斜坡+平顶 ⇒ 梯形），
  部分是**我的判据设计问题**（对梯形用单直线拟合本来就该低）⇒ 应改**三段模型拟合**。

## 阶段 3A 状态更新
从"**FAIL 一片**"→"**主要判据通过**"：T1/T2b/T5 三窗口全 PASS，剩 T2（判据要改）、T3（尖峰待查）。
★ 本轮值得记的**过程质量**：预测先写在前面、结果被定量命中（0.575A）——**这才叫验证**，
  而不是"跑完了看着还行"。

---

# 2026-09-18（第十二段）—— 判据两处改进 + **阶段 3A 收口**

## 改进一：T3 改用**稳健峰值**（前 1% 中位数），并显式标毛刺
`max(vh)` 只要撞上一次毛刺就把 T3 判死 ⇒ **判据本身不稳健**，分不开"真缺陷"与"单点毛刺"。
改后：三窗口 **1.05×A / 1.05×A / 1.05×A 全 PASS** ✓，且 #3 自动标注
`max 3.26×A ← 有单点毛刺（max/稳健=3.10）` ✓ —— **判据既能通过、又能指出毛刺**。

## 改进二：T2 加**噪声底闸门**（先量"平顶段 std/mean"，再决定判不判）
```
噪声底（平顶段）= 87.2% / 83.8% / 92.2%   ← ★ 我的测速器噪声高达 ~90%
⛔ ⇒ T2'（斜坡段线性度）**判无效（SKIP）**
```
**为什么噪声这么大**：20 ms 滑窗撞上**突发式**编码器回填 ⇒ 有时窗内 0 次更新（v≈0）、
有时 2 次（v≈2×）⇒ 方差爆炸。⇒ **在"速度"上判形状线性度，在这个装置上不可能**。
⇒ 正解（登记）：**在"位置"上判** —— 对半周期内的 `raw(t)` 做二次拟合 `a·t²+b·t+c`，
`a≠0` 即斜坡，且 `a` 的量级应等于 `slope/2`（位置是积分量、噪声低一个数量级）。

## ★ 阶段 3A 收口状态（三窗口）
| 判据 | 结果 |
|---|---|
| T1 命令侧 0/A 方波（50~51%）| ✅ 全 PASS |
| T2b 逐半周期斜率正负交替 | ✅ 全 PASS |
| **T3 稳健峰值（1.05×A）** | ✅ 全 PASS（#3 附"有单点毛刺"标注）|
| **T5 累积平均（676/677/683 ≈ 0.575A）** | ✅ 全 PASS |
| **T2 斜坡线性度** | ⛔ **判无效（SKIP）** —— 测速噪声底 84~92%，此判据在本装置上不可用 |

⇒ 阶段 3A 从"**SKIP**"→"**FAIL 一片**"→"**4 PASS + 1 SKIP**"，且每个 SKIP 都**指名了正解**。

## 遗留（明确挂账）
1. **T2'' 位置域判据**（对 `raw(t)` 做二次拟合）—— 工具改进，不需硬件
2. **3.26×A 单点毛刺**（1/3 窗口出现，≈1e6/256 ⇒ 像某一拍 `ARR` 被写成 256）
   ⇒ 已由稳健峰值兜住；机制待查（查 `step_rate_apply_light` 的写序/预装载）

---

# 2026-09-18（第十三段）—— T2''（位置域判据）落地：**积分检查可用，但斜率反认有歧义**

## 实现
- **解卷绕** `raw`（单圈 0..4095 ⇒ 单调序列），前提：0.5 ms 间隔下逐记录 Δ ≤2 counts ⇒ 安全
- **T2''-1 位移比**：每个半周期算"实测位移 / 模型预测位移"（模型 = 斜坡 A↔0.15A + 平顶）
  ⇒ 不用速度求导 ⇒ **不受 84~92% 测速噪声的伤害**（§5.27/§5.28 那条族）
- **T2''-2 斜坡窗二次拟合**：在**模型预测的斜坡窗**内拟合 `raw(t)=a t²+bt+c`，`|a|` 应 ≈ `slope·K/2`
  ⇒ 加**弱判别闸门**：若各候选斜率的比值都接近 1（极差 <2%）⇒ 认不出斜率 ⇒ 判 **SKIP**（不给漂亮数）

## 结果
```
T2''-1 位移比（实测/模型）各候选：
  #1: 4000→0.844  12000→1.014  30000→0.999
  #2: 4000→0.782  12000→0.940  30000→0.989
  #3: 4000→1.565  12000→1.340  30000→1.052
```
⇒ ✅ **积分检查本身是稳定的**（比值都在 1±6% 内，且与 T5 自洽）
⇒ ⛔ **但三个窗口都最佳匹配"最快那档（30000）"** —— 而我依次跑的是 12000 / 4000 / 30000 ✗

## 这个歧义必须切开（两条互斥解释）
- (a) **窗口↔斜率的对应关系不是我以为的顺序** ⇒ 我的假设错
- (b) **斜坡的真实 slew 比 `sub=17` 设定的快得多** ⇒ ①层问题（斜坡没按声明执行）
⇒ ★ **一刀切开的办法：跑一档、立刻判**（最新窗口必然属于刚跑的那次 ⇒ 映射无歧义）。

## 工具改进的元教训（又一次）
- `_judge_position` 里调了 `_judge` 的**局部函数** `rec_ok` ⇒ NameError（作用域）
- **引号嵌套第 9 次**：又用 heredoc 写含中文引号的 print ⇒ SyntaxError
  ⇒ ★ 我自己刚写下的纪律"改文件一律用 Edit"**没执行**。⇒ 这条纪律要**写在动作里**，不能只写在记忆里。

---

# 2026-09-18（第十四段）—— 单档（slope=12000）+ 位置域：**真实曲线接近方波；并查出我自己的违规**

## 做法（消除映射歧义）
`run --slope 12000` 只跑一档 ⇒ `analyze --slope 12000` 判**最新的那个窗口** ⇒ 斜率是**已知参数**、
不做"反认"（第一版把单候选也走弱判别闸门 ⇒ 误判成 SKIP，已修）。

## 结果（T2''-1 按方向分开后）
```
升段：位移比中位 1.401（10 半）← 比模型快
降段：位移比中位 0.433（10 半）← 比模型慢
T5 累积平均 = 686 ≈ 0.575A      T2''-2 |a|/(slope·K/2) = 0.39
```
★ 三者**互为佐证**，指向同一个物理图像：**真实曲线接近"0.15A ↔ A 的方波"**
（过渡远快于配置的 12000 Hz/s；方波均值 0.575A = 690 ✓ 与 T5 的 686 吻合；
 而 `|a|=0.39` 也一致 —— 真实过渡很短，在"预期 85 ms 斜坡窗"上大部分是平的）。
★ 之前"混着算中位数"给出 1.309 ⇒ **既不像升也不像降 ⇒ 什么都说明不了** ⇒ 判据必须**按方向分开**（已修）。

## ★★ 查出我自己的违规：「设了就算必错」
工具只打印"斜率 = 12000"—— 那是**我发的**、不是**它读回的**。⇒ "实际 slew 比设定快"这个结论里，
**混着"斜率根本没生效"的可能** ⇒ **不可归因**。
⇒ 已在 `run` 里补 **`sub=19` 读回**（+0 ramp_hz_s / +4 rate_cmd / +8 rate_out / +12 rate_actual）：
```python
if rb[0] != slope: 打印 「回读 != 设定」并明确告警「本轮 slew 结论不可归因」
```

## 下一步（一次换卡，决定性）
跑一档 → 看 `sub=19 +0` 是否等于我设的值：
- **读回 ≠ 设定** ⇒ 是**写入路径**问题（我的工具/手柄），不是硬件 ⇒ 先修写入
- **读回 == 设定** ⇒ **斜坡真的没按声明限幅** ⇒ **①层问题**（`step_tick` 的斜坡推进或"轻量路径"）

---

# 2026-09-18（第十五段）—— ★★★ **根因定案：`sub=17` 的斜率在主循环快于 1 ms 时语义不成立**（①层一行）

## 决定性观测：运动过程中连读 `sub=19`（只需串口）
```
  t(s)  ramp_hz_s  rate_cmd  rate_out  rate_actual
   0.01     12000     1200      302      302      ← 10 ms 内到 302 ⇒ **≥30 kHz/s**
   0.06     12000     1200     1200     1200      ← 随后一直停在目标
   0.11     12000      180      180      180      ← 命令一翻立刻到位
```
⇒ `rate_out` **几乎不经过中间值**（每个过渡只撞见一次）⇒ **斜坡基本没在爬**。
⇒ 而且 **读回 `ramp_hz_s = 12000` = 我设的值** ⇒ **不是写入问题**，是**斜坡没按声明限幅**。

## 根因（`src/step.c:495-497`）
```c
uint32_t dt_ms = (dt + 9u) / 10u;        /* 拍→ms，向上取整 ← ★★ 病灶 */
uint32_t dv    = (g_step_ramp_hz_s * dt_ms) / 1000u;
```
`step_tick` 由**主循环**每 **~0.37 ms（≈3.7 拍）** 调一次 ⇒ `(3+9)/10 = 1` ⇒ **`dt_ms` 被算成 1 ms，
而真实间隔只有 0.37 ms** ⇒ 每次多爬 **2.7 倍** ⇒ **声明 12000 Hz/s 实测 ≈32 kHz/s** ✓
★ 与实测 **≥30 kHz/s** 精确吻合；也与"模型匹配 30000"、"T5=686≈0.575A"、"升1.40×/降0.43×"全部自洽。

## 修法（一行，①层）
```c
dv = (uint32_t)(((uint64_t)g_step_ramp_hz_s * dt) / 10000u);   /* dt 单位=拍, 10 kHz */
```
（不改语义、不改接口；改后判据：**半周期位移比应回到 1±15%**，且 `rate_out` 在过渡期**能看到多级中间值**。）

## 这条线索的完整链条（值得复盘）
"74% 之谜" → `hz==0` 是立即停（③层改法）→ T5 稳定 686（预测命中）→ T3 稳健峰值（判据改进）
→ T2 噪声底 84~92%（判 SKIP，不是 FAIL）→ **T2'' 位置域判据** → 升1.40×/降0.43×
→ "混着算中位数"是假信息（判据按方向分开）→ **查出我没读回 `sub=17`**（"设了就算必错"）
→ 补读回（=12000，排除写入问题）→ 连读 `sub=19` 看 `rate_out` 轨迹 → **落到一行代码**。

★ 每一步都**先判无效、再换方法**，而不是"调参数调到通过"。

---

# 2026-09-18（第十六段）—— ★★★ ①层修复 **A/B 验证通过**，把这条线收口

## 改动
`src/step.c` 斜坡推进：`dt_ms=(dt+9)/10` → 按**真实 dt（拍）**算 + **余数累加器**；
A/B 开关 `DCL_STEP_RAMP_FIX`（1=交付 / 0=对照），已登记进 CMakeLists + build.sh DEFAULTS ✓

## A/B 结果（同一天实机两次构建 + 两次运行；判据同一套）
| 判据 | 窗口#1 **对照档**（改前）| 窗口#2 **交付档**（修后）|
|---|---|---|
| 位置域**升段**位移比 | **1.397**（比模型快）| **0.952** ✓ |
| 位置域**降段**位移比 | **0.439**（比模型慢）| **1.070** ✓ |
| 斜坡窗 `\|a\|/(slope·K/2)` | **0.40**（0.37~0.46）| **0.99**（**0.98~1.04**）✓ |
| T5 累积平均 | 676 | 670 |
| T3 稳健峰值 | 1.05×A | 1.03×A |

⇒ **对照组量到了病、交付组落回 1** ⇒ 判据能失败 ✓；`|a|` 0.40→0.99 ⇒ **曲率精确等于"声明斜率的一半"** ✓✓

## 两个值得记的工程细节
1. **对照档编译失败**（`s_ramp_acc` 定义了没用到 ⇒ `-Werror=unused-variable`）⇒ 累加器的定义也必须进 `#if`
   ⇒ "**A/B 的对照档编不过，就等于没有对照**"。
2. 对照档烧录时 pyocd 报 `programmed 0 bytes, skipped 68608` ⇒ **这次是对的**（对照产物与板上固件逐字节相同）
   ⇒ ★ 与上午那次"**构建失败却烧旧镜像**"的区别是：**这次构建 RC=0**。
   ⇒ 结论：`programmed 0 bytes` 本身不是错误信号，**要结合构建的 RC 一起看**。

## 收口状态
- ①层一行修复：**已修 + A/B 两侧验证** ✓
- 待做：**①层改动的纪律要求 = 全量回归**（需要卡回到板子上）
