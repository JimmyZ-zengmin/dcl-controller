# PLAN-closedloop-stepper-v2 —— 闭环步进 · 全面实验规划 + 架构优势评估

> **这不是愿景文档。本文每一行要么是一个已实测的数字，要么是一条"能失败"的判据 + 跑它的脚本。**
> 前身：`PLAN-step-motion-v1.md`（运动能力面，已落地）。本文是**全面版**。
> ★ 用法：**阶段 0 是唯一关键路径**，其余阶段只有在阶段 0 通过后才动手。

---

## 1. 已确认的事实（全部当日实测；含**我讲错过的**）

| 指标 | 实测 | 怎么量的 | 可信度 |
|---|---|---|---|
| 执行频率（环在 ③ 层每拍）| **10 kHz**（20000 拍 @200MHz）| `0x38` 的 `period_min/max` | ✓ 硬 |
| **输出**确定性 | **σ = 3.6 ns**（CPU 直写 `BSRR`）| DWT + LA（有权威源）| ✓ 硬 |
| 拍周期抖动（**调度**）| 跨度 **90 ns**（19990~20008 拍）| `0x38` | ✓ 硬 |
| **PC 在环死时间** | **87 ms**（3 次复现 87.4/87.8/80.1）| `track sine` 拟合 | ✓ 硬 |
| **反馈更新率天花板** | **≈163 Hz**（周期改 100→10→4 拍都停在 163）| `g_as_ok_n` 增量 | ✓ 硬 |
| 编码器 I2C 事务 | ~250 µs（**8 拍 ≈800 µs**，走 `i2c_sm`）| 源码 + 实测 | ✓ 硬 |
| 黑匣子 | `256B × 960 = 96 ms`，**拍级时间轴（tick 在字 2）** | SWD API dump | ✓ 硬 |

### ★★ 撤回清单（我讲错过的，已从 README 改掉）

| 我曾说 | 真相 |
|---|---|
| 闭环带宽 **×700**（13.5 Hz → 10 kHz）| ★ **10 kHz 是执行频率**；带宽上限 = **反馈更新率** ⇒ 实际 **~80 Hz ⇒ ×6** |
| 斜坡"**被砍 47%**"（627 vs 1100 rpm）| ★ **测量混叠伪影**（31000 Hz 下 2 s 走 38 圈 vs 26 命令/s 采样）|
| 驱动有"**最低有效频率**"（15 Hz 转不动）| ★ 假象：**编码器当时是停的** |
| `SENSOR[0]/[1]` 更新率 1 kHz | ★ 实测 **163 Hz 天花板**，瓶颈是**主循环 6.1 ms** |

---

## 2. 核心命题（要证明的一句话）

> **把"反馈"从主循环搬进拍内之后，闭环带宽能从 ~80 Hz 提到 ≥1 kHz；
> 并在同一判据下，把"PC 在环的 87 ms 死时间"换成"片内的 ≤1 ms"，跟随误差从 ±25.9° 降到 ~残余量级。**

**为什么只差"搬发起"一件事**（源码已核实）：
| 环节 | 现状 | 位置 |
|---|---|---|
| I2C 状态机**推进** | **已在拍内**（`i2c_sm_tick()` 在 ISR 最尾，每拍一相位）| ✓ |
| **发起**（谁下 `i2c_sm_request`）| ★ **在主循环** ⇒ 发起率 ≤ 主循环频率（163 Hz）| ✗ **唯一瓶颈** |
| 取结果 | 主循环（`dev_bind_service` / `as5600`）| ✓ 不需要快 |

---

## 3. 阶段划分（每阶段：目标 · 判据 · 脚本 · **止损条件**）

### 阶段 0 ★关键路径：**把"发起"搬进拍内**

- **做什么**：在拍 ISR 里按"每 N 拍"发起 `i2c_sm_request`（读 AS5600 raw），
  结果由 `i2c_sm_take_result()` 在**拍内或主循环**取；N 可配（`op=19 sub=20` 已有）
- **判据（能失败）**：
  1. ★ **`g_as_ok_n` 增量 ÷ 时间 ≥ 1000 Hz**（现在 163）
  2. `err_n == 0`、`skip_bus_n == 0`（信道干净）
  3. ★ **ISR 预算**：`0x38` 的 `isr_overrun == 0`（`i2c_sm` 一相位 ≈9000 cyc，而拍预算 32000）
  4. **全量回归零新失败**（动 ISR ⇒ 必经）
- **脚本**：`build/_asrate.py`（已存在）+ `tools/h723_full_regress.sh`
- **★ 止损**：若 ①做不到（例如拍预算不够）⇒ **闭环带宽上限就是 ~80 Hz，×6 就是我们的天花板** ⇒
  **立即停止"提带宽"这条线**，转向"用现有带宽把确定性/可靠性做实"。

### 阶段 1：**用同一判据做 A/B（证明收益）**

- **公平条件（写进脚本注释，不满足即判无效）**：两侧同增益/同误差带/同限幅/同目标/同采样；
  **唯一变量 = 环路在哪**
- **臂 A（PC 在环）**：读 `0x39 op=18`（**阻塞路径 = 实时**，`longrun/roundtrip` 一直用它）
- **臂 B（片内）**：读 `sensor[]`（阶段 0 后的反馈率）
- **判据（能失败）**：
  1. **上升时间之差 ≈ 死时间之差**（预测：阶段 0 前 ~87−6 ms；阶段 0 后 ~87−1 ms）
  2. ★ **低频跟随误差**：`track sine 0.2 Hz` 下"纯滞后项"应**从 23.5° 掉到 ≤0.5°**（ω×死时间）
  3. 残余项两侧同量级（控不住的就是控不住 —— 这句本身是反空判据）
- **脚本**：`build/_ab_step_response.py`（已有骨架，**要按上表改**：PC 臂换阻塞路径）

### 阶段 2：**把"高速段"测出来（黑匣子）**

- **前置**：阶段 0 完成（否则 96 ms 窗口里反馈不变）
- **判据**：实际/请求比 vs 频率曲线，**用"实测最大单步 < 90°"判不混叠**（不是理论值）
  ⇒ 给出**失速拐点**与"突加 vs 斜坡"的真实差异（这才是"斜坡收益"的正确测量）
- **脚本**：`docs/audit/_bb_measure_2026-09-17.py` + `_bb_dump_2026-09-17.py`（已归档）

### 阶段 3：只有阶段 0–2 全过才做

- **多轴**（`step.c` 单例 → 实例化；插补放 ③ 层）
- **轨迹规划完善**（三角波/阶跃已在，正弦需累加折返）

---

## 4. 架构优势评估（逐项；**标明已证/未证**）

| 主张 | 现状 | 判据脚本 | 结论 |
|---|---|---|---|
| 执行频率可达拍级（10 kHz）| **已证** | `0x38` period_min/max | ✅ **已证** |
| 输出确定性 σ=3.6 ns（结构给的，非调优）| **已证** | DWT/LA | ✅ **已证** |
| **换算法不改固件** | **已证**（今天多次：`.dcl` 改完秒级重跑）| 墙钟计时 | ✅ **已证**（待量化）|
| **外设是"数据"不是"代码"** | **已证**（换编码器=改绑定表一行）| `h723_dev_bind_test` 52/0/1 | ✅ **已证** |
| 原语不够能"凑"（无 ABS/MOD 也做最短弧）| **已证** | `h723_step_bounded_position.dcl` | ✅ **已证** |
| **闭环带宽** | ★ **仅 ~80 Hz**（受主循环限）| `g_as_ok_n` 增量 | ⛔ **未达工业伺服（1~20 kHz）** ⇒ **阶段 0 是唯一出路** |
| **失速边界 / 斜坡收益** | **未证**（仪器不够 ⇒ 黑匣子）| 阶段 2 | ⛔ **未证** |
| 轨迹规划（加减速）| ⚠️ 已实现、收益未证 | `h723_step_ramp_test` **4/6** | ⚠️ **部分** |
| 多轴 | ✗ 单例 | — | ⛔ **未做** |
| 电流环 | ✗ TB6600 无接口 | — | ❌ **硬件缺，不做** |

**★ 一句话评估**：
**"执行确定性 + 可配置性"达到了工业级；"闭环带宽"是最大的短板（×6 而非 ×700）；
"运动功能"仍是点到点级。** —— 而**阶段 0 决定了这份评估的上限**。

---

## 5. 明确**不做**（避免铺摊子）

- **电流环**：TB6600 只有 PUL/DIR/ENA ⇒ 换驱动器才行（选型问题，非架构缺陷）
- **用 PC 采样量高速运动**：三次实测证明必然混叠
- **"先堆功能再测"**：今天的四次失败全是这个模式
- **把 `mismatch_n`（PE9 被他人拉高）当新的功能问题**：它是独立缺陷，**单独立案**，不塞进本规划

## 6. 风险与止损

| 风险 | 止损条件 |
|---|---|
| 阶段 0 拍预算不够（`i2c_sm` 一相位 9000 cyc / 32000）| `isr_overrun` 涨 ⇒ **回退**，接受 ×6 天花板 |
| `as5600` 走 `i2c_sm` 后与 `dev_bind` **争同一总线** | `skip_bus_n`/`DB_SKIP_N` 涨 ⇒ 需**统一调度**（一个发起者）|
| 阶段 0 完成后 A/B 差异**不显著** | 若 <30 ms ⇒ **"搬进片内"的收益不成立** ⇒ 停止该叙事（★ 不许找理由）|
| 回归出现新失败 | 按纪律**先归因再继续**（不许"看起来无关"）|
