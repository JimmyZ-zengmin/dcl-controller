# 路由表执行时间的**可算性**：方法、已确立的结构事实、与明确的未算部分

> **这份文档回答一个问题**：路由表程序的执行时间，**哪些部分可以被算出来**？
>
> 它与 `README.md`（TCM 周期确定性实验）的分工：
> · `README.md` 回答"**实测**下来稳不稳" ⇒ 已确立（典型拍 0.02~0.06 cyc 可复现）
> · **本文**回答"能不能**不靠实测**算出来" ⇒ 这是"确定性"能否成为**设计属性**的关键
>
> **本文的纪律**：只写**已经被结构证据支持**的东西。所有"还没算的"单独列在 §5，
> 并且**明确标注它是"未算"而不是"算不出"** —— 这两者完全不同。

---

## 1. 为什么选路由表：它是**为可算性**做的架构选择

常规 PLC 跑的是通用指令流：循环次数不定、间接跳转、递归、动态数据结构。
这四样恰好是通用 WCET 分析**四大难题**的来源。通用 IEC 61131-3 运行时之所以
只能靠"实测最坏值 + 安全系数"，根子就在这里。

**路由表把这四样全砍了**：

| 通用 WCET 难题 | 路由表的情况 |
|---|---|
| 循环边界分析 | **不需要**：19 个原语是定长体，路由表按拍展开成**线性序列** |
| 间接跳转 / 函数指针分派 | **已删除**（本项目刻意去掉间接分派） |
| 递归 | 无 |
| 不可行路径组合爆炸 | 路径数 = 1（线性序列，没有分支合流） |

⇒ **路由表在结构上就是一条"线性指令序列"，正是静态时序分析（STA）最擅长的形态。**

★ 这是本文最重要的一句话：**"可算"不是运气，是架构选出来的。**
所以下面所有工作都不是在"给通用程序硬套分析"，而是在**收割一个已经付过代价的设计决定**。

---

## 2. 方法：把 FPGA 的静态时序分析搬过来

FPGA 工具**从不跑电路**就知道一个时钟周期够不够。它做四件事：

```
1. 综合 → 逻辑网表（LUT / 进位链 / 触发器）
2. 每个原语有厂商标定的传播延迟（timing model）
3. 沿依赖链累加，取最长 = 关键路径
4. 关键路径 > 时钟周期 ⇒ 时序违例
```

★ 关键在于：**它不是仿真、不是实测、不是拟合。**
它靠 **"原语延迟已知 + 结构已知 ⇒ 路径延迟可沿结构累加"**。

对应到 Cortex-M7：

| FPGA | 本平台对应物 |
|---|---|
| LUT / 进位链 / 触发器 | 指令类（ALU / 乘 / **除** / 加载 / 存储 / 分支） |
| 厂商标定的原语延迟 | **ARM 文档化的指令执行时间**（Cortex-M7 TRM） |
| 网表 = 连接结构 | 反汇编得到的**控制流图 + 数据流图** |
| 关键路径 | 该拍执行的**最长依赖链** |
| 时钟周期约束 | **拍预算**（时基档 0 = 40000 cyc） |

⇒ **本平台的做法应当是**：逐原语取指令序列 → 按文档延迟赋权 → 沿依赖链累加 → 得结构成本。

★ 与"给界留安全系数"的区别（这是本文与既有做法的分界）：

| | 依据 | 门槛 | 能失败吗 |
|---|---|---|---|
| 观测路线 | 跑很多次取 `MAX` | 低 | ❌ 没观测到的更坏情况依然可能发生 ⇒ **假保证** |
| **结构路线（本文）** | 文档延迟 + 依赖链**推**出来 | 高 | ✅ 实测一旦超过界，就是一次真缺陷 |

---

## 3. 已确立的结构事实（**可复算，与板子无关**）

以下全部由 `build/dcl_h723` 反汇编得到，**不需要连板子**。

| # | 事实 | 数值 | 怎么得到的 |
|---|---|---|---|
| S1 | 扫描体规模 | `engine_scan_itcm` = **599 条指令**，0x0CF0..0x1578 | `objdump -d`（`tools/sta1_disasm.py`）|
| S2 | 扫描体**完全内联** | `prim_exec` **没有独立符号** —— 19 个原语体全部内联进扫描体 | 符号表（900 个符号里无 `prim_exec`）|
| S3 | op 分派是**跳转表** | 3 个 `tbh`：主分派 @0x0DF0（表 @0x0DF4，**18 项**）+ 子表 @0x0F80 / @0x11A0（各 **5 项**）| 表内容按"下一个基本块起址"截断后解析 |
| S4 | **分派语义精确** | `subs r6,#1` → `cmp r6,#17` → `bhi` → `tbh` ⇒ **表项 i 对应 op = i+1**；`OP_DIRECT`(0) 是 fall-through @0x0E56 | 0x0DEA..0x0DF0 的指令序列 |
| S5 | 指令类分布 | fp **220**(36.7%) · `?` 128 · ld 80 · alu 67 · br 57 · st 26 · mul 15 · move 6 | 分类直方图 |
| S6 | **CFG 可自动构建** | **71 个基本块**；`tbh` 目标可从 ELF 表数据精确解析 | `tools/sta10_pathcost.py` |
| S7 | ★ 循环结构 | 回边 = `b.n 0xDE6` @块6（尾块）→ **块3（分派块）**；循环前导 = 块0（44 条，只执行一次）；**每路由共用尾块 = 块6（27 条）** | CFG 打印 |
| S8 | **逐原语入口块可精确切出** | 19/19 个 op 有确定的入口块，大小 **4 ~ 29 条** | 跳转表 + 基本块划分 |
| S9 | 共享结构定量 | 分派块 3 个（含 2 个子表）、尾块 2 个（块6 被 17 个 op 到达） | CFG 可达统计 |

★ **S8 是本节最有价值的一条**：它说明"原语 ↔ 代码区间"的映射是**机器可判定的**，
不是靠人工标注。这意味着**结构量可以每次构建自动重算** —— 这正是 STA 能进构建闸门的前提。

### ★★ 4 个 CFG 陷阱（每一个都曾安静地给出**错误但看着正常**的结果）

这一段是本轮最该保留的东西。**四个坑全部导致"数字照常打印、结论完全错误"**：

| # | 陷阱 | 症状 | 正解 |
|---|---|---|---|
| **T1** | **按地址排序猜布局** | `LPF` 得 255 条、`PID` 7 条、`SR` 区间**为负** | 跳转表**项序 ≠ 代码布局序**；必须按控制流走 |
| **T2** | **`tbh` 没有后继** | 可达图在此断掉 ⇒ "可达 8 条"比自己的入口块（12 条）**还小**（自相矛盾却不报错） | `tbh` 的目标在**表数据**里，必须从 ELF 字节区解析 |
| **T3** | **表长靠猜** | 按固定 40 项读 ⇒ 把普通代码当表项 ⇒ 可达集膨胀到整个函数，**19 个 op 全部变成 283 条**；而 D2 判据**居然还"通过"**（预测值全同 ⇒ 残差只剩常数项）= **假绿** | 表是紧跟 `tbh` 的连续数据，用**下一个基本块起址**截断 |
| **T4** | **在带环图上求"最长路径"** | 尾块 → 分派块 → case → 尾块 构成环；DFS 触到深度上限后**返回 0**，而**不报错** | 环上最长路径**无定义**；必须换口径 |

> **教训（比结论更重要）**：这四类错误的共同形态是
> **"程序不崩、数字照出、结论全错"**。
> 本项目的既有纪律"判据必须能失败"在这里救不了 —— 因为**判据本身在跑，只是输入是错的**。
> 唯一有效的防线是**让每一步都能自相矛盾**：比如"可达条数 ⊇ 入口块条数"这条，
> 就是在 T2 上唯一亮红灯的地方（8 < 12）。⇒ **口径之间要互相约束。**

---

## 4. 结构量到底有多少信息量：三次尝试，两次失败

这一节是本文**最该保留**的部分，因为它记录的是**被推翻的方法**。
本项目纪律：被数据推翻的判据本身是资产（见 `RETRACTIONS.md`）。

### 4.1 逐原语结构表（S6/S7 的产出，可复算）

| op | 指令数 | 实测 cyc | cyc/指令 | 分类直方图 |
|---|---|---|---|---|
| DIRECT | 29 | 56 | 1.93 | br=1 ld=4 st=2 fp=1 alu=14 ?=7 |
| CMP | 23 | 76 | 3.30 | br=2 ld=4 st=1 mul=2 div=1 fp=8 alu=2 ?=3 |
| HYST | 12 | 79 | 6.58 | br=1 ld=3 st=1 fp=5 ?=2 |
| CLAMP | 12 | 74 | 6.17 | br=1 ld=2 fp=6 ?=3 |
| LPF | 15 | 97 | 6.47 | br=1 ld=2 st=1 mul=2 div=1 fp=6 ?=2 |
| PID | 32 | **145** | 4.53 | br=1 ld=7 st=2 mul=2 div=1 fp=14 alu=1 ?=4 |
| RATE | 7 | 68 | 9.71 | br=1 ld=1 st=1 div=1 fp=2 ?=1 |
| DEADBAND | 12 | 76 | 6.33 | br=1 ld=2 st=1 fp=6 ?=2 |
| MUX | 9 | 70 | 7.78 | br=1 ld=2 fp=3 alu=2 ?=1 |
| EDGE | 31 | 87 | 2.81 | br=2 ld=4 st=1 fp=14 alu=4 ?=6 |
| LUT | 17 | 89 | 5.24 | br=1 ld=2 mul=1 fp=7 alu=3 ?=3 |
| CNT | 29 | 83 | 2.86 | br=1 ld=3 st=2 fp=15 alu=2 ?=6 |
| TIMER | 19 | 80 | 4.21 | br=1 ld=3 st=1 fp=8 alu=2 ?=4 |
| ARITH | 26 | 74 | 2.85 | br=2 ld=4 st=1 fp=10 alu=4 ?=5 |
| SCALE | 7 | 67 | 9.57 | br=1 ld=2 mul=1 fp=2 ?=1 |
| AND | 5 | 77 | 15.40 | br=1 fp=3 ?=1 |
| OR | 9 | 78 | 8.67 | br=1 ld=1 fp=5 ?=2 |
| NOT | 7 | 71 | 10.14 | br=1 fp=4 alu=1 ?=1 |
| SR | 42 | 85 | 2.02 | br=1 ld=6 st=2 fp=8 alu=15 ?=10 |

（"实测 cyc" = `engine.c` 的 `k_op_cost_itcm[]`；它**不参与**结构计算，只作独立对照。）

### 4.2 尝试一：指令类加权最小二乘 ⇒ ❌ **过定参数化，被自己推翻**

用 7 个指令类 + 总条数（8 个特征）在 19 个样本上做最小二乘，再做**留一交叉验证（LOO）**：

| 模型 | 中位 LOO 误差 | 最大 | 平均相对 |
|---|---|---|---|
| 类加权（8 特征） | **29.1 cyc** | 110.4 | 45.6% |
| 基线：只数指令条数（1 变量） | **5.6 cyc** | 64.8 | 13.1% |

而且拟合出的系数含**物理上不可能**的负值（`fp −4.60` / `alu −8.69`）。

⇒ **判据 FAIL 是对的**：8 个特征配 19 个样本是过定参数化，
负系数是过拟合的典型症状。**这条路线不能作为"结构→成本"的证据。**

> ★ 教训：**特征数必须与样本数相称。** LOO 是"能预测"与"只是拟合"的分界，
> 而它在这里直接把"加权"这一层否掉了 —— 而如果只看拟合内残差，它会"看起来很准"。

### 4.3 尝试二：单一结构常数（**零拟合自由度**） ⇒ ❌ 不成立

去掉全部自由度：`K = median(实测_i / 结构量_i)`（只是取中位，不是优化），
预测 `= K × 结构量`。这样残差是**真残差**。

### 4.4 ★★ 在**比例形式**下：四种结构量全部不成立（§4.5 用仿射形式把它们救了回来）

> ★ 本节写于"步 A″/C"之前，当时**比例形式**是唯一试过的形状。
> §4.5 证明换成**仿射**后同一个结构量就站住了 ⇒ **本节的结论只对比例形式成立**，
> 标题已按此限定（原文写作"没有一种纯结构量能解释"，**过强，已改**）。

四种口径全部试过，逐条列出（**同一个坑的四种表现**）：

| 结构量 | K | 中位\|残差\| | 最大 | 超25% | 判定 |
|---|---|---|---|---|---|
| 地址序区间（T1 污染） | 6.73 | 30.6 | 190.8 | **13/19** | ❌ |
| **入口块条数**（最干净） | 9.50 | **33.0** | 192.5 | **11/19** | ❌ |
| 可达闭包（T2/T3） | 1.744 | 3.9 | 62.1 | 3/19 | ⚠️ **假绿，见下** |
| 入口→尾块 路径集（T4） | 0.233 | 6.0 | 68.0 | 2/19 | ⚠️ **假绿，见下** |

**★ 后两行是假绿，必须点名**：它们之所以"看起来很好"，是因为
**在循环里，任何一个 case 都能经"尾块 → 分派块"到达其它所有 case**
⇒ 该口径对每个 op 都给出**几乎相同的数**（283 或 330 条）。
一个对谁都一样的预测值，其残差就只剩下"实测均值"这一项 ⇒ 中位残差自然小。
**这不是模型好，这是模型没有区分度。**（D3 的秩相关 ≈ 0 / 0.08 正是这个症状。）

⇒ **根因**：`P(入口) ∩ P⁻¹(尾块)` 里，`P⁻¹(尾块)` **就是整个函数**（谁都能走到尾块）。
所以"每路由执行哪些块"**在纯 CFG 可达性下不可恢复** ——
区分一条路由用的是哪个 case，靠的是**运行期的 op 值**，而那是数据，不是结构。

**⇒ 由此得到一条对方法本身重要的结论：**

> **纯控制流结构量不足以确定逐原语成本。**
> 要往下走，必须引入**数据流/操作数**信息（哪条指令吃哪个寄存器的值），
> 即从"块可达性"升级到**依赖链**（这正是 §2 里 STA 的第三步，也是 §5 的 N2）。

**唯一仍然站得住的弱结论**：入口块条数与实测成本的 **Spearman 秩相关 = 0.639**
⇒ 两者**正相关但不是充分统计量**（11/19 超 25%，且指令数多的 `CNT/EDGE` 明显被高估）。
⇒ "指令条数"这个量**有信息、但不够**。

### 4.5 ★★★ 尝试三（步 A′）：**仿射结构模型** —— 第一个站得住的模型

★ **线索来自数据而不是凑**：入口块只有 **2 条**指令的 `DIRECT` 实测 **56 cyc**；
4~5 条的 `AND/OR/NOT` 实测 **71~78 cyc**。若成本 ∝ 条数，这些点该逼近 0
⇒ 存在一个 **~60 cyc 的地板**（每路由与 op 无关的固定开销）。
**⇒ 正确形式是仿射，不是比例。**

| 模型 | 中位 LOO 残差 | 最大 | 平均相对 |
|---|---|---|---|
| 比例 `K·n`（零截距） | 35.4 | 193.5 | 56.4% |
| **仿射 `c0 + m·n`** | **8.0** | 63.1 | **13.2%** |

```
cost ≈ 67.7 + 1.16 × (入口块条数)      ← c0 > 0（H2 判据通过）, LOO 中位残差 8.0 cyc
```

★ **而且两个离群点正好是物理上应该离群的**（这比"误差小"更有说服力）：

| op | 入口块 | 实测 | 仿射预测 | 残差 | 解释 |
|---|---|---|---|---|---|
| **PID** | 18 | **145** | 88.5 | **+56.5** | ★ 含 **`vdiv`**：ARM 文档 VDIV 是 **2~14 cyc 操作数相关** ⇒ 结构模型**不可能**算准它（正是 §5 的 **N3**）|
| **DIRECT** | 2 | 56 | 70.0 | **−14.0** | ★ 空操作: 结构侧没有可摊薄的工作, 固定项占比最大 ⇒ 偏差方向也符合预期 |
| 其余 17 个 | — | — | — | **全部 ≤ 18.3**（多数 < 10） | 无系统偏差 |

★ **H3 自洽性检查**：截距 67.7 cyc ÷ 共享尾块 27 条 = **2.5 cyc/条**，
而斜率是 **1.16 cyc/条** —— 两者**同一量级**，说明"固定开销"与步 A 独立量出的
"每路由共用尾块"是**自洽的**（两个独立路径得到一致结论）。

**⇒ 这是本轮第一个"结构 → 成本"站得住的定量关系**，而且它把误差来源
**收敛到两个已被预言的具体项**（操作数相关的除法、空操作）。

### 4.6 三种口径的最终对照（**含两次假绿的点名**）

| 结构量 | 预测形式 | 中位 LOO | 超25% | 判定 |
|---|---|---|---|---|
| 地址序区间（T1 污染） | 比例 | 30.6 | 13/19 | ❌ |
| 可达闭包（T2/T3 污染） | 比例 | 3.9 | 3/19 | ⚠️ **假绿**（对每个 op 给出几乎相同的数, 秩相关 ≈ 0） |
| 入口→尾块路径集（T4） | 比例 | 6.0 | 2/19 | ⚠️ **假绿**（同上, 283/330 条） |
| **入口块条数** | **仿射** | **8.0** | **2/19** | ✅ **成立**（离群点可解释） |

★ 注意第 3、4 行的中位残差比第 5 行**更小**，但它们是假绿 ——
**判据必须同时报"区分度"（秩相关 / 预测值极差），否则"常数预测器"永远最优**（§5.49）。

### 4.7 依赖链（STA 第三步）结果：**关键路径是最差的预测量**

按 §2 的 STA 定义实现 def-use 依赖链（节点权重 = Cortex-M7 **文档**延迟，非实测）：

| 量 | Spearman(·, 实测) |
|---|---|
| **依赖链（关键路径）** | **+0.180** ← **最差** |
| 串行和（所有指令延迟之和） | +0.586 |
| 入口块条数 | **+0.641** ← 最好单项 |
| FP 指令数 | +0.583 · 分支数 +0.539 · ALU +0.474 · 访存 +0.282 |

**★ 结论：本内核是「吞吐受限」，不是「延迟受限」。**
关键路径最差正说明**不是"等最长的依赖"在决定时间** —— 这些原语体是直线代码、
M7 双发射 + 转发让独立指令重叠，于是**总工作量**（条数）比**最长依赖**更能解释时间。

⇒ 这解释了一个反直觉的事实：**照搬 FPGA 的"关键路径"直觉在这个微核上是不适用的**，
因为 FPGA 的组合逻辑是**真串行**（信号必须沿路径传播），而超标量 CPU 的直线代码**可以重叠**。
**要照抄的是 STA 的"结构可算"精神，不是"关键路径"这个具体量。**

---

## 5. 明确的未算部分（**"未算" ≠ "算不出"**）

本节是本文对"先计算可算的"这句要求的执行。每一项都标注**为什么现在没算**。

| # | 未算的部分 | 为什么现在不算 | 它属于哪一类 |
|---|---|---|---|
| N1 | ~~共享尾码的归属~~ **已完成** | 已定量（尾块 = 块6 的 27 条 + 块12；分派块 3 个）；但把它归入"每路由成本"后**仍无区分度**（§4.4）| ✅ 已做，结论是"不够" |
| N2 | **依赖链（真正的 STA）** | ★ **本轮结论：这是唯一出路** —— 纯 CFG 可达性已被证明不够（§4.4），必须引入**数据流/操作数**（哪条指令吃哪个寄存器的值）| **方法主体，下一步** |
| N3 | **除法/开方 `VDIV`/`VSQRT`** | 时延**依赖操作数**（迭代式）⇒ 文档只给上界，只能算**界**不能算精确值 | 物理上只能给界 |
| N4 | **分支预测** | 预测器状态依赖历史 ⇒ 只能**当最坏处理**（保守假设） | 需要保守假设 |
| N5 | **存储缓冲深度 / 写缓冲** | 类似分支预测，依赖历史与总线状态 | 需要保守假设 |
| N6 | **Flash 取指（L1 I-cache / prefetch）** | FLASH 档下取指延迟依赖 cache 状态 ⇒ 这是**为什么交付走 ITCM** 的量化理由之一 | 需要 §3 的通道证据 |
| N7 | **AXI / MDMA 争抢** | 已有实测（典型拍 0.26 cyc/拍）但不是**结构**结论 | 属 E3 干涉通道 |
| N8 | **`?` 类指令（128 条，21.4%）** | 分类器未识别的助记符（`vmov`/`it`/`vseleq`/`vcmpe` 族）⇒ 分类表要补全 | 工具缺口，**便宜** |
| N9 | **拍内非引擎工作** | `di` = 整段 ISR（含 bb_kick/adc_poll_kick/rtc_latch/i2c_sm_tick）⇒ 观测量与"引擎扫描"不是同一个量 | 观测口径，见 §6 |
| N10 | **分支两侧的不可行路径** | 基本块把条件分支**两侧都算进块**，而一拍只走一侧 ⇒ 这正是"块条数"高估大块的原因（§4.5）| 需要路径可行性分析 |

★ **这 10 项里没有一项是"证明算不出来"**：N1 已完成（结论是"单靠它不够"），
N2/N8/N10 是**方法没做完**，N3/N4/N5 是**只能算界**
（但 FPGA 的 STA 本身算的就是最坏路径，所以这不构成路线问题），
N6/N7/N9 是**需要另外的实验**。

---

## 6. 一条必须同时修的前提：观测量与要解释的量不是同一个

**目前的实测 `di` 量的是整段 ISR，不是引擎扫描段。**

```
di = 引擎扫描 + bb_kick + adc_poll_kick + rtc_latch + i2c_sm_tick + 数据相关分支
```

⇒ 即使结构模型完美，它解释的也是"ISR 有多长"，而需求要的是**"这个程序要跑多少周期"**。

★ 已有的一条实测支持"这项可以减出来"：黑匣子开关（`0x39` op=4）做对照，
`--bb on/off` 两次测得 **1375.3 vs 1375.5 TB**（标准误 1.8）⇒ **`bb_kick` 每拍净成本 ≈ 0**，
即"逐个关掉拍内工作、看 `di` 变化"这条**减法的路子是可行的、且可控**。

⇒ **在结构模型成形之前或同时**，需要把扫描段单独夹出来（引擎内加一对时间戳）。
否则结构侧算出的数与实测侧的数**不是同一个量**，没法比对。

---

## 7. 下一步（按依赖关系排序，每步都带**可失败判据**）

★ **步 A 已执行完毕，结论是"做到了但不够"**：共享尾码已定量分离（T1~T4 四个 CFG 陷阱全部修掉），
但把它归入"每路由成本"后**仍然无法区分原语**（§4.4）⇒ **纯 CFG 可达性这条路到此为止**。

| 步 | 做什么 | 可失败的判据 | 状态 / 依赖 |
|---|---|---|---|
| **A** | 共享尾码归属 + 真 CFG | 入口块 ⊆ 每路由块集 | ✅ **已做**：71 块、19 入口块、尾块 27 条定量 |
| **A′** | 口径升级：依赖链（数据流） | Spearman > 0.8 | ✅ **已做**：**关键路径只有 0.180（最差）** ⇒ 内核是**吞吐受限**（§4.7）|
| **A″** | ★ **仿射结构模型**（`c0 + m·n`） | LOO 中位残差 < 15 cyc；`c0 > 0`；截距与独立量自洽 | ✅ **已做**：**67.7 + 1.16n，LOO 中位 8.0 cyc，2/19 超 25%**（§4.5）|
| **B** | 补全指令分类表（消掉 `vmrs` 58 + `ite` 2 = 10%） | `?` 应降到 <5% | ✅ **已做**：占用表覆盖后 **未识别 0 种 / 0 条**（§4.8）|
| **C** | 用 Cortex-M7 **文档延迟**给模型赋权（把 `m` 从实测里解放出来） | Spearman > 0.8；结构 ≥ 实测 | ❌ **已做，未达标**：**0.583**，且 **0/19 全低估**（§4.8）|
| **C1** | ★ 找缺失的 ~34 cyc 固定开销 | 结构 ≥ 实测 >80% | ◐ **已做**：补上分派块+前导后 **19/19 结构 ≥ 实测**（有了界），**但过估 ~75%（不紧）且前导计价混入非每路由分支**（§4.9）|
| **C1b** | ★ **前导按"路径"而不是"区间"计价**（只走每路由实际那条腿）| 固定开销应落到 **56~70 cyc**；仍须保持 `结构 ≥ 实测` | **下一步（最高优先）** |
| **C2** | 重审占用表：文档值把多数类记为 1、除法记 14 ⇒ **排序反而比裸条数差**（0.583 < 0.641）| 修正后 Spearman 须 **> 0.641**（裸条数基线）| 依赖 C1 |
| **C′** | **把 `vdiv` 单独参数化**（操作数相关） | PID 残差应从 **+75** 降到 <20 cyc | 高价值（PID 是最贵的原语）|
| **D** | 把扫描段从整段 ISR 里夹出来 | 夹出段 + 已测拍内工作 ≈ 原 `di`（差 <5%）| 未做；**需烧一次实验档**，可并行 |
| **E** | 用 D 的数据重做 C 的比对 | 结构预测 vs 扫描段实测，逐原语对齐 | 依赖 C, D |

★ **A′/A″/B/C 完全不依赖板子，可以离线做**（只需要 ELF）。
★ **D 需要烧一次实验档**，属于"观测口径"的修正，可并行。

★ **本轮的路线修正（重要）**：原计划"靠依赖链（关键路径）"是**基于 FPGA 直觉的误判** ——
实测证明关键路径是**最差**的预测量（§4.7），因为 FPGA 的组合逻辑真串行、
而超标量 CPU 的直线代码可以重叠。

★ **现状一句话**（防止把进度读成结论）：
**"结构能排序"有证据（0.641）；"结构能算绝对值"还没有（0/19 全低估，缺口 ~34 cyc 已定位）。**
⇒ **照抄 STA 要抄"结构可算"的精神，不是"关键路径"这个具体量。**

---

## 8. 这份文档目前**不**主张什么（防止被过度引用）

本文**尚不主张**：

1. ❌ 不主张"每个原语的执行时间已经能算出来" —— §4 的**比例**口径全部不成立
2. ❌ **不主张"关键路径/依赖链能解释成本"** —— 实测 **Spearman 仅 0.180，是全部量里最差的**（§4.7）
3. ❌ 不主张"可达闭包/路径集口径效果好" —— 那是**假绿**（§4.6：对每个 op 给出几乎相同的数，
   于是残差只剩常数项）。**这一条尤其要写下来，因为它看起来最漂亮。**
4. ❌ 不主张"模型已可用于闸门" —— `67.7 + 1.16n` 的系数目前**仍来自实测拟合**，
   还没做到"用文档延迟独立算出来"（那是 §7 的 C 步）
5. ❌ 不主张 `vdiv` 的代价已被模型覆盖 —— 它是 PID 残差 +56.5 cyc 的**已定位来源**，
   但**尚未单独参数化**（§7 的 C′）

本文**可以主张**：

1. ✅ **结构是完全可提取、可复算的**（S1~S9，含 71 个基本块与 19 个 op 入口块，机器可判定）
2. ✅ **"可算"是架构选择的结果**：路由表把通用 WCET 的四大难题全部绕开（§1）
3. ✅ ★★ **存在一个站得住的仿射结构模型**：`cost ≈ 67.7 + 1.16 × (op 独有块条数)`，
   **LOO 中位残差 8.0 cyc、平均相对 13.2%**，且**两个离群点都被物理原因解释**
   （PID 的 `vdiv`、DIRECT 的空操作）—— 见 §4.5
   ⚠️ **但它的系数仍来自实测拟合**，不是"零实测输入的预测"
4. ✅ ★★ **存在一个成立的保守上界**：按文档占用 + 每路由块（尾块+分派+前导）计价，
   **19/19 满足 `结构 ≥ 实测`**（§4.9）—— **它没有反例**，只是不紧（过估 ~75%）
5. ✅ **截距与另一个独立量自洽**：67.7 cyc ÷ 共享尾块 27 条 = 2.5 cyc/条，
   与斜率 1.16 cyc/条同量级（§4.5 H3）
6. ✅ ★★ **本内核是吞吐受限而非延迟受限**（§4.7）—— 这是一条**方法层面的实质结论**，
   它纠正了"照搬 FPGA 关键路径"的直觉误判
7. ✅ **未算的部分被明确列出并分类**（§5，10 项），**没有一项被证明"算不出来"**

> ★★ **"紧"与"零实测输入"尚未统一** —— 目前是二选一：
> 拟合形式准（8.0 cyc）但带实测系数；文档形式零实测但松（过估 ~75%）。
> **把两者同时做到，是明确且唯一的下一步。**

> ★ 最后一句是本文最需要的态度：**"还没算"和"算不出来"是两件事。**
> 本文的价值主要在于把前者**逐条列清并给出判据**，而不是给出一个漂亮的总数。
### 4.8 ★★ 尝试四（步 C）：**用文档延迟独立算** ⇒ ❌ 未达标（但缺口被定位得很干净）

把 A″ 里**从实测拟合**的系数换掉，改成全部由结构 + ARM 文档表算出：

```
结构成本 = c0_结构 + Σ(每类指令的文档占用 × 该类条数)
c0_结构  = 共享尾块（步 A 独立量出的 27 条）按**同一套文档表**累加 = **34 cyc**
```

| 判据 | 目标 | 实测 | 结果 |
|---|---|---|---|
| **J1** 排序 Spearman(结构, 实测) | > 0.8 | **+0.583** | ❌ |
| **J2** 方向性（结构 ≥ 实测） | 越高越安全 | **0/19 = 0%** | ❌ **全部低估** |
| **J3** 离群须为 `vdiv`/空操作 | PID/RATE/DIRECT | PID(+75)、**LUT(+46)**、LPF(+40) | ❌ LUT 不在预期内 |
| **J4** 占用整体缩放 ±1.5× | ρ 不变 | +0.583 → +0.583 | ✅（缩放不改**秩**）|

**★★ 缺口被定位得非常干净（这是本步真正的产出）**：

1. **低估是系统性且近似恒定的**：19/19 全部低估，差额 **+12 ~ +75 cyc**，
   多数集中在 **+20 ~ +40**。
   ⇒ 说明有一块**每路由固定开销没被算进去**。
2. **量级对照**：`c0_结构 = 34` vs A″ 从实测拟合出的截距 **67.7** ⇒ **差约 34 cyc**。
   ⇒ **这块"缺失的固定开销 ≈ 34 cyc"就是全部缺口的来源。**
   它不在我量的那个尾块里（尾块只解释了固定开销的 **一半**）。
3. **★ 文档占用权重不如"裸条数"**：`Spearman(结构, 实测) = 0.583`
   **低于** `Spearman(入口块条数, 实测) = 0.641`（§4.7）。
   ⇒ 按类加权**没有带来信息，反而损失了排序** —— 因为文档值把多数类都记为 1，
   而除法给了 14，于是 PID 的相对位次被**推得过高**（实测名次第 1，结构给它的相对位次更高）。

**⇒ 步 C 的结论：文档延迟模型目前不成立**，但它给出了一条**明确的下一步**：
去把那 **~34 cyc 的固定开销**找出来（候选：分派块本身、`read_source` 的分支、
每路由的 `src`/`wb` 有限性检查）——**而不是继续调占用表**。

★ **另一条必须记下的方法论教训**：J4 的"缩放不变性"是**秩统计量的天然性质**，
不是模型稳健的证据。把它当"稳健性通过"会**高估**结论强度 ——
真正该做的稳健性检验是"**换一个独立的构建/程序，看同一套文档表是否仍然给出同样的排序**"。

### 4.9 ★★ 尝试五（步 C1）：找那块缺失的固定开销 ⇒ **找到的是"界"，不是"精确值"**

步 C 的缺口是 `c0_结构 34` vs `实测截距 67.7`。逐步补：

| 口径 | 固定开销 | 结构下限 | 与实测下限 56 之差 | 残余缺口中位 | **结构 ≥ 实测** |
|---|---|---|---|---|---|
| (c) 只尾块 | 34 | 34 | **−22** | +29 | **0/19** |
| (b) 尾块+分派块(8) | 42 | 42 | −14 | +21 | 0/19 |
| (d) 尾块+分派+**每路由前导**(56) | 98 | 98 | **+42** | −35 | **19/19** |

★★ **这一步的结论必须分开说，否则会被读成"成功"或"失败"**：

1. ✅ **拿到了一个成立的方向性界**：(d) 口径下 **19/19 全部满足 `结构 ≥ 实测`**。
   这正是"可证上界"需要的那一半 —— **它没有反例**。
2. ❌ **但它不紧**：固定开销 98 vs 实测下限 56，**过估 ~75%**。
3. ⚠️ **而且"每路由前导"这 56 cyc 本身是可疑的**：我取的地址区间
   `0x0D3C..0x0DE6`（47 条）里含有**"路由非 ACTIVE ⇒ 跳过"那条分支的腿** ——
   它**不是每路由都执行**。所以 98 这个数**同时含"漏算"与"多算"**，
   它"碰巧"给出 19/19，**不能当作模型正确的证据**。
   ⇒ **必须如实记为"一个偏保守的估计"，而不是"缺口已补齐"。**

★ **本步真正的产出是一条边界声明**：

> **现状**：结构模型可以给出**成立的保守上界**（无反例），但**不紧**；
> 也可在拟合形式下给出**较准的绝对值**（LOO 中位 8.0 cyc），但**系数来自实测**。
> **两者尚未统一** —— 把"紧"与"零实测输入"同时做到，是下一步。

**剩余缺口的方向（点名，不许静默）**：
- 前导里"每路由只走一条腿"的分支（ACTIVE 判定、period 三档分派、`read_source` 按 src_type 分派）
  ⇒ 结构侧必须按**路径**计价，而不是按区间求和（这正是 STA-17 线性走地址失败的同一原因）
- `read_source()` 的间接跳转本身
- 每路由的 `_finite_f(src)` / `_finite_f(wb)` 检查

### 4.10 ★★ 尝试六（步 C1b）：用**显式不相交区间**计价 ⇒ **界成立（19/19），但仍偏松（2.0×）**

按 STA-21 打印出的指令事实，把每路由固定开销拆成**两两不相交**的地址区间：

| 区间 | 地址 | 条数 | 文档占用 |
|---|---|---|---|
| 活跃前导（ACTIVE 判定 / 周期分档 / `read_source` 分派） | 0x0D3C..0x0D8A | 22 | 27 cyc |
| 公用段前半（param/state 地址 + wire2 + 有限性检查） | 0x0D8A..0x0DE6 | 25 | 29 cyc |
| 分派块（`tbh`） | 0x0DE6..0x0DF4 | 5 | 8 cyc |
| 公用段后半 | 0x0DF4..0x0E5C | 19 | 22 cyc |
| 尾块（写回 + 指针推进 + `cmp`） | 0x0E5C..0x0E9C | 21 | 26 cyc |
| **合计（活跃路由）** | | **92** | **112 cyc** |

| 口径 | 固定开销 | 结构下限 | 与实测下限 56 之差 | **结构 ≥ 实测** | 过估比 |
|---|---|---|---|---|---|
| (b) 只尾块+分派 | 34 | 34 | −22 | **0/19** | — |
| (c) + 活跃前导 | 91 | 91 | +35 | **19/19** | 1.62× |
| **(d2) 本版不相交区间** | **112** | **112** | **+56** | **19/19** | **2.00×** |

★ **判据 Q1（固定开销落在 56~80）未通过** ⇒ **如实记为"界成立但偏松"**。

★★ **本轮又抓到两个"重复计价"** —— 而且都是**机器判据**抓的，不是人眼：
第一版把 `common` 写成 `0x0D8A..0x0E5C`（**包含了 `dispatch`**），
`tail` 又与 `skip_leg` 相叠。修法是**在构造处切开**，并保留一个运行时断言：

```python
# 区间两两不相交的机器判据
for i in range(len(spans)-1):
    if spans[i][1] > spans[i+1][0]:   # 重叠 ⇒ 报出来
```

> **教训：把"每路由固定开销"拆成显式区间时，区间必须两两不相交 ——
> 否则就是「同一个语义两处存放」的计价版。**
> 而这个错误**不会让数字变红**：它只会让固定开销变大（界更松），
> 而"界更松"永远是"安全"的方向 ⇒ **没有任何下游判据会报警**。
> ⇒ 所以它必须由**结构断言**来抓，不能指望结果判据。

**⇒ 步 C1b 的结论**：文档侧模型能给出**成立且无反例**的保守上界（**19/19**），
但**偏松 2.0×**。**"松"是界可以接受的代价，但它还不是"能算出来的执行时间"。**

