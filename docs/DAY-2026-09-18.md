# 2026-09-18 工作日总结（00:08 → 次日 00:14，**80 个提交**）

> 这一天的主线只有一句：**把"说的话"变成"能失败的事实"**。
> 具体是三件事：①把"可算"从宣称变成**实测过的门**；②把"秒/毫秒"的语义在三个域端到端验证；
> ③把门面（README / SCOPE / claims / 保障图谱）改成与事实一致。
> 而这一天最值钱的产出不是新功能 —— 是**抓出 9 处我自己的缺陷**，其中 5 处是**判据/机制自己坏了**。
>
> 本文是**日汇总**（可读的一页）。逐段细节见 `.workbuddy/memory/2026-09-18.md`（第一~十九段），
> 撤回台账见 `.workbuddy/memory/RETRACTIONS.md`（P24~P32），计划与进度见 `docs/PLAN-completion-2026-09-18.md`。

---

## 一、日程（按提交时间，全部可用 `git log` 复核）

| 时段 | 主线 | 代表提交 | 结果 |
|---|---|---|---|
| 00:08–00:09 | 门面重写 | `6f918d5` `d4a25b7` | README 外人可读；并**当天就留案**更正上一提交 message 里两处体积数字 |
| 09:11–10:53 | **运动形状线**（黑匣子 + SD 卡两条路） | `8f1f21c` … `963bd66` | 黑匣子路径打通（覆盖不足判 SKIP）→ 卡上"找不到运动"根因 = **日志没在记** → 74% 之谜：先撤回"过冲"，再定案 `hz==0` 走**立即停**并绕过斜坡 → ③层改写下限 0.15 A 验证通过 → **①层一行根因修复 + A/B**（曲率 0.40→0.99）→ 全量回归**只差一行且已归因** ⇒ **这条线闭合** |
| 11:13–12:49 | 收口 + 工具链 + 可信度复核 | `f08c054` `8a362fa` `ff1b09b` | 实验完成度对账（答案是"没有完成"）· 记忆二次拆分 29.4→17.9 KB · 推送工具链（裸 push→H723PLC，对外→dcl-controller 人工发布）· **首次"原始对话口径"复核**（7 块，头号指控 4/7 过度）· 记忆继承断层扫描 |
| 13:26–14:32 | **超载实验**（G3 缺口闭合） | `3dfe2f2` `8f1fac6` `c460e1b` `3885fb6` | 预算门从"假定"到"实测过"；补 FLASH 路径成本价后**静态门首次真的拦住超载**；★ 单标量 3.03 **当天被自己的实验证伪** ⇒ 换**两张逐原语表** |
| 15:49–18:04 | STA 线（静态结构 ⇒ 执行时间） | `51caba4` … `9bbbe39` | 拍级确定性决定性问题第一次被回答（换仪器后 0.06 cyc）· 可加性**不成立**但非可加性成了一条**精确的律** · 第一个站得住的仿射结构模型 · 文档延迟模型**未达标**但缺口定位到 ~34 cyc |
| 18:27–20:25 | **成本模型线** | `b9845cb` … `ee8fd04` | div0 的 2.1 倍之谜破除 · 19 原语 `m_op` 全表 + 两条独立方法交叉验证（差 1.0%）· ★ **`div2` 相位溢出 6 位字段 ⇒ 36 条路由静默不执行（已修并验证）** · ★ `DT_SLOW` 偏大 56%（改为**派生**）· 门的**转变项**进门（纯可加不是上界）· FLASH 档门**端到端验证**（原 TIMEOUT 是烧录假象）· 转变代价 `k` = 两个 op 的成本差（**R²=1.0000**） |
| 21:22–23:06 | **「秒」语义 + 拍长统一** | `c96aeb1` `41ed742` `08188a8` `812b687` | E-Q：顺序档**第二份 dt 表**（div2 超时早 36%）⇒ 合并成一份 · E-R：限时把拍→ms 的**余数丢掉**（部署条件流逝 **747/1000 ms·s⁻¹**，且**依赖有没有人在读**）· E-S：「走 N 步」时长与过冲，更正三处注释里的**假界**（实测 **282 倍**）+ 一次自我撤回 · E-U：**拍长可配**（+6 处硬耦合）· `C_other` 拆解（542.4 TB）· 门边界 · 两处判据自我更正 |
| 23:33–23:47 | 计划收口 | `3e84703` `f629b03` `bd2ccae` | 阶段 1 门面=事实 + **公开仓发布** + 2.1/2.2 固件欠账 → 然后 E-V 把"闸门真洞"这个判定**当场推翻** |
| 次日 00:00–00:14 | **③层表达力 + 机制**（本轮） | `6bfcf27` `e89aaaf` `8ca69a8` `d56a667` `e53479f` `483dd7d` | E-W/E-X（3.1/3.2/3.3）· E-Y（3.4）· 2.4 审计分诊 · 2.3 耦合 #6 · 4.3 台架依赖机制 —— 七项，全部有判据、全部已发布 |

---

## 二、五条战线的成果

### 1. 「可算性」：从宣称到**实测过的门**
- 成本模型 `di(t) = C_other + C_scan(op) + Σ m_op×条数 + k×转变数`，**每一项都实测**：
  `C_other = 542.4 TB`（骨架）· `C_scan(op) = 33~55 TB`（19 项）· `m_op` 19 项表 · `k = Δc`（R²=1.0000）。
- 端到端留点验证（E-K）：**模型能在 0.04% 内算出程序执行时间**，跨 division 成立。
- 可加性**不成立**（E-L/E2，低估最多 5.3%），但含转变项的**上界成立且部署期可算** ⇒ 门加转变项。
- 门边界**逐位复现**：FLASH DIRECT **105/106**、PID **60/61**；交付档**结构性不触发**（128×56 = 门 28%）。
- ⚠️ 诚实边界：模型是**标定**的，不是纯计算（E-F 是负结果）。

### 2. 「秒/毫秒」语义：三个域端到端验证
- E-Q（时序 dt 语义）· E-R（运动域声明时间量）· E-S（"走 N 步"时长与过冲）—— 判据 **30+11+11**，末者 **11/0/0**。
- 修掉的真缺陷：顺序档第二份 dt 表（div2 超时早 36%）· 限时余数丢失（**747/1000**）· 三处注释假界（实测 282 倍）。
- **拍长统一**：一处定义（`clock.h`）+ 别名/派生 + 3 条断言；200 µs 档语义**逐条对拍 10/10**。

### 3. 运动形状线**闭合**
74% 之谜 → ①层一行（`sub=17` 斜坡在主循环快于 1 ms 时语义不成立）→ A/B 验证 → 全量回归**只差一行且已归因**。

### 4. 门面 = 事实
README 重写 + §诚实标注 **3 条** · `SUPPORTED-SCOPE.md` 新增 **G10 八行能力表** ·
`claims.md` 登记 · `ARCH-GUARANTEE-MAP.md` · **公开仓 `dcl-controller` 已发布**（发布差额 0，人工 opt-in 闸门）。

### 5. ③层表达力 + 机制（本轮七项）
| 项 | 判据 | 关键结论 |
|---|---|---|
| E-V（3.1 前置） | 9/0/0 | 我判定的"闸门真洞"**被实测推翻**；真结论是"摊薄式对的"+"它与 staging 之间一条**未声明的依赖**"；代码回退（指纹逐字节不变） |
| E-W（3.1） | 18/0/0 | `OUTPUT ... PERIOD=` 可用；★ `PERIOD=10ms` 是**假承诺**（真实 div2 = **6.4 ms**）；在板三档速率 2.0000 / 0.2000 / 0.03125（偏 +0.0/+0.1/+0.2%） |
| E-X（3.2/3.3） | 14/0/0 | `ABS = MAX(x, 0−x)` 组合展开（在板**按位模式**等于 `abs()`，含 `−0.0→+0.0`）· 变量阈值 `SUB`+`CMP` · 数字文法 **11 份→1 份** |
| E-Y（3.4） | 8/0/1 | LUT 表**所有权量清**：`0x23` 写得进 · `deploy` 不碰 · SD/持久化不带 · `0x13` 清零 · `fill_tables` 会覆盖 ⇒ 三条架构路线待拍板 |
| 2.4 | 13/0/0 | 审计 1.1 分诊：`record(...,True)` **15 处全部可失败**（分支内 6 + 守卫落空 9 + **真无条件 0**）⇒ `--offline` **12 PASS / 0 FAIL**；新增 `--selftest-11` |
| 2.3（E-Z） | 8/0/0 | `EXEC_BUDGET_CYCLES` 改**派生**（拍长×80%）+ 四断言 ⇒ 关掉**耦合 #6**；★ 第四条"展开式逐位相等"才承重；编译器当预言机 + 变异对照 |
| 4.3 | 13/0/0 | 台架依赖**分开记账**成机制：登记表 + 审计 axis 1.3 机械校验（条目指向不存在的判据就 FAIL） |

---

## 三、数字

| 项 | 值 |
|---|---|
| 提交 | **80**（09-18 00:08 → 09-19 00:14） |
| 改动文件 | **137** |
| 今日新增工具 | **65**（`tools/*.py` 总数 **164**） |
| 文档 | `docs/*.md` **90**（递归 **107**） |
| 交付档指纹链 | `80c05c0e…` → `6af8ca28…` → `40f24cf1…` → `322f6f27…` → `e5a9aaa6…` → `c7c366c1…` → **`6daa7e65…`（当前）** |
| 200 µs 档 | `38e8d180…` → **`87a4a5ac…`**（2.1/2.2 之后重建） |
| 板级状态 | 协议探活 **12 PASS / 0 FAIL**；两档构建 0 warning；顺序域已清除（`N_SEQ=0`） |
| 远端 | `origin`(H723PLC) 与 `dcl`(dcl-controller) 均在 **`483dd7d`**（`git ls-remote` 权威） |

---

## 四、★ 这一天我自己造的缺陷（9 条，全部已进撤回台账）

| # | 我曾以为 | 真值 | 是什么抓到的 |
|---|---|---|---|
| **P24** | 路径成本差可用**一个倍率 3.03** 表示 | **错**：逐原语 2.98×~4.41×，标量**既非上界也非下界**，ACK 了实跑 77.5% 拍的程序 | 自己的对照实验 |
| **P25** | 成本表取 `g_eng_cyc_min` 就行 | **口径错**：门要**最坏**情况；FLASH 下 min 给 363 而实测 434 | 板级实测 |
| **P26** | 审计 = **12 PASS / 0 FAIL** | 复跑不出来；且**真因是审计自己不分诊**（15 处全可失败，我"至少 1 处恒真"的猜测**错了**） | 复跑 + 逐处读上下文 |
| **P27** | 到点自停延迟 **≤0.37 ms**（三处注释） | 0.37 ms 是**名义**周期；实测注入阻塞后过冲 **282 倍** | E-S |
| **P28** | 紧轮询会把过冲放大 | **单点 A/B 给出反号结论**；区间重叠 ⇒ 连方向都不能宣称 | 自己第三次运行 |
| **P29** | `RATE`/`PID-D` 的 div0 结构性不可测 | **理由反了**：是我当初删了锁存路由 | 补上后一次就测到 |
| **P30** | 交付档已恢复（脚本打印指纹 + 探活 12/0） | **CMake 选项粘性**⇒ 产出的是 **200 µs**"交付档" | E-Q 的 Q0a（与源码无关的独立测量） |
| **P31** | 预算闸门有"摊薄 vs 每拍"的**真洞** | **不是洞**：staging **覆写**载荷相位 ⇒ 闸门与 staging 之间是一条**看不见的依赖**；我改的那版会**误拒**合法程序 64× | E-V 实测（128 条全 phase 0 只得 262 cyc） |
| **P32** | 恢复脚本加了指纹校验 ⇒ 这道防线闭环 | **只做了一半**：`EXPECT_MD5` 自 2.1/2.2 起过期 ⇒ 脚本**每次都中止**（恢复路径已死） | 重建交付档时发现 |

> **规律**：9 条里有 **6 条**是"**判据/机制自己坏了**"（P26/P28/P29/P30/P31/P32）——
> 比固件缺陷更贵，因为它们会让后续每一次判断都建立在错前提上。
> 另有 2 条是"**中心值/名义值当界限**"（P24/P25/P27）—— 同日第三次。

---

## 五、待决 / 待办

**待你拍板（本轮唯一一项）**
- **LUT 表住哪**：A 扩 deploy 载荷（我推荐）· B 进持久化镜像（要动 `PERSIST_VERSION`）· C 工具侧约定（表与程序会不同步，实测证明风险**可达**）。三条都要顺手回答"`0x13 RESET` 之后表该不该回来"。

**计划剩余**
- **2.5** `C_other`（542 TB）的内部组成：adc / rtc / i2c / 黑匣子各占多少 —— 模型第二大常数，现在是一整块黑箱。
- **4.1 文档 ⇄ 文档闸门**（跨文档否定式断言检查）—— 项目自评的"**最大系统性风险**"。
- **4.2 撤回进台账的机制** —— P29/P30/P32 都是**事后复盘**才想起来的，不是流程逼出来的。
- **4.4 前两条的机器拦**：物理常数一处定义 · 判据阈值由被测量派生（已立规、已在 E-W/E-Z 实践，**尚未机械化**）。

**挂账（已知、不在回归判据表）**
- `h723_step_ramp_test.py` R5（台架依赖，已登 `RIG-1`）· E-Y 的 LUT 现状观察（历史依赖，已登 `RIG-2`）
- `RATE`/`PID-D` div1 次要轮询 SKIP · `nrun<1` 不可测 · `C_scan(op)` 只在名义 div0 配置下测过

---

## 六、不能宣称的（诚实边界）

1. 成本模型是**标定 + 留点**，不是纯计算（E-F 是负结果）。
2. 拍级**绝对**精度 ~0.1%；「走 N 步」的过冲界 = **主循环最大间隔**（不是 0.37 ms），规则 `f ≤ k / 主循环最大间隔`。
3. 门的**摊薄口径成立只因为** staging 轮转铺相 —— 这是一条**未声明的跨函数依赖**，现已立判据守（载荷相位一被尊重，V2/V3 立刻失败）。
4. `PERIOD=` 只承诺 **div 档（速率）**，**不承诺相位**（相位归固件）。
5. LUT 表今天**无主**：住易失内存、复位即失、还能被 reinit 的斜坡覆盖。
6. 200 µs 档：E-Q/E-R 的 A/B 在板上跑过（全绿），但**本轮新增的判据只在 100 µs 档跑过**；200 µs 档只做了整机构建 + E-Z 的编译期预言机。
7. 阶段 3A 的轨迹形状是 **4 PASS + 1 SKIP（覆盖不足）** —— 不是"通过"。
