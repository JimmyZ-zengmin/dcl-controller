# H723 迁移进度盘点（开发规划基线）

> ## ⚠️ 历史快照，勿作现行依据（2026-09-15 加）
> 本文是 2026-09-11 当天的状态快照。此后发生了多轮结论推翻与修复，**现状请看**
> `docs/audit/H723-MOTION-QUALITY-AUDIT.md`（最新实测总账）
> 与 `docs/ARCH-TIMELINE-CPU-MDMA.md`（顶部修正段）。
> ★ 其中 09-11 那份是"12 套 195 PASS"这个数字的出处，该总数**已无处可复跑**。

日期: 2026-09-11 00:4x · 基线提交 `31c2681` · 对照基线 esp32-core0 `7bb0b1e`

---

## 0. 一句话

> ★★ **本文件是 2026-09-11 00:4x 的开发规划基线 (基线提交 `31c2681`), 已被实现超越。**
> 下面 §1/§3 的数字与缺口清单**当时是对的, 现在过时** —— 保留它是因为它是"当时的判断依据",
> 有历史价值; **当前真实状态看本节末尾的"傍晚更新"**, 不要拿 §1 的数字当现状
> (陈旧状态文档 = 会骗人的宣称, 与本项目"宣称=实现"铁律冲突)。

**引擎内核已迁移完成且优于 S3；协议层只做了 3/20 条命令。**
缺的全部是"运行期控制面"（读写 SHM / 运行控制 / Force / Seq / persist / Modbus），
不是引擎本身。通信卡点已解，现在可以连续开发。

### ★ 傍晚更新 (2026-09-11 15:xx, 提交 `c5d78c0`) —— 以上全部完成

| | 基线时的判断 (§1) | **现在** |
|---|---|---|
| 命令实现 | 3 / 20 | **21 条全实现** (含 W4 Modbus 3 条 / W5 macro 3 条 / 0x36 自检 / 0x37 通道扫描) |
| W1 运行控制+SHM | 缺口 | ✅ 28/28 |
| W2 Force + persist | 缺口 | ✅ 13/13 + 26/26 |
| W3 Sequencer | 缺口 | ✅ 27/27 |
| W4 Modbus | 缺口 | ✅ 15/15 (含审计 M1 修复) |
| W5 外设域 (macro/DI/AI/HIL) | 缺口 | ✅ 18/18 + 14/14 |
| 规模 | src ~2100 / 21 提交 | **src 8114 行 (15+14 文件) / tools 6524 行 (19 脚本) / 51 提交** |
| 审计 | 未做 | ✅ W1W2W3 批 8 项 + **W4 批 M1(P1)/M2/M3/M4 + P3 全部闭环** |
| S3 回归平移 | 目标 18 套全绿 + 2 套 N/A | 🟢 **22/30 通过 (脚本零改动)**; 7 项 S3 平台专属 (DHT22/shell/fs/ESP32-GPIO 地址); **1 项半可修 (T15 = 需真实复位, 见下)**; T26 已转绿 |

**验证**: H723 自建 10 套件 **177 PASS / 0 FAIL** (audit_m234 12 · proto 12 · w1 28 ·
w2_probe 14 · seq 27 · persist 26 · modbus 15 · macro 18 · w5 14 · **t26 11**)。
S3 套件 `test_dcl.py COM14` **脚本零改动 → 22/30**。

### ★ 深夜更新 (2026-09-11 16:xx) —— T26 转绿, 顺带修掉向量表落位

| | 傍晚 (c5d78c0) | **现在** |
|---|---|---|
| S3 套件 | 21/30 | **22/30 (T26 转绿)** |
| 真缺口 | T15 + T26 | **只剩 T15 (半可修)** |
| 结构性缺陷 | 未发现 | ✅ 修掉: **向量表原在 FLASH ⇒ 擦除期间取向量被 stall** (实测丢 **8153** 拍 → **0** 拍) |

- **T26 修法**: 不是"把 flush 加回去" (那件事失败过三次, 每次都把套件打到 5/30),
  而是**换触发权的归属** —— 由上位机的**纯查询型 0x43** 登记、主循环裁决 (RUN 则放弃)。
  两段分开之后, "RUN 中被问两次都放弃" 成了可读回的证据 (`g_persist_auto_gate=2`)。
  详见 `docs/FIX-REPORT-T26-persist-flush.md`。
- **T15 = 半可修**: 落盘半段已绿 (`持久化=ok`); 重启半段需**真实复位**, 而 CH340 的 RTS
  **没接到 NRST** (实测 RTS 翻转后 samples 20031→31602 继续增长) ⇒ 只能**手按板上 RST**
  或**补一根 RTS→RST 的线**。不为过测伪造重启证据。
- **顺手收的三处"假故障"**: 验收脚本被自己的 print 编码崩掉 (19 个工具注入守护) ·
  w2_probe 的隐含前提导致跑测顺序依赖 (改为脚本自建前提 + PRE 判据) ·
  build.sh 依赖环境 TEMP (钉到 build/tmp)。


---

## 1. 规模对照（实测，排除第三方 esp_littlefs）

| | S3（老，已完成） | H723（新，当前） | 覆盖 |
|---|---|---|---|
| 自研代码行数 | **4340** | **~2100** (+ tools 2292) | 48% |
| 提交数 | 90 | 21 | 23% |
| 命令实现 | **20 / 20** | **3 / 20** | **15%** |
| 测试套件 | 20 套件 + 34 项离线 | 12 项协议 + 9 例 deploy 自检 | 30% |

### S3 组件对照表（哪些已迁、哪些没迁）

| S3 组件 | 行数 | H723 状态 | 说明 |
|---|---|---|---|
| `core0/shared_mem.h` | 378 | ✅ 已迁（`engine.h` 475 行，更严） | 加了 `==` 精确相接断言 |
| `core0/primitives.h` | 101 | ✅ 已迁（228 行） | 算法逐字保留，加 19 原语成本表 |
| `core0/core0_isr.c` | 474 | ✅ 已迁（`engine.c` 667 行） | 双实例 A/B + 桶化分档已含 |
| `transport/uart_protocol.c/h` | 135 | ✅ 已迁（205 行） | 逐字沿用 |
| `main/main.c`（协议分发部分） | 1089 | ⚠️ **部分**（1060 行，但只 3 命令） | 这是当前主要缺口 |
| `core0/modbus.c` | 370 | ❌ 未迁 | W4 |
| `persist/persist.h` | 341 | ❌ 未迁 | W2 |
| `macro/macro_loop.h` + `macro_exec.h` | 331 | ❌ 未迁 | W5 |
| `display/st7735.h` | 199 | ❌ **不迁** | H723 无 TFT，且与引擎无关 |
| `fs/fs_wrapper.h` | 177 | ❌ **不迁** | 依赖 littlefs/SPIFFS |
| `shell/shell.c/h` | 206 | ❌ **不迁** | 依赖 fs |
| `hil/hil.c/h` | 178 | ❌ 未迁 | W5 |
| `ai/ai.c/h` | 143 | ❌ 未迁 | W5 |
| `di/di.h` | 92 | ❌ 未迁 | W5 |
| `sensors/dht22.h` | 86 | ❌ **不迁** | 改用 H723 内部 ADC |

**结论**: 真正要迁的缺口 = `main.c` 的 17 条命令 + modbus(370) + persist(341) + macro(331) + hil/ai/di/sensors(499) ≈ **1541 行新增**。

---

## 2. H723 已经做到什么（且大多优于 S3）

| 能力 | S3 实测 | H723 实测 | 评价 |
|---|---|---|---|
| 拍周期 | 99.9973 μs | **100.0000 μs**（LA 实测众数，96% 命中）| ✅ 更准 |
| 拍抖动 | 极差 0 cyc | **极差 0 cyc**（min=max=40000）| ✅ 持平 |
| 空拍骨架 | 49 cyc @240MHz = 204ns | 95 cyc @400MHz = **237ns** | ⚠️ 略慢（绝对时间），但抖动同为零 |
| 单条路由 | 234 cyc | **56.0 cyc** | ✅ **快 4.2×** |
| 128 条全表扫占拍 | — | FLASH 65.8% / **ITCM 18.3%** | ✅ ITCM 落位是设计前提 |
| 分档调度收益 | 2.59× | **2.59×**（同语义） | ✅ 一致 |
| 热重载延迟 | — | **3.6 μs**（ITCM 字拷贝） | ✅ 远优 |
| 协议帧格式 | — | **逐字沿用，脚本零改动** | ✅ |

### 已落地的机制（这些不用再做）
- ITCM/DTCM 落位 + 同镜像 A/B 实验（16 次构建坐实成本 = f(addr mod 32)）
- 桶化分档调度（div0 全跑 + div1 tick%10 + div2 tick%64，**已修 H9 缺陷**）
- 19 原语 + 逐原语实测成本表 `k_op_cost_itcm[19]`
- deploy：下载前静态校验 → staging → 热重载 → `APPLIED_SEQ` 生效确认
- transport 解析器（7 态状态机 + F1 分块 CRC）
- USART1 驱动（RXNE 中断 + 环形缓冲 + 错误标志逐个清）
- 观测面：64 个变量 + `obs_anchor()` 统一锚定
- 冷启动单一入口 `cold_start_reset()` + 栈哨兵

---

## 3. 缺口清单（按优先级）

### W1（P0）运行控制 + SHM 读写 — 7 条命令
```
0x11 START   S3 main.c:895  —— 含 F11 预算兜底 + OA13 幂等 + Seq 运行态重置
0x12 STOP    S3 main.c:925  —— 含 P1-2 outputs_safe() 安全态
0x13 RESET   S3 main.c:927  —— cold_start_reset()
0x20 READ           S3 main.c:163  —— valid_addr 守卫
0x21 WRITE          S3 main.c:179  —— write_allowed NaN/Inf 守卫
0x22 READ_BURST     S3 main.c:170  —— valid_range 守卫
0x23 WRITE_BURST    S3 main.c:188  —— 全量预检后写
```
**必须逐字搬的 4 个安全守卫**（S3 main.c:83-140）：
- `valid_addr()` —— 4 字节对齐 + 地址区间白名单（H723 要换地址表）
- `valid_range()` —— burst 起止都须同区
- `is_finite_bits()` + `shm_off_is_float()` + `write_allowed()` —— NaN/Inf 注入防护
- `outputs_safe()` —— STOP/RESET 后执行器归零

### W2（P0）Force + persist — 2 条命令 + 1 个模块
```
0x24 FORCE        S3 main.c:204  —— [idx:u16][mode:u8][val:f32]，OA9 强制值须写 FORCE_VAL
0x43 persist 观测  S3 persist/persist.h (341 行)
```
**前置**: SHM 要新开 FORCE_MASK / FORCE_VAL 区 —— 当前落在 `OFF_RSVD_EXEC_DOMAIN`（0x47F0，704B）。
- `FORCE_MASK_WORDS = MAX_WIRES/32 = 4` → 16B
- `OFF_FORCE_VAL = 128 × f32` = 512B
- 合计 528B < 704B ✅ 但**必须改断言**（把保留洞拆成真区，`_Static_assert` 尺寸常量要同步改）

### W3（P1）Sequencer — 1 条命令
```
0x44 SEQ_DEPLOY   S3 main.c:650  + DESIGN-sequencer.md
```
**前置**: SHM 要新开 `OFF_SEQ_CTRL`（`SeqCtrl_t` 数组）—— 当前落在 `OFF_RSVD_DSL_DOMAIN`（0x3840，3136B）。

### W4（P1）Modbus 通信域 — 3 条命令 + 370 行
```
0x60 MB_INJECT   0x61 MB_RESP   0x62 MB_CFG
core0/modbus.c (370 行) —— 含 mb_tick 每拍限速
问题: S3 用 UART1，H723 当前只有 USART1。需配 USART2 (PD5=TX/PD6=RX) 或复用单口。
```
**前置**: `OFF_MB_SET`（0x4AA0，已预留 64 WORD）+ `OFF_MB_TAIL`。

### W5（P2）外设域 — macro + ADC + DI/HIL/AI
```
0x30 MACRO_UPLOAD / 0x31 MACRO_CTRL  (macro_loop.h 234 + macro_exec.h 97)
ADC 16 位 + DMA → SENSOR_MAP          (替代 DHT22)
di/di.h (92) + hil (178) + ai (143)
```
**前置**: SHM `OFF_RSVD_DSL_DOMAIN` 还要放 macro run 区。

---

## 4. 明确不迁的东西（要在 README 里写清楚）

| 模块 | S3 行数 | 不迁原因 |
|---|---|---|
| `display/st7735.h` | 199 | H723 板无 TFT；且 display 从不属于"确定性引擎"卖点 |
| `fs/fs_wrapper.h` + littlefs | 177 + 7161 | 依赖 SPIFFS；persist 改用裸 Flash 扇区更简单 |
| `shell/shell.c/h` | 206 | 交互式 shell 需要 fs 与行编辑，与"下载前静态校验"路线冲突 |
| `sensors/dht22.h` | 86 | DHT22 是单总线时序位敲，精度低；而 H723 有 16 位 ADC（**升级**）|

---

## 5. 验收标准重定义

S3 的 `test_dcl.py` 有 20 套件，其中：

| 套件 | H723 状态 |
|---|---|
| T1 T2 T4 T6 T7 T8 T9 T10 T13 T16 T17 T18 T19 T20 T22 T23 T24 T25 T26 T27 | ✅ 应可全绿 |
| T3（DHT22 → LPF → WIRE1）| ⚠️ **改用 ADC 通道**（等价语义）|
| T5（Shell：sys info / ls）| ❌ **不适用**（不迁 shell/fs）|
| T11（FS write/cat/rm）| ❌ **不适用** |
| T14（MACRO VM）| W5 后再评估 |

**目标: 18 套全绿 + 2 套标注不适用**（不是"20 套全绿"）。

---

## 6. 开发纪律（本次采用的"先开发后审计"变体）

用户决定改为**先开发后审计**（不再每步停下等审计）。补偿措施：

1. **每步必测**：任何命令实现后立刻跑对应的 Python 验证脚本，绿了才继续
2. **回归闸门**：每完成一个 W 波次，重跑 `h723_proto.py`（必须仍 12/12）
3. **宣称=实现**：改 `DCL_CAP_H723_IMPL` 必须同时改 `transport.h` 的 NOTYET 清单（有 `_Static_assert` 兜底）
4. **新域必登记**：任何新 SHM 区必须同时改 ①偏移宏 ②`_Static_assert` 布局断言 ③`cold_start_reset()`
5. **事故留痕**：每个踩坑在代码旁写注释 + 更新 docs/

审计推迟到全部 W 波次完成后一次性做（对照 S3 的 4 轮审计口径）。

---

# ★★ 收口阶段完成度盘点 (2026-09-11 21:xx · 基线提交 `a2140cd`)

> **本节取代上面所有更新行的数字**（傍晚/深夜两节已被超越）。
> 口径: **完成度 = 有实现 + 有判据 + 判据能失败**；不接受"宣称完成"。

## 7.1 四个维度

| 维度 | 完成度 | 依据 |
|---|---|---|
| **功能面**（相对 S3） | **≈ 100%** | 命令 21 条全实现；该迁的组件全迁（4 项明确不迁，见 7.5） |
| **验证面** | 自建 **11 套 186 PASS / 0 FAIL**；S3 平移 **22/30** | 失败 8 项 = 7 项 S3 平台专属 + 1 项半可修 ⇒ **无功能型缺口** |
| **确定性指标** | **6 / 6 已复测** | 见 7.3（拍长抖摆 σ ≲ 1.6 cyc，且与负载无关） |
| **可靠性收口** | **3 / 13 ≈ 23%** | 见 7.4（#1 含④ / #2 / #5 已闭环；#4 由你搁置；**新增时基问题**） |

## 7.2 命令覆盖（实测自 `main.c` 命令分发表）

* 已实现 **21** 条：
  `0x01 0x10 0x11 0x12 0x13 0x20 0x21 0x22 0x23 0x24 0x36 0x37 0x38 0x40 0x41 0x42 0x43 0x44 0x60 0x61 0x62`
* 范本 19 条里**唯一的差集 = display 3 条**（`0x51/0x52/0x53`，已标 `DCL_RESERVED`，不迁）
* H723 **独有 5 条**：`0x36` 引脚自检 · `0x37` ADC 通道扫描 · `0x41/0x42` macro 循环上传/控制 · `0x43` persist 查询/落盘
  * ★ **口径待核**：`0x41/0x42` 的注释写着"同 S3"，但范本**没有这两个命令号**（范本只有 `0x40`）。
    这属"注释声称的范本对齐"与事实不符，收口时应改成"H723 扩展"或核清范本的真实触发方式。

## 7.3 确定性指标（对照 `PLAN-DEV-continuous.md` 的最终验收）

| 指标 | S3 | H723 目标 | 现状 | 判定 |
|---|---|---|---|---|
| 单路由成本 | 234 cyc | < 100 | **56.0** | ✅ |
| 分档收益 | 2.59× | ≥ 2.59× | **2.59×** | ✅ |
| 热重载 | — | < 1 拍 | **3.6 µs** | ✅ |
| 热重载落位 | — | — | 向量表 + ISR + 表 **三样都不在 flash**（A/B 实测 8153→0 拍） | ✅ |
| 拍周期 | 99.9973 µs | ≥ 100.0000 µs | **99.99745 µs**（−25.5 ppm；三轮重复性 0.008 ppm） | ✅ 已复测，但**旧目标作废**（见下） |
| 拍抖动 | 极差 0 cyc | 0 cyc | 引脚边沿 **σ = 0.30 采样网格**（= 量化理论值 1/√12）⇒ 真实抖摆 **≲ 1.6 cyc** | ✅ 已复测，**旧目标作废**（见下） |

★★ **2026-09-11 深夜：两项复测完成**（完整证据 `docs/REPORT-DETERMINISM-2026-09-11.md`）
* **双仪器交叉**：固件 DWT 统计（串口读，**不建调试会话** ⇒ 避开"DWT 在暂停期冻结"的污染）
  + Saleae LA **引脚边沿**（外部独立，**不调 pyocd** ⇒ 不清运行期配置）。
* **LA（外部权威）**：四负载（骨架 / 空 / div0 满 / 分档满）均值周期互差 **0.005 ppm**；
  鲁棒拟合残差 **σ = 0.30 网格**，恰等于量化理论值 `1/√12 = 0.289`
  ⇒ **残差就是边沿落在采样网格上的量化，没有可分辨的真实抖摆**；
  悬空对照 **N=2 vs 5300** 证明探针确实接在 PA8 上（不是拾噪）。
* **固件侧**：`pmin/pmax` 极差 18~24 cyc，**且不随负载变化**（ISR 时长 407→7287，差 18 倍）。
  两个嫌疑已被**实测排除**：① USART1 不会延时 TIM2（`IPR[28]=0x00` 最高 / `IPR[37]=0x80` 最低）；
  ② 栈在 **DTCM**（`_estack = DTCM 末端`），异常入栈无总线竞争。
  ⇒ 那 18~24 cyc 是**中断入口延迟**，**不是拍长抖动**；观测量命名待改（报告 §9）。
* **旧口径为何作废**：
  "**拍周期 ≥ 100.0000 µs**"当初**不可测**（没有独立时间源），且现测偏差 −25.5 ppm 是
  **两台钟的相对偏差**，不是程序错；"**拍抖动 极差 0 cyc**"是"没超载的**结果**"，
  不是"解耦的**证明**"（`AUDIT-H723-stage2 §7` 自己已降级，本次彻底换成可复算的口径）。
* 毛刺：0~2 / 5000 周期（0~0.04%），方向是"**多一个边沿**"（111/154/188/240 µs），
  而固件每拍只翻一次 ⇒ 判为**接线/采样**，非固件输出（与 `HARDWARE-PINOUT.md` 既有记载一致）。

## 7.4 审计闭环状态（迁移保真度审查 `d557054` + 后续修复）

| 级别 | 项 | 状态 |
|---|---|---|
| 一级 | **#1 停机安全态**（含子项 ④ actuator 校验） | ✅ 闭环 `eebeba7` + `9588557` |
| 一级 | **#2 `0x08/0x18` 同址反义** | ✅ 闭环 `a2140cd` |
| 一级 | #3 FORCE 迁址（"零改动"承诺已破） | ⬜ 未决 |
| 一级 | #4 div2 相位 / 两条建桶路径矛盾 | ⏸ **用户搁置**（留给时间槽深度优化） |
| 二级 | **#5 `ov` 恒 0（T9 半条是空判据）** | ✅ 闭环（`docs/FIX-REPORT-R5-overrun-and-timebase.md`）：真计数 + 真实配置对照 **18343 vs 0** |
| 二级 | #6 TICK_STATS 语义漂移 · #7 确定性可被旋钮关 · #8 桶扫丢 `nr` 上界 · #9 `OP_COST_DIV2` 注释 | ⬜ 4 项未决 |
| **新增** | ★★ **时基可被调试器静默停掉**（R5 连带发现）：pyocd 会话后 `DWT_CYCCNT` 不计数，而固件只在启动使能一次 ⇒ `di=0` ⇒ **一切 DWT 计时判据静默读 0**（`ov` 也永远不可能 >0），而 `samples`/协议/表全部正常 | ⬜ **未处置**（机制已定位；建议见 R5 报告 §3） |
| 三级 | #10 macro 持久性口径 · #11 SRC_HMI · #12 `flash.h` 宣称 · #13 TC-vs-TXE · #14 `MB_MAX_FRAME` 注释 | ⬜ 5 项未决 |

★ 附带**实测新发现**（属 #4 家族，已记档待时间槽优化一并收）：
`engine_bucket_dead_slots()` 把 `off2[p]`（**前缀和/桶起始偏移**，只要有路由就非零）也当死槽数
⇒ **任何非空程序恒报 36**（实测板上 `g_bucket_zero_slots = 0x24`）
⇒ 这条"死槽不变量"判据**恒红、零判别力** —— 而"H723 宣称槽 64..99 恒 0"正建立在它上面。

## 7.5 明确不迁（能力声明需写清）

`display` · `fs`/`esp_littlefs` · `shell` · `sensors(DHT22)` —— 4 项，均有平台理由。

## 7.6 收口到"可交付"还差什么

1. **审查未决 10 项**（#3 + 二级 5 + 三级 5）。其中 **#3（FORCE 契约）与 #5（`ov` 空判据）**
   直接影响"承诺成色"，建议优先；其余多为口径/文档。
2. ~~确定性两项复测（拍周期 + 抖动）~~ ✅ **已完成**（2026-09-11 深夜，
   `docs/REPORT-DETERMINISM-2026-09-11.md`）：双仪器交叉，结论是"拍长抖摆 σ ≲ 1.6 cyc
   且与负载无关"，并把旧的两条过度声称换成可复算口径。**连带 3 项遗留**见该报告 §9。
3. **T15 半可修**：`RTS ——100nF—— NRST` 一根线，或手按板上 RST。
4. **文档一致性**：本节已订正本文件与 `README.md` 的套件表；
   `docs/STATUS-2026-09-10.md` 等**历史文档保留旧值**（历史价值，不删），但**不要当现状读**。
5. ★ **产品化收口是否在本线承接**：S3 线路线图里有"顺序域示范 demo / README / 录屏 /
   ST 标准语言前端"四项未完成。H723 线的既定目标是"达到 S3 完成度"——
   **这四项要不要一并承接，需要定调**（它决定"完成度"的终线画在哪）。
