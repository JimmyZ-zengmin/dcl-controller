# PLAN-completion —— 项目完善计划（2026-09-18 立）

> **判据**：这个项目唯一的资产是「**说的话算数**」。所以"完善" = ①把话说完（门面=事实）
> → ②把欠账清掉（一行/一条命令级）→ ③把路打通（让别人真能写出程序）。
> **不加运行时功能**（瓶颈已移出运行时——今天找到的全是耦合/派生级问题，不是缺功能）。

---

## 阶段 0：判据与边界（先定，免得做一半改口径）

- **完成的定义**：每一项都要有**能失败的判据**，读法遵循既有纪律（SKIP ≠ PASS、口径与被测量自洽）。
- **不做**：深挖运动品质（会撞测量口径的墙，已三次）· 追"零实测输入的编译期计算"（E-F 已证伪）·
  为多轴先买/先做 mux（另两个阻塞没解）· 加运行时功能。
- **每个阶段的产物**：代码/文档改动 + 判据 + 一段可复跑的命令 + 指纹（若动部署期代码）。

---

## 阶段 1：把话说完（门面 = 事实）—— **最便宜、当天可完成**

| # | 动作 | 权威源 |
|---|---|---|
| 1.1 | README §一 证据表补今天最硬的三条：成本模型**留点 0.04%** · **G10 三域 46 判据** · **门的两档端到端验证 + 决定性翻转实例** | `docs/exp-EK/EM/EN/EO/EQ/ER/ES/EU` |
| 1.2 | README §诚实标注 补：**门在交付档结构性不触发**（128×145=18560 ≤ 26000 ⇒ 只有 FLASH 档有牙）；**"可算"是标定模型 + 留点 3%，不是纯计算** | `docs/exp-EU` §⑥ · E-F（负结果） |
| 1.3 | `docs/SUPPORTED-SCOPE.md` §2「可以依赖的」加 **G10**（声明的秒/毫秒/斜率/步数 = 跑出来的） | `ARCH-GUARANTEE-MAP.md` G10 |
| 1.4 | `docs/claims.md` 登记今天的判据入口（G10 三域 + 门的转变项） | 同上 |
| 1.5 | 能力位文字 `DCL_CAP_MULTICYCLE` 由「100µs/1ms/10ms」改成**「1 / 10 / 64 拍」** —— 纯注释，**不改二进制**；顺带修 `PERIOD_DIV_IDX_SLOW` 与 `OFF_TICK_STATS` 里同样过期的 ms 口径 | `transport.h:133` · `engine.h:914/293` |
| 1.6 | 公开仓发布：`git push dcl main`（落后一个工作日，0 分歧 fast-forward）—— **必须在 1.1~1.5 之后** | `git ls-remote dcl` |

---

## 阶段 2：把欠账清掉（单价都小）

| # | 动作 | 为什么值得（今天的证据） |
|---|---|---|
| 2.1 | **顺序域清除语义**：`0x44` 允许 `n_seq=0` = 清除全部实例 | 实测残留 8 实例 ⇒ **每拍 +274 TB**，且**污染一切成本测量**（E-T 第一次就栽在这上面） |
| 2.2 | **`g_step_stop_tick`**：`step_tick` 停脉冲那行记一个拍号 + `sub=16` 一个保留槽 | 关掉 G10 上**最后一个 SKIP**，让"声明的 N/f 时长"可独立判定（现在是组合出来的） |
| 2.3 | **`EXEC_BUDGET_CYCLES` 的决定**：改成"拍的 80%"或明确它就该是绝对 µs | 拍长可配后它对**合法程序误报 OVERRUN**（耦合 #6，已登记未改）⇒ ✅ **已按"拍的 80%"派生**（E-Z：注释自己就写着 80% ⇒ **不是新决定**；交付档逐位无回归） |
| 2.4 | 审计 1.1 的 14 处 `record(...,True)` 分诊 | `RETRACTIONS` P26 挂着"12 PASS / 0 FAIL 复跑不出来"，而那是**审计自己**的问题 |
| 2.5 | `C_other`（542 TB）的内部组成：adc / rtc / i2c / 黑匣子各占多少 | 模型第二大常数，现在是一整块黑箱 |

---

## 阶段 3：把路打通（③层表达力）—— **决定项目天花板**

侦察结论（2026-09-18）：这一项的**真实代价参差**，不能一概说"接线就行"。

| # | 动作 | 代价（已侦察） |
|---|---|---|
| 3.1 | **`OUTPUT ... PERIOD=`** | **纯 `dclc` 改动** ✓ —— 周期字段本来就在 `RouteEntry.period` 低 2 位、`engine_build_buckets` 也在用；DSL 只是拒收。**它解锁的是"能把路由放到 div1/div2"** ⇒ 这是核心能力宣称 |
| 3.2 | **`ABS`（用组合实现）** | **纯 `dclc` 改动** ✓ —— `ARITH` 有 SUB/MAX ⇒ `ABS(x) = MAX(x, 0−x)` 可展开成两条路由。★ 要判据证明**展开后语义等价**（含 `x=0`、负零、NaN 被上游挡掉） |
| 3.3 | `THR=` 放宽到变量 · `CONST` 接受科学计数法 | 纯 `dclc` 解析器 ✓ |
| 3.4 | **`LUT` 暴露** | ★★ **不是接线活**：`0x23` 的能量守卫允许写 `g_shm+OFF_LUT_DATA`（表**今天就能上传**），但 **`0x10 deploy` 不碰 LUT 区** ⇒ **表不在程序里**：上电自动装载后是残留/零，且**不进 SD A/B 持久化**。⇒ 需要先决定"表住哪"（扩 deploy 载荷 / 新命令进持久化镜像 / 声明"LUT 表由上位机每次重下"）。**先做最小验证把这个缺口钉成判据，再决定** ⇒ ★ **最小验证已做（E-Y 8/0/1）**：缺口被钉成 8 条判据，三条路线的代价已摆开（见 `docs/exp-EY-lut-provenance.md` §③），**决定待拍板** |
| 3.5 | `MOD` | **引擎没有**（`ARITH` 六种模式里没有）⇒ 要动原语（①层）。**除非有明确需求，否则不做** |

---

## 阶段 4：机制（护城河）

| # | 动作 | 依据 |
|---|---|---|
| 4.1 | **文档 ⇄ 文档闸门**：跨文档否定式断言检查（"某文档说没有 X，而 `examples/` 里有 X 的例程"） | 项目自己的评估：这是"花最小代价暴露的**最大系统性风险**"（实测同一日报前后矛盾 8 处、三个数字查无出处） |
| 4.2 | **撤回进台账的机制** | P29/P30 都是我**事后复盘**才想起来的，不是流程逼出来的 |
| 4.3 | **判据的"台架依赖"分开记账** | ✅ **已机制化**：`.workbuddy/rig-dependent.json` 登记表 + 审计 **axis 1.3** 机械校验（tool 文件必须存在 · criterion 文本必须在该文件里 · mechanism/reproduce 必填）⇒ 条目不会静默腐烂，命中时**先读机制**而不是当回归重查 |
| 4.4 | 今天三条新纪律**固化成机器**：物理常数一处定义 + 别名/派生 · 判据阈值由被测物理量派生 · **构建档位粘性 ⇒ 恢复脚本必须比指纹** | `MEMORY` 5.87~5.91（已写进纪律，但还没有机器拦）。4.4 的第三条今天已落地（`h723_restore_delivery.sh`） |

---

## 执行顺序（按"收益/代价"排，不按编号）

1. **1.1~1.5**（门面）→ **1.6**（发布）—— 让今天的证据**对外有效**
2. **2.1 + 2.2**（两次都是小改动，一次构建一次烧录一起验）
3. **3.1 + 3.2**（纯 dclc，做完立刻能写以前写不出来的程序）
4. **3.4 的最小验证**（把 LUT 的"表不在程序里"钉成判据）
5. **2.3 / 2.4 / 2.5 / 3.3 / 4.x** 视剩余时间穿插

> **进度记录**：每一项做完在本文件下面追加「**结果**」一行（含指纹/判据数/未做的部分）。

---

## 进度记录

### 2026-09-18 · 阶段 1 全部 + 阶段 2.1 / 2.2 —— **已完成并验证**

| 项 | 结果 | 判据 / 证据 |
|---|---|---|
| 1.1 | README §一 补 3 行（算得出来 · 门验过 · 声明的时间量） | 三个 `tools/exp_*.py` 可复跑 |
| 1.2 | §诚实标注 1 条 → **3 条**（标定模型非纯计算 · 交付档门结构性不触发 · 轨迹形状未完成） | `docs/exp-EF`（负结果）· `exp-EU` §⑥ |
| 1.3 | `SUPPORTED-SCOPE.md` §2 新增 **G10 八行能力表 + 四条边界** | `docs/ARCH-GUARANTEE-MAP.md` G10 |
| 1.4 | `claims.md` 增 G10 / 转变项登记段 | ★ **说明性条目，未按 8 类机检格式**（那要求判据脚本里有对应 `record(...)` 文本）—— 如实标注 |
| 1.5 | 能力位 `DCL_CAP_MULTICYCLE` → 「**1 / 10 / 64 拍**」+ 修两处过期 ms 口径 | ★ **hex 逐字节不变**（`6daa7e65…`）⇒ 证明改的只有注释 |
| 1.6 | **公开仓已发布**：`dcl/main = 3e84703`，发布差额 **0** | `git ls-remote dcl`（opt-in 闸门 `DCL_ALLOW_PUBLIC_PUSH=1`） |
| 2.1 | **顺序域清除语义**：`0x44` 且 `n_seq=0` = 清除 | E-U/M4：ACK + `N_SEQ=0` + 每拍开销 **735.8 → 545.4 TB**；负对照 NAK 且原因可读 |
| 2.2 | **「走 N 步」两个拍号**（`sub=16` → 40 B） | E-S/S7：实测时长 **0.9916 s vs 声明 1.0000 s**；E-S **11 条 0 FAIL / 0 SKIP** |

★ **本轮第三个「我自己的判据坏了」**：E-S 的 S3 把"撤回方向性结论"实现成了
「两条件区间**必须重叠**」—— 那**仍然是关于噪声的方向性断言**，于是第三次运行（区间分离）把它判 FAIL。
⇒ S3 改成**只报数、不进断言表**；能判的只有界（S2/S4a）。

交付档指纹：`c7c366c1…` → **`6daa7e65…`**（2.1/2.2 动代码）· 协议 12 PASS / 0 FAIL · 两档 0 warning

### 计划项状态（★ 原表名是"未执行"，而 3.1~3.4 已全部落地 ⇒ 改名：
### 标题说"未执行"、行里写"已做"正是**同一份文件内前后矛盾**这条纪律要防的东西）

| 项 | 状态 |
|---|---|
| **3.1 `OUTPUT PERIOD=`** | ✅ **已做**（E-W，18/0/0）—— 见下方进度记录 |
| 3.2 `ABS`（组合展开） | ✅ **已做**（E-X，14/0/0） |
| 3.3 `THR=` / `CONST` 解析放宽 | ✅ **已做**（E-X）—— ★ 实际做的比计划多：`THR=` 不只是"放宽解析"，而是**支持变量阈值**（组合展开） |
| 3.4 **LUT 暴露** | **已验（E-Y 8/0/1）**：侦察结论全部变成**能失败的事实** （Y1s/Y2s/Y3s 源码级 + Y5/Y6/Y7 在板）⇒ 表**确实**住在无主的易失内存里，且 `fill_tables` 的斜坡在 reinit 时会静默覆盖它。★ **三条路线的代价已摆开（A 扩 deploy 载荷 / B 进持久化镜像 / C 工具侧约定），推荐 A；本轮不拍板** —— 见 `docs/exp-EY-lut-provenance.md` §③ |
| 3.5 `MOD` | 不做（引擎无此模式，要动 ① 层） |
| 2.3 `EXEC_BUDGET_CYCLES` | ✅ **已做**（E-Z 8/0）：改成**派生量**（拍长 × 百分比）+ 四条断言；★ 交付档**逐位无回归**（指纹不变）。**不是新设计决定** —— 注释自己就写着"80%" |
| 2.4 审计 1.1 的 `record(...,True)` | ✅ **已做**（分诊版：**分支内 6 + 守卫落空 9 + 真无条件 0**；`--offline` 从 11 PASS/1 FAIL → **12 PASS / 0 FAIL**；新增 `--selftest-11` 证伪启发式自身） |
| 2.5 `C_other` 内部组成 | 未做 |
| 4.1~4.4 机制 | 4.4 的第三条已落地（恢复脚本比指纹）；其余未做 |

---

### 2026-09-18 · **E-V** —— 3.1 的前置侦察顺手抓出的一条**未声明的跨函数依赖**（结果：**不改代码，只立判据**）

> 这一段的价值不在"补上了什么"，而在**判据把我自己的改动判成了错的**。

| 项 | 结果 | 判据 / 证据 |
|---|---|---|
| 起点 | 我判定预算闸门有「**摊薄 vs 每拍**」的真洞（`engine_prog_budget` 按 `Σceil(成本/相位数)` 算，而运行期约束是每拍），**据此改写了闸门** | 该改动的算例：按载荷相位算最坏桶 |
| 实测 | **不是洞**：128 条 PID **全写 phase 0** 部署 ⇒ ACK，实测扫描段最大只 **262 cyc**（真挤一相位应是 ~10⁴）；桶表 `cnt2` 前 6 槽 `[2,2,2,2,2,2]` | `tools/exp_ev_gate_worst_phase.py`（V1/V2） |
| 根因 | `engine_stage_program` 尾部**覆写**载荷相位：`ph = q2++ % BUCKET_DIV2_PHASES_USED` ⇒ 相位**归固件**，载荷那 6 位被忽略 | `src/engine.c`（约 1071 行） |
| 结论① | **摊薄式是对的**：前提 = 轮转铺开（固件确实这么干），`ceil` 使其成为最坏桶的**上界**，余量 94 ≤ 一条路由 145 cyc | E-V/V4 · V4′ |
| 结论② | ★★ 而该前提**无任何断言牵连** —— 闸门 ⇄ staging 之间一条**看不见的依赖**；按载荷相位重写会 55296 ⇒ **误拒**真实最坏桶 864 的合法程序 **64×** | E-V/V5 |
| 处置 | **回退代码**（指纹 `6daa7e65…` 逐字节不变）· 依赖写进 `engine.c`/`engine.h` 注释 · 立 **9 项判据 0 FAIL / 0 SKIP** · 记 `RETRACTIONS` **P31** + `MEMORY` **5.92** | `docs/exp-EV-gate-phase-dependency.md` |
| 对 3.1 的结论 | **`PERIOD=` 不能承诺"相位"** —— 载荷相位被覆写。⇒ 3.1 的语义只能是「**选 div 档**」，且必须在文档里写明相位由固件轮转 | 本表 + `engine.h` `RouteEntry_t.period` 注释 |

交付档指纹**未变**：`6daa7e65…`（本项一行固件都没改 ⇒ 这也是"判据改的是认识，不是产品"的证据）。

---

### 2026-09-18 · **E-W**（3.1）+ **E-X**（3.2/3.3）—— ③ 层表达力，**固件一行未动**

| 项 | 结果 | 判据 / 证据 |
|---|---|---|
| 3.1 | **`OUTPUT ... PERIOD=` 可用** —— 原来第一遍（固定槽预检）拿的是没剥后缀的原文 ⇒ 报语法错误；新增 `split_period()` 预扫（PERIOD= **唯一解析点**，两 pass 共用） | E-W **18 项 0 FAIL / 0 SKIP**（`tools/exp_ew_output_period.py`） |
| 3.1★ | **`PERIOD=10ms` 是假承诺**：编译器写死 `{100us,1ms,10ms}`，而引擎 div2 = **64 拍 = 6.4ms**（phase 只有 6 位）。⇒ 三档周期**全部由 `src/` 派生**（与固件 `DT_*` 同规则），`10ms` 只作标称别名**并报真实周期**，不存在的档被拒 | E-W/W2（dclc 三档 == 固件 `DT_*`，两边各自派生后比对）、W4/W5 |
| 3.1★★ | **在板行为判据**：三档程序各自部署 ⇒ `OFF_TICK_STATS` 增量**只落自己那一档**且速率相符（2.0000 / 0.2000 / 0.03125，偏差 +0.0% / +0.1% / +0.2%） | E-W/W8+W9（他档增量恒 0 作负对照） |
| 3.2 | **`ABS` = `MAX(x, 0−x)`** 组合展开（引擎无此原语），接线逐字段核对；在板 15 个输入**按位模式**等于 Python `abs()`（含 `−0.0→+0.0`） | E-X/X1 · X7 |
| 3.3 | **数字文法 11 份 → 1 份**（`NUMPAT`+`parse_num`）：收下 `1e-3`/`2.5E-7`，非法写法**干净报错**（原来抛 Python 栈回溯）；**`THR=<sig>` 变量阈值**（`SUB`+`CMP` 组合，引擎 `prim_cmp` 无 wire 阈值）；在板 9 组等于 Python `a > b` | E-X/X2 · X3 · X4 · X8 |
| ★ 自查 | 两条**我自己的判据坏了**：① X0 —— 把驱动输入写在 `SENSOR[2]`（**HIL 反馈槽**，固件每拍覆写）⇒ 三条判据一起 FAIL，真因是**前提没验证**；② X9 —— 失败条件写成"被拒 或 槽内有限"⇒ **恒真必 FAIL** | E-X/⑤ |
| ★ 顺带 | **`0x21` 写 `SENSOR[2]` 返回 ACK 但值随即被覆写**（静默无操作）；自由槽改为从 `src/` 派生（`[7, 11, 12, …]`） | E-X/X0′ + `free_sensor_slots()` |
| ★ 顺带 | **`dclc_rate_selftest.py` 的 C1 从来是假 FAIL**：`subprocess.run` 没给 `encoding=` ⇒ Windows 用 GBK 解 dclc 的 UTF-8 输出 ⇒ 比对中文报错文本恒不命中（"这条检查能红"的**唯一证明**一直是无效的）。修后 **3/3** | `tools/dclc_rate_selftest.py` |

交付档指纹：**`6daa7e65…` 不变**（E-W/E-X 全在主机侧 `dclc.py`）· 协议 12 PASS / 0 FAIL

---

### 2026-09-18 · **E-Y**（3.4 最小验证）—— 零改动，把一个架构问题**量清楚**

| 项 | 结果 | 判据 / 证据 |
|---|---|---|
| 3.4 | **表的所有权问题已量清**：`engine_reload_active` 不含 LUT（源码）/ 持久化 payload 只覆盖 routes·params·states（`PERSIST_PAYLOAD_MAX`=6144）/ 程序包唯一附加段是 dev_bind / `fill_tables` 会写 LUT 斜坡；在板：`0x23` 写入回读**逐字节一致**、`0x10 deploy` 后 LUT **不变**（routes 变 = 负对照）、`0x13 RESET` 后 LUT **全 0** | E-Y **8/0/1**（`tools/exp_ey_lut_provenance.py`） |
| ★ 观察 | 第一次跑读到的是**冷启动斜坡**，第二次读到**全 0**（上一次 Y7 的 RESET 清过）⇒ 该观察**依赖台架历史**、单次会话不可复现 ⇒ 已按纪律打 **SKIP 并在表外单列** | 本表 + PLAN 4.3 |
| 待拍板 | **表住哪**：A 扩 deploy 载荷（推荐）· B 进持久化镜像（要动 `PERSIST_VERSION`）· C 工具侧约定（表与程序会不同步——本次实测证明该风险**可达**） | `docs/exp-EY-lut-provenance.md` §③ |

交付档指纹 `6daa7e65…` 不变（本项**没改一行固件**）。

### 2026-09-18 · **2.4**（审计 1.1 分诊）—— 把"元判据"自己修成能证伪的

| 项 | 结果 | 判据 / 证据 |
|---|---|---|
| 分诊 | `record(...,True)` **15 处全部可失败**：条件分支内 **6** · 守卫落空 **9**（紧邻上方有 `record(同一判据, False)+return`）· **真无条件 0** | `python tools/h723_audit_full.py --offline` ⇒ **12 PASS / 0 FAIL** |
| 修法 | 1.1 从"正则计数"改成 `classify_true_records()` 三类分诊；报告**逐处打印文件:行号** | `tools/h723_audit_full.py` |
| ★ 元判据自检 | 新增 `--selftest-11`：四份合成样例（守卫落空 / 分支内 / 真无条件 / **他函数同名 False**）必须各归各位 ⇒ **这条"抓坏判据的判据"自己能红** | `python tools/h723_audit_full.py --selftest-11` |
| ★ 换实现对数 | 中途把"剥字符串+正则"换成"按参数切分"时，覆盖面**从 14 悄悄变 11**（标签里含逗号/括号的抓不到）⇒ 重新数到 **15**（比原来还多 1 处） | 同上；`RETRACTIONS` P26 |
| 闭环 | `RETRACTIONS` **P26** 从"复跑不出来/需分诊"改为**已闭环**（"至少 1 处是真无条件"是我当时的猜测，**错了**） | `RETRACTIONS.md` P26 |

### 2026-09-18 · **2.3**（运行期预算随拍长缩放）—— 关掉登记在案的**耦合 #6**

| 项 | 结果 | 判据 / 证据 |
|---|---|---|
| 缺陷 | `EXEC_BUDGET_CYCLES` 是**手写的 32000u**，而同一段注释自己写着"拍长的 80%" ⇒ 200 µs 档上只剩 **40% 拍**，超载判据被**静默放宽一倍** | `docs/exp-EZ-exec-budget-scale.md` §① |
| 修法 | `EXEC_BUDGET_PCT 80u` + `EXEC_BUDGET_CYCLES = (CLK_TICK_CYCLES × PCT)/100`，配 **4 条断言** | `src/engine.h` |
| ★ 关键 | **只加前三条不够**：200 µs 档上手写 `32000u` 仍满足 ①②③ ⇒ 耦合原样复发。**第 ④ 条**（展开式逐位相等）才是承重的 | E-Z/Z4（变异对照：手写数在 200 µs 档被抓住，在 100 µs 档仍编过） |
| 判据 | **8 项 0 FAIL**：源码级 2 + **编译期预言机** 2 + 负对照 1 + **变异对照** 2 + 交付档指纹 1 | `python tools/exp_ez_exec_budget_scale.py` |
| 回归 | 交付档 hex **`6daa7e65…` 不变**；★ **200 µs 档整机固件也构建通过** ⇒ 该档 hex 现值 **`87a4a5ac…`**（替代旧记的 `38e8d180…`，2.1/2.2 动过代码） | Z5 + `bash build.sh -DDCL_TICK_US=200` |
| ★ 顺带查出 | **`h723_restore_delivery.sh` 的 `EXPECT_MD5` 自 2.1/2.2 起就没更新** ⇒ 该脚本此后**每次都中止**（fail-closed，方向安全，但**恢复路径已死**）⇒ 已更新基线，并把失败信息做成可判断的（打印"最近一次改 `src/` 的提交与被改文件"） | `RETRACTIONS` P32 · `tools/h723_restore_delivery.sh` |

### 2026-09-18 · **4.3**（台架依赖判据**分开记账** —— 从纪律变成机制）

| 项 | 结果 | 判据 / 证据 |
|---|---|---|
| 登记表 | `.workbuddy/rig-dependent.json`：**RIG-1** = `h723_step_ramp_test.py` R5（转速对照依赖机械状态）· **RIG-2** = `exp_ey_lut_provenance.py` Y4s'（LUT 现状依赖板子历史，单次会话不可复现） | 每条含 `mechanism` + `reproduce` |
| 机制 | 审计新增 **axis 1.3**：`tool` 文件必须存在 · `criterion` 文本必须**在该文件里出现** · `mechanism`/`reproduce` 必填 ⇒ **条目不会随代码改动静默腐烂** | `python tools/h723_audit_full.py --offline` ⇒ **13 PASS / 0 FAIL** |
| ★ 负对照 | 把 RIG-1 的 `criterion` 改成 `R5-NOPE` ⇒ 1.3 **必须 FAIL**（实测 `12 PASS / 1 FAIL`，报错点名"条目已腐烂"） | `python .tmpctl/negctl_43.py`（跑完自动复原） |
| 口径 | 登记在案的判据**不进回归判据表**；命中时先读 mechanism/reproduce。要判定固件侧有无缺陷，必须换**不依赖台架/历史**的观测量 | 审计 1.3 的 INFO 行 |
