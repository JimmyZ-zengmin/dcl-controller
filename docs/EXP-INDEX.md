# EXP-INDEX —— 实验索引（由 `tools/exp_registry.py` **生成**，勿手改）

> **唯一源 = `tools/exp_registry.py` 的 `EXPS` 表**（改那里，再跑 `python tools/exp_registry.py --index`）。
> 为什么这样做：实测 **21 份实验文档 / 3 个实验目录 / 31 个实验工具**，而 `doc` 与 `tool` **不是一一对应**
> （有的实验有工具无专文、有的一个实验多个工具、有的文档名与实验号不齐）=> 靠翻目录找不到「做过什么、用什么跑的、结论是什么」。
> ★ 登记表被 `--check` 校验（工具与文档必须存在、索引必须与表一致、状态必须在枚举里）=> 本页不会像手写索引那样悄悄过期。

| # | 实验 | 验证目标 | 实验流程 | 实验工具 | 实验结果 | 实验文档 | 状态 |
|---|---|---|---|---|---|---|---|
| 1 | **E-A/B/C** div0 的 2.1 倍之谜破除 · 桶开销销案 · 19 原语 m_op 全表 | 成本模型第一版在 div0 上差 2.1 倍：是模型错还是实现错？ | 1. 两点法逐原语标定（n=0 与 n=128 做差分）<br>2. 把「每拍固定开销」与「每条路由成本」分离<br>3. 19 个原语各测一遍，与 Python 预测逐项对账 | `exp_ea.py`<br>`exp_ea_fit.py`<br>`exp_eb.py`<br>`exp_ec.py` | m_op 全表拿到；2.1 倍来自「每条成本随 op 变」而不是实现缺陷 | **（无专文）** | 完成（含撤回） |
| 2 | **E-Cscan** C_scan 分解：扫描段只由两项构成 | 扫描段（ISR 里的路由扫描）由哪些项决定？ | 1. 程序条数扫 0/32/64/128<br>2. 每个规模夹取扫描段（调用前后各读一次时基）<br>3. 对条数做线性拟合，看残差能否被 op 混合解释 | `exp_cscan_split.py` | 两项闭合（固定项 + 逐条项）；m_op 被两条独立方法交叉验证（差 1.0%） | [`exp-Cscan-decomposition.md`](exp-Cscan-decomposition.md) | 完成 |
| 3 | **E-D** 扫描段已从整段 ISR 中夹出 | 能不能只量「扫描段」而不是整段 ISR？ | 1. 在 engine_tick 调用前后各读一次时基<br>2. 把差值写进 OFF_SCAN_CYC_*<br>3. 空程序与满程序对照，确认夹取区间只含扫描 | `exp_ed.py` | 拿到「拍内其它工作 = 474 TB」这个新常数 | [`exp-ED-scan-isolation.md`](exp-ED-scan-isolation.md) | 完成 |
| 4 | **E-E1** 口径修好 —— 模型第一次三项闭合 | 统一口径后，模型能否三项同时闭合？ | 1. 修口径（源成本与扫描成本的分子分母同口径）<br>2. 用同一程序复算三项<br>3. 与实测差分逐项比对 | `exp_ee1.py` | 模型第一次三项闭合；m_op 逐位复现；并纠正了我自己的判据 | **（无专文）** | 完成（含撤回） |
| 5 | **E-F** m_op 不能从指令级吞吐下界推导（负结果） | 能不能不靠实测、纯从反汇编结构推出 m_op？ | 1. 反汇编扫描体<br>2. 按指令级吞吐算下界<br>3. 与实测 m_op 比 | `exp_ef_deducible.py` | 负结果：推不出来（证据本身也不够干净，已如实记）=> 「可算」是标定，不是纯计算 | [`exp-EF-mop-deducibility.md`](exp-EF-mop-deducibility.md) | 完成（含负结果） |
| 6 | **E-G** m_op 的程序无关性验证 + 一次失败的测试设计 | m_op 是「原语属性」还是「程序属性」？ | 1. 构造让 m_op 可能随程序变的排列<br>2. 对照同一 op 在不同程序里的成本<br>3. 撤回一次设计失败的测试 | `exp_eg_mop_validity.py` | 有效证据汇总 + 撤回一次失败的测试设计 + 登记一个桶表矛盾 | [`exp-EG-mop-validity.md`](exp-EG-mop-validity.md) | 完成（含撤回） |
| 7 | **E-H** 桶表矛盾判定（实测 vs 自相矛盾的固件状态） | 桶表分桶与路由表 period 是否一致？矛盾在哪？ | 1. 从路由表逐条取 period，自己算相位直方图<br>2. 与固件桶表逐槽对照<br>3. 定位「同一语义三处存放（100/64/63）」的根因 | `exp_eh_bucket_probe.py` | 根因 = 一次「改了一半」的修正；同一个语义三处存放 | [`exp-EH-bucket-contradiction.md`](exp-EH-bucket-contradiction.md) | 完成 |
| 8 | **E-I** 重标定：m_op 在两代固件上逐位复现 | 修完 div2 相位缺陷后，m_op 还是同一张表吗？ | 1. 在修复后的固件上重跑两点法<br>2. 与上一代固件逐项比<br>3. 算最大偏差 | `exp_ei_recalib.py`<br>`exp_ei_b.py` | 逐位复现（不超过 0.6%）=> 「原语属性」从假设变成事实 | [`exp-EI-recalibration.md`](exp-EI-recalibration.md) | 完成 |
| 9 | **E-J** 分档吞吐：分档真的把每条路由都轮到了吗 | 行为判据：分档调度的实际吞吐与结构预测是否一致？ | 1. 按桶表预测 nrun 的均值<br>2. 实测 Δroutes_total 除以 Δticks<br>3. 与预测比（偏差即缺陷证据） | `exp_ej_throughput.py` | 偏差 0.00%（结构对等于行为对）；并发现残留代价是 dt 偏 56% | [`exp-EJ-throughput.md`](exp-EJ-throughput.md) | 完成 |
| 10 | **E-K** 端到端留点验证：模型能算一个程序的执行时间 | 模型能否在没见过的程序上算准执行时间（不只是拟合）？ | 1. 取留点程序（不在标定集里）<br>2. 用模型算预算<br>3. 部署实测 di 并与预测比<br>4. 跨 division 再验一遍 | `exp_ek_holdout.py` | 留点误差 0.04%，且跨 division 成立（3.1%） | [`exp-EK-holdout.md`](exp-EK-holdout.md) | 完成 |
| 11 | **E-L** 混合 op：可加性不成立，但上界成立 | 把每条成本相加能不能当上界？ | 1. 构造 PID 与 DIRECT 各半的排列<br>2. 只改排列不改条数，测 di<br>3. 拟合「相邻 op 改变次数」的斜率 | `exp_el_mixed.py` | 可加性不成立（低估最多 5.3%）；但含转变项的上界成立且部署期可算 | [`exp-EL-mixed-op.md`](exp-EL-mixed-op.md) | 完成（含负结果） |
| 12 | **E-M** 把转变项加进门的预算 | 门必须用上界 => 转变项怎么进公式？ | 1. 写出含 trans 项的预算式<br>2. 与手工算术逐位对账（5 个样例）<br>3. 部署验证（不再放行超载） | `exp_em_budget.py` | 预算算术 5/5 逐位相等；OP_TRANS_COST=25 的判定不改 | [`exp-EM-budget-transition.md`](exp-EM-budget-transition.md) | 完成 |
| 13 | **E-N** 门的两档实测（交付档与 FLASH 档） | 静态门在真板上到底会不会按预测拦住？ | 1. 二分法找边界（DIRECT 与 PID 各一条）<br>2. 交付档与 FLASH 档各跑一遍<br>3. 与源码算术逐条复现 | `exp_en_gate.py` | FLASH 档边界 DIRECT 105/106 · PID 60/61 逐条复现；原 TIMEOUT 是烧录假象 | [`exp-EN-gate-both-builds.md`](exp-EN-gate-both-builds.md) | 完成 |
| 14 | **E-O** 决定性翻转实例：纯可加会放行超载 | 拿一个「可加模型 ACK、真实超载」的程序当门失效的证据 | 1. 构造标量档 ACK 的程序<br>2. 部署并测真实 di 与 ov<br>3. 换含转变项的门重跑同一程序 | `exp_eo2_flip.py`<br>`exp_eo_transition_in_gate.py` | 标量档 ACK 了实跑 30988 cyc（77.5% 拍）的程序且 ov 未触发 => 静态门是承重的 | [`README.md`](exp-2026-09-18-overload/README.md) | 完成（含撤回） |
| 15 | **E-P** 转变代价 k 跟什么有关 | k 是常数？还是两个 op 的成本差？ | 1. 六对 op 相邻排列，各测 di<br>2. 对 k 做线性拟合与残差检查<br>3. 与「k 等于两个 op 成本差」的预测比 | `exp_ep_op_pair_k.py` | k 等于两个 op 的成本差（R²=1.0000，六对零残差） | [`exp-EP-op-pair-k.md`](exp-EP-op-pair-k.md) | 完成 |
| 16 | **E-Q** 「秒」语义端到端验证（声明的秒 vs 跑出来的秒） | LPF τ / PID Ki / TIMER PT / SEQ 超时 在三档上是不是声明的那个秒？ | 1. 判据里不出现被测常量，只测「声明的物理量」<br>2. div0/1/2 三档各跑一遍<br>3. 另加 Q0a：tick 速率与上位机墙钟比（与源码无关的独立测量） | `exp_eq_dt_semantics.py` | 30 判据 0 FAIL / 2 SKIP；顺带修顺序档第二份 dt 表（div2 超时早 36%） | [`exp-EQ-dt-semantics.md`](exp-EQ-dt-semantics.md) | 完成 |
| 17 | **E-R** 运动域的「声明时间量」实现了吗（限时与斜坡斜率） | 把 E-Q 的方法搬到运动域：声明的限时与斜率，跑出来是不是那个数？ | 1. 差分与最小二乘测真实流逝率<br>2. 与声明值比<br>3. 修余数丢失后复测 | `exp_er_motion_time.py` | 抓出限时把拍转 ms 的余数丢掉（部署条件流逝 747/1000 ms/s，且依赖有没有人在读）；修后 11 判据 0 FAIL | [`exp-ER-motion-time.md`](exp-ER-motion-time.md) | 完成（含撤回） |
| 18 | **E-S** 「走 N 步」的时长与过冲的界 | 声明的 N/f 时长与实际是否一致？到点自停的过冲界是多少？ | 1. 用固件拍号直读起止时刻<br>2. 对过冲多次取样并报区间（不宣称方向）<br>3. 注入一次大块读，测阻塞对过冲的影响 | `exp_es_step_duration.py` | 11 判据 0 FAIL / 0 SKIP；实测时长 0.9916 s 对声明 1.0000 s；并更正三处注释里的假界（注入阻塞后过冲 282 倍） | [`exp-ES-step-duration.md`](exp-ES-step-duration.md) | 完成（含撤回） |
| 19 | **E-T** 成本拆解：C_other / C_scan / m_op / k 四项分摊 | 把整段 ISR 拆成可以分开算的四项，并给出每一项的实测值 | 1. 空程序（n=0）测 di(0) 得 C_other<br>2. 19 个原语各测 n=1 得 C_scan(op)<br>3. 变条数测 m_op、变排列测 k<br>4. 与历史值对账（总和与拆分分别比） | `exp_et_cost_decomposition.py` | 干净 di(0) = 542.4 TB；C_scan(op) = 32.5~54.5 TB；历史值 474 + 100~122 总和对、拆分错 | [`exp-Cscan-decomposition.md`](exp-Cscan-decomposition.md) | 完成（含撤回） |
| 20 | **E-U** 收尾七项：拍长可配 · C_other 拆解 · 门边界 · 判据自我更正 | 把上一轮列出的全部未完成项做掉（含拍长可配这一结构性改动） | 1. 拍长一处定义 + 派生 + 断言（-DDCL_TICK_US）<br>2. C_other 内部拆解<br>3. 门边界逐条复现<br>4. 两处判据自我更正 | `exp_eu_wrapup_measure.py` | 拍长可配（加 6 处硬耦合）；SEQ 清除语义实测（每拍 735.8 降到 545.4 TB）；交付档指纹链加一环 | [`exp-EU-tick-and-cost.md`](exp-EU-tick-and-cost.md) | 完成（含撤回） |
| 21 | **E-V** 闸门摊薄口径的隐含依赖（它凭什么成立） | 闸门按摊薄计价而运行期约束是每拍 —— 这是不是一个真洞？ | 1. 把 128 条 PID 全写 phase 0 部署，看扫描段会不会爆<br>2. 读桶表看相位到底怎么分的<br>3. 对照 div0-only 程序做恒等性回归 | `exp_ev_gate_worst_phase.py` | 不是洞（实测扫描段最大 262 cyc）：staging 覆写载荷相位 => 摊薄式成立，但依赖未声明；我改的那版会误拒合法程序 64 倍，已回退 | [`exp-EV-gate-phase-dependency.md`](exp-EV-gate-phase-dependency.md) | 完成（含撤回） |
| 22 | **E-W** 三层能真的把路由放到 div1/div2 | OUTPUT ... PERIOD= 能不能用？三档周期是不是编译器承诺的那个数？ | 1. 编译期：档位表与固件 DT_* 各自派生后比对<br>2. 部署三档程序，读 OFF_TICK_STATS 看增量落在哪一档<br>3. 再加「他档增量恒 0」的负对照 | `exp_ew_output_period.py` | 18 项 0 FAIL / 0 SKIP；在板三档速率 2.0000 / 0.2000 / 0.03125（偏 +0.0 / +0.1 / +0.2%）；并拆掉 PERIOD=10ms 这个假承诺（真实 6.4 ms） | [`exp-EW-output-period.md`](exp-EW-output-period.md) | 完成 |
| 23 | **E-X** 三层表达力：ABS 组合展开 · 数字文法收敛 · 变量阈值 | 引擎没有的原语，能不能用已有原语的组合表达且语义等价？ | 1. 结构核对（展开后的接线逐字段）<br>2. 在板数值等价（按 float 位模式比）<br>3. 负对照：非法字面量必须干净报错 | `exp_ex_composed_blocks.py` | 14 项 0 FAIL / 0 SKIP；ABS 对 15 个输入逐位等于 abs()（含 -0.0 变 +0.0）；数字文法 11 份收敛成 1 份 | [`exp-EX-composed-blocks.md`](exp-EX-composed-blocks.md) | 完成 |
| 24 | **E-Y** LUT 表到底住在哪（先量，再决定架构） | 0x23 写得进 LUT 区，那 deploy 与持久化与复位会不会动它？ | 1. 源码级：deploy 生效路径是否碰 LUT<br>2. 在板：写表、部署、复位，逐步读回<br>3. 查 fill_tables 是否覆盖它 | `exp_ey_lut_provenance.py` | 8 项 0 FAIL / 1 SKIP：表住在无主的易失内存里（deploy 不碰、复位清零、reinit 会覆盖）=> 据此定了归属方案 A | [`exp-EY-lut-provenance.md`](exp-EY-lut-provenance.md) | 完成 |
| 25 | **E-Z** 运行期预算随拍长缩放（关掉耦合 #6） | EXEC_BUDGET_CYCLES 是手写的绝对周期数 —— 换拍长后它还成立吗？ | 1. 用编译器当预言机读编译期常量（探针 TU 加静态断言）<br>2. 负对照：200 µs 档期望 32000 必须编不过<br>3. 变异对照：把派生式换回手写数，200 µs 档必须红 | `exp_ez_exec_budget_scale.py` | 8 项 0 FAIL；改为派生加四条断言；交付档指纹不变（逐位无回归）；顺带查出恢复脚本基线过期（P32） | [`exp-EZ-exec-budget-scale.md`](exp-EZ-exec-budget-scale.md) | 完成 |
| 26 | **E-FA** 内存宪法判据总跑（账本 · 归属 · 容量 · 性质） | 内存的四类判据能不能一起跑、且每条都有变异对照？ | 1. 账本 C2~C8（含文档容量对账）<br>2. 归属 C0/C9/C12 加 C9 变异对照<br>3. 容量 C11 变异（改上限看编译器是否跟上）<br>4. 在板栈水位 F1/F2/F3 | `exp_fa_mem_account.py` | 离线 14 项 0 FAIL / 1 SKIP（在板项因板子不在场，已登记 RIG-3）；账本第一次运行就抓出 7 处过期数字（ITCM 已满，见 P33） | [`PLAN-memmap-constitution.md`](PLAN-memmap-constitution.md)<br>[`MEMORY-LAYOUT.md`](MEMORY-LAYOUT.md) | 完成（在板项挂台架） |
| 27 | **TCM-1** 拍级确定性的决定性问题（换仪器后精度 0.06 cyc） | 拍级执行时间到底有多确定？用什么仪器才量得准？ | 1. 换仪器（避开调试单元被 pyocd 关掉的坑）<br>2. 重复测同一程序<br>3. 看分布与最小可分辨差 | `h723_tick_determinism.py` | 换仪器后精度 0.06 cyc —— 决定性问题第一次被回答 | [`README.md`](exp-TCM-cycles/README.md) | 完成 |
| 28 | **MDMA-1** MDMA 锁存链为什么没按设计工作 | 设计承诺的「输出由硬件定时器锚定」到底成不成立？ | 1. 扫 MDMA CTBR.TSEL 全部 16 个取值<br>2. 看引脚时刻是否随触发源变<br>3. 与「CPU 直写」档对照 | `h723_latch_sweep.py`<br>`h723_latch_attrib.py` | 未达成：TSEL 全扫都照常工作 => 通道在自循环、不消费请求源 => 引脚时刻等于 CPU 最后写影子加 0.12 µs（决策：默认走 CPU 直写档） | [`README.md`](exp-2026-09-14-mdma-trigger/README.md)<br>[`ARCH-TIMELINE-CPU-MDMA.md`](ARCH-TIMELINE-CPU-MDMA.md) | 完成（含负结果） |

**统计**：登记实验 **28** 个 · 涉及工具 **34** 个 · 涉及文档 **27** 份 · 无专文的实验 **2** 个。

