# STATUS — 2026-09-16

> 接 `STATUS-2026-09-15.md`。**本文是入口层** —— 想知道"现在能做什么、接口在哪、哪些坑长什么样"，先读这一份。
> 本轮主题：**DCL 程序持久化（S3–S7）从零打通**，并挖出四个真 bug。

---

## 0. 一句话现状

**"用这个系统 = 写 DCL 程序" 这条链现在是通的**：
程序**走通信上传 → 落 SD 卡（A/B 双副本）→ 上电自动装载进引擎**，
全程**不碰 flash 烧录**，且每一环都有能失败的判据。
引擎内核（100 µs 硬拍 / ISR 全在 ITCM / 19 原语 / 热重载 / 下载前静态校验）**未受影响**。

## 1. ★ 新增的对外接口（`0x45`–`0x4A`）

| 码 | 名称 | 载荷 → 应答 |
|---|---|---|
| `0x45` | PROG_BEGIN | `[total_len:u32][crc32:u32][prog_id:u32][prog_ver:u16][min_fw:u16][req_caps:u32]` → `[total_len:u32]` |
| `0x46` | PROG_DATA | `[offset:u32][chunk...]` → `[next_offset:u32]`（**必须顺序、不跳不重**） |
| `0x47` | PROG_COMMIT | 空 → `[rc:u32][budget:u32]` |
| `0x48` | PROG_STATUS | 空 → **96 B**（见 §2） |
| `0x49` | PROG_ERASE | 空 → `[rc:u32]` |
| `0x4A` | DEVICE_DESC | 空 → `[fw:u16][cap_lo:u16][cap_hi:u16][n:u16]` + `n×[device_type:u16][product_code:u16][revision:u16]` |

**`0x48` 的 96 字节布局**（★ 它同时是"程序存储体检表"和"事务对账表"）：
```
+0  part_ok       +4  ab_valid(bit0=A bit1=B)   +8  seq_a      +12 seq_b
+16 crc_a         +20 crc_b                      +24 len_a      +28 len_b
+32 active(0=A 1=B 0xFFFFFFFF=无)                +36 ok_n       +40 fail_n
+44 reject_n      +48 last_rc                    +52 txn_begin  +56 txn_commit
+60 txn_abort     +64 boot_rc                    +68 boot_loaded
+72 deploy_routes +76 deploy_seq
+80..92  ★ 从卡上读回的载荷头 16 字节（判"我到底读回了什么"）
```

## 2. ★★ AXI 真实地图（**320 KB 已用满 —— 加缓冲前必须先看这张表**）

| 区间 | 占用者 | 大小 |
|---|---|---|
| `0x24000000`–`0x24000600` | 引擎诊断结构固定区（`SD_DIAG`/`BB_DIAG`/`SD_CFG`/`BOOT_AXI`）；**协议 `0x20 READ` 的放行窗口** | 1.5 KB |
| **`0x24000600`–`0x24003000`** | ★ **唯一空档** —— 程序存储缓冲在此（`0x24000800` / `0x24002800`） | ≈10.5 KB |
| `0x24003000` / `0x24003100` | MDMA 锁存快照 / 链表节点（`do.c`） | — |
| `0x24004000`–`0x24040000` | 黑匣子环（`BB_SLOTS=960` × 256 B = **240 KB**） | 240 KB |
| `0x24040000`–`0x24050000` | 日志冻结区 `SD_STAGE` | 64 KB |

★ `prog_store.c` 里有**构建期 `#if/#error`**：缓冲地址越出那个空档就**编译不过**。
★ `blackbox.h` 原写"AXI 完全空闲 / 128 KB / 512 槽" —— **三处都是过期的**，已更正。
  **照过期注释找空档的代价是白查一整轮**（缓冲落进环里，每拍被覆写）。

## 3. ★ SD 卡分区（S3）

```
LBA0                     日志头（扩展段 h[112]=LOG_END, h[113]="LEND"）
LBA1 .. LOG_END          日志数据区（环形回卷）
g_sd_prog_a .. +15      程序副本 A（16 块 = 8 KB）
g_sd_prog_b .. +15      程序副本 B
LOG_END = capacity - 1 - 32     ← 由**容量**算出，不是硬编码
```
★ **绝不升 `LOG_VERSION`**（一升版本 `sd_log_open` 就当新卡重建 ⇒ 从 LBA1 覆写毁掉卡上记录）。
★ 旧卡无该字段 ⇒ 不影响任何既有校验（`h[0]/h[1]/h[2]/h[6]` 四条照旧）⇒ 向后兼容。
★ **`s_log_blk` 必须钳位**：旧卡游标可能超出新上限，不钳则 `cap_data - s_log_blk` **下溢**、写飞出卡外。

## 4. 四个真 bug（**都是同族的，看一般化那一列**）

| # | 症状 | 根因 | 一般化 |
|---|---|---|---|
| 1 | 整条 SD 路崩（`blk_this_boot=0, batch_err=3099`） | 我按 HAL 骨架重写 `sd_read_block` ⇒ **它返回的数据是错的** ⇒ 日志头读不对 ⇒ 当新卡重建 | **"半个对齐"比不对齐更坏**：只抄 while 骨架、换掉其下的数据搬运机制（IDMA→手工排 FIFO）= 没对齐前提 |
| 2 | `PROG_RC_WRITE` 写超时 | 缓冲在 **DTCM**，而 SDMMC 的 **IDMA 读不到 TCM 端口** | 外设 DMA 能到哪块内存**按外设成立**，不能按"上次那个能"类推 |
| 3 | 读回载荷头 = `"DLBK"`（`BBLOG_REC_MAGIC`） | 缓冲撞进黑匣子环（960 槽 × 256 B = 240 KB） | **"当前完全空闲"这类注释活不过两周** |
| 4 | 正常批写被判超时 | `SD_TMOUT_DATA` 被我挤到 200 万（≈250 ms） | **超时给够 + 调用方声明阻塞窗口**；挤超时是用判据掩盖慢 |

### 4.1 ★★ 两条**方法学**教训（比 bug 更值钱）
1. **`pyocd halt` 会把核留在停住状态** —— 我据此得到过一整串假结论（"`g_tick_count` 冻结 / ISR 死了 / 板子不复位"）。
   干净 `pyocd reset` 后不发 halt，串口立刻全好。
   **而 `0x48` 的应答本身就带 `txn_*` 计数 ⇒ 根本不需要 SWD。** ⇒ 违反了自己写的第 1 条纪律（观测先走协议内通道）。
2. **构建闸门不要用管道把门**：`build.sh | grep … && flash` —— `grep` 的退出码掩盖构建失败 ⇒ **烧了旧镜像**并据此误判一轮。

## 5. 现行权威入口（更新）

| 想知道什么 | 看哪一份 |
|---|---|
| 现在什么状态 | **本文** |
| **程序契约（应该是什么）** | ★ `docs/REF-program-contract.md`（§9 GAP 表已按实施结果更新） |
| 本轮实施纲领 | `docs/PLAN-dcl-standardization.md` |
| 引擎内部运作 | `docs/CORE-ENGINE.md` + `docs/ARCH-engine-datalink.md` |
| 平台细节 | `docs/ARCH-H723.md`（★ §5.2 命令表**尚未补 `0x45–0x4A`**，见 §6） |
| **AXI 地图** | `src/prog_store.c` 顶部（唯一权威）+ `src/blackbox.h` |
| SD/黑匣子 | `src/sd.c` 顶部 + `src/blackbox.h` |
| SHM 布局 | ★ **以 `src/engine.h` 为唯一权威** |
| 上一状态 | `docs/STATUS-2026-09-15.md` |

## 6. 未闭合

| # | 项 | 备注 |
|---|---|---|
| 1 | `ARCH-H723.md` §5.2 命令表缺 `0x45–0x4A` | 入口文档必须跟上（本轮的纪律） |
| 2 | 能力位仍是 u16 | 扩 u32 会动 `0x01` 线上格式，待 v1 评估 |
| 3 | **程序里不得出现引脚号**的静态检查（契约 §3.1） | 未做 |
| 4 | 外设能力做成"通用事务 op"（契约 §3.2-C） | I2C 仍是具名设备形态 |
| 5 | ~~`0x49 PROG_ERASE` 曾报 `erase failed`~~ → **✅ 已复测通过** | 实测：`ab_valid 3→0`、`seq A/B 3/4→0/0`、`生效=无`。先前那次失败发生在"坏固件期"，不是本版问题 |
| 6 | ~~未跑回归~~ → **已跑，见 §8** | 结果：**零回归**（所有失败项在基线上同样存在） |

## 8. ★ 回归套件结果（2026-09-16 跑 · **关键是"基线对照"，不是绝对数**）

> ★ 方法论：**单看"多少 PASS"没有意义** —— 必须先烧回基线跑同一批，才知道哪些失败是"本来就有的"。
> 下表就是这样得出的（同一块板、同一批脚本、连续两次烧录）。

| 套件 | 基线 | 本版 | 判读 |
|---|---|---|---|
| `proto` | 12/0 | **12 PASS / 0 FAIL** | ✓ |
| `seq` | 27/0 | **27 PASS / 0 FAIL** | ✓ |
| `macro` | 18/0 | **18 PASS / 0 FAIL** | ✓ |
| `jitter` | 9/0 | **9 PASS / 0 FAIL** | ✓ |
| `w2_probe` | 7 PASS / 7 FAIL | **14 PASS / 0 FAIL** | ★★ **满分（+7）** |
| `audit_m234` | 7 PASS / 5 FAIL | **9 PASS / 3 FAIL** | ★ **+2**（M2 两项经查是**测试缺陷**，已修工具） |
| `persist` | 8 PASS / 18 FAIL | **9 PASS / 17 FAIL** | ★ +1；失败面是 `DCL_PERSIST_SAVE=0`（按设计 NAK） |
| `w1` | 27 PASS / 1 FAIL | 27 PASS / 1 FAIL | 相同 ⇒ **预先存在**（`R11`：测试把 marker 写在 `SENSOR[0]`，被 `as5600_poll` 每 10 ms 覆盖 ⇒ **测试缺陷，固件正确**） |
| `modbus` | 5 PASS / 10 FAIL | 5 PASS / 10 FAIL | 相同 ⇒ **接线/环境**（需 RS-485 回路） |
| `w5` | 10 PASS / 3 FAIL / 4 SKIP | 10 PASS / 3 FAIL / 4 SKIP | 相同 ⇒ **设计内**（3 项是 HIL/TIM3 PWM，而 **TIM3 已被步进模块接管** `g_step_owns_tim3=1`） |

**⇒ 结论：本版 S3–S7 的改动没有引入任何回归，且比基线**多 10 项 PASS**。**
（`w5` 曾出现一次 9/4，连跑两次都回到 10/3 ⇒ 是抖动项，不是回归。）

### 8.1 失败项逐条归因（2026-09-16 追查）

| 失败项 | 归因 | 结论 |
|---|---|---|
| `w1` **R11**（`0x23 含 NaN → NAK 且前段未被写`） | ★ **测试缺陷，固件正确**。测试把 marker 写在 `SHM + OFF_SENSOR_MAP` = **`SENSOR[0]`**，而 **`as5600_poll` 每 10 ms 就往 `SENSOR[0]` 写角度原始值** ⇒ marker 被固件自己覆盖，判据必然误报。<br>读代码确认：`h_write_burst_w1` 是**两相**的（先全查 NaN、**再全写**）⇒ 拒绝时不产生副作用 ✓ | ✅ **固件无问题**（测试应换一个"固件不拥有的槽"，如 `WIRE[100]`） |
| `audit_m234` **M3-b / M3-d / M3-e** | **设计内**：`DCL_PERSIST_SAVE=0` ⇒ `0x43` 落盘明确 NAK | ✅ 预期 |
| `audit_m234` **M2-1 / M2-2** | ✅ **已定案并修复（测试缺陷，固件正确）**。真因：`c->tx_uart` **默认 = 1 = 响应从物理口发**（`modbus.c:798`），而物理口模式下发完就清 TX 缓冲（`modbus.c:560/569`）⇒ `0x61` 的 `tx_len` 恒 0 ⇒ 测试拿到的 `resp` 永远为空。**本测试验的其实是"0x61 在默认配置下读不到"，而不是 Modbus 的 qty 边界。**<br>⇒ 测试里加 `0x62 [0][0]` 切缓冲模式后实测：**qty 10/59/60/64 全部得 8 字节正常响应**（`0110 9c81 00xx …`）、**qty=65 得 5 字节异常 02**（`0190 02 …`）⇒ **`MB_MAX_FRAME=255` 的修复完全成立**，连它特意测的"旧代码 60/64 必 NAK"边界也过了。<br>**⇒ `audit_m234` 从 7 PASS/5 FAIL 变为 9 PASS/3 FAIL** | ✅ **已修工具** |
| `modbus` 10 项 | 需 RS-485 物理回路 | 环境 |
| `w5` 3 项 | HIL/TIM3 PWM，而 **TIM3 已被步进模块接管**（`g_step_owns_tim3=1`） | 设计内 |

### 8.1.1 ★ 另一条我先前的误判（**撤回**）
我写过"`dclc.py` 成功提示不严谨 —— `deploy+START` 返回 `(None)` 时照样打 `[OK]`" —— **错了**。
那个 `(None)` 是**未解析的请求端口**（`port=None` 走自动找板），**deploy 其实成功了**。
★ 但它确实有**两个真小毛病，已修**：
1. 提示里印的是**请求**端口（自动找板时为 None）⇒ 改成印**解析后**的（`dcl.port`）；
2. **`0x11 START` 只发不判返回** —— 而"deploy 成功、START 被拒"（如 F11 预算门拦下历史毒药表）
   是一个真实状态，那时脚本照样打 `[OK] … 引擎 RUN` **而引擎其实没跑** ⇒ 已改成判返回并报错。
★ 纪律提醒：**这两次误判都是"读代码/看输出之前先下了结论"。**

### 8.2 ★★ 设计耦合点：**实测推翻了理论 —— 不能宣称"有问题"也不能宣称"余量充足"**

```
src/uart.c:35       RX_RING_SZ        = 512      ← 接收环
src/transport.h:148 FRAME_PAYLOAD_MAX = 6150     ← 协议宣称的最大帧
```

**先把"帧大小受不受环限制"测清（`0x23` 往固件不拥有的 `WIRE[100]` 写，扫 qty）：**

| qty | 50 | 100 | 124 | **125** | **126** | 128 | 160 | 200 | **256** |
|---|---|---|---|---|---|---|---|---|---|
| 帧总长 | 212 | 412 | 508 | **512** | **516** | 524 | 652 | 812 | **1036** |
| 结果 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | **✓ 整帧收到并写对** |

**⇒ 帧大小本身不受环限制** —— 主循环**边收边排空**，环只是延迟缓冲。
（★ 这同时**证明了 `w1 R11` 的 1036 字节帧是完整到达的** ⇒ 它的 NAK 确实是为 NaN ⇒ R11 是测试缺陷，有了实证。）

**再量"阻塞时长 vs 环能吸收多久"（COMMIT 的 SD 落盘为阻塞源）：**

| 载荷 | 块数 | COMMIT 往返 | 占理论预算(44.4 ms) | uart_ore / drop / frame_bad |
|---|---|---|---|---|
| 54 B | 1 | 31.8 ms | 72% | **0 / 0 / 0** |
| 1030 B | 3 | 31.3 ms | 70% | 0 / 0 / 0 |
| 2054 B | 5 | **48.2 ms** | **108%** | **0 / 0 / 0** |
| 4102 B | 9 | **48.4 ms** | **109%** | **0 / 0 / 0** |

**⇒ 阻塞已达理论预算的 108%，却一个字节都没丢。**
⇒ **`512 B ÷ 11.52 B/ms = 44.4 ms` 这个上界是偏保守的**（环的实际可吸收量更大，或有额外的硬件/驱动缓冲）。
⇒ **诚实结论：这是一个"已量化、已加观测闸门"的耦合点，但当前实测没有触发。**
   **既不能说"出问题了"，也不能说"余量充足"** —— 要知道真实上界，得**故意把阻塞拉长**再做一次。

### 8.2.1 ★ 本轮为此补的观测闸门（**这是本节真正的交付**）
`0x38` 的应答从 **39 → 51 字节**，尾部追加：
```
+39  uart_ore     (硬件 ORE 溢出次数)
+43  uart_drop    (软件环满丢弃次数)
+47  frame_bad    (坏帧数)
```
**判据：正常收发下这三个必须恒为 0。非 0 ⇒ 有帧被截断 ⇒ 该次"NAK/无应答"不是固件的判断，
而是数据没到全。**
★ 为什么必须补：这三个量原来**只刷进全局变量、不进任何应答面** ⇒
**"帧被截断"在协议面上完全不可见**（只能 SWD 读 DTCM，而 SWD 会停核）。
这违反本项目自己的纪律"**新观测变量必须能被外部读走**"，也和 `r[38] out_surfaces` 是同一个理由 ——
**把"读数一样、事实不同"的两件事分开。**

### 8.3 更正：注释里的**旧数字**比代码更容易被当成事实
`modbus.c` 里那句"`MB_MAX_FRAME(128, 请求上限)`"**是过期注释**（常量早已升到 255）。已更正，
并写明纪律：**注释里不要写常量字面值，写常量名**（或紧跟 `_Static_assert`）。
★ 本项目已因此白查过一整轮（`blackbox.h` 的"AXI 完全空闲 / 128KB / 512 槽"）。

## 7. 复现（**全程串口，零 pyocd**）

```bash
python docs/audit/h723_prog_store_test.py status   # 0x48 体检 + 事务对账
python docs/audit/h723_prog_store_test.py desc     # 0x4A 设备描述
python docs/audit/h723_prog_store_test.py gate2    # 闸2: 篡改 ⇒ NAK crc mismatch
python docs/audit/h723_prog_store_test.py gate5    # 闸5: 结构非法 ⇒ NAK payload short
```
★ 客户端用 `docs/audit/h723_motion_probe.py` 的 `Dut`，或 `tools/h723_client.py` 的 `Dcl` —— **两者都自带 CRC16 校验与错位重找**。

---

## 9. ★★★ 发布就绪评估（2026-09-16 12:00）

### 9.1 结论：**可以发布** —— 但 release notes 必须写清下面三条

| 维度 | 状态 | 证据 |
|---|---|---|
| 代码 | ✅ 工作区干净；本地 = `origin` = `dcl` = `0cf007d` | `ls-remote` 直连核对 |
| 构建 | ✅ 零警告闸门 + ISR 调用树闸门通过 | `bash build.sh` exit=0 |
| 回归 | ✅ **零回归**（12 套；所有红项基线上同样存在，且 2 项变好） | 见 §8 的基线对照表 |
| **全链** | ✅ **`.dcl 源 → dclc 编译 → 通信上传 → 存 SD → 上电自动装载` 实测通过** | 编译 `examples/hiloop.dcl` → 上传 → 复位 → `n_routes=7` |
| 板上/卡上 | ✅ 干净：`n_routes=7`（真实示例）、`uart_ore/drop/frame_bad = 0` | `0x38`/`0x48` 实测 |

### 9.2 ★ 必须写进 release notes 的**破坏性变更**
> **`0x38 ENGINE_STATUS` 应答 39 → 51 字节**（尾部新增 3 个 u32：`uart_ore` / `uart_drop` / `frame_bad`）。
> **前 31 字节布局与语义未动**（与 S3 一致），新增量一律往**尾部**追加 ⇒
> **按 `len >= 39` 或按绝对偏移读前 39 字节的客户端仍然兼容**；
> 只有"长度必须恰好 39"的客户端会失效。

### 9.3 已知问题（**均已定案；无一阻塞发布**）

| # | 项 | 性质 | 状态 |
|---|---|---|---|
| 1 | `audit_m234` **M2-1/M2-2** 曾全红 | **测试缺陷，固件正确** —— `tx_uart` 默认 =1（响应走物理口，缓冲随即清空），而测试从没切到缓冲模式 | ✅ **已修工具**（`0x62 [0][0]`）⇒ 该项 **7P/5F → 9P/3F** |
| 2 | `w1` **R11** 曾报"NaN 被拒但前段已写" | **测试缺陷，固件正确** —— marker 写在 `SENSOR[0]`，被 `as5600_poll` 每 10 ms 覆盖；`h_write_burst_w1` 实为**两相**（先全查后全写）⇒ **拒绝时无副作用** | ✅ 已定案（我先前误判为"非原子"，**已撤回**） |
| 3 | `dclc.py` 提示印 `(None)` | **我误判** —— 那是未解析的**请求**端口（自动找板），deploy 其实成功了 | ✅ 已修：改印解析后的端口；**并补上 `0x11 START` 的返回判定**（原来只发不判） |
| 4 | `w5` 3 项 | HIL/TIM3 PWM，而 **TIM3 已被步进模块接管**（`g_step_owns_tim3=1`） | 设计内 |
| 5 | `modbus` 10 项 | 需 RS-485 物理回路 | 环境 |
| 6 | `audit_m234` M3 3 项 | `DCL_PERSIST_SAVE=0` 按设计 NAK | 预期 |
| 7 | `RX_RING(512) vs FRAME_PAYLOAD_MAX(6150)` | **已量化 + 已加观测闸门**；实测到 **108% 预算仍未触发** ⇒ 真实上界未知 | 已知 |
| 8 | 能力位 u16→u32、程序面"不得出现引脚号"静态检查、通用外设事务 op | — | 下一代 |

### 9.4 发布动作（**待用户定**）
- tag 名：现有 `v0.1.0` / `v0.1.1` / `v1.0.0` ⇒ 下一个自然是 **`v2.0.0`**
- 打哪：**`84e1f15`**（两个远程都已同步）
- ★ **本节只做评估，没打 tag** —— 打 tag + 写 release 属对外动作，等确认。

### 7.1 ★★ 更正一条我先前的错误结论 + 一个真的协议缺陷
**我先前写过"`h723_client.Dcl.send()` 会返回假 ACK（帧错位）"—— 查证后这是错的。**
`h723_modbus.Link.xact()` 里有 `if crc16(body) == crc_rx: return …`，也做了 `del buf[0]` 的错位重找。
**它没有撒谎。**

真正发生的事：当时固件在复位循环，串口上不停出现**开机 banner**：
```
c1 00 04 00 | 00 02 f7 0d | f4 c2      ← CRC 完全合法的一条帧
```
而**协议帧里既没有"命令码回显"、也没有序号** ⇒
**客户端无法判断这条应答是不是属于自己刚发出的那条请求** ⇒ banner 被当成了 ACK。
（那轮我看到的 `BEGIN/DATA/COMMIT` 全部 ACK，其实是 banner；固件侧 `txn_*` 恒为 0 才是真话。）

> **⇒ 这是协议层的真缺陷，建议进 v1**：应答里带 **`最近命令码`**（1 字节）或**序号**，
> 让客户端能判"这条应答是我的吗"。现在的口径是"收到任何合法帧就当应答"，
> **任何多余帧（开机 banner、上一条的迟到应答、别的写者）都会污染判据。**

★ 顺带：`h723_modbus.main()` 里已有 `revive_if_dead()`（"M4 sentinel: un-halt the core if a pyocd
session left it halted"）⇒ **"pyocd 会把核留在 halted"这件事项目里早已知道**，我这一轮又踩了一次。
