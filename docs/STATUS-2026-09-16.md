# STATUS — 2026-09-16

> 接 `STATUS-2026-09-15.md`。**本文是入口层** —— 想知道"现在能做什么、接口在哪、哪些坑长什么样"，先读这一份。
> 本轮主题：**DCL 程序持久化（S3–S7）从零打通**，并挖出四个真 bug。

---

## 0. 一句话现状

**"用这个系统 = 写 DCL 程序" 这条链现在是通的**：
程序**走通信上传 → 落 SD 卡（A/B 双副本）→ 上电自动装载进引擎**，
全程**不碰 flash 烧录**，且每一环都有能失败的判据。
引擎内核（100 µs 硬拍 / ISR 全在 ITCM / 19 原语 / 热重载 / 下载前静态校验）**未受影响**。

## 1. ★ 新增的对外接口（`0x45`–`0x4A`）

| 码 | 名称 | 载荷 → 应答 |
|---|---|---|
| `0x45` | PROG_BEGIN | `[total_len:u32][crc32:u32][prog_id:u32][prog_ver:u16][min_fw:u16][req_caps:u32]` → `[total_len:u32]` |
| `0x46` | PROG_DATA | `[offset:u32][chunk...]` → `[next_offset:u32]`（**必须顺序、不跳不重**） |
| `0x47` | PROG_COMMIT | 空 → `[rc:u32][budget:u32]` |
| `0x48` | PROG_STATUS | 空 → **96 B**（见 §2） |
| `0x49` | PROG_ERASE | 空 → `[rc:u32]` |
| `0x4A` | DEVICE_DESC | 空 → `[fw:u16][cap_lo:u16][cap_hi:u16][n:u16]` + `n×[device_type:u16][product_code:u16][revision:u16]` |

**`0x48` 的 96 字节布局**（★ 它同时是"程序存储体检表"和"事务对账表"）：
```
+0  part_ok       +4  ab_valid(bit0=A bit1=B)   +8  seq_a      +12 seq_b
+16 crc_a         +20 crc_b                      +24 len_a      +28 len_b
+32 active(0=A 1=B 0xFFFFFFFF=无)                +36 ok_n       +40 fail_n
+44 reject_n      +48 last_rc                    +52 txn_begin  +56 txn_commit
+60 txn_abort     +64 boot_rc                    +68 boot_loaded
+72 deploy_routes +76 deploy_seq
+80..92  ★ 从卡上读回的载荷头 16 字节（判"我到底读回了什么"）
```

## 2. ★★ AXI 真实地图（**320 KB 已用满 —— 加缓冲前必须先看这张表**）

| 区间 | 占用者 | 大小 |
|---|---|---|
| `0x24000000`–`0x24000600` | 引擎诊断结构固定区（`SD_DIAG`/`BB_DIAG`/`SD_CFG`/`BOOT_AXI`）；**协议 `0x20 READ` 的放行窗口** | 1.5 KB |
| **`0x24000600`–`0x24003000`** | ★ **唯一空档** —— 程序存储缓冲在此（`0x24000800` / `0x24002800`） | ≈10.5 KB |
| `0x24003000` / `0x24003100` | MDMA 锁存快照 / 链表节点（`do.c`） | — |
| `0x24004000`–`0x24040000` | 黑匣子环（`BB_SLOTS=960` × 256 B = **240 KB**） | 240 KB |
| `0x24040000`–`0x24050000` | 日志冻结区 `SD_STAGE` | 64 KB |

★ `prog_store.c` 里有**构建期 `#if/#error`**：缓冲地址越出那个空档就**编译不过**。
★ `blackbox.h` 原写"AXI 完全空闲 / 128 KB / 512 槽" —— **三处都是过期的**，已更正。
  **照过期注释找空档的代价是白查一整轮**（缓冲落进环里，每拍被覆写）。

## 3. ★ SD 卡分区（S3）

```
LBA0                     日志头（扩展段 h[112]=LOG_END, h[113]="LEND"）
LBA1 .. LOG_END          日志数据区（环形回卷）
g_sd_prog_a .. +15      程序副本 A（16 块 = 8 KB）
g_sd_prog_b .. +15      程序副本 B
LOG_END = capacity - 1 - 32     ← 由**容量**算出，不是硬编码
```
★ **绝不升 `LOG_VERSION`**（一升版本 `sd_log_open` 就当新卡重建 ⇒ 从 LBA1 覆写毁掉卡上记录）。
★ 旧卡无该字段 ⇒ 不影响任何既有校验（`h[0]/h[1]/h[2]/h[6]` 四条照旧）⇒ 向后兼容。
★ **`s_log_blk` 必须钳位**：旧卡游标可能超出新上限，不钳则 `cap_data - s_log_blk` **下溢**、写飞出卡外。

## 4. 四个真 bug（**都是同族的，看一般化那一列**）

| # | 症状 | 根因 | 一般化 |
|---|---|---|---|
| 1 | 整条 SD 路崩（`blk_this_boot=0, batch_err=3099`） | 我按 HAL 骨架重写 `sd_read_block` ⇒ **它返回的数据是错的** ⇒ 日志头读不对 ⇒ 当新卡重建 | **"半个对齐"比不对齐更坏**：只抄 while 骨架、换掉其下的数据搬运机制（IDMA→手工排 FIFO）= 没对齐前提 |
| 2 | `PROG_RC_WRITE` 写超时 | 缓冲在 **DTCM**，而 SDMMC 的 **IDMA 读不到 TCM 端口** | 外设 DMA 能到哪块内存**按外设成立**，不能按"上次那个能"类推 |
| 3 | 读回载荷头 = `"DLBK"`（`BBLOG_REC_MAGIC`） | 缓冲撞进黑匣子环（960 槽 × 256 B = 240 KB） | **"当前完全空闲"这类注释活不过两周** |
| 4 | 正常批写被判超时 | `SD_TMOUT_DATA` 被我挤到 200 万（≈250 ms） | **超时给够 + 调用方声明阻塞窗口**；挤超时是用判据掩盖慢 |

### 4.1 ★★ 两条**方法学**教训（比 bug 更值钱）
1. **`pyocd halt` 会把核留在停住状态** —— 我据此得到过一整串假结论（"`g_tick_count` 冻结 / ISR 死了 / 板子不复位"）。
   干净 `pyocd reset` 后不发 halt，串口立刻全好。
   **而 `0x48` 的应答本身就带 `txn_*` 计数 ⇒ 根本不需要 SWD。** ⇒ 违反了自己写的第 1 条纪律（观测先走协议内通道）。
2. **构建闸门不要用管道把门**：`build.sh | grep … && flash` —— `grep` 的退出码掩盖构建失败 ⇒ **烧了旧镜像**并据此误判一轮。

## 5. 现行权威入口（更新）

| 想知道什么 | 看哪一份 |
|---|---|
| 现在什么状态 | **本文** |
| **程序契约（应该是什么）** | ★ `docs/REF-program-contract.md`（§9 GAP 表已按实施结果更新） |
| 本轮实施纲领 | `docs/PLAN-dcl-standardization.md` |
| 引擎内部运作 | `docs/CORE-ENGINE.md` + `docs/ARCH-engine-datalink.md` |
| 平台细节 | `docs/ARCH-H723.md`（★ §5.2 命令表**尚未补 `0x45–0x4A`**，见 §6） |
| **AXI 地图** | `src/prog_store.c` 顶部（唯一权威）+ `src/blackbox.h` |
| SD/黑匣子 | `src/sd.c` 顶部 + `src/blackbox.h` |
| SHM 布局 | ★ **以 `src/engine.h` 为唯一权威** |
| 上一状态 | `docs/STATUS-2026-09-15.md` |

## 6. 未闭合

| # | 项 | 备注 |
|---|---|---|
| 1 | `ARCH-H723.md` §5.2 命令表缺 `0x45–0x4A` | 入口文档必须跟上（本轮的纪律） |
| 2 | 能力位仍是 u16 | 扩 u32 会动 `0x01` 线上格式，待 v1 评估 |
| 3 | **程序里不得出现引脚号**的静态检查（契约 §3.1） | 未做 |
| 4 | 外设能力做成"通用事务 op"（契约 §3.2-C） | ~~I2C 仍是具名设备形态~~ → **✅ 已闭合（见 §12）**：G6-1..G6-4 全部落地 |
| 5 | ~~`0x49 PROG_ERASE` 曾报 `erase failed`~~ → **✅ 已复测通过** | 实测：`ab_valid 3→0`、`seq A/B 3/4→0/0`、`生效=无`。先前那次失败发生在"坏固件期"，不是本版问题 |
| 6 | ~~未跑回归~~ → **已跑，见 §8** | 结果：**零回归**（所有失败项在基线上同样存在） |

## 8. ★ 回归套件结果（2026-09-16 跑 · **关键是"基线对照"，不是绝对数**）

> ★ 方法论：**单看"多少 PASS"没有意义** —— 必须先烧回基线跑同一批，才知道哪些失败是"本来就有的"。
> 下表就是这样得出的（同一块板、同一批脚本、连续两次烧录）。

| 套件 | 基线 | 本版 | 判读 |
|---|---|---|---|
| `proto` | 12/0 | **12 PASS / 0 FAIL** | ✓ |
| `seq` | 27/0 | **27 PASS / 0 FAIL** | ✓ |
| `macro` | 18/0 | **18 PASS / 0 FAIL** | ✓ |
| `jitter` | 9/0 | **9 PASS / 0 FAIL** | ✓ |
| `w2_probe` | 7 PASS / 7 FAIL | **14 PASS / 0 FAIL** | ★★ **满分（+7）** |
| `audit_m234` | 7 PASS / 5 FAIL | **9 PASS / 3 FAIL** | ★ **+2**（M2 两项经查是**测试缺陷**，已修工具） |
| `persist` | 8 PASS / 18 FAIL | **9 PASS / 17 FAIL** | ★ +1；失败面是 `DCL_PERSIST_SAVE=0`（按设计 NAK） |
| `w1` | 27 PASS / 1 FAIL | 27 PASS / 1 FAIL | 相同 ⇒ **预先存在**（`R11`：测试把 marker 写在 `SENSOR[0]`，被 `as5600_poll` 每 10 ms 覆盖 ⇒ **测试缺陷，固件正确**） |
| `modbus` | 5 PASS / 10 FAIL | 5 PASS / 10 FAIL | 相同 ⇒ **接线/环境**（需 RS-485 回路） |
| `w5` | 10 PASS / 3 FAIL / 4 SKIP | 10 PASS / 3 FAIL / 4 SKIP | 相同 ⇒ **设计内**（3 项是 HIL/TIM3 PWM，而 **TIM3 已被步进模块接管** `g_step_owns_tim3=1`） |

**⇒ 结论：本版 S3–S7 的改动没有引入任何回归，且比基线**多 10 项 PASS**。**
（`w5` 曾出现一次 9/4，连跑两次都回到 10/3 ⇒ 是抖动项，不是回归。）

### 8.1 失败项逐条归因（2026-09-16 追查）

| 失败项 | 归因 | 结论 |
|---|---|---|
| `w1` **R11**（`0x23 含 NaN → NAK 且前段未被写`） | ★ **测试缺陷，固件正确**。测试把 marker 写在 `SHM + OFF_SENSOR_MAP` = **`SENSOR[0]`**，而 **`as5600_poll` 每 10 ms 就往 `SENSOR[0]` 写角度原始值** ⇒ marker 被固件自己覆盖，判据必然误报。<br>读代码确认：`h_write_burst_w1` 是**两相**的（先全查 NaN、**再全写**）⇒ 拒绝时不产生副作用 ✓ | ✅ **固件无问题**（测试应换一个"固件不拥有的槽"，如 `WIRE[100]`） |
| `audit_m234` **M3-b / M3-d / M3-e** | **设计内**：`DCL_PERSIST_SAVE=0` ⇒ `0x43` 落盘明确 NAK | ✅ 预期 |
| `audit_m234` **M2-1 / M2-2** | ✅ **已定案并修复（测试缺陷，固件正确）**。真因：`c->tx_uart` **默认 = 1 = 响应从物理口发**（`modbus.c:798`），而物理口模式下发完就清 TX 缓冲（`modbus.c:560/569`）⇒ `0x61` 的 `tx_len` 恒 0 ⇒ 测试拿到的 `resp` 永远为空。**本测试验的其实是"0x61 在默认配置下读不到"，而不是 Modbus 的 qty 边界。**<br>⇒ 测试里加 `0x62 [0][0]` 切缓冲模式后实测：**qty 10/59/60/64 全部得 8 字节正常响应**（`0110 9c81 00xx …`）、**qty=65 得 5 字节异常 02**（`0190 02 …`）⇒ **`MB_MAX_FRAME=255` 的修复完全成立**，连它特意测的"旧代码 60/64 必 NAK"边界也过了。<br>**⇒ `audit_m234` 从 7 PASS/5 FAIL 变为 9 PASS/3 FAIL** | ✅ **已修工具** |
| `modbus` 10 项 | 需 RS-485 物理回路 | 环境 |
| `w5` 3 项 | HIL/TIM3 PWM，而 **TIM3 已被步进模块接管**（`g_step_owns_tim3=1`） | 设计内 |

### 8.1.1 ★ 另一条我先前的误判（**撤回**）
我写过"`dclc.py` 成功提示不严谨 —— `deploy+START` 返回 `(None)` 时照样打 `[OK]`" —— **错了**。
那个 `(None)` 是**未解析的请求端口**（`port=None` 走自动找板），**deploy 其实成功了**。
★ 但它确实有**两个真小毛病，已修**：
1. 提示里印的是**请求**端口（自动找板时为 None）⇒ 改成印**解析后**的（`dcl.port`）；
2. **`0x11 START` 只发不判返回** —— 而"deploy 成功、START 被拒"（如 F11 预算门拦下历史毒药表）
   是一个真实状态，那时脚本照样打 `[OK] … 引擎 RUN` **而引擎其实没跑** ⇒ 已改成判返回并报错。
★ 纪律提醒：**这两次误判都是"读代码/看输出之前先下了结论"。**

### 8.2 ★★ 设计耦合点：**实测推翻了理论 —— 不能宣称"有问题"也不能宣称"余量充足"**

```
src/uart.c:35       RX_RING_SZ        = 512      ← 接收环
src/transport.h:148 FRAME_PAYLOAD_MAX = 6150     ← 协议宣称的最大帧
```

**先把"帧大小受不受环限制"测清（`0x23` 往固件不拥有的 `WIRE[100]` 写，扫 qty）：**

| qty | 50 | 100 | 124 | **125** | **126** | 128 | 160 | 200 | **256** |
|---|---|---|---|---|---|---|---|---|---|
| 帧总长 | 212 | 412 | 508 | **512** | **516** | 524 | 652 | 812 | **1036** |
| 结果 | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | **✓ 整帧收到并写对** |

**⇒ 帧大小本身不受环限制** —— 主循环**边收边排空**，环只是延迟缓冲。
（★ 这同时**证明了 `w1 R11` 的 1036 字节帧是完整到达的** ⇒ 它的 NAK 确实是为 NaN ⇒ R11 是测试缺陷，有了实证。）

**再量"阻塞时长 vs 环能吸收多久"（COMMIT 的 SD 落盘为阻塞源）：**

| 载荷 | 块数 | COMMIT 往返 | 占理论预算(44.4 ms) | uart_ore / drop / frame_bad |
|---|---|---|---|---|
| 54 B | 1 | 31.8 ms | 72% | **0 / 0 / 0** |
| 1030 B | 3 | 31.3 ms | 70% | 0 / 0 / 0 |
| 2054 B | 5 | **48.2 ms** | **108%** | **0 / 0 / 0** |
| 4102 B | 9 | **48.4 ms** | **109%** | **0 / 0 / 0** |

**⇒ 阻塞已达理论预算的 108%，却一个字节都没丢。**
⇒ **`512 B ÷ 11.52 B/ms = 44.4 ms` 这个上界是偏保守的**（环的实际可吸收量更大，或有额外的硬件/驱动缓冲）。
⇒ **诚实结论：这是一个"已量化、已加观测闸门"的耦合点，但当前实测没有触发。**
   **既不能说"出问题了"，也不能说"余量充足"** —— 要知道真实上界，得**故意把阻塞拉长**再做一次。

### 8.2.1 ★ 本轮为此补的观测闸门（**这是本节真正的交付**）
`0x38` 的应答从 **39 → 51 字节**，尾部追加：
```
+39  uart_ore     (硬件 ORE 溢出次数)
+43  uart_drop    (软件环满丢弃次数)
+47  frame_bad    (坏帧数)
```
**判据：正常收发下这三个必须恒为 0。非 0 ⇒ 有帧被截断 ⇒ 该次"NAK/无应答"不是固件的判断，
而是数据没到全。**
★ 为什么必须补：这三个量原来**只刷进全局变量、不进任何应答面** ⇒
**"帧被截断"在协议面上完全不可见**（只能 SWD 读 DTCM，而 SWD 会停核）。
这违反本项目自己的纪律"**新观测变量必须能被外部读走**"，也和 `r[38] out_surfaces` 是同一个理由 ——
**把"读数一样、事实不同"的两件事分开。**

### 8.3 更正：注释里的**旧数字**比代码更容易被当成事实
`modbus.c` 里那句"`MB_MAX_FRAME(128, 请求上限)`"**是过期注释**（常量早已升到 255）。已更正，
并写明纪律：**注释里不要写常量字面值，写常量名**（或紧跟 `_Static_assert`）。
★ 本项目已因此白查过一整轮（`blackbox.h` 的"AXI 完全空闲 / 128KB / 512 槽"）。

## 7. 复现（**全程串口，零 pyocd**）

```bash
python docs/audit/h723_prog_store_test.py status   # 0x48 体检 + 事务对账
python docs/audit/h723_prog_store_test.py desc     # 0x4A 设备描述
python docs/audit/h723_prog_store_test.py gate2    # 闸2: 篡改 ⇒ NAK crc mismatch
python docs/audit/h723_prog_store_test.py gate5    # 闸5: 结构非法 ⇒ NAK payload short
```
★ 客户端用 `docs/audit/h723_motion_probe.py` 的 `Dut`，或 `tools/h723_client.py` 的 `Dcl` —— **两者都自带 CRC16 校验与错位重找**。

---

## 9. ★★★ 发布就绪评估（2026-09-16 12:00）

### 9.1 结论：**可以发布** —— 但 release notes 必须写清下面三条

| 维度 | 状态 | 证据 |
|---|---|---|
| 代码 | ✅ 工作区干净；本地 = `origin` = `dcl` = `1c203477`（2026-09-16 复核更新） | `ls-remote` 直连核对 |
| 构建 | ✅ 零警告闸门 + ISR 调用树闸门通过 | `bash build.sh` exit=0 |
| 回归 | ✅ **零回归**（12 套；所有红项基线上同样存在，且 2 项变好） | 见 §8 的基线对照表 |
| **全链** | ✅ **`.dcl 源 → dclc 编译 → 通信上传 → 存 SD → 上电自动装载` 实测通过** | 编译 `examples/hiloop.dcl` → 上传 → 复位 → `n_routes=7` |
| 板上/卡上 | ✅ 干净：`n_routes=7`（真实示例）、`uart_ore/drop/frame_bad = 0` | `0x38`/`0x48` 实测 |

### 9.2 ★ 必须写进 release notes 的**破坏性变更**
> **`0x38 ENGINE_STATUS` 应答 39 → 51 字节**（尾部新增 3 个 u32：`uart_ore` / `uart_drop` / `frame_bad`）。
> **前 31 字节布局与语义未动**（与 S3 一致），新增量一律往**尾部**追加 ⇒
> **按 `len >= 39` 或按绝对偏移读前 39 字节的客户端仍然兼容**；
> 只有"长度必须恰好 39"的客户端会失效。

### 9.3 已知问题（**均已定案；无一阻塞发布**）

| # | 项 | 性质 | 状态 |
|---|---|---|---|
| 1 | `audit_m234` **M2-1/M2-2** 曾全红 | **测试缺陷，固件正确** —— `tx_uart` 默认 =1（响应走物理口，缓冲随即清空），而测试从没切到缓冲模式 | ✅ **已修工具**（`0x62 [0][0]`）⇒ 该项 **7P/5F → 9P/3F** |
| 2 | `w1` **R11** 曾报"NaN 被拒但前段已写" | **测试缺陷，固件正确** —— marker 写在 `SENSOR[0]`，被 `as5600_poll` 每 10 ms 覆盖；`h_write_burst_w1` 实为**两相**（先全查后全写）⇒ **拒绝时无副作用** | ✅ 已定案（我先前误判为"非原子"，**已撤回**） |
| 3 | `dclc.py` 提示印 `(None)` | **我误判** —— 那是未解析的**请求**端口（自动找板），deploy 其实成功了 | ✅ 已修：改印解析后的端口；**并补上 `0x11 START` 的返回判定**（原来只发不判） |
| 4 | `w5` 3 项 | HIL/TIM3 PWM，而 **TIM3 已被步进模块接管**（`g_step_owns_tim3=1`） | 设计内 |
| 5 | `modbus` 10 项 | 需 RS-485 物理回路 | 环境 |
| 6 | `audit_m234` M3 3 项 | `DCL_PERSIST_SAVE=0` 按设计 NAK | 预期 |
| 7 | `RX_RING(512) vs FRAME_PAYLOAD_MAX(6150)` | **已量化 + 已加观测闸门**；实测到 **108% 预算仍未触发** ⇒ 真实上界未知 | 已知 |
| 8 | 能力位 u16→u32、程序面"不得出现引脚号"静态检查、通用外设事务 op | — | 下一代 |

### 9.4 发布动作（**待用户定**）
- tag 名：现有 `v0.1.0` / `v0.1.1` / `v1.0.0` ⇒ 下一个自然是 **`v2.0.0`**
- 打哪：**当前 HEAD**（以 `git rev-parse HEAD` 为准；两个远程均已同步 —— `git ls-remote` 直连核对）。
  ★ 本轮 `1c20347` 之后的提交**只含 docs / memory**，`src/` 与 `CMakeLists.txt` **逐字节一致**
  （`git diff 1c20347 HEAD -- src/ CMakeLists.txt` 为空）⇒ **打哪个都不改变固件内容**。
- ★ **本节只做评估，没打 tag** —— 打 tag + 写 release 属对外动作，等确认。

### 9.5 ★★★ 复核时查出一个**环境级 Git 缺陷**：本机 `refs/remotes/**` 写不进去（静默返回 0）

**发现方式**：复核时 `git status` 报 `[ahead 27]`，但上一条记录说"本地 = origin = dcl" —— 矛盾 ⇒ 直接查。

**结论：两个远程其实都已同步，是**本机远程跟踪引用根本写不进去**。**
```
git ls-remote origin refs/heads/main = 1c203477  ✓   (直连远程，权威)
git ls-remote dcl    refs/heads/main = 1c203477  ✓
但 refs/remotes/{origin,dcl}/main 一直停在 089fd97（两天前）
```

**逐层隔离（四组，只有最后一组异常）**

| 动作 | 结果 |
|---|---|
| `git fetch --all` | 打印 `089fd97..1c20347 main -> origin/main`、`rc=0`；**但 `packed-refs` 值不变、无松散文件** |
| `git rev-parse` / 纯 shell 操作 | 不影响 `refs/remotes` 下已建内容 |
| `git update-ref refs/heads/x HEAD` | **正常**（文件真出现） |
| `git update-ref refs/remotes/origin/foo HEAD` | **`rc=0`，且把 `origin/` 整个目录删掉**（连探针文件一起消失） |

已排除：`.git/config` 格式（`repositoryformatversion=0`，非 reftable）、`hooks`（全是 `.sample`）、
lock 文件（无）、以及"脏目录"（**删掉整个 `refs/remotes` 树重建后再 fetch，依然不写**）。
⇒ **只有"写 `refs/remotes/**`"这一条路静默失败：`rc=0`、无报错、无写入。**

**★ 两条判据修订（已写入长期记忆 §7.2）**
1. **写完引用必须 `git show-ref` 确认真的落进去了** —— 这是唯一可信读法。
2. ★★ **同步状态的权威判据是 `git ls-remote <remote> refs/heads/main`，不是 `git status`** ——
   后者依赖远程跟踪引用，在本机会**永远误报 ahead**。

**治标修法（本机唯一有效，已核验）**：手工写松散引用
```
mkdir .git/refs/remotes/origin                  # dcl 同
printf '%s\n' <hash> > .git/refs/remotes/origin/main
```
⇒ `git status -sb` 立刻变 `## main...origin/main`、`git rev-parse origin/main` = `1c203477` ✓
（操作前已按纪律备份 `../.git_bak_2026-09-16_refs`，13 MB。）

> 这条属于**取证工具链的可靠性问题**，不属于固件缺陷，但**不修会让以后每一轮"是否已推送"的判断都出错**，
> 因此与 release notes 同等对待。

---

## 10. ★★★ 标准化复核中挖出的两个**真 bug**（2026-09-16 下午，已修 + 已上机 A/B 回归）

> 背景：在做"入口文档回填"的事实核准时，三份并行调研各带回一条硬结论。
> 两条**不是文档问题，是代码问题**。按契约 §2"核心改动必须 A/B + 回归"，两条都做了上机对照。

### 10.1 缺陷 A：交付档下 **DO 0..7 是死的**（`src/do.c`）

**机理**（读码取证，逐条可复核）
1. MDMA 节点 `CTCR=0x00020000`（**byte 尺寸 + 地址固定**）+ `CBNDTR=4`
   ⇒ 4 次字节搬运**全落在 `GPIOE_ODR + 0`** ⇒ 只覆盖 **PE0~PE7**；
2. 源 = `OFF_DO_SHADOW`，而 0 档的 `do_poll()` 走 `#else` 分支**只写 `BSRR`、从不写影子**
   ⇒ **影子恒 0**（冷启 memset 后没有任何写者）；
3. ⇒ 每 100 µs（TIM2_UP）MDMA 把 **0** 锁进 `ODR` 低字节
   ⇒ **CPU 用 BSRR 置起来的 PE0~PE7 会在下一拍被静默清掉**。

★ 这是"DCL_DO_LATCH 切 0 档"的**必要补丁**：当初只算了抖动（3.6 ns vs 54~60 ns），
**没算到"锁存链还在跑、而影子已经没人写"** ⇒ 切档顺手把 DO 0..7 弄死了，
且**观测面上完全看不出来**（寄存器读回全对、`g_do_write_n` 照涨，只是引脚不动）。

**修法**：0 档在 `do_latch_init()` 开头直接 `return` ⇒ 不启用 DMA2/DMAMUX1/MDMA，
也不置 `TIM2.DIER.UDE`（顺带消掉 ~350 万次/秒的 AXI 链表搬运与 AHB4 写）。

### 10.2 ★★ A/B 实测（同一块板 COM21、同一套判据、同一个程序，只有那一处补丁不同）

判据：登记 `GPIO_MASK=0x00FF` → `ACTUATOR[0..7]=1.0` → 等 200 拍 → 连采 `GPIOE_IDR`。
工具：`tools/h723_do_hold_test.py`（★ 看 **`IDR`**，不是 `ODR`）。

| 固件 | `GPIOE_IDR` | 低字节 | 命中 | 判定 |
|---|---|---|---|---|
| **改前（对照，`HEAD~1` 的 `do.c`）** | `0x0F00` | `0x00` | **0/5** | **FAIL** |
| **改后（交付）** | `0x0FFF` | `0xFF` | **5/5** | **PASS** |

★ 对照档 `ODR=0x0F00` —— 高字节 8~11 位完好、**低字节被清成 0**，与上面推出的机理**逐位对上**。
⇒ **判据会失败**（对照实测），**修复使判据通过**（交付实测）—— 这才构成证据。

### 10.3 缺陷 B：`0x38` 应答**栈越界 11 字节**（`src/main.c`，**本轮我自己引入的**）

把 `0x38` 从 39 扩到 51 字节时，**只改了写的偏移，没同步改缓冲声明**：
`uint8_t r[40]` 而 `ack(r, 51)` ⇒ 越界 11 字节写坏调用者栈。
★ 与缺陷 A 同族：**"改了一半"**。
**修法**：不用"把 40 改成 51"（那是同一个坑——一个语义两处存放），
改成 `#define ENG_STATUS_LEN 51u` **同时定缓冲大小与发送长度**。
**验收**：修复后 `0x38` 实测 `ACK len=51`（见 10.2 的表头行）。

### 10.4 ★ 新增**静态**判据并接入构建闸门（族级修复，不是单点修复）

同类缺陷**已犯两次**：`h_pin_pattern()` `r[40]`→`ack(r,68)`（28 B）、本次 `r[40]`→`ack(r,51)`（11 B）。
共同点：**不报错、不崩溃，读回来的字段全是合理值** ⇒ **动态判据永远发现不了**。

- `tools/h723_ackbuf_check.py` —— 静态扫 `ack()/put32()/put16()` 的写入长度 vs 局部缓冲声明；
- 已接进 `build.sh`（与 ISR 调用树闸门同级），**构建期拒绝通过**；
- ★ **该工具第一版自己是"空判据"**：本仓库函数左花括号在下一行 ⇒ 一个函数都没解析到，
  却对任何输入都打印 `[OK] 未发现越界`。**"0 发现"与"根本没扫"长得一模一样。**
  ⇒ 本版强制**自报覆盖度**（覆盖不足即判无效）+ 带 `--selftest` 夹具（造好的红必红、造好的绿必绿）。
- 实测覆盖：**224 个函数 / 22 个带局部缓冲**；当前 1 处越界（即缺陷 B）已修 ⇒ `[OK]`。
  期间修掉 2 处**误报**（`= {...}` 初始化列表被当成写入）。

### 10.5 `DCL_DO_LATCH` 从"假开关"变**真开关**

`src/do.h` 的注释**早就宣称**"可被 CMake `-DDCL_DO_LATCH=0` 覆盖"，
但 `CMakeLists.txt` **从未** `set(... CACHE ...)`、也从未传 `-D`
⇒ 那是个**空头承诺**：CMake 连缓存项都不建，`build.sh` 那节
"**生效开关（读自 CMakeCache）**"里**根本不会出现 `DCL_DO_LATCH`** ⇒ **你以为切了，其实没切**。
（本项目**第 2 次**栽在"声称可覆盖但没接线"，第 1 次是 ISR 调用树闸门。）

已接线 + 写进 `build.sh` 的 `DEFAULTS`，现在**能跑 A/B**：
```
DCL_DO_LATCH=1 对照档  .bin 57424 B
DCL_DO_LATCH=0 交付档  .bin 56992 B   (差 432 B = 0 档不再编入 MDMA 初始化链)
```
两档均通过全部构建闸门（零警告 + ISR 调用树 + 应答缓冲区越界）。

### 10.6 本次改动清单与状态

| 项 | 文件 | 状态 |
|---|---|---|
| DO 0..7 清零 | `src/do.c` | ✅ 已修 + **A/B 上机回归通过** |
| `0x38` 栈越界 | `src/main.c` | ✅ 已修 + 协议面实测 `len=51` |
| 静态闸门 | `tools/h723_ackbuf_check.py` + `build.sh` | ✅ 已接入**构建期** |
| 假开关 | `CMakeLists.txt` + `build.sh` | ✅ 已接线 |
| 判据 | `tools/h723_do_hold_test.py` | ✅ 已加入 |

---

## 11. ★★★ 发布前全量回归 + 两处差异的对称 A/B（2026-09-16 晚，上机）

### 11.1 12 套全跑：11 套与基线逐项一致，2 套有差异 ⇢ 逐条查清

| 套件 | 结果 | 对基线的判定 |
|---|---|---|
| `h723_proto` | 12 PASS / 0 FAIL | 一致 ✓ |
| `h723_seq` | 27 PASS / 0 FAIL | 一致 ✓ |
| `h723_macro` | 18 PASS / 0 FAIL | 一致 ✓ |
| `h723_jitter` | 9 PASS / 0 FAIL | 一致 ✓ |
| `h723_r1_actuator` | 5 / 5 PASS | 一致 ✓ |
| `h723_audit_m234` | 9 PASS / 3 FAIL | 一致 ✓（M3 三项 = `DCL_PERSIST_SAVE=0` 设计内）|
| `h723_w1` | 27 PASS / 1 FAIL | 一致 ✓（R11 = 已定案测试缺陷）|
| `h723_modbus` | 5 PASS / 10 FAIL | 一致 ✓（需 RS-485 回路）|
| `h723_w5` | 10 PASS / 3 FAIL / 4 SKIP | 一致 ✓ |
| `h723_t26` | 7 / 11 | 一致 ✓ |
| **`h723_w2_probe`** | 首跑 **11 PASS / 3 FAIL** | ⚠️ **查清：不是回归**（见下）|
| **`h723_persist`** | **7 PASS / 19 FAIL** | ⚠️ 与记录的 9/17 不同 ⇒ 查清：**板态**（见下）|

### 11.2 ★ 差异①：`w2_probe` 的 3 项 FAIL —— **我自己留下的板态**

三项同源，都指向"**引擎表是空的/不是 bench 的 128 条**"：
```
[FAIL] PRE 前提: 表=0 条 (期望 bench 的 128)
[FAIL] T0' N_ROUTES: N_ROUTES=0 N_PARAMS=0
[FAIL] A   无强制: 路由确实在写 wire[3]
```
**根因**：上一轮为"全链验收"往 **SD 上传了 `hiloop.dcl`** ⇒ 上电装载 **7 路由**，
而该套件的前提是"**空卡 ⇒ bench profile 128 路由**"。
**实测确认**：`0x49 PROG_ERASE` + 复位 ⇒ `n_routes` **128**、`run=1` ⇒ `w2_probe` **14 PASS / 0 FAIL**。
⇒ 这是**跑测顺序/板态依赖**，不是代码回归。

★ **回归前提（必须写进跑测规程）**：跑套件前 `0x49` + 复位，让板子回 bench 态。
（本项目此前已记过同族："套件的隐含前提会造成跑测顺序依赖"。）

### 11.3 ★★ 差异②：`w2_probe` 自身**非确定** —— 别拿它的 PASS 计数当细粒度指标

**同一固件连续跑三次**：
```
第1次: 13 PASS / 1 FAIL
第2次: 11 PASS / 3 FAIL      ← 与首跑的"11/3"完全同形
第3次: 13 PASS / 1 FAIL
```
⇒ **11/3 是这个套件的正常波动**，不是缺陷信号。
★ 纪律：对这类套件，判据只能是"**没有出现新的失败模式**"，不能是"PASS 数不低于某值"。

### 11.4 ★ 差异③：对称 A/B（同板 / 同判据 / **同一固定板态** / 只差待测改动）

固定板态 = 烧录 → 复位 → `0x49` 清 SD → 复位（bench 态 128 路由）。

| 套件 | 改前（`255ec8c` 的 `src/do.c`+`src/main.c`） | 改后（本版） |
|---|---|---|
| `h723_w2_probe` | 13 PASS / 1 FAIL | **14 PASS / 0 FAIL** |
| `h723_persist` | **7 PASS / 19 FAIL** | **7 PASS / 19 FAIL（逐项相同）** |

⇒ **`persist` 两臂完全一致** ⇒ 与记录的 9/17 之差是**板态/环境**，与本次改动无关。
⇒ **`w2_probe` 改后不劣于改前**（13/1 vs 14/0，且该套件本身 ±1~2 项波动）。
⇒ **结论：本次改动无回归**；且改动面（`do_latch_init` 门控 + `0x38` 缓冲）**在原理上也够不到**
路由表/持久化/通信域（前者的影响域只有 `GPIOE_ODR` 低字节与 `TIM2.DIER` 一位）。

### 11.5 发布时的板态（**交付态**）
**SD 已擦干净**、`n_routes=128 / run=1`（bench profile）。程序是**资产**：
要用时由上位机上传，不该由"出厂板态"预置一个示例程序占用 SD。
★ 这也解释了为什么上一轮的"卡上放 hiloop"看起来更"干净"，实际却**让 11 个套件里最敏感的那个变红**。

### 7.1 ★★ 更正一条我先前的错误结论 + 一个真的协议缺陷
**我先前写过"`h723_client.Dcl.send()` 会返回假 ACK（帧错位）"—— 查证后这是错的。**
`h723_modbus.Link.xact()` 里有 `if crc16(body) == crc_rx: return …`，也做了 `del buf[0]` 的错位重找。
**它没有撒谎。**

真正发生的事：当时固件在复位循环，串口上不停出现**开机 banner**：
```
c1 00 04 00 | 00 02 f7 0d | f4 c2      ← CRC 完全合法的一条帧
```
而**协议帧里既没有"命令码回显"、也没有序号** ⇒
**客户端无法判断这条应答是不是属于自己刚发出的那条请求** ⇒ banner 被当成了 ACK。
（那轮我看到的 `BEGIN/DATA/COMMIT` 全部 ACK，其实是 banner；固件侧 `txn_*` 恒为 0 才是真话。）

> **⇒ 这是协议层的真缺陷，建议进 v1**：应答里带 **`最近命令码`**（1 字节）或**序号**，
> 让客户端能判"这条应答是我的吗"。现在的口径是"收到任何合法帧就当应答"，
> **任何多余帧（开机 banner、上一条的迟到应答、别的写者）都会污染判据。**

★ 顺带：`h723_modbus.main()` 里已有 `revive_if_dead()`（"M4 sentinel: un-halt the core if a pyocd
session left it halted"）⇒ **"pyocd 会把核留在 halted"这件事项目里早已知道**，我这一轮又踩了一次。

---

## 12. ★★★ GAP-6 收口（G6-1..G6-4 全部落地，2026-09-16 晚）

### 12.1 交付了什么
| 件 | 内容 |
|---|---|
| G6-1 | 拍内推进的 I2C 状态机（`src/i2c_sm.c`）—— 每拍**一个相位**，一次 2 字节读 = **8 拍**（`tick_cnt == 6+n`）|
| G6-2 | 总线独占门（`src/i2c_bb.c`，**带引用计数**）—— 门住在**资源的定义处** |
| G6-3 | SHM 事务区 `OFF_I2C_XACT=0x7200`（`req_seq/done_seq` 握手 = **就绪门**）|
| **G6-4** | **具名设备绑定表** `OFF_DEV_BIND=0x7300`（`src/dev_bind.{c,h}`）—— 上位机可写 8 槽；`DCL_CAP_DEVBIND=0x1000`（实现字 `0x0DF7→0x1DF7`）|

**GAP-6 的原话承诺是"接一个新器件 = 写一段程序"。** 源码事实是 ③层程序**无法发起任何 I2C 事务**
（契约 §3.7.1）⇒ 一条 raw 事务命令兑现不了它。**正解 = 数据驱动的具名设备绑定表**：
上位机下发"每 N 拍读 `地址/寄存器/长度` → 写 `SENSOR[z]`"，由拍内状态机执行。
⇒ **"接新器件"现在真的只是上传一段配置**（不重烧固件、不写代码）。契据见契约 **§3.8**。

### 12.2 ★★★ 落地过程挖出并修掉的四个**真缺陷**（都是同族：*静默错数据 / 判据退化*）
| # | 缺陷 | 为什么它会**静默** | 判据（都能失败）|
|---|---|---|---|
| A | **在飞事务期间换表** ⇒ 收尾用**新表**的 lane 解释**旧事务**的数据（`s_idx` 张冠李戴：越界或写错槽）| `reject=0`、计数照涨，**无任何错误迹象** | 提交新表后 `n_valid`/`done_seq` 必须**延后到收尾之后**（暂存 + 延后回执）|
| B | **事务结果归属**：只查 `status==OK` ⇒ 另一个使用者（`i2c_shm_service`）在收尾前插入并完成，**他的读数被写进我的槽** | 同上 | 令牌 `i2c_sm_done_req() == 我的受理号`，否则**丢弃重发**（新增状态码 `I2C_SM_ST_STOLEN=7`）|
| C | **一个槽两个语义**：`DB_ERR_N` 被"轮询失败"与"上传被拒"共用；`DB_LAST_ERR` 被 `DB_RC_*` 与 `I2C_SM_ST_*` 共用（两个码空间**数值重叠**）| "err 在涨"**无法解释** ⇒ 判据作废 | 拆成 `ok_n / err_n / skip_n / rej_n` 四个计数 + `reject`/`last_err` 两个独立码槽 |
| D | **序号不单调不挡**（码 2 只留常量）⇒ 重放一张旧表就把现场**打回旧配置** | 旧表字段全合法、crc 也对；`reject=0`、`n_valid` 合法 | `(int32_t)(req_seq - done_seq) < 0` ⇒ 拒绝，且**不回退** `done_seq` |

**D 是验收脚本的 T3 抓出来的；A/B/C 是对抗式审计抓出来的。**（审计的价值在于它**没在跑我写的判据**。）

### 12.3 ★★ 一个**我自己的**错，与抓它用的判据（值得留档）
`dev_bind_service()` 的"总线被阻塞路径占用就跳过"我照抄了 `main.c` 里 AS5600 那一句
`i2c_bus_owner() != I2C_OWNER_SM`。**语义反了**：本模块**自己就走 SM 这条路**，
空闲时 `owner == NONE`，而 `NONE != SM` ⇒ **每一轮都判"忙"**。
现象：**提交全部成功、`n_valid` 正常、而 `ok_n`/`err_n` 永远不动**（上机 30 PASS / 3 FAIL）。

抓到它的是**本轮专为"判据可解释"新加的那个计数** —— `skip_n` 一路涨到 **3327** 并**冻结**在
"表空 ⇒ 提前 return"那一刻。⇒ **一个为可解释性加的计数器，顺手把一次静默空转变成了可读指纹。**
★ 教训一般化：**同一个表达式在两个调用点语义可以不同**——
"我要避开的人"≠"我怕被谁避开"（同族：守卫必须住在资源处）。
★ 修法不是改成 `== I2C_OWNER_BLOCKING`，而是写成 `owner != NONE && owner != SM`
（"**有人在占且不是我**"）—— 将来出现第三个 owner 时，前者仍然正确。

### 12.4 上机验收（`tools/h723_dev_bind_test.py`，**只走协议，零 pyocd**）
```
PASS 36 / FAIL 0 / SKIP 1       器件: **实际读到**
```
- **T5.4 是本次最硬的一条**：绑定表写出的 `SENSOR[7]=3347` 与 SHM 事务区（G6-3）**独立读回**的值
  **相同** —— 即 `i2c_bb.h:8` 那条纪律的兑现：**两条独立路径对上才算数**。
- T6：改绑到不存在的从机 `0x50` ⇒ `err_n` 涨（**证明判据不是空的**）、`last_err=3(NAK)`、
  而 `SENSOR[7]` **仍是哨兵 1234.0**（"没读到"没有伪装成"值=0"）。
- T4 **SKIP 一条**：`dst=16` 不可达（`dst` 只有 4 bit ⇒ `if (dst > 15u)` 是**死码**）。
  诚实标注：`DB_RC_DST` 与 `DB_RC_NOSEQ` 两条**目前都不可达**，不能当"已验证的拒绝路径"记账。
- ★ 脚本用了**CRC 口径自证**：先拿"故意错的 crc"做阳性对照，再试 words/bytes 两种口径
  ⇒ **实证**固件吃的是 **words 口径**，而不是靠读代码猜。

### 12.5 顺带修掉的两个**非 G6-4** 缺陷
1. **工具侧认口是"精确等值"**（`cap == 0x0DF7`）⇒ 固件**加一位**就让**所有工具一起认不到板子**，
   而症状是"找不到板子"（会把人引向接线/驱动方向）。⇒ 改**必备位掩码** `(cap & expect) == expect`。
   ★ 保留 `h723_proto.py` **T1.4 的等值比较**（那里是"宣称 = 实现"审计，职责不同，刻意不合并）。
2. **`h723_i2c_sm_test` 的 J3 顺序依赖**：原判据断言**开机累计** `nak == 0`，而累计值
   `0x13 RESET` **不清**（实测 49→49）⇒ 任何先前 NAK（如 G6-4 验收里故意读 `0x50`）会让它**永久变红**，
   且失败文案说的是"400 kHz 时序太快"—— **误导性诊断**。⇒ 改**增量判据**（本次运行期间不得新增）。
3. ★ 另一处**编译期**缺陷：`i2c_xact_reset()` 写好了却**编不过**（`i2c_sm.c` 缺 `engine.h`）——
   即"声明了却没人编过"。**"没接线"与"没编过"是同一族**。

### 12.6 与 §7.1 的呼应（同一个缺陷族的两层）
§7.1 记的协议层缺陷是"**应答里没有命令码回显/序号 ⇒ 客户端分不清这条应答是不是自己的**"，
与本节缺陷 B（**状态机里没有事务归属 ⇒ 收尾方分不清结果是不是自己的**）**是同一个病**：
**"谁的结果"这件事没有被显式携带**。
⇒ 两层都该按同一处方治：**给每次交互发号，并让结果带上号**。
协议层那条属 v1 评估项（会动 `0x01` 线上格式）。

---

## 13. ★★★ G6-4 第二轮：独立审计 + 上机反证（2026-09-16 深夜）

第一轮交付（commit `028865e`）之后做了**两轮独立核实**：一次**对抗式审计**（只读，逐文件核契据）、
一次**上机反证**。结果是**又抓到三条真缺陷**，并**推翻了一条验收脚本的结论**。

### 13.1 又修掉的三条真缺陷（都在 `src/`，第一轮之后）

| # | 缺陷 | 为什么它是"会静默错" | 修法 |
|---|---|---|---|
| ① | **令牌必要但不充分**：`s_status` 是**实时态**，一个**被拒**的请求（门忙/BADARG）会覆写它 ⇒ "令牌匹配但状态已变" ⇒ 收尾方**丢弃自己的好数据并计一次错** | 数据路径**没有**错，错的是"该拿到的结果被丢掉"，且表现为**计数涨**、值不变 | **完成记录** `{req, status, len}`：完成那刻快照、**受理新请求即失效**；取数只走 `i2c_sm_take_result(my,…)` ⇒ **不再依赖任何实时字段**（契约 §3.7.2）|
| ② | ★★ **引用计数泄漏**：`i2c_sm_request()` **先拿总线门、再校验参数**，三条"参数非法"分支 `return 0` 而**不放门** ⇒ `owner` 永久卡在 `SM` | 症状是**所有依赖阻塞路径的判据一起变红**（AS5600 轮询、G6-2 的 G1/G4/G5、G6-3 的 S6），错误方向指向"总线被谁占了"，**与真因（一次 `len=9` 的非法请求）相隔极远** | **先验参、再拿门**（结构性写法，不靠"记得成对释放"）。判据：发一次 `len=9` ⇒ `IX_STATUS==5(BADARG)` 且 `refs==0 / owner==0` |
| ③ | **拒绝理由被我的新代码弄丢**：引入完成记录后，"被拒"路径也去读 `done_status()` ⇒ 报回 `IDLE(0)`，上位机分不清"没做事"与"被拒了" | 判据能"过"但信息量归零 | 收尾**分两条路**：被拒 ⇒ 报**实时**状态（GATE_BUSY/BADARG）；完成 ⇒ 报完成记录 |

★★ **①②都是我这轮刚加的观测抓到的**：①由"完成记录缺了 status/len"暴露、②由 G6-2 **专为泄漏准备**的
`i2c_bus_refs()` 计数暴露（`owner=2` 冻结、`refs!=0`）。⇒ 又一次印证：
**为可解释性加的观测，会顺手把静默故障变成可读指纹。**

★★ **③的抓法值得单独记**：那条判据里我写了断言
**"没跑到该分支 ⇒ 本判据无效，不是通过"** —— 正是它把"拒绝理由丢了"从"看起来通过"里揪出来。
**判据要能区分"通过"与"没跑到"。**

### 13.2 ★★★ 推翻一条结论：验收脚本报的"**用错 lane**"是**串帧**，不是固件问题

`h723_dev_bind_test.py` 的 T8（在飞换表）报过一次 FAIL：
> `SENSOR[15]` 被改动 = `6.538242356772269e-21` ⇒ **用错 lane**（新表的 dst/len 被用于旧事务的数据）

**解出原始位型 = `0x1DF70200`**：低 16 位 `0x0200` = **fw_ver**，高 16 位 `0x1DF7` = **能力字**
⇒ 那是 **`0x01 GET_VERSION` 的应答载荷**被当成了 `0x22 READ_BURST` 的应答。

- 它**不可能**是"用错 lane"的产物：那条路径写进去的是 `(float)d[0]` 或 `(float)((d[0]<<8)|d[1])`，
  值域 **0..65535**，不可能是 `6.5e-21`。
- **最坏的一种巧合**：`0x22` 读 **1 个字**的应答长度也是 **4 字节**，与 `0x01` 的应答**长度完全相同**
  ⇒ **连长度判据都分不开**。
- **我另写了一份最小复现**（8 轮真实在飞换表 + `SENSOR[15]` 哨兵，只用协议原语）：**0/8 命中**
  ⇒ **暂存/延后逻辑没有错**。

⇒ 这**就是 §7.1 早已记录的那条协议层缺陷**（"应答里没有命令码回显/序号 ⇒ 客户端无法判断
这条应答是不是自己的"）第一次**在一个判据上真的咬到人**。**优先级因此上调为 v1 首选**（GAP-12）。
⇒ 客户端侧今天就能加固（已要求）：读**两个字**（帧长 8）挡掉 4 字节杂帧 · 命中需**连续两次**读到同值 ·
命中时**打印原始 u32**（不要只打 float —— 我这次全靠原始位才定性）。

### 13.3 ★★ G6-2 变红的真因：**验收脚本自己没做清理**（又一个"跑测顺序依赖"）

现象：`dev_bind_test` 之后紧跟的 `h723_i2c_gate_test` **G6-2 全红**（"G1 占不住"、`owner=2`），
而**单独跑 G6-2 全绿**。真因：T8 在收尾时抛了 `NameError: period_restore` ⇒ **`DB_PERIOD` 留在 1**
⇒ 服务方几乎一直占着总线门 ⇒ 阻塞路径拿不到总线。
⇒ 纪律：**验收脚本的状态复原必须放在 `finally`，且判据 FAIL 时也要还原**。
★ 这已经是本项目**第三次**被"跑测顺序依赖"咬（前两次见 §11.2/§11.3）——
**症状总是"固件像坏了"，真因是上一条测试留下的板态。**

### 13.4 ★★★ 第四条真缺陷：**延迟释放协议在丢释放**（`refs` 泄漏 61 个）

**这是本轮最隐蔽的一条，而它同时解释了三个表面无关的症状。**

- **现象**：`0x39 op=22` 读回 `owner=2(SM)` **冻结**、**`refs=61`**，而绑定表是空的、`period=10`、
  状态机早已 idle ⇒ **阻塞路径（AS5600）再也拿不到总线**。
- **根因**：`i2c_sm_request()` 在**受理新请求时**把 `g_i2c_sm_release_pending` **清 0**
  （原意："别把刚拿到的这次占用放掉"）。但**上一个事务**若恰好在 `i2c_sm_service()` **之后**、
  本次请求**之前**完成，那一次"欠的释放"就被**丢掉** ⇒ 每丢一次泄漏 1 个引用。
  `DB_PERIOD=1` 时高频发生 ⇒ 累积到 61。
- **修法（结构性）**：把"待释放"从 **bool 改成配对计数**（完成一次 +1，主循环 `while` 还清）；
  并**删掉**受理处那次清 0（它本身就是病）。另加主循环**引用计数对账**：
  不变式 = "SM 空闲且无欠释放 ⇒ 不该占着门"；违反则当圈归还，并计入 **`g_i2c_ref_leak_n`**
  （自愈**必须可见**，否则下次泄漏又变成查不出来）。
- **★ 决定性 A/B（同一块板、同一套判据）**：
  | | `dev_bind_test` 跑完后的 `op=22` | AS5600 阻塞路径 |
  |---|---|---|
  | 修复前 | `owner=2`，**`refs=56~68`**（多次复现） | **饿死**（`tx` 不涨） |
  | 修复后 | **`owner=0`，`refs=0`** | **恢复**（`tx` 1 s 内 2158→2340） |
- **★★★ 一处修复，三个症状一起消失**（这条值得单独记住）：
  1. **NF-1**：`refs` 泄漏 + AS5600 被饿死（回归 worker 独立报告：`refs=56~68`）；
  2. **NF-2**：**跑测顺序依赖** —— `dev_bind_test` 之后紧跟的 `h723_i2c_gate_test` **G6-2 全红**
     （"G1 占不住"），单独跑却全绿；修复后**坏顺序也全绿**；
  3. 修复前 `dev_bind_test` 只能跑到 `53/1/2`（因为设备被饿死）⇒ 修复后稳定 **55/0/1**。
  ⇒ **"症状看起来是三个问题"，实际是一个**：不共用一份"谁还欠着什么"的账，就会在各处以不同面貌出现。

### 13.5 遗留与未定案（**如实标注，不猜**）

| # | 项 | 状态 |
|---|---|---|
| **NF-3** | `h723_seq` 的 **T28.B1** 失败（26/1）：判据期望"起点 `WIRE[0]` 空闲"，而实测 `WIRE[0]=3347` = **AS5600 的 RAW**（bench profile 有路由把 `SENSOR[0]`→`WIRE[0]`）⇒ 序贯器一起步就推进了 2 步（`cur=2`）。**按它的文档前置条件 `--wipe` 复跑仍失败**（`wipe OK` 后 `cur=2`）。| ⚠️ **判据缺陷（环境相关）**，非 G6-x 引起（`engine.c` 本轮一字未改）。**根因与 T8 选 `SENSOR[15]` 同族**：**用了"还有别人在写"的槽**。★★ **已溯源到 `file:line`**：`engine.c:655` 跑满 `MAX_ROUTES(128)` + `:688` 的 `dst_channel = i % MAX_WIRES(128)` ⇒ **128 条路由恰好覆盖全部 128 个 WIRE 槽** ⇒ **"没被路由写过的 WIRE 槽"不存在**；把 SENSOR 灌进 WIRE 的是 `:684`(`i%3==0 → SRC_SENSOR`) + `:686`(`src_index = i%64`)，**i=0 即 `SENSOR[0]`→`WIRE[0]`**。⇒ 正解 = **改用一个"被常值源供给"的槽**：`k ≡ 2 (mod 3)` 即 `SRC_CONST`，首选 **`WIRE[17]` = 0.005**（远低于程序 `>0.5` 阈值）。★ 反例否决：**"START 前预写 `WIRE[0]=0`"无效** —— 它每拍被引擎从 `SENSOR[0]` 覆写 |
| **NF-4** | `h723_i2c_shm_test` 的 **S5** 在 `-m 10` 下偶发：`TICKS=2` 而不是 3（行"ping 经 SHM"）。单跑 6/6 通过。| ⚠️ **未定案**：SM 早已 idle，无法归因到 pending/release（本轮新增的那些）。倾向"测试自身的观测窗口太紧"，需按"停止后 TICKS 是稳定性质，不是瞬时性质"去查 |
| **NF-5** | `h723_persist` / `h723_w2_probe` 未跑到 / 板态依赖 | ⚠️ 未验证（板态被中途复位打断）|

### 13.6 NF-3 关闭（2026-09-16 收口）+ 本轮最终验收

**NF-3 的修法**（`tools/h723_seq.py`，仅 2 处改动、`src/` 零改动）：
把 T28 序贯器**条件用的 WIRE 槽** 从 `WIRE[0]` 换成 **`WIRE[17]`**。
- 机制核实：**不是 FORCE**（全文无 `0x24`）；设值走 `_w32(A(OFF_WIRE_MAP)+SRC_W*4, f32b(x))`（行 509-512）。
  直写在本链成立，因为 **T28 自己的 `0x10 deploy` 把路由表换成 1 条**，此后无路由再写该槽。
- `WIRE[17]` 的上电值来源：bench `i=17`，`17%3==2 ⇒ SRC_CONST`，`src_index=17`
  ⇒ `read_source` 取 `param[17].value_a = 0.002+3×0.001 = 0.005 < 0.5`（`src/engine.c:227`）。
- **结果：`27 PASS / 0 FAIL / 0 SKIP`**（上一轮 26/1）。逐项对照：**唯一变化项就是 T28.B1**
  （`cur 2→0`、`step_tick 286→0`、`W5 3.00→1.00`、`W6 1.00→0.00`）；段1/2/4 数值**逐字不变**，
  **T29 全 13 项逐字不变**，未改任何断言。
- ★ **改动"只拿掉了要拿掉的东西"**：计数器 `g_seq_writes 9→3`、`steps_sum 9→3`
  —— 正好是"起步被误判推进"的那 **2 次写 + 2 步**。

**本轮最终验收（单进程串行，避免抢口 —— 见 §13.7）**
```
h723_i2c_leak_check   PASS          G6-1 h723_i2c_sm_test    PASS (10/10)
h723_i2c_gate_test    PASS (G6-2)   h723_dev_bind_test       52 PASS / 0 FAIL / 1 SKIP
h723_i2c_shm_test     PASS (G6-3)   h723_seq                 27 PASS / 0 FAIL / 0 SKIP
```
⇒ 回归基线在 `de2541e` 上**已回到"零差异"**（除设计内的 `persist 7/19`、`audit_m234 9/3`、
`w1 27/1(R11)`、`modbus 5/10(需 485 回路)`、`t26 7/11`、`w5 10/3/4SKIP`）。

### 13.7 ★★★ 一条**编排纪律**（我自己的错，值得写进规程）

我曾让**两个 worker 同时**在 `COM21` 上跑套件，我自己还插进去读状态。后果：
一个进程发 `0x01`，另一个的**密轮询**把它当成 `0x22` 的应答吃掉 ⇒ 读出**不可能的值**
（`SENSOR[15]` 位型 `0x1DF70200` = `fw_ver(0x0200) | cap(0x1DF7)<<16`），并被误报成"固件用错 lane"。
- 证据：`COM21: PermissionError(13, 拒绝访问)`（同一口被两个进程抢）。
- ★ 一个**被排除了的错误归因**：不是"COM11 也通到这块板子"—— COM11 是 `0x0D28:0x0204`（micro:bit 类 CDC），
  我们的板子是 COM21 的 CH340（`0x1A86:0x7523`）。
- ⇒ 纪律：**串口是独占资源，同一时刻只允许一个进程/worker 使用**；冲突时**退让重试**，不并行硬闯。
- ★ 这次假故障之所以**无法自动归因**，根因仍是 **GAP-12（应答不带归属）** —— 它因此更该进 v1。

### 13.8 那次偶发红的**定案**：又是并发占用（第三个症状），以及两条方法学教训

**现象**（工具侧 8 次复跑里唯一 1 次红）：`T8.6` 命中 `SENSOR[15]=7.5`（位型 `0x40F00000`），
且**已复读确认**（连读 3 次同值 ⇒ 不是串帧）；同一次跑里 `T9.2` 的 `0x13` **无应答**。

**定案（有唯一性证据）**：`7.5` 在仓库里**只有一个来源** ——
`tools/h723_w2_probe.py:81` 的 **`V_FORCE1 = 7.5`**（它刻意用**非零值**做 FORCE 判据，见该文件头 OA9 事故记录）。
而那个套件当时正被**另一个 worker 并发跑**。⇒ 那是**别人进程的强制值 / 别人进程的读回帧**，
与固件无关。`0x13` 无应答同源（板子/串口被另一个进程占着）。
★ 这已经是同一次编排错误的**第三个症状**（① 一次串帧假故障；② G6-2 顺序依赖被误读；
③ 这次 7.5 假命中）。**一个错误在三个地方以三种面貌出现** —— 与"一处 refs 泄漏消掉三个症状"正好互为镜像。

**两条方法学教训（都已写进 `.workbuddy/memory/MEMORY.md` §九）**
1. ★★★ **不要用"当前固件的读数"去归因"历史缺陷"**（工具侧自己纠正的）：
   它拿"30 次纯 SM 事务 ⇒ `refs` 增量 +0"去推断"泄漏不在 SM 自身"，但**板子上那时已是修复后的固件**
   ⇒ 那是"修复生效"的证据，**不是"本来没缺陷"的证据**。
   ⇒ 判据分两类：**回归判据**（在当前版本上量）与**归因判据**（必须钉住版本/前置状态）。
   **用前者当后者 = 得出与真因相反的结论。**
2. ★★ **提交信息必须描述"代码里实际是什么"，不是"我要求了什么"**（我自己的问题）：
   `95c4d46` 的信息写"命中需连续两读同值 + 打印原始 u32"（那是我的**指令**），
   而实际落地的是 `_confirm_hit`（**连读 3 次 + `DB_MAGIC` 旁证**，更严）。
   这与本项目最爱抓的"**注释/描述与实现不符**"是同一族 —— 只不过这次发生在 **git 历史**里，
   而 git 历史**不能改** ⇒ 只能在这里更正：**以代码为准**。
