# 以「一台 PLC」为标尺的整体评估 — 2026-09-13

> 口径来源：`docs/GAP-plc-readiness.md`（2026-09-12，10 维度锚点）+ 本次**重新取证**
> （不引用旧文档结论，逐项重新 grep/计数；旧文档中"无 RTC"一项已过时，见 §4）。
> 评估对象：`9.10 H723newest`（H723 干净重建线）为当前主线；
> `esp32-core0`（S3 线）自 2026-09-10 冻结，只作历史与设计来源。

---

## 0. 一句话结论

- **作为「确定性运行时内核」：完成度很高，且有可复跑证据** —— 这一块超出同类个人项目的常见水平。
- **作为「一台能上现场的 PLC」：完成度约一半，成熟度不够。** 缺的**不是内核**，
  而是四块外围：**IEC 语言前端 / 组态工具 / 通信面 / 工业级验证与认证**。
- **作为「求职作品」：内核够硬，门面不够** —— README 已与代码脱节，无演示、无录屏。

---

## 1. 先给数字（证据底座）

| 项 | 实测值 |
|---|---|
| 自研 C 代码 | **20** 个 `.c/.h`，**11,986** 行（含注释） |
| git 提交 | **132**（2026-09-10 → 09-13，**集中 4 天**） |
| 协议命令 | **22** 个 `CMD_*`（含 0x60/0x61/0x62/0x63 通信域） |
| 测试工具 | **52** 个 Python；4 个套件（E2E 20 PASS/0 FAIL/1 SKIP、审计 M234、shm_channel_audit、485 五段复现） |
| 文档 | 37 份 `docs/`，含 **4 轮外部审计**与多份 `FIX-REPORT` |

**⇒ 证据底座是它最强的地方**：几乎每个结论都有可复跑脚本 + 设备读回值，
并且养成了「能失败的判据」+「A/B 对照」+「宣称=实现」的习惯。
这比"功能多少"值钱得多，也是它能拿去对标主流的最大资本。

---

## 2. 十维度盘点

| # | 维度 | 状态 | 证据 / 差距 |
|---|---|---|---|
| 1 | 执行确定性 | ✅ **强** | 100µs 硬拍、抖动 0、EXEC_BUDGET、VTOR 根因修复（对照量到 8153 拍缺口） |
| 2 | 程序可热更新 | ✅ | ≤1 拍原子切换 + 双副本 A/B 掉电保持（T26） |
| 3 | 下载前静态校验 | ✅ | 双写/速率/NaN/预算全拒（审计轴 2 全绿） |
| 4 | 三域表达（连续/逻辑/**顺序**） | ✅ | `engine_seq_tick` + seq_demo + SEQ 区 |
| 5 | 标准块库 | ✅ 19 块 | 标尺是"IEC 常用 FB >20" ⇒ **差一口气**（19） |
| 6 | I/O 子系统 | ✅ 四类齐 | DI(4) + AI(3) + PWM(HIL) + DO(16)；含去抖/滤波/安全态 |
| 7 | 工程工具 | ⚠️ 半 | 监控/强制/离线校验齐，但**全部是 CLI 脚本，没有 IDE/图形工具** |
| 8 | 通信 | ⚠️ **薄** | **只有 Modbus RTU 从站**；无主站、无 CAN、无以太网/HMI 协议 |
| 9 | 文本语言 | ⚠️ | 自家 DSL（`dclc.py` 支持 DSL/FBD）；**无 IEC 61131-3 ST** |
| 10 | 作品门面 | ❌ | README 停在 **09-11 23:46**（此后所有改动未进）；**无 demo、无截图、无录屏** |

---

## 3. 三个「本质」缺口（比功能清单更要紧）

### ① 无看门狗 ⇒ **没有自恢复能力** ★ 最致命
取证：`grep -rn "IWDG\|WWDG" src/` ⇒ 只命中 `main.c` 里**读复位原因用的位名注释**
（`WWDG1RSTF`/`IWDG1RSTF`），**零实现**。
⇒ 我们所有"确定性"成果都建立在**"程序一直正常"这个未验证的假设**上。
PLC 的可靠性底线是"跑飞 / 受干扰 ⇒ **自动恢复** ⇒ 进安全态"——**这条路径目前不存在**。

### ② 成熟度最薄弱处：**异常路径与长稳几乎零覆盖**
**今天这一件事就是实证**：一个 **115200 连续帧下几乎必然触发**的缺陷
（ORE 溢出标志未清 ⇒ 接收自锁死），**存活了整条开发周期**，
靠 4 轮外部审计 + 一整天排查才定位。它表现为"偶发"，本质是"必然"。
取证：`故障注入 / 长稳 / 老化 / soak` 在 docs 里**零命中**（只有 GAP 文档说"应该做"）。
⇒ 缺两类测试：**错误注入（0）** 与 **长稳 72h（0）**。
**这才是"成熟度"的真正门槛：能不能在异常路径下仍然确定。**

### ③ 只有一条通信通道
取证：`ETH_BASE / CAN1_BASE / FDCAN / 0x40028000` 在 `src/` 里**零命中**。
⇒ 工业场景的"双通道冗余"做不到；H723 硬件其实带以太网 MAC+DMA，代码为 0。

---

## 4. 两个「半缺」+ 一项已关闭

| 项 | 已有 | 缺 |
|---|---|---|
| 报警系统 | DSL 的 `ALARM` 关键字（编译成普通路由） | 报警表 / 确认 / 历史 / 时间戳 —— 现场"HMI 看列表并确认" |
| 数据记录 | 黑匣子：SD 裸块落盘、每拍记录、12.5× 压缩、零丢包（**这是加分项，很多小型 PLC 没有**） | 趋势 / HMI 侧 / PC 端程序保存 |
| ~~RTC 时间戳~~ | ✅ **已关闭** | `src/rtc.c` 已落地（含影子寄存器防撕裂、不依赖 `INITS` 的启动判据）—— **旧 GAP 文档的这一项已过时** |

---

## 5. 判定（三种用途，口径不同）

| 用途 | 判定 | 依据 |
|---|---|---|
| **技术验证 / 求职作品** | **够，且内核偏强** | 100µs 硬拍 + 抖动 0 / 预算保护可观测 / ≤1 拍热更新 / 下载前静态校验 / 19 个 IEC FB / Modbus RTU / 黑匣子 / 4 轮外部审计 —— **全部有可复跑证据** |
| **实验室自用（自己搭台子跑）** | **基本够** | 补"看门狗 + 长稳"即可 |
| **能上现场 / 能卖** | **远不够** | 缺 IEC 语言、组态 IDE、通信面、EMC/隔离/认证、I/O 保护（短路/断线检测）、长稳与故障注入数据 |

---

## 6. 如果只做三件事（按性价比）

1. **看门狗 + 失效安全闭环**（半天~1 天）
   补上"可靠性身份"，且**天然可录屏**：故障注入 ⇒ 200ms 内复位 ⇒ 进安全态 + 复位原因可读。
   一句话叙事："异常注入下 200ms 内自恢复并进安全态"。★ 与 GAP 文档的排序一致。

2. **错误注入 + 长稳回归**（1~2 天）
   把今天的教训固化成套件：通信侧覆盖"非法帧 / 半帧 / 波特率错 / 持续灌满 / 长时间跑"；
   I/O 侧覆盖"断线 / 超量程 / 越界"。**这是从"能跑"到"成熟"的唯一路径。**

3. **README + 演示 + 录屏**（1 天）
   求职必需。当前 README（455 行）内容不差，但**停在 09-11**，与今天的 485 结案、
   性能优化、编码闸门全部脱节 —— 外人读到的是一份过时快照。

---

## 7. 元层面（比清单本身重要）

按你自己定的"收尾优先于开新线"：**现在缺的不是能力，是"可被第三方在 10 分钟内看懂并相信"的收口。**

建议对外的口径是：

> **"STM32H7 单核 100µs 确定性 PLC 内核：Modbus RTU + 下载前静态校验 + ≤1 拍热更新
> + SD 黑匣子；全部结论可复跑、有 A/B 对照。"**

而不是"我做了一台 PLC"。**前者你已经做到了；后者还差一个量级** ——
差的正好是"语言前端 / 组态工具 / 通信面 / 工业级验证"这四块外围。
