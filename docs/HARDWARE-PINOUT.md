# 硬件接线与引脚速查（鹿小班 LXB723ZG-P1 / STM32H723ZGT6）

来源标注：
- `[实测]` = 本项目亲自验证过
- `[原理图]` = 从板商原理图文本层读到（`D:/STM/work/dcl-controller/Hardware Official Documentation/板子资料/【4】原理图尺寸图/LXB723ZG-P1原理图.pdf`），**尚未全部实测**

---

## 1. 逻辑分析仪接线（Saleae Logic 8，无模拟输入）

| LA 端 | 接板子 | 用途 | 状态 |
|---|---|---|---|
| **CH4** | **PA8** | 拍输出（ISR 每拍翻转一次） | `[实测]` 已接通，100μs 拍验证通过 |
| **GND** | 板子 **GND** | **共地（必须）** | `[实测]` |
| CH1 | PA9（USART1_TX）| 协议 UART 波形 —— 阶段 1 起用于协议级外部验证 | 待接 |
| CH0 / CH2 / CH3 | 待定 | 预留（DO 输出 / 外部触发） | — |

### ★ 三条硬性注意

1. **必须共地。** LA 的 GND 一定要接板子的 GND。不共地时读出来的全是垃圾
   （电平参考点漂移，看起来像"信号不对"）。
2. **信号线尽量短。** 实测 PA8 线上存在约 **0.01~0.12% 的窄毛刺**（几十~几百 ns），
   疑为跳线串扰或 LA 采样。缩短线长、或让 GND 线与信号线并走（双绞）可改善。
   → 测频工具已按"异常项单独计数"处理，毛刺**不影响拍周期**（众数稳定 100.0000μs）。
3. **本机（克隆 Logic 8）双通道数字采集不可靠。**
   单通道 16MHz 稳定；实测双通道 8MHz / 4MHz（CH0+CH4）时 **CH4 恒为 1、无边沿**。
   → 现阶段只用**单通道**采集，需要多信号时分时复用。

### 通道分配约定（建议，后续阶段沿用）

```
CH4  拍心跳 / 关键时序信号 (PA8)      ← 现在
CH1  协议 UART TX (PA9)               ← 阶段 1 起
CH2  DO 输出 / 位置脉冲                ← 接负载后
CH3  外部触发                          ← 需要时
GND  共地 (必接)
```

工具侧用 `--ch N` 指定通道，例如 `python la_tick_freq.py --cpu 400 --ch 4`。

---

## 2. 调试口 H1（卧贴 2×4P，2.54mm）`[原理图]`

| 脚 | 信号 | 说明 | 来源 |
|---|---|---|---|
| 1 | SWCLK_PA14 | DAPLink SWCLK | `[原理图]` |
| 2 | SWDIO_PA13 | DAPLink SWDIO | `[原理图]` |
| 3 | GND | | `[原理图]` |
| 4 | +5V | | `[原理图]` |
| 5 | **USART1_RX_PA10** | CH340 **TXD** 接此 | `[实测]` 回声测试通过 |
| 6 | **USART1_TX_PA9** | CH340 **RXD** 接此 | `[实测]` 收到横幅 |
| 7 | RST | 经 **1K 限流电阻**（防把电源错接到 RST）| `[原理图]` |
| 8 | 3.3V | | `[原理图]` |

> ★★ **勘误 (2026-09-11)**: 本表此前把第 5/6 脚**写反了** (旧: 5=PA9, 6=PA10)。
> 实测依据: CH340 的 RXD 接第 **6** 脚 → 收到固件横幅; TXD 接第 **5** 脚 → 回声成功。
> 另: 第 **7** 脚是 **RST**(非 PA9)。误把 TX 线接到第 7 脚会持续按住板子复位,
> 现象是"板子像死了一样", 且 SWD 也连不上。
> ★ 接线只需 3 根: RXD / TXD / **GND(必须共地)**。**不要接 VCC** (板子已由 DAPLink 供电)。

> ★ **USART1 = PA9(TX) / PA10(RX) 已经引到这个排针上** —— 这就是 DCL 协议 UART 的天然落点,
> 不需要再从大排针飞线。接 LA 时只需从 H1 的 6 脚引一根到 LA 的 CH1。

原理图注（原文）：
1. 此处 1K 电阻的目的是在用户将电源线错接到 RST 时起限流保护作用
2. 推荐使用 5V 供电，再经板载稳压芯片得到 3.3V 给单片机，避免意外输入高压而损坏单片机

---

## 3. 运行指示

| 信号 | 说明 |
|---|---|
| **LED_PG7** `[原理图]` | 板上 LED 接在 PG7 → 可直接做状态灯 / 错误码指示 |
| **PA8** `[实测]` | 本项目当前用作**拍输出**（接 LA）；时钟初始化失败时闪 `|错误码|` 次 |

---

## 4. 扩展排针

| 连接器 | 说明 |
|---|---|
| **2.54-2×30（60 脚）** `[原理图]` | 几乎所有 IO 引出，**含 PA8**；板上丝印标注引脚名 |

原理图文本层能看到 PA8 与 PC11 在相邻位置（编号 47/48 一带），
**具体脚号以板上丝印为准**（原理图文本层的奇偶脚对应关系不够可靠，未实测）。

---

## 5. 时钟与电源

| 项 | 值 | 来源 |
|---|---|---|
| HSE | **25MHz 无源晶振 + 6pF × 2** | `[原理图]` + `[实测]`（LA 反推确认 25.000MHz）|
| LSE | 32.768kHz + 22pF × 2 | `[原理图]` |
| VCAP | 引脚 106；原理图文本层出现 `4.7uF × 2`，**未确认是否为 VCAP** | `[原理图]` |
| 去耦 | 100nF × 18（多处）| `[原理图]` |

> ⚠️ **VCAP 是当前"频率天花板 ≈465MHz"的第一嫌疑**：
> AN5419 Rev3 p7 要求 VCAP 外接 **2.2μF / ESR<100mΩ**。
> 若板上那两个 4.7μF 是 VCAP 且 ESR 偏大，可能导致 VOS0 电压不稳 → 高频跑不住。
> **待验证手段**：万用表/示波器量 VCAP 引脚电压（LA Logic 8 无模拟输入，测不了）。
