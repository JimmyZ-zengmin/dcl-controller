# EXP-2026-09-18-OVERLOAD —— 超载实验：预算门**能不能拦住超载**？

> **这是一次"补判据"的实验**，不是功能开发。
> 起因：`docs/ARCH-GUARANTEE-MAP.md` 的 **G3（拍内按时完成）** 只有一个缺口 ——
> `src/engine.h:832-855` 的绊线断言写着：
>
> ```
> 128 条上限 × 最贵原语 145 cyc = 18560 cyc = 拍长的 46.4%
> ⇒ 在 MAX_ROUTES=128 的前提下，任何合法程序都不可能把拍吃满，预算门当前永远不会触发。
> ⇒ 一旦断言失败（门变成真门），届时必须做一次超载实验
>    （构造 >门 的程序，确认 NAK + 确认拍没被拉长），而不是相信一个从没被触发过的判据。
> ```
>
> 本实验就是那次超载实验。**判据必须能失败** —— 所以每个结论都配了对照样本。
> 板：COM22（CH340）· 交付固件 `fw=0x0200` / `cap=0x7DF7` / `h723_proto 12 PASS 0 FAIL`。

---

## 0. 先读代码（不拿猜的模型设计实验）

| 事实 | 出处 |
|---|---|
| 静态门 `EXEC_DEPLOY_BUDGET = 26000`（下载期，预判，不等式） | `engine.h:813` |
| 动态门按**时间**表达 80 µs：`EXEC_BUDGET_TB`（0 档 32000 DWT cyc / **1 档(TIM5) 16000 tick**） | `main.c:1446` + `engine.h:825` |
| 每拍成本 = `Σ_ACTIVE ceil((op_cost + src_cost) / mult)`；mult: div0=**1** / div1=10 / div2=64 | `engine.c:807-822` |
| **`k_src_cost[4] = {0,0,0,0}`** —— 四种源类型成本**全为 0** | `engine.c:799` |
| 成本表最大值 = **PID = 145**（`OP_MAX=0x12`） | `engine.c:771-773` |
| 越界 op 的成本兜底 = `145 × 2 = 290` | `engine.c:804` |
| `0x10` ACK 载荷 = **`[seq:u16][budget:u32]`** ⇒ **能把算出的预算读回来** | `main.c:1899-1902` |
| `prog_validate` 门序：`short → counts → payload short → ①参数有限 → ②逐条 validate+dst冲突 → ②b rate mismatch → ③预算门` | `main.c:1786-1848` |

### ★ 解析结论（实验设计的依据）

1. **静态门不可达**：`128 × 145 / 1 = 18560 < 26000`（src_cost 全 0、div0 的 mult=1、PID 是表内最大值）。
   余量 **26000 / 18560 = 1.40×**。
2. **唯一的"超额来源"也被门序挡住**：让某条路由成本变 290 的**唯一**途径是 `op > 0x12`，
   而 `engine_route_validate`（**第 ② 步**）的白名单会先返回 `"bad op"`，**永远走不到第 ③ 步**。
   ⇒ `128 × 290 = 37120 > 26000` 这个理论超额**结构上无法表达**。
3. ⇒ **"构造一个 >门 的程序"在交付配置下做不到**（不是"没试过"，是**做不到**）。
   ⇒ 实验改成：**用可读回的 `budget` 实测最重合法载荷，把"不可达"从断言变成实测事实**。

---

## 1. OVL-2 —— 静态门（源码无关：不改固件、不烧录）

脚本 `.tmpctl/ovl_static.py` · `.tmpctl/ovl_heavy.py` · `.tmpctl/ovl_o2b.py`

> ★★ **可复跑性声明（2026-09-18 补）**：`§1`–`§5` 的脚本住在 `.tmpctl/`，而该目录
> **被 `.gitignore` 忽略** ⇒ **它们不在版本控制里**（克隆本仓拿不到）。这是本记录的一个
> **真实缺口**，不要把它当"已交付的工具"。**§7 的验收脚本已提升进 `tools/`**
> （见本节末的对照表）。若要复跑 §1–§5，需要 `.tmpctl/` 的本地副本，或按本记录的
> 表格重写探针（载荷构造在 §1 的表里已给全：`counts exceed max` 之前的字段布局）。

| 载荷 | 手算期望 | **实测 ACK 的 `budget`** | 判定 |
|---|---|---|---|
| 128 × DIRECT(56) div0 | 128×56 = **7168** | **7168** | ✅ 精确命中 |
| 128 × PID(145) div1 | 128×ceil(145/10)=128×15 = **1920** | **1920** | ✅ 精确命中（÷10 摊薄生效）|
| **128 × PID(145) div0**（★最重合法） | 128×145 = **18560** | **18560** | ✅ 精确命中；**ACK（未被拒）** |
| 128 × op=0xFF（若按 290 计 = 37120） | — | **NAK `bad op`** | ✅ **不是** `exec budget exceeded` ⇒ **门序确实挡住了** |

**辅助判据**：被 NAK 的那次 `n_routes` 前后不变（**拒绝无副作用**）。
**注**：op=0xFF 那条在两次运行间出现过一次 TIMEOUT。隔离实验（正对照：1 条 op=PID ⇒ ACK；
1 条 op=0xFF ⇒ TIMEOUT；128 条 op=0xFF ⇒ NAK "bad op"）表明是**传输层偶发丢应答**
（端口打开会 DTR/RTS 复位板子 ⇒ 首次事务易撞上启动窗），**与 op 是否非法无关**。

### 1.1 顺带量到的：最重合法程序的真实成本

`emax` 与 `EXEC_BUDGET_TB` 同域（TIM5 @200 MHz ⇒ **1 tick = 5 ns**）；动态门 = **16000 tick = 80 µs**；拍长 = 20000 tick = 100 µs。

| 程序 | `budget` | `n_routes` | `emax` | µs | **占动态门** | **占拍长** | `ov` | `pmin/pmax` |
|---|---|---|---|---|---|---|---|---|
| 基线（`0x13 RESET` 后空引擎） | — | 0 | **632** | 3.2 | 4.0% | 3.2% | 0 | 19988/20012 |
| 128 × DIRECT div0（ITCM 档） | 7168 | 128 | **6240** | 31.2 | 39.0% | 31.2% | 0 | 19988/20012 |
| **128 × PID div0（最重合法，ITCM 档）** | 18560 | 128 | **10682** | **53.4** | **66.8%** | **53.4%** | **0** | 19988/20012 |
| 收尾（回 bench） | — | 0 | 632 | 3.2 | 4.0% | 3.2% | 0 | 19994/20006 |

1. **拍没被拉长** ✅ —— `pmax` 在"空引擎 / DIRECT×128 / PID×128"三种负载下**完全相同（20012 tick）**
   ⇒ 拍仍由硬件定时器决定。这正是 `engine.h` 要求的"**确认拍没被拉长**"。
2. **动态门对合法程序也不可达** —— 最重合法只吃到门的 **66.8%**（余量 1.50×），`ov=0` 与 `emax≤门` **自洽**。
3. ★★ **由此暴露一条更本质的事实**：`ov == 0` 对任何合法程序**都不可能失败** ⇒ **它仍是空判据**
   —— 只是原因从"字段被写死 0"（09-11 修掉的那个）变成"**门比最坏合法负载还高 50%**"。
   ⇒ **两个门是冗余的**：静态门保证动态门永不触发。动态门的真正用途只能是
   **"模型外负载"的兜底** —— 这正是 OVL-1 要验的。
4. **成本模型误差的上界**：实测扫描 ≈ 10050 tick（= 20100 DWT cyc）vs 模型预测 18560 ⇒ **实测/预测 ≈ 1.083**。
   ★ 边界：`emax` 是"**相位撞在一起的那一拍**"（含热重载、顺序域/通信域/传感域在该相位的份额），
   所以 **+8% 是扫描成本误差的上界，不是点估计**。

---

## 2. OVL-1 —— 动态门（需要"模型外负载"）

**目的**：`ov` 在 **TIM5 时基**下是否仍能触发。
**为什么必须验**：09-11 量到 `ov = 18343` 是在 **DWT 时基**下；之后交付档换成 TIM5，`di` 的单位从
DWT cyc 变成 TB tick，阈值改走 `EXEC_BUDGET_TB`。**若换算错了，`ov` 会永远为 0 而无人察觉**
—— 而 `ov == 0` 是 `h723_jitter`/T9 的判据之一。

### 2.1 第一次尝试失败（并且这本身是发现）

用 09-11 的同款配置 `-DDCL_BOOT_SEL=0 -DDCL_BOOT_PROFILE=2`
（构建 RC=0，`programmed 68608 bytes`）⇒ 复位后 **板子完全不答协议**，
等 12 s 仍 `h723_proto` 全 SKIP。

**隔离**：换成 `-DDCL_BOOT_SEL=0 -DDCL_LOOP_RESET=0`（**BOOT_PROFILE 保持 0**）⇒ 空引擎下
**12 PASS / 0 FAIL**。
⇒ **不应答是 `BOOT_PROFILE=2` 造成的，不是 `BOOT_SEL=0`**。机理：profile 2 的负载让 ISR 长期
超过拍长 ⇒ 主循环被饿死 ⇒ 撞 `DCL_LOOP_RESET`（1.2 s 停滞）⇒ **复位循环**，而复位会清零
`ov`/`samples` ⇒ **这个配置根本测不到 `ov`**。
★ **教训**：要测"超载下的计数"，必须**先关掉会因超载而复位板子的自愈机制**，
否则观测手段会毁掉被观测对象（与铁律 0 同源）。

### 2.2 受控超载（FLASH 扫描路径 = 成本模型外的负载）

配置：`-DDCL_BOOT_SEL=0`（扫描走 **FLASH**，每条约 3.6× 慢；成本表 `k_op_cost_itcm` **只为 ITCM 标价**）
`-DDCL_LOOP_RESET=0`。负载由协议侧逐档部署 N 条 PID div0 控制。

脚本 `.tmpctl/ovl1b_sweep.py`：

| N（PID 路数） | `emax` | µs | **占动态门** | 占拍长 | `ov` | `pmax` |
|---|---|---|---|---|---|---|
| 40 | 10108 | 50.5 | 63.2% | 50.5% | 0 | 20010 |
| 48 | 11864 | 59.3 | 74.2% | 59.3% | 0 | 20018 |
| 56 | 13564 | 67.8 | 84.8% | 67.8% | 0 | 20010 |
| 64 | 15320 | 76.6 | **95.8%** | 76.6% | **0** | 20012 |
| **72** | **17118** | **85.6** | **107.0%** | 85.6% | **1** ★ | 20010 |

- **M0 时基活着**：`0x39 op=21` ⇒ 档=**1(TIM5)**、`TB_HZ=200000000`、自检 Δ=**6040 ≠ 0** ✅
- **M1 `emax` 随路数单调上升** ✅（负载真的在加）
- **M3 反向保护** ✅ —— 未超过门的 4 档 `ov` **全为 0**（阈值换算没反）
- **M2 ★ 动态门触发** ✅ —— `ov` 从 0 跳到 1，跳变点**正好卡在门=16000 tick** 上

### 2.3 `ov=1` 是瞬态还是持续？（决定性的一刀）

脚本 `.tmpctl/ovl1c_growth.py`：

| N | 观察 | 判定 |
|---|---|---|
| **72** | `emax=17020` 恒定；`ov` **保持 1 不变**（`samples` 从 19,152 涨到 109,955，跨 10.6 s）| ⇒ **唯一那次超载是瞬态** |
| **80** | `emax=18748`；`ov` **线性增长** 16,987 → 65,364（与 `samples` 同步，比值 80→94%）| ⇒ **持续超载**；约 8 s 后协议不再应答（主循环饿死；因 `LOOP_RESET=0` 未复位）|

⇒ ★ **`ov` 是"每拍超了就 +1"的真计数**（不是布尔、不是每段一次），实测与 `samples` 同步线性增长。
⇒ ★★ **瞬态那次是"热重载那一拍"**：`reload` 让该拍多花 ~720 tick，足以把 N=72 的
持续态（≈16,400 tick）顶过 16,000 的门。
**这是"热重载要吃掉预算余量"的直接实测证据** —— 也正是 `engine.h:810` 给热重载那一拍留
14000 cyc (35 µs) 的理由。
⇒ **拍周期始终没被拉长**：N=72（ISR 85.6 µs）时 `pmax` 仍 = 20010 tick，与空引擎相同。

---

## 3. 结论

| # | 结论 | 证据 |
|---|---|---|
| **C1** | **静态门 `EXEC_DEPLOY_BUDGET` 结构上不可达** | 解析（max=128×145=18560<26000）+ 实测（最重合法载荷 ACK 且 `budget` 精确读回 **18560**）+ 实测（唯一超额路径被门序挡住：NAK **`bad op`** 而非 `exec budget exceeded`）|
| **C2** | **动态门在 TIM5 时基下有效，阈值精确** | `ov` 在 `emax` 跨过 16000 tick 的那一档从 **0→1**；未过门的 4 档全为 0 |
| **C3** | **`ov` 是真计数**（每拍 +1），不是布尔 | N=80 时与 `samples` 同步线性增长（16,987→65,364）|
| **C4** | **拍周期未被 ISR 拉长**（门要求的第 2 条只证） | 空引擎 / DIRECT×128 / PID×128 / FLASH×72 四种负载 `pmax` 均 ≈20010 tick |
| **C5** | ★ **`ov == 0` 对合法程序仍是空判据** | 最重合法只到门的 66.8% ⇒ 该判据**不可能失败** ⇒ 动态门只是**模型外负载的兜底** |
| **C6** | **热重载那一拍本身就是一次超载来源** | N=72 持续态在门下，`ov` 却为 1；N=80 持续超载时 `ov` 线性增长 |

**对 G3 的处置**：G3 那个缺口**已闭合**。原来的表述是"**设计成立、未压力验证**"，
现在两个门都从"假定"变成"**实测过**"：
- 静态门：**不可达**（且已证明唯一超额路径被门序阻断）
- 动态门：**可达且有效**（需模型外负载：FLASH 扫描路径 / 热重载拍 / 事后新增的 ISR 服务）

---

## 4. 恢复动作（已执行并核对）

1. 重建**默认档** ⇒ `md5 = 80c05c0eb8f00ade3ba5b8985b711826`
   = 实验前保存的 `.tmpctl/delivery.hex` **逐字节相同**（交付产物可复现）。
2. 烧回 + `pyocd reset` + 等 12 s ⇒ `h723_proto.py --port COM22` = **12 PASS / 0 FAIL / 0 SKIP**。
3. 板子回收尾态（`0x12 STOP` / `0x13 RESET` + `0x11 START`）。
4. ★ **实验档固件没有留在板子上**（本项目铁律：`-D` 是缓存的，曾把"自检版"当交付固件烧过）。

**产物**（`.tmpctl/`，gitignored）：`delivery.hex` · `overload.hex`（BOOT_SEL=0+PROFILE=2，
证明会饿死主循环）· `ovl1b.hex` · `restored.hex` · `ovl_static.py` · `ovl_heavy.py` ·
`ovl_o2b.py` · `ovl1b_sweep.py` · `ovl1c_growth.py` · `ovl1b_build.sh` · `restore.sh`

## 5. 建议的后续（不在本次范围内）

1. **把"热重载拍的预算"显式建模** —— C6 说明它是一次真实的超载源；当前只在注释里留了 35 µs 余量，
   没有判据。可加一条：`deploy 那一拍的 di` 单列统计（现在被 `emax` 混在一起）。
2. **给 `ov` 一条"能失败"的用法** —— 因为 C5，`ov == 0` 不能作为"负载健康"的判据；
   应改为"**`emax` ≤ 门 × 0.8**"这类**有余量**的判据。
3. **成本表补 FLASH 路径的价** —— 现在 `k_op_cost_itcm` 只给 ITCM 标价，
   一旦有人用 `DCL_BOOT_SEL=0` 跑，静态门会**严重低估**（实测 FLASH 路径约 3.6×）。★ 这是
   **"预算模型有盲区"** 的真缺口，与 S3 的 OA12→OA22 同族。

---

## 6. 后续工作 ①②③ 的执行结果（同一天完成）

### 6.1 ★ 补 FLASH 路径成本价（缺口 ①→已闭合；这也是**静态门拒绝路径的首次实测**）

**先量倍率**（同一套方法，两条路径各量斜率）：
| 路径 | 斜率 | 出处 |
|---|---|---|
| ITCM（交付档 `BOOT_SEL=1`） | (6018−4300)/24 = **71.6 tick/条** | §2.1 的 ovl_heavy |
| FLASH（`BOOT_SEL=0`） | (15320−10108)/24 = **217.2 tick/条** | §2.2 的 sweep |
| ⇒ **倍率** | **3.03** | 历史 DIRECT 档曾实测 **3.63** ⇒ 倍率**随原语而变**，本值是中心值 |

**改法**（`src/engine.h` + `src/engine.c`，①层）：
- 新增 `OP_COST_PATH_NUM/DEN`，`#if (BOOT_SEL == 0)` ⇒ **303/100**；否则 **1/1**
- `engine_prog_budget()` 里把缩放与 mult 合并成**一次向上取整**：
  `ceil((op+s)·NUM / (DEN·mult)) = ((op+s)·NUM + DEN·mult − 1) / (DEN·mult)`
- 绊线断言改成**两侧**（因为"门具不具约束力"现在随 `BOOT_SEL` 变）：
  - `BOOT_SEL=1`：断言 **≤** 门（不具约束力，同旧语义）
  - `BOOT_SEL=0`：断言 **>** 门（**应当**具约束力 —— 这是正确的保守行为）

**验证（三条，全部实测）**：
| # | 判据 | 结果 |
|---|---|---|
| V1 | **交付档零回归** | 重建后 md5 = **`80c05c0eb8f00ade3ba5b8985b711826`**，与改动前 `delivery.hex` **逐字节相同**（编译器把 ×1/1 折掉）⇒ 交付产物**一个字节没变** |
| V2 | FLASH 档能编过 | 旧单边断言下它**编不过**；改两侧后 **RC=0** |
| V3 | ★ **静态门真的拦得住** | 见下 |

**V3 明细**（FLASH 档，每受试路数预算 = N × ceil(145×303/100) = N × **440**；门 = 26000）：

| N | 期望预算 | 结果 | `n_routes` |
|---|---|---|---|
| 32 | 14080 | **ACK** `budget=14080` ✅ | 59→32 |
| 58 | 25520 | **ACK** `budget=25520` ✅ | 32→58 |
| **59** | **25960** | **ACK** `budget=25960` ✅（门下沿）| 58→**59** |
| **60** | **26400** | ★★ **NAK `exec budget exceeded`** | 59→**59**（**无副作用**）|
| 64 | 28160 | **NAK** ✅ | 59→59 |
| 128 | 56320 | **NAK** ✅ | 59→59 |

⇒ ★★★ **这是本项目第一次看到静态预算门真的拒绝一个载荷**，而且边界**精确落在成本模型
预测的 59/60 上**（`26000/440 = 59.09`）。修复前这件事**结构上做不到**（§0 结论 3）。

★ 途中又踩一次同类坑，记在案：部署用 `expect_len=6` ⇒ NAK 理由 `"exec budget exceeded"`
是 **20 字节** ⇒ 长度不符被 `Dcl.send()` 丢弃并重试 3 次 ⇒ **假 TIMEOUT**（与 §1 的 OVL-2 同族）。

### 6.2 ★ 把 `ov == 0` 换成"能失败"的判据（缺口 ③→已处置）

**新判据**：`tools/h723_budget_fidelity.py` —— **预算模型保真度**
| 判据 | 内容 | 实测（交付档） |
|---|---|---|
| **F4 前置** | 时基活着（`0x39 op=21` 自检 Δ≠0），否则**拒答** | 档=1(TIM5), Δ=6040 ✅ |
| **F1 ★模型保真** | 实测扫描 / 预测预算 ≤ 1.30 | 实测 **18828 cyc** / 预测 **18560 cyc** = **1.014** ✅ |
| **F2 有余量** | `emax ≤ EXEC_BUDGET_TB × 0.80` | 10690 ≤ 12800 ⇒ **余量 33.2%** ✅ |
| **F3 反向保护** | 轻程序 emax 必须显著小于重程序 | 2548 < 10690 ✅ |

**能失败（已自证）**：`--ratio-limit 1.0` ⇒ **F1 FAIL**（1.018 > 1.00）✅
**真实失败模式（已观测）**：修复前 FLASH 路径的同一比值是 **3.03** ⇒ F1 当时**必然是红的**
—— 也就是说，**这条判据本来就能自动抓到缺口 ①**。

★ 顺带的独立收获：**F1 = 1.014 是"整程序预算"的首次端到端验证**。
此前成本表只用 `h723_op_sweep.py` **逐原语**验过；这次是"预测的整程序预算 vs 实测 ISR"
⇒ 模型误差 **1.4%**（远好于 09-18 早先估的 8% —— 那 8% 是拿错了空引擎基线造成的）。
⇒ 同时 `h723_io_isr_check.py` 判据 ④ 已改成**显式标注**"必要条件而非充分 + 指向本工具"
（**条件本身不变 ⇒ 不破坏任何套件**）。

### 6.3 尚未做（登记）
- **缺口 ②**（热重载那一拍的预算无判据）：`deploy` 拍的 `di` 仍被 `emax` 混在一起，
  应单列一个观测槽。
  ⇒ ★ **§7.4 已部分闭合**：F5 给出了重载拍的**上界**（0.94 µs ≤ 35 µs 余量）；
  精确归因仍需把 `g_reload_cyc` 暴露到协议面。
- ~~**19 个原语的 FLASH 逐条成本表**：本次用单倍率（3.03）近似~~ ⇒ **§7 已完成，且单倍率被证伪**。

---

## 7. 把"单倍率"换成两张逐原语表（缺口 ① 的真正闭合；单倍率 3.03 被**证伪**）

### 7.0 一句话

§6.1 用**一个标量**（303/100 = 3.03）代表两条扫描路径的成本差。**这个近似是错的** ——
实测逐原语比值从 **2.98× 到 4.41×** 不等，标量**既不是上界也不是下界**，
两个方向的错**都实测到了**。已换成 `engine.c` 里的两张**逐原语**表
（`k_op_cost_itcm[]` / `k_op_cost_flash[]`，由 `engine_op_cost()` 按 `BOOT_SEL` 选表），
`engine_prog_budget()` 里的标量缩放已**删除**。

### 7.1 为什么标量必错：逐原语比值不是常数

`tools/h723_op_sweep.py --sel 1|0`（两点法 n=128 / n=64，各 **19/19 可信**）：

| 路径 | DIRECT | PID | 最贵 | 出处 |
|---|---|---|---|---|
| ITCM（交付档） | 56 | **145** | PID | `build/op_sweep_itcm.json` |
| FLASH（`--stat max`） | **247** | **432** | PID | `build/op_sweep_flash_max.json` |
| ⇒ **比值** | **4.41×** | **2.98×** | — | 逐原语极差 **1.48 倍** |

⇒ 标量 3.03 相对真实值：**DIRECT 低估 1.45 倍**、**PID 几乎命中（440 vs 432）**。

★★ **标量最危险的地方正在它"看起来是对的"**：它在最贵的原语上恰好安全，
于是没人会去怀疑它，而它已经在便宜原语上把门打穿了（下节）。

### 7.2 两个方向的错，各自都有板级实测证据

**方向一：标量**放行**会超载的程序**（静态门失守）

在**标量档**（`BOOT_SEL=0`，303/100 那个构建）上实测（`python tools/h723_path_cost_criteria.py --mode scalar`）：

| 受试点 | 模型自报 | 判决 | 实跑 `emax` | 拍占比 | `ov` |
|---|---|---|---|---|---|
| **128×DIRECT** | 21760 cyc（54% 拍）| **ACK** ❌ | **30988 cyc** | **77.5%** | **0** |
| 129×DIRECT | — | NAK（理由 `counts exceed max` = 条数门）| — | — | — |
| 59×PID | 25960 | ACK ✅ | | | |
| 60×PID | 26400 | NAK（预算门）✅ | | | |

★★ 三条结论：
1. **门对自己的契约失守了**：`engine.h` 说 `EXEC_DEPLOY_BUDGET` 的作用是
   "保证**任何可部署程序**每拍执行都在确定性预算内"，而它 ACK 了一个实跑
   **30988 cyc = 77.5% 拍**的程序 —— 超它承诺的 26000 cyc **1.19 倍**。
2. **运行期兜底 `ov` 没触发**（80% 拍 = 32000 cyc）⇒ **这条失守不会被 `ov` 兜住**
   ⇒ **静态门是承重的**，不能靠"先用标量凑合"。
3. **标量档的门的拒绝路径对 DIRECT-only 程序结构性不可达**：
   门边界在 153×170 = 26010，而 **153 > MAX_ROUTES(128)**
   —— 钉住这一点用的是 N=129 被**条数门**（`counts exceed max`，`main.c:1790`）拦下，
   而不是预算门。⇒ 不是"我们没试到边界"，是**边界在合法域之外**。

**方向二：`min` 统计量**放行**会 `ov` 的程序**（更隐蔽）

`h723_op_sweep.py` 的历史实现取 `g_eng_cyc_min` = **最好情况**（cache 全中）。
确定性门要的是**最坏情况**。ITCM 零等待 ⇒ min≈max，**交付表用 min 无害**
（F1 = 1.014 佐证）；FLASH 走 L1 I-cache ⇒ 两者差 **7~19%**：

| 口径 | PID cyc/条 | 与**部署程序** emax 斜率（**434**）的关系 |
|---|---|---|
| `min`（旧表） | 363 / 401 | 低估 16% / 8% |
| **`max`（新表）** | **432** | **吻合（432 vs 434）** ✅ |

⇒ 在 **min 表档**上实测：**71×PID 被 ACK**（模型 25773 cyc = 64.4% 拍），
实跑 **33592 cyc = 84% 拍** ⇒ ★ **`ov = 1`**。
运行期计数器**抓到了**这个超载 —— 但**计数器不是门**：程序仍留在板上、每拍超载，
只是被记了一笔。⇒ 静态门与运行期判据**不能互相替代**，这一点现在有实测。

### 7.3 换表之后的验收（两档全绿）

**交付档零回归**：重建 md5 = `80c05c0eb8f00ade3ba5b8985b711826`，
与改动前 `delivery.hex` **逐字节相同**（两次构建都验过）。
实现上这是可预期的：`engine_op_cost()` 在 `BOOT_SEL=1` 时仍返回 `k_op_cost_itcm[]`，
`engine_prog_budget()` 回到**原公式**（标量项已删）⇒ 交付档的代码生成与改动前一致。

**FLASH 档成对判据**（`python tools/h723_path_cost_criteria.py --mode table`，max 表，10/10 PASS）：

| 受试点 | 模型预算 | 判决 | 实跑 | 说明 |
|---|---|---|---|---|
| 128×DIRECT | 31616 | **NAK**（预算门）✅ | 31608 cyc = 79.0% 拍 | **洞已闭合** |
| 105×DIRECT | 25935 | **ACK** ✅ | 25928 cyc = 64.8% 拍 | 门下沿 |
| 106×DIRECT | 26182 | **NAK** ✅ | 26175 cyc = 65.4% 拍 | 边界精确 |
| 60×PID | 25920 | **ACK** ✅ | 25916 cyc = 64.8% 拍 | 门下沿 |
| 61×PID | 26352 | **NAK** ✅ | 26348 cyc = 65.9% 拍 | 边界精确 |
| 拒绝各次 | — | — | `n_routes` 不变 | **无副作用** ✅ |

**FLASH 档保真度判据**（`tools/h723_budget_fidelity.py`，6/6 无 FAIL）：

| 判据 | 实测 |
|---|---|
| F0 自适应 N | 固件自报单价 432 ⇒ **N=60**（写死 128 会 NAK ⇒ 静默 SKIP）|
| **F1 ★模型保真** | 实测 **26296 cyc** / 预测 **25920 cyc** = **1.015**（**修前同一点是 3.03**）|
| F2 余量 | emax 14420 TB = **72.1% 拍** ≤ 80% ✅ |
| F3 反向保护 | 轻 4916 < 重 14420 ✅ |
| F5 热重载上界 | **0.94 µs** ≤ 35 µs ✅ |

⇒ 两条门现在**自洽**：静态门（65% 拍给引擎）+ 非引擎 ISR（≈7% 拍）= **72% 拍**
< 动态门 80% 拍 ⇒ 合法程序不再触发 `ov`。

### 7.4 本次顺带修的工具与判据

| 工具 | 改动 | 为什么 |
|---|---|---|
| `tools/h723_op_sweep.py` | 加 `--sel`（1=ITCM / 0=FLASH）、`--stat min\|max` | 一张表只能测一条路径；统计量必须能选最坏情况 |
| 同上（单位坑） | 读回值 ×`CPU_HZ/tb_hz` 换算回 cyc | 固件时基 2026-09-16 换成 TIM5(5 ns) 后，`g_eng_cyc_*` 的**单位变了而名字没变** ⇒ 不换算就直接当 cyc 用，**预算门会静默放宽一倍** |
| `tools/h723_budget_fidelity.py` | 加 **F0 自适应 N** | `--heavy 128` 隐含"交付档预算"前提；路径成本价接上后 FLASH 档 128×PID = 46464 > 门 ⇒ deploy NAK ⇒ 工具**静默变 SKIP** |
| 同上（F2 口径） | 阈值从 `EXEC_BUDGET_TB×0.80`（=64% 拍）改成 **`拍长×0.80`** | 静态门本身允许引擎占 65% 拍 ⇒ 旧口径与被测对象**自相矛盾**，FLASH 档任何近门程序必然 FAIL。**判据口径错会伪装成"被测对象坏"** |
| `tools/h723_audit_full.py` | 加 **4.5~4.8**：FLASH 表存在/覆盖 19 项/最贵是 PID/常量与表同源 + **4.8 逐原语比值不是常数**（极差 > 1.15）| 把"新表也必须与常量同源"变成机械检查；4.8 是**反向判据**：若哪天两表真成比例了，单标量重新合法，这条会 FAIL 提醒人重新评估 |

★ **本节的验收脚本已提升为受版本控制的工具**（原先只在被 gitignore 的 `.tmpctl/` 里 ⇒
**文档引用了一个下一位拿不到的脚本**，那本身就是"宣称不可复跑"）：

| 工具 | 作用 |
|---|---|
| `tools/h723_path_cost_criteria.py` | 本节 §7.2/§7.3 的**成对判据**全部内容（`--mode scalar` / `--mode table`）|
| `tools/h723_build_variants.sh` | 一次构出**两档**（否则只求值了一半的绊线断言）；★ 只构建不烧录 |
| `tools/h723_restore_delivery.sh` | 恢复交付档 + 复位 + 自愈窗 + 探活（**不许把实验档留在板子上**）|

### 7.5 尚未隔离（诚实登记）

1. ★ **FLASH 路径的单条成本随构建布局而变**：同一个 `--stat min` 在两个构建上给
   PID = **363**（标量档构建）与 **401**（表档构建），差 **10%**。
   候选原因：`.scan_flash` 的落点 / 缓存组别名随链接布局变化。**原因未隔离**。
   ⇒ 两条可操作结论：① 扫描体或其布局相关改动 ⇒ **两张表都必须重测**（铁律 2 的更严版本）；
   ② 真正的兜底是**运行期** `emax`/`ov` —— 静态表是预判，只能逼近。
2. `tools/h723_audit_full.py` 的 **1.1「无恒真判据」当前是 FAIL（14 处）**，
   而 `docs/audit/H723-FULL-AUDIT.md` 等多处仍写着 "**12 PASS / 0 FAIL**"
   ⇒ **该宣称当前复跑不出来**。已核查：命中的 `h723_dev_bind_test.py` /
   `h723_devbind_persist_test.py` **本次未改动**（最后动于 `d74e114`）⇒ **既有问题**。
   其中至少 1 处（`h723_dev_bind_test.py:597`，落在 `if s["reject"] == 0:` 分支内）
   是**判据的文本启发式误报** —— 条件本身就是判据；但另有多处是**无条件** `record(...,True)`。
   ⇒ 需要人来分诊（误报 vs 真恒真），**不在本次范围**，登记。

