# h723_step_closedloop_demo.dcl — ★★★ 闭环步进：**闭环写在 DCL 程序里**（H723 原生）
#
# ══════════════════════════════════════════════════════════════════════════
# 为什么这个例程值得存在
# ══════════════════════════════════════════════════════════════════════════
# 在此之前，"闭环"是 **PC 在环**：读编码器 → PC 算 → 再下发命令。
# 实测环频只有 **13.5 Hz**、等效滞后 **~80 ms**（审计 §9.1）⇒ 只能控慢轴。
#
# 本程序把环路搬进 **③层（引擎，每拍执行）** ⇒ 环频由**拍频**决定，而不是由命令通路决定。
# ⇒ 走的是**程序面**：③层写 `wire[12..15]`（运动请求；**③层的输出面只有 `wire[]`**），由 `step_service_motion()` 消费。
#    见 `docs/PLAN-step-motion-v1.md` 与 `src/step.h` 的"运动能力面"。
#
# ★★ 用法（**先切运动源**，否则程序写的槽没人读）：
#     0x39 op=19 sub=13 arg=1      # 运动源 = 程序面（默认 0 = 脚手架直控）
#   ★ 切回：sub=13 arg=0（脚本/上位机收尾必须做 —— 否则脚手架设的频率会被每圈覆盖）
#
# ══════════════════════════════════════════════════════════════════════════
# ★★★ 四条必须知道的边界（都是如实声明，不是"以后再补"）
# ══════════════════════════════════════════════════════════════════════════
# ① **环绕**：`sensor[0]` 是 AS5600 **单圈**角度 0..360。误差在 ±180° 处会**跳变**
#    （359° − 1° 算出 358° 而不是 −2°）。DCL 的算术原语**没有 ABS / 没有环绕解算**
#    ⇒ 本程序限定**目标与起点同在 ±180° 相邻区间内**（例：起点 0°、目标 90°）。
#    ★ 要跨 ±180° 必须补原语（ABS/MOD）或在固件侧解算 —— **这是已知缺口，不是 bug**。
# ② **方向映射必须实测**：`dir` 与"角度往哪边走"的关系**不是约定俗成的**
#    （本项目实测：`dir=0` ⇒ 编码器 raw 增大）。★ 若本程序**发散**，先把 `d` 从 0.0 改成 1.0。
# ③ **频率下界 15 Hz**：TIM3 是 16 位定时器 ⇒ `1e6/65536 = 15.26 Hz` 是硬下界，
#    写更小的值会被**静默钳到 15 Hz**（审计 §15.2 / D10）⇒ 这里的 `MN=15.0` 是**显式**的。
# ⑤ ★★★ **驱动+电机有一个"最低有效频率"，远高于 TIM3 的 15 Hz 下界**（2026-09-17 实测）：
#    在 **15 Hz** 下 `rate=15 / CC1E=1 / PA6 确实在翻转`（**脉冲真的发出去了**），而**轴纹丝不动**
#    （`raw` 只有 ±1 LSB 噪声底）。同一块板在 **500 Hz** 下长跑正常（−0.0%）。
#    ⇒ **这不是固件问题**（脚手架直控 15 Hz 同样不动）。
#    ⇒ 后果：**P 控制在小误差时把频率压到下限 ⇒ 电机不动 ⇒ 闭环卡住**。
#    ⇒ 处方：`hz_min` 取**实测可用下限**（本程序取 200 Hz），或对误差带外做"**恒定最小可用频率**"
#      （bang-bang）而不是线性 P。★ 这条与审计 D3（"限时字段有不可用下界"）是**同一族**。
#
# ④ **"走 N 个脉冲"不存在**：TIM3 没有 RCR，且 `gate_isr_itcm.py` 要求 ISR 住 ITCM 而
#    **ITCM 已 100% 占满** ⇒ 无法为"每脉冲一次"的事件加中断。⇒ 本程序用**误差带自停 + 限时**
#    定位，不用脉冲计数（`PLAN-step-motion-v1` §6 已如实登记）。
#
# ══════════════════════════════════════════════════════════════════════════

# ── 参数（全部 CONST ⇒ 行为可预测、可复算）──
CONST   tgt     = 40.0          # 目标角 (°)；★ 与起点同在 ±180° 区间内（见边界①）
CONST   kp      = 8.0           # P 增益 (Hz per deg)：err=1° ⇒ 8 Hz
CONST   band    = 1.0           # 死区 (°)：|err| ≤ band ⇒ 停（这就是"到位自停"）
CONST   hz_min  = 200.0         # ★★★ **实测的可用下限**，不是 TIM3 的 15 Hz（见边界⑤）
CONST   hz_max  = 3000.0        # 上限：远低于突加失速点 16650 Hz，先保证能跟随
CONST   lim_ms  = 4000.0        # 限时（安全网）：到点固件自动停脉冲（step_tick）
CONST   one     = 1.0           # 使能
CONST   d       = 0.0           # 方向（★ 必须实测，见边界②）

# ── 反馈：AS5600 角度（0..360）—— `src/as5600.h` 的 AS5600_SENSOR_DEG = 1 ──
SENSOR  ang     FROM sensor[1]     # ★ 角度 0..360（`AS5600_SENSOR_DEG=1`）；sensor[0] 是 **raw 0..4095**

# ── 误差 = 目标 − 反馈 ──
SUB     err     FROM tgt BY=ang

# ── 死区判据：(err ≥ +band) OR (err ≤ −band) ⇒ drive=1；带内 ⇒ 0（停）──
GE      hi      IN=err THR=1.0
LT      lo      IN=err THR=-1.0
LOGIC   drive   = hi OR lo

# ── P 控制：频率 = kp × err ──
MUL     raw     FROM err BY=kp

# ── ★ 顺序很重要：**先钳位、后门控** ──
#    若反过来（先乘 drive 再钳），死区内 0 会被 `LIMIT MN=15` **抬到 15 Hz** ⇒ 永远停不下来。
LIMIT   hz_lim  IN=raw   MN=15.0 MX=3000.0
MUL     hz      FROM hz_lim BY=drive        # 死区内 ⇒ 0 ⇒ 停

# ── 运动请求（**程序面**：③层写这 4 个槽）──
OUTPUT  o_hz    TO wire[12] FROM hz        # 频率 Hz（0 = 停）
OUTPUT  o_dir   TO wire[13] FROM d         # 方向
OUTPUT  o_ena   TO wire[14] FROM one       # 使能（★ 未声明极性会被 fail-closed 拒）
OUTPUT  o_lim   TO wire[15] FROM lim_ms    # 限时 ms（安全网）

# ── 观测（便于上位机一次 burst 读回；不参与控制）──
OUTPUT  w_err   TO wire[60] FROM err        # ★ 观测槽；64/65 是**固件保留**(运动镜像)
OUTPUT  w_drv   TO wire[61] FROM drive
OUTPUT  w_hz    TO wire[62] FROM hz

# ══════════════════════════════════════════════════════════════════════════
# 复跑
# ══════════════════════════════════════════════════════════════════════════
#   python tools/dclc.py examples/h723_step_closedloop_demo.dcl        # 编译（③层静态闸门会拒引脚号）
#   （上传走 0x45/0x46/0x47 分片路径；之后 sub=13 arg=1 切程序面）
#   ★ 判据（每条都能失败）：
#     C1 切程序面后速率非 0，且方向使 err 单调减小（不减小 ⇒ 把 `d` 改成 1.0，见边界②）
#     C2 到位：|err| ≤ band 且 `wire[12]` 读到 **0**（死区自停，不是被限时停）
#     C3 ★ 反空判据：把 `tgt` 改成当前角度 ⇒ 程序**一开始就不该发脉冲**（hz=0）
#     C4 ★ 对照：同一目标用**上位机在环**跑一次 ⇒ 环频/超调/到位时间的差异（这才是"搬进 DCL"的价值证据）
