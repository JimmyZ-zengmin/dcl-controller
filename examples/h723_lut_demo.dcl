# ══════════════════════════════════════════════════════════════════════════
# h723_lut_demo.dcl —— LUT 查表演示：**表随程序走**（方案 A, 2026-09-19）
#
# 用法:  python tools/dclc.py examples/h723_lut_demo.dcl COM22
#
# ## 为什么这个例子值得存在
# 在方案 A 之前，`LUT` 原语是通的，但**表没有归属**：`0x23` 能把表写进板上，
# 而 `deploy` 不碰它、SD 程序包不带它、`0x13 RESET` 清零它 ⇒ 换个程序表还在（或没了），
# 上电后表是零（或 `fill_tables` 的斜坡）。**生产上不可用**。
# 现在：`TABLE` 声明的那张表会**随本次 deploy 一起生效**，也随程序包持久化。
#
# ## 两条语法约束（都是引擎的硬事实, 编译期就拦）
#   ① **一个程序只有一张表** —— `prim_lut` 只认 `OFF_LUT_DATA` 这一个 256×f32 区。
#      需要多条曲线 ⇒ 拼进同一张表的不同索引区间。
#   ② **用了 `LUT` 就必须声明 `TABLE`** —— 否则会退化成"用板上残留的表"，
#      那种程序在新板/复位后会**静默给出错值**。
#
# ## 查表语义（src/primitives.h 的 prim_lut）
#   索引 = 源值本身（不是自动归一化），越界**夹到** [0, 254]，相邻点**线性插值**。
#   ⇒ 源值是"索引量"（例如已经 SCALE 到 0..N 的工程量）。
# ══════════════════════════════════════════════════════════════════════════

SENSOR  raw   FROM sensor[7]        # 自由槽（固件不写它；自动推导见 tools/exp_ex_composed_blocks.py）

# 表：8 个采样点（至少 2 点；上限 256 = 板上限）
TABLE   curve = 0.0, 0.5, 1.5, 3.0, 6.0, 12.0, 24.0, 48.0

LUT     y     FROM raw              # 查表（全程序共享上面那张表）
OUTPUT  out   TO wire[40] FROM y    # 固定槽输出，便于上位机直接读
