## 改动说明
（一句话讲清这个 PR 做了什么，关联哪个 issue）

## 关联 Issue
Closes #

## 测试（务必包含，否则不合并）
- [ ] **编译器**：`python ide/compiler/dcl_compiler.py <你的程序>.dcl --json` 通过，路由表 / 参数 / 状态计数符合预期
- [ ] **固件**：本地 bench 实测（贴 RTT 状态行或逻辑分析仪截图），确定性指标未被破坏
- [ ] **清洁度**：未引入 ST 版权 PDF / 私有 AI 工具缓存 / 大型生成物（如 `*.csv` dump、构建目录）

## 检查清单
- [ ] 代码风格与现有 `main.c` / `dcl_compiler.py` 一致
- [ ] 必要时更新了 `docs/` 对应文档
- [ ] `git diff` 范围仅限本次改动

## 备注
（如有架构权衡、未验证项、或需要讨论的点）
