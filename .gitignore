build/
build_min/
.la/
tmp_dap/

# 一次性诊断/核对临时目录 (raw 盘读出的大 CSV 等) —— 可复跑, 不入版本控制
.tmpctl/

# LA 导出临时文件 (h723_la_modbus.py 生成)
tools/_la_modbus.txt

# Python 字节码缓存 —— 可随时重生成, 且**每次跑套件都会被改写** ⇒ 纳入版本控制只会让
# `git status` 永远脏, 掩盖真正的工作区改动 (本项目"零噪声"纪律的同款理由)。
__pycache__/
*.pyc

# 最小固件的独立构建目录 (DCL_MIN_UART2 档) —— 与 build/ 同理, 可复跑
build_min485/

# 本工程的临时工作区 —— 里面是**可复跑**的中间产物 (例: tools/pdf_grep.py 从
# RM0468 抽出的 6MB 全文文本, 读手册查规格用)。抽出来的文本随时能重生成,
# 且体积大 ⇒ 与 .tmpctl 同理, 不入版本控制。工具本体在 tools/pdf_grep.py (受控)。
.workbuddy/scratch/
